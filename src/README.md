# src/

**Moved.** This file's full content — the production-quality checklist for anything moving from
`improved-quality/` into `src/` — now lives in [`../SPECIFICATION.md`](../SPECIFICATION.md),
**Part D — `src/` Production-Quality Checklist** (sections D.0-D.16), as part of a first-pass
doc-scatter cleanup that consolidated `DRIVER_SPEC.md`, `src/README.md`, `tests/README.md`,
`toolchain/README.md`, and parts of the root `README.md` into one central document.

This stub stays in place so existing links/references to `src/README.md` elsewhere in the repo
still resolve to a real file.

See `../SPECIFICATION.md`'s own front matter for the full rationale, and its "Further reading"
entry in the root `README.md` for the complete map of every project doc.
