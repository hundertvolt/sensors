# Harvest requirements G7 — TWIN.md (380), WEB.md (286) (HEAD 06eff58)

Read-only harvest pass 1. Every item of both catalogs read in full; claims checked against HEAD's
tree, the MicroPython v1.29.0 checkout (scratchpad `mp`), git history and the retired
`WEBSITE_PLAN.md` (`git show 15ed282^:WEBSITE_PLAN.md`). Tier names follow OR45.a (L0 host,
L1 unit, L2 twin, L3 flash, L4 bench).

## Candidates

### G7.001 Twin and unit-tier fakes stay independent
- **Req**: The digital twin's fakes (`machine`, `network`, `neopixel`, chip fakes, CRC8, fault injector) are independent reimplementations: they never import `tests/` fakes or `src/` helpers and are never imported by `tests/` fakes, so a bug shared between oracle and code under test cannot hide. Where the two tiers expose the same test-facing API (fault queue `inject_fault()`/`maybe_raise()`), they follow one convention.
- **Class**: test-standard
- **Sources**: TWIN.N005, TWIN.N025, TWIN.N026, TWIN.N036, TWIN.N113, TWIN.N114, TWIN.N148, TWIN.N258 · stated at digital_twin/README.md:12-14, :102-106, :181-191; digital_twin/_crc8.py:1; digital_twin/_fram_chip.py:2; scripts/run_unix_port_integration.sh:6-8
- **Provenance**: O-confirmed — `b8791e6` (2026-08-12): "Kept fully independent from tests/machine.py per the owner's explicit instruction - a separate module, never imported by or importing it"; the CRC/FRAM "deliberately not shared" wording is A (`3795d93`).
- **Truth**: holds — no `digital_twin/*.py` imports a `tests/` fake (only `_http_client.py`'s lazy `_strict_json`, G7.002); `_crc8.py` reimplements poly 0x31/init 0xFF; no golden vector ties it to the SGP40/SCD30 datasheet CRC example (TWIN.N114 — the independence has no external anchor).
- **Fit**: refines OR17 (twin as an independent oracle) · pillar P5
- **Home**: digital_twin/README.md → SPECIFICATION.md Part E (tier ladder, L2 definition, OR45.a (1))

### G7.002 The twin boots with no tests/ on its path
- **Req**: A twin launch uses its own `MICROPYPATH` (`src`, `digital_twin`, `ext`, `frozen_modules`/`.frozen`) and never carries `tests/`; the one allowed reach into `tests/` is `_http_client.HttpResponse.json()`'s lazy `_strict_json` import, and no twin module calls `.json()`. `digital_twin/machine.py` keeps MicroPython-only imports out of module scope so CPython tooling can import it.
- **Class**: test-standard
- **Sources**: TWIN.N032, TWIN.N037, TWIN.N169, TWIN.N279, TWIN.N293, TWIN.N294, TWIN.N295 · stated at digital_twin/README.md:125-128, :183-188; digital_twin/_http_client.py:32; tests_scripts/test_twin_never_needs_tests_on_its_path.py:1-3, :51-52; tests_scripts/test_buildgen_twin_wiring.py:29-31
- **Provenance**: A — `83b727e` (2026-09-24, "Close two conditions the merged code leaves unhandled") added the guard and its removal condition; the path rule itself is `b8791e6`.
- **Truth**: partly — the guard scans only the two launch sites it names; `tests_scripts/test_digital_twin_generated_boot.py:153` and the `tests_js` twin launches set their own `MICROPYPATH` unscanned (TWIN.N294); the CPython-import convention (TWIN.N279) is unenforced.
- **Fit**: refines OR20.a (tiny twin, logic outside) · pillar P5
- **Home**: digital_twin/README.md; tests_scripts guard → Part E L2 definition

### G7.003 Product code carries no twin awareness
- **Req**: `src/` and generated code contain no branch, public attribute or hook that exists for the digital twin; the swap to the twin is pure module-path ordering, and twin code reaches the object graph only through the product's normal interfaces.
- **Class**: product
- **Sources**: TWIN.N001, TWIN.N035, TWIN.N096, TWIN.N207 · stated at digital_twin/README.md:151-156; digital_twin/machine.py:302-304; src/asy_uart_link_driver.py:57; digital_twin/run_generic_integration.py:202-216
- **Provenance**: A (`b8791e6`, twin finish criteria) and O (OR36.a (2): "its own test seams (e.g. `self.uart` public for the twin, `:57`) still go").
- **Truth**: partly — `src/asy_uart_link_driver.py:57` keeps `self.uart  # public: the digital twin reaches the underlying machine fake through it`; `run_generic_integration.py` reads private `bus._i2c`/`bus._spi.device` of `src/` wrappers (TWIN.N207), a coupling to private names.
- **Fit**: restates OR36/OR36.a; conflicts none · pillar P4
- **Home**: digital_twin/README.md → SPECIFICATION.md principles P4 / Part D checklist

### G7.004 Every promoted module joins the twin in the same change
- **Req**: A new bus-facing driver lands together with its chip fake (`digital_twin/_<name>_chip.py`), its `machine.py` bus-map dispatch and a `tests/test_digital_twin_<name>.py`; any other new module joins the twin automatically through `build_system()`. The twin's chip-fake catalog and buildgen's `BUS_ATTACHED_DRIVERS` agree, checked by a test rather than by hand.
- **Class**: process
- **Sources**: TWIN.N070, TWIN.N089, TWIN.N280, TWIN.N299, TWIN.N305, TWIN.N330 · stated at SPECIFICATION.md C.11 item 9, K.5 (:5818-5830), A.10 (:671-675); digital_twin/README.md:799-801; tests_scripts/test_buildgen_twin_wiring.py:103-105
- **Provenance**: A — `51d0c21` (2026-08-12, "Require a digital-twin extension whenever a new sensor driver is added").
- **Truth**: drifted — C.11 item 9 still says "Also update `html/definitions/<device>.json` ... Same session", contradicting OR43.a (3)'s retirement of hand-written definitions (@web tags the only source); catalog sync is hand-kept (`test_buildgen_twin_wiring.py` pins wozi/dev only, TWIN.N281); a missing fake fails only at bus construction (`machine.py:208-231`).
- **Fit**: refines OR10.a (Part K step), OR16.a (1) · pillar P10
- **Home**: SPECIFICATION.md C.11/K.5/A.10 (three places) → the one ordered checklist (harmonization 4)

### G7.005 Chip fakes model the datasheet and the driver's exact transactions
- **Req**: A chip fake answers the exact raw transaction shape the real driver sends (confirmed against the driver source) with values and ranges cited to a datasheet page; every value that is a judgment call (step bounds, room-value ranges, fixed identity words) is labelled "not datasheet-derived" where it is defined.
- **Class**: test-standard
- **Sources**: TWIN.N014, TWIN.N045, TWIN.N071, TWIN.N072, TWIN.N116, TWIN.N117, TWIN.N125, TWIN.N127, TWIN.N140, TWIN.N156, TWIN.N198 · stated at digital_twin/README.md:46-48, :813-821, :332-336; digital_twin/_sgp40_chip.py:32-43; _scd30_chip.py:1, :81-84; _bmp3xx_chip.py:107-109; _isl29125_chip.py:73-89; launch.py:278
- **Provenance**: A — `51d0c21` ("answering the *exact* raw transaction shape ... confirmed directly against that file's own source").
- **Truth**: partly — step bounds are labelled; "datasheet-ranged" claims cite no page for SGP40 26000-34000 (TWIN.N116) and SCD30's 400-2000 ppm/15-30 C are room values, not the datasheet range (TWIN.N125); ISL29125 cites p1 but defaults 5-9000 lux (TWIN.N156); SCD30's NVM set is confirmed against driver docstrings, not the Interface Description (TWIN.N045).
- **Fit**: refines OR17.a (3) (tuned to evidence) · pillar P5
- **Home**: digital_twin/README.md "Adding a new chip fake" → Part E/K twin step

### G7.006 A fake's reaction to unspecified input needs evidence
- **Req**: Where a datasheet does not specify a chip's reaction (unknown command, wrong-length frame, write to an unknown register, read before any command, data-register reset value, zero-filled MISO), the fake's behaviour and any test expectation built on it are either backed by a measurement or marked as an assumption to be confirmed on silicon (hardware queue), never stated as "real hardware would just ignore it".
- **Class**: test-standard
- **Sources**: TWIN.N122, TWIN.N126, TWIN.N133, TWIN.N134, TWIN.N141, TWIN.N143, TWIN.N145, TWIN.N164, TWIN.N222, TWIN.N228, TWIN.N248, TWIN.N252 · stated at digital_twin/_sgp40_chip.py:72-75; _scd30_chip.py:42, :183, :196; _bmp3xx_chip.py:116-118, :154-156, :169-171; _isl29125_chip.py:288; tests/test_digital_twin_bmp3xx.py:214-215; test_digital_twin_fram.py:80-82; test_digital_twin_scd30.py:81-83; test_digital_twin_sgp40.py:137-139
- **Provenance**: A — no owner wording; derived here from OR17.a (4) ("every fact in doubt ... becomes a hardware-queue row").
- **Truth**: partly — each site states the claim without a measurement or datasheet page; SCD30 `_FIRMWARE_VERSION` "never checked by the driver" was not re-verified here (not checked).
- **Fit**: new (closes the gap between OR17.a (4) and assertions encoded in L2 tests) · pillar P5
- **Home**: code comments → twin fidelity table (G7.008) + BACKLOG real-hardware section

### G7.007 Silicon measurement, not the fake, decides a disagreement
- **Req**: Chip behaviour measured on real silicon overrides both the datasheet and the fake; each bus-facing chip gets a conformance probe (raw `machine.I2C` only, run on silicon and on the fake, diffed host-side), and a disagreement is decided from the datasheet plus a fresh measurement, never from the fake. Deliberately non-nominal fake parameters (ISL29125 range ratio 25.9) exist only where a test needs something to converge on.
- **Class**: test-standard
- **Sources**: TWIN.N017, TWIN.N018, TWIN.N155, TWIN.N157, TWIN.N231, TWIN.N232, TWIN.N235, TWIN.N257, TWIN.N306, TWIN.N333 · stated at SPECIFICATION.md C.11.1 (:2348-2365), M.1.2 (:6660); digital_twin/README.md:53-68; _isl29125_chip.py:62-63, :90-93; tests_hardware/README.md:406-414
- **Provenance**: A — `75d222e` (2026-09-14, ISL29125 promotion); the silicon facts themselves are F (M.1.2, measured).
- **Truth**: partly — only `tests_hardware/device_scripts/isl29125_mock_conformance_probe.py` exists; C.11.1 says "Build one for every new bus-facing chip" and SGP40/SCD30/BMP3xx/FRAM fakes, older than the rule, have none; the 12-bit cycle (19 ms) is derived, not measured (TWIN.N155/N232).
- **Fit**: refines OR17.a (3)-(5), OR29.a (2) · pillar P5
- **Home**: SPECIFICATION.md C.11.1 (stays); gap per older chip → BACKLOG real-hardware section

### G7.008 One fidelity table says which tier proves each fact
- **Req**: Every hardware fact the twin does not or cannot model (G7.009-G7.016) is listed once in a twin fidelity table with its evidence and the tier that proves it instead (L3/L4 test or hardware-queue row); a property proven only at L2 (e.g. wozi's bus grouping, ISL29125 auto-range continuity) is named as such.
- **Class**: docs
- **Sources**: TWIN.N226, TWIN.N233, TWIN.N234, TWIN.N256, TWIN.N297 · stated at tests/test_digital_twin_bus_hazard_concurrency.py:177-178; test_digital_twin_isl29125_autorange.py:2, :68-70; tests_hardware/manual/manual_sensor_accuracy.py:20; SPECIFICATION.md:209-213
- **Provenance**: O — OR17.a (2) "a fidelity table: how the twin models each fact — match, mismatch, or unmodelled".
- **Truth**: wrong (absent) — no fidelity table exists; `digital_twin/README.md` records gaps in prose at scattered places (grep "fidelity" finds only :114); BMP3xx compensation is validated only by a manual script; the default AutoRangeDwell is never run at L2.
- **Fit**: restates OR17.a (2); extends CONSOLIDATION §4 home ("`digital_twin/README.md` fidelity table") · pillar P5, P7
- **Home**: → `digital_twin/README.md` fidelity table (CONSOLIDATION §4)

### G7.009 Twin timers and interrupts: accepted boundary
- **Req**: The twin's `Timer` is an asyncio task on a wall-clock schedule and its Pin IRQs run synchronously in the caller: no pre-emption, no soft-IRQ queue or drop, no hard-IRQ allocation limits, drifting periods, and callbacks never run while the loop is blocked. This is accepted only while every real Timer/IRQ callback stays trivial; soft-IRQ drop, pre-emption and cadence accuracy are proven at L3.
- **Class**: test-standard
- **Sources**: TWIN.N006, TWIN.N007, TWIN.N080, TWIN.N081, TWIN.N101, TWIN.N102, TWIN.N103, TWIN.N230, TWIN.N240, TWIN.N241, TWIN.N247, TWIN.N375 · stated at digital_twin/README.md:18-22; digital_twin/machine.py:84-137, :500-502, :539-540, :808-816; commit 39b89ec
- **Provenance**: A — `b8791e6` (deliberate asyncio Timer); "callbacks trivial" is an unverified going-forward assumption.
- **Truth**: holds — as a description (`machine.py:808-816`); the "callbacks trivial" premise has no check; Timer/WDT fakes add asyncio tasks absent on the board (commit 39b89ec caveat) — not re-verified that this caveat survived into current docs.
- **Fit**: extends OR17.a (fidelity row) · pillar P5
- **Home**: digital_twin/README.md → fidelity table (G7.008)

### G7.010 The twin watchdog records, never resets
- **Req**: The twin WDT models the RP2040 cap (8388 ms, id 0) and counts would-have-reset events but never resets the process; its countdown cannot run during a real freeze, so it credits an already-elapsed window before re-arming. Post-reset behaviour and the runtime feed path's failure classes are proven with planted faults on the real feed site (OR31.a (5)) and at L3/L4.
- **Class**: test-standard
- **Sources**: TWIN.N065, TWIN.N105, TWIN.N106, TWIN.N195, TWIN.N200, TWIN.N250 · stated at digital_twin/README.md:692-708; digital_twin/machine.py:835-868; launch.py:84-86, :294-298; tests/test_digital_twin_sensortask_integration.py:425-463
- **Provenance**: A — `b8791e6`/later twin commits; the 8388 ms cap is F.
- **Truth**: holds — `mp ports/rp2/machine_wdt.c:34-38` `WDT_TIMEOUT_MAX 8388`; twin constant `_WDT_TIMEOUT_MAX_MS = 8388` (`machine.py:835`). The integration watchdog test uses its own 1 s feed loop, not the supervisor's feed site (TWIN.N250), so it does not prove OR31.a (3).
- **Fit**: refines OR31.a (5) · pillar P3, P5
- **Home**: digital_twin/README.md → fidelity table

### G7.011 Bus and peripheral timing: accepted boundary
- **Req**: Twin buses take zero transaction time and model no electrical behaviour: I2C `freq`/`timeout`, clock stretching, repeated start, SDA-stuck, ghost addresses, SPI baud/mode and floating MISO, UART framing other than 8N1 and interrupt-driven delivery, NeoPixel busy-wait, and per-bus singleton objects are unmodelled; interleaving happens only between awaits. Any test whose verdict depends on these runs at L3/L4.
- **Class**: test-standard
- **Sources**: TWIN.N011, TWIN.N015, TWIN.N041, TWIN.N079, TWIN.N090, TWIN.N091, TWIN.N092, TWIN.N093, TWIN.N094, TWIN.N099, TWIN.N100, TWIN.N186, TWIN.N224, TWIN.N225, TWIN.N239, TWIN.N242, TWIN.N298, TWIN.N317 · stated at digital_twin/README.md:35-52, :288-297; digital_twin/machine.py:62-67, :239-294, :410-435; neopixel.py:1-2; SPECIFICATION.md:512-520, F.5.1 (:3715-3717)
- **Provenance**: A — twin commits; the F.5.1 fresh-object divergence is a stated deliberate choice (A).
- **Truth**: holds — as a description (checked `machine.py:239-294, 435`); the general call reaches no chip fake, so SGP40/SCD30 soft-reset reactions are untested at L2 (TWIN.N092/N225); `buf[:] = data` can shrink a caller's buffer (TWIN.N094) — a fidelity defect candidate inside this boundary.
- **Fit**: extends OR17.a, OR41.a (3) (silicon-only concurrency queued) · pillar P5
- **Home**: digital_twin/README.md → fidelity table

### G7.012 Chip-fake functional gaps: accepted boundary
- **Req**: The chip fakes model only what the driver consumes: no conversion or self-test time, compensation inputs never affect readings, CRC corruption on one fixed word, one identity variant (BMP390 only), hand-picked BMP3xx calibration, no ERR/BP enforcement, no FRAM wraparound, a linear ISL29125 lux model; each gap is listed in the fidelity table and closed in the fake when a driver starts depending on it.
- **Class**: test-standard
- **Sources**: TWIN.N074, TWIN.N118, TWIN.N119, TWIN.N120, TWIN.N121, TWIN.N123, TWIN.N124, TWIN.N130, TWIN.N131, TWIN.N132, TWIN.N135, TWIN.N136, TWIN.N137, TWIN.N138, TWIN.N144, TWIN.N146, TWIN.N152, TWIN.N153, TWIN.N154, TWIN.N159, TWIN.N160, TWIN.N163, TWIN.N221 · stated at digital_twin/_sgp40_chip.py:46-81; _scd30_chip.py:145-228; _bmp3xx_chip.py:1-2, :21, :162-191; _fram_chip.py:112-155; _isl29125_chip.py:152-179, :253-281; tests/test_digital_twin_bmp3xx.py:72-75; digital_twin/README.md:876-881
- **Provenance**: A — per-fake commits; none owner-attributed.
- **Truth**: holds — as a description (spot-checked `_bmp3xx_chip.py:21` serves only 0x60 while `test_digital_twin_bmp3xx.py:72-75` accepts 0x50 or 0x60 — the BMP388 path is untested).
- **Fit**: extends OR17.a (2) · pillar P5
- **Home**: fake docstrings/README → fidelity table

### G7.013 Twin network: accepted boundary
- **Req**: The twin fakes WLAN connection state only; traffic uses the host Linux stack, so lwIP pool limits (`MEMP_NUM_TCP_PCB`), CYW43 `isconnected()` false positives, spontaneous link loss, AP-mode addressing/DHCP, per-interface singletons and real connect timing are unmodelled, and connection-ceiling or hotspot verdicts come from L4 (the 32-bit frozen twin being an ad-hoc instrument, never a gate).
- **Class**: test-standard
- **Sources**: TWIN.N027, TWIN.N028, TWIN.N029, TWIN.N057, TWIN.N181, TWIN.N182, TWIN.N183, TWIN.N184, TWIN.N185, TWIN.N251, TWIN.N277, TWIN.N307, TWIN.N326, TWIN.N366 · stated at digital_twin/README.md:109-116, :569-580; digital_twin/network.py:30-147; toolchain/versions.toml:41-52; SPECIFICATION.md:3169-3170, H.7 (:4612-4617)
- **Provenance**: A — twin commits (`dcb007d` for the AP gap).
- **Truth**: holds — `network.py:35-37` static `192.168.1.42`; AP `ifconfig()[0]` = `0.0.0.0` asserted as a known gap (`test_digital_twin_sensortask_integration.py:610-612`); `_CONNECT_DELAY_S = 0.7` sits under the `src/` 5 s budget, a coupling with no check.
- **Fit**: extends OR17.a, OR49.a (1) (load verdicts at the tier where the load is real) · pillar P5
- **Home**: digital_twin/README.md → fidelity table

### G7.014 Twin heap and throughput numbers do not transfer to silicon
- **Req**: Heap, fragmentation, contiguity and throughput figures taken on the 64-bit Unix-port twin are harness baselines only: they may gate twin-side regressions but are never quoted as board figures; board memory claims come from L3/L4, and any calibrated twin figure is re-derived after a binary, manifest or object-graph change.
- **Class**: test-standard
- **Sources**: TWIN.N282, TWIN.N311, TWIN.N329, TWIN.N340, TWIN.N347, TWIN.N349, TWIN.N351, TWIN.N352, TWIN.N353, TWIN.N354, TWIN.N355, TWIN.N356 · stated at SPECIFICATION.md E.8 (:3317-3325), I.4(f) (:5125-5129); HEAP_FRAGMENTATION_MEASUREMENTS.md:97-238; BACKLOG.md:288-291, :800-803; tests_scripts/test_digital_twin_boot_contiguity.py:46-48
- **Provenance**: A — measurement docs; F for the dated figures (single measurements).
- **Truth**: holds — `test_digital_twin_boot_contiguity.py:46-48` states twin-only bounds; HEAP file states re-derivation; the "write-phase failures silent at default debug" trap (TWIN.N353) is a live hazard for any twin memory verdict.
- **Fit**: refines OR39.a (2), OR40.a (3) · pillar P5, P6
- **Home**: HEAP_FRAGMENTATION_MEASUREMENTS.md / Part E.8 (stays)

### G7.015 Simulated reset: accepted boundary
- **Req**: `machine.reset()`/`bootloader()` in the twin raise a `SimulatedRebootError` subclass instead of restarting, and the frozen firmware image itself is never executed below L3; reboot paths are proven at L2 only up to the raise, and the real restart at L3/L4.
- **Class**: test-standard
- **Sources**: TWIN.N243, TWIN.N346 · stated at digital_twin/machine.py:916-946; README.md:636-639; tests/test_digital_twin_real_website_integration.py:1-3
- **Provenance**: A — `770cf13` (class hierarchy).
- **Truth**: partly — README.md:637 says the same process "keeps serving afterward"; asserted, not shown (TWIN.N346). A broad `except Exception` in `src/` can swallow the simulated reset (TWIN.N109, defect-candidate bucket).
- **Fit**: extends OR17.a · pillar P3, P5
- **Home**: digital_twin/README.md → fidelity table

### G7.016 Twin RTC does not advance: accepted boundary
- **Req**: The twin RTC holds a fixed tuple uncoupled from `time.time()`; clock jumps from an NTP-set RTC are unmodelled at L2 and proven at L4 (NTP) or by unit tests with a fake clock.
- **Class**: test-standard
- **Sources**: TWIN.N107, TWIN.N108 · stated at digital_twin/machine.py:899-913
- **Provenance**: A — `b8791e6`.
- **Truth**: holds — `_shared_datetime = (2000, 1, 1, ...)` never advances; set path returns the tuple where rp2 returns None (checked code, not the rp2 source).
- **Fit**: extends OR17.a; relates P1 (clock drift over months) · pillar P5
- **Home**: fidelity table

### G7.017 Twin configuration hooks precede construction
- **Req**: Every twin `configure_*()` hook (wiring, FRAM/SCD30 state path, random source) is called before the buses it affects are constructed; the last call before construction wins and later calls have no effect, so a multi-device process calls `configure_wiring()` immediately before each `build_system()`.
- **Class**: test-standard
- **Sources**: TWIN.N004, TWIN.N008, TWIN.N043, TWIN.N082, TWIN.N085 · stated at digital_twin/machine.py:144-146, :174-181; digital_twin/README.md:22-25, :310, :341; tests/_webserver_concurrency_scenarios.py:96-98
- **Provenance**: A — `c4046a5` (2026-09-11, "Self-audit fixes: ... a real ordering bug").
- **Truth**: holds — convention only; `configure_wiring()` validates the plan shape (`machine.py:179-180`) but nothing detects a call after construction.
- **Fit**: refines OR38.a (1) (no crosstalk between tests) · pillar P5
- **Home**: digital_twin/README.md (stays)

### G7.018 Twin wiring comes from buildgen's generated plan
- **Req**: The twin's bus/address/IRQ-pin layout is read from buildgen's per-device wiring plan (`compute_twin_wiring()`), never hand-typed, and a caller that constructs a bus without configuring wiring fails loudly rather than silently getting wozi's layout; chip-intrinsic facts (fixed I2C addresses, FRAM RDID per size) live in one table each.
- **Class**: test-standard
- **Sources**: TWIN.N009, TWIN.N010, TWIN.N086, TWIN.N087, TWIN.N088, TWIN.N097, TWIN.N188, TWIN.N189, TWIN.N215, TWIN.N281, TWIN.N331 · stated at SPECIFICATION.md L.4 (:6305); digital_twin/README.md:26-35; digital_twin/machine.py:185-204, :318-323; launch.py:369-375; buildgen/twin_wiring.py:12
- **Provenance**: A — `fcc5339` (2026-09-11, "generalize wiring past the wozi/dev 2-profile enum").
- **Truth**: drifted — L.4:6305 "No hand-typed wiring literal exists anywhere" vs `launch.py:369-375` (`I2C(0, scl=Pin(13), ...)`, `WDT(timeout=8000)`, addresses — they match `devices/wozi.toml` today, unchecked), `buildgen/twin_wiring.py:12` `FIXED_ADDRESSES`, `machine.py:323` RDID by size with silent MB85RS64V fallback; `_current_wiring_plan()` (`machine.py:198-204`) silently defaults to wozi; `launch.py`/`segfault_stress_repro.py` rely on that default and a CWD-relative path (`machine.py:194`). Suppression `machine.py:204` `type: ignore[return-value]` rests on this control flow.
- **Fit**: refines OR43.a (device set derived), P8 · pillar P8
- **Home**: SPECIFICATION.md L.4 (claim to correct) + digital_twin/machine.py

### G7.019 Twin SPI wires one device per bus id
- **Req**: The twin SPI fake wires one device per bus id with no chip-select routing; a driver that shares an SPI bus with the FRAM chip cannot be promoted until the twin gains CS routing in the same change.
- **Class**: test-standard
- **Sources**: TWIN.N073, TWIN.N098, TWIN.N304, TWIN.N359 · stated at digital_twin/README.md:844-850; digital_twin/machine.py:326-336; SPECIFICATION.md C.11 item 9 (:2335-2337); commit 51d0c21
- **Provenance**: A — `51d0c21`.
- **Truth**: holds — `machine.py:328` reads one `spi{bus_id}` attachment; no current device shares an SPI bus (devices/*.toml, not exhaustively checked).
- **Fit**: extends OR41.a (1) (shared-bus recombinations) · pillar P10
- **Home**: SPECIFICATION.md C.11 item 9 → one ordered checklist

### G7.020 Chip fake and driver share the IRQ Pin object
- **Req**: A chip fake's ready/INT pin is constructed with the same pin id as the driver's IRQ `Pin`, the twin `Pin` registry returns the same object per id, and a line that idles high is driven high at construction; a test guards the identity.
- **Class**: test-standard
- **Sources**: TWIN.N012, TWIN.N158, TWIN.N237 · stated at digital_twin/README.md:36-41; _isl29125_chip.py:21-25, :96-100; tests/test_digital_twin_machine.py:178-180
- **Provenance**: A — `b93d5a5` (2026-09-03).
- **Truth**: holds — identity guarded by `test_digital_twin_machine.py:178-180`; pin-id agreement between fake and driver is derived from the wiring plan (SCD30 IRQ 8 vs 11), not separately checked.
- **Fit**: refines OR16 (silent broken chains) · pillar P5
- **Home**: digital_twin/README.md (stays)

### G7.021 Poll-set prewarm: workaround with a removal trigger
- **Req**: Every entry point that boots a device with real sockets under the Unix port calls `prewarm_poll_set()` as its first statement, because `extmod/modselect.c` rebases non-fd poll entries' NULL `pollfd` into a dangling pointer when `pollfds` grows; the workaround stays until upstream fixes the growth path, an upstream issue is filed for the residual case, and the 512-slot margin is re-checked against the highest concurrency any tier drives.
- **Class**: platform
- **Sources**: TWIN.N002, TWIN.N019, TWIN.N020, TWIN.N021, TWIN.N077, TWIN.N176, TWIN.N177, TWIN.N178, TWIN.N179, TWIN.N210, TWIN.N223, TWIN.N255, TWIN.N362, TWIN.N365 · stated at digital_twin/unix_port_poll_prewarm.py:1-49; digital_twin/README.md:76-88, :917-919; commits 54d451a, f7ceab8
- **Provenance**: F — `mp extmod/modselect.c:157-178` (on non-in-place growth, `poll_obj->pollfd = new_fds + (poll_obj->pollfd - poll_set->pollfds)` is applied to every entry, including non-fd objects whose `pollfd` is NULL); workaround A (`12f65db`).
- **Truth**: partly — mechanism confirmed at v1.29.0; the first-statement rule is convention only: `tests/test_digital_twin_run_generic_integration.py` boots a device through `main()` without importing the prewarm; no upstream issue filed (TWIN.N365); port 0 is unusable because the Unix port has no `getsockname()` (confirmed: no hit in `mp ports/unix`, `extmod`).
- **Fit**: extends OR4 (upstream sources), OR5 (removal trigger) · pillar P5
- **Home**: digital_twin/README.md + SPECIFICATION.md A.10 → one place (Part F Unix-port facts)

### G7.022 Unix-port UDP shim and port-53 capability
- **Req**: Twin entry points patch `AsyUDPSocket` with `_unix_port_udp_addr_shim` before any socket is constructed, working around three Unix-port-only `socket` quirks (tuple sockaddr rejected, `sendto()` micropython#6924, raw `recvfrom()` struct) with only numeric addresses passed; the DNS test runs only with `CAP_NET_BIND_SERVICE` and a failed bind fails the run loudly; the shim goes when upstream fixes the quirks.
- **Class**: platform
- **Sources**: TWIN.N003, TWIN.N067, TWIN.N068, TWIN.N069, TWIN.N170, TWIN.N171, TWIN.N172, TWIN.N173, TWIN.N174, TWIN.N270, TWIN.N271, TWIN.N301, TWIN.N361, TWIN.N364 · stated at digital_twin/_unix_port_udp_addr_shim.py:1-76; digital_twin/README.md:749-795; SPECIFICATION.md A.10 (:685-688); scripts/_digital_twin_ci_suite.py:944-967
- **Provenance**: A — `dfb8975` (2026-08-20); upstream issue #6924 cited (F, not re-read here).
- **Truth**: partly — call order is convention only (7 call sites); the shim reads the native family field little-endian (TWIN.N174, host assumption); Run 7's port-53 bind failure was once silent (`_digital_twin_ci_suite.py:962-967`) — not re-checked whether it now fails loudly; three `method-assign` suppressions (`:74-76`) follow CLAUDE.md's per-site rule; `no-redef` (`:15`) is a runtime-cast fallback.
- **Fit**: extends OR38.a (3) (port 53 fixed-port use) · pillar P5
- **Home**: digital_twin/README.md + SPEC A.10 → Part F Unix-port facts

### G7.023 The heap-unwedge helper is retired, its mechanism kept
- **Req**: `digital_twin/unix_port_gc_unwedge.py`, its test and its call sites are retired once a test proves the `unix_kbd_intr` override (`MICROPY_ASYNC_KBD_INTR=0`) is applied to every Unix-port build; SPECIFICATION.md F.6 keeps the mechanism and the recovery (`gc.collect()`, not `micropython.heap_unlock()`) so it can be restored.
- **Class**: tooling
- **Sources**: TWIN.N022, TWIN.N023, TWIN.N024, TWIN.N175, TWIN.N199, TWIN.N212, TWIN.N254, TWIN.N302, TWIN.N320, TWIN.N321, TWIN.N322, TWIN.N323, TWIN.N324, TWIN.N325, TWIN.N335, TWIN.N336, TWIN.N372 · stated at SPECIFICATION.md F.6 (:4067-4150); CLAUDE.md "Known intermittent-MemoryError cause #2"; digital_twin/README.md:89-101; launch.py:426-432; run_generic_integration.py:389-395, :432-437
- **Provenance**: O — OR52.a (6) "retire if it's no longer needed but document how it was done in case we need it again".
- **Truth**: drifted — at HEAD the helper is wired at `run_generic_integration.py:395, 435` and `launch.py:432`, and F.6's amendment (`SPECIFICATION.md:4119-4133`) and CLAUDE.md say the calls are "kept, deliberately, as defense in depth" — both superseded by OR52.a (6); the retirement's precondition (a test proving the override applies) is not verified here.
- **Fit**: restates OR52.a (6); conflicts SPEC F.6 amendment/CLAUDE.md bullet (later owner row wins) · pillar P4
- **Home**: CLAUDE.md known-cause bullet + F.6 amendment → F.6 only (mechanism), code removed

### G7.024 asyncio.run's SIGINT gap: synchronous outer cleanup
- **Req**: Because MicroPython's `asyncio.run()` does not run its own `try`/`finally` when `KeyboardInterrupt` lands while the coroutine is suspended, every twin `__main__` repeats its cleanup (state flushes, WDT report) from plain synchronous code in an outer `except KeyboardInterrupt:`; exceptions swallowed during that cleanup are limited to the re-delivered interrupt and cancellation.
- **Class**: platform
- **Sources**: TWIN.N211, TWIN.N217, TWIN.N315, TWIN.N316 · stated at SPECIFICATION.md F.1 (:3564-3567); digital_twin/run_generic_integration.py:404-437; segfault_stress_repro.py:103-106
- **Provenance**: A (workaround), F claimed (asyncio gap; `extmod/asyncio` not re-read here — not checked).
- **Truth**: partly — `run_generic_integration.py:430-437` does the outer cleanup; `segfault_stress_repro.py:103-106` swallows `(asyncio.CancelledError, Exception)` wholesale, broader than the rule.
- **Fit**: refines OR26.a (swallowing excepts) for tooling · pillar P5
- **Home**: SPECIFICATION.md F.1 (stays)

### G7.025 Twin state persists only through an explicit flush
- **Req**: The FRAM and SCD30 twin state files are written only by an explicit flush at a clean shutdown, never automatically, to spare the host SSD; every entry point wraps its run so both flushes happen on SIGINT (the harness stops the twin with SIGINT, never SIGTERM/kill), an empty or absent path means in-memory only, and user docs state which paths a given launcher persists.
- **Class**: tooling
- **Sources**: TWIN.N038, TWIN.N042, TWIN.N050, TWIN.N084, TWIN.N205, TWIN.N268, TWIN.N296, TWIN.N338, TWIN.N344 · stated at digital_twin/README.md:197-204, :301-304, :428-430; digital_twin/machine.py:164-165, :310-312; run_generic_integration.py:124-152; scripts/_digital_twin_ci_suite.py:487-491; tests_js/_live_twin_command.js:67-70; README.md:465-467, :579-619
- **Provenance**: A — twin commits; the SSD reason matches CLAUDE.md's wear rule (O, 2026-09-17, "the host's own SSD included").
- **Truth**: drifted — README.md:465-467 says in-memory by default (true: `run_generic_integration.py:124-125` default `None`), while README.md:579-582 says Ctrl-C "flush[es] the FRAM twin's state to disk" and :615-619 calls `digital_twin/scd30_state.json` "this entry point's own default persisted path"; `scripts/run_unix_port_integration.sh` passes neither path, so the walkthrough's survive-a-restart steps do not hold as written.
- **Fit**: refines CLAUDE.md wear rule (host side), OR37.a (1) · pillar P9
- **Home**: digital_twin/README.md + README.md walkthrough (fix README)

### G7.026 Corrupt twin state degrades to a blank chip
- **Req**: A missing, malformed or truncated twin state file loads as the chip's factory-fresh state without raising; saves are plain (non-atomic) writes, accepted because a truncated file degrades the same way and the state is a test convenience, not evidence.
- **Class**: test-standard
- **Sources**: TWIN.N016, TWIN.N128, TWIN.N129, TWIN.N149, TWIN.N150, TWIN.N151, TWIN.N229 · stated at digital_twin/_scd30_chip.py:110-143; _fram_chip.py:57-106; tests/test_digital_twin_fram.py:219-237; digital_twin/README.md:50-52
- **Provenance**: A — pinned by `test_digital_twin_fram.py:219-237`.
- **Truth**: partly — FRAM: the file's `size` field is ignored, so another chip's image loads partially (TWIN.N150); SCD30: a non-dict JSON raises on `.get()` (TWIN.N128), contradicting "degrade, not raise".
- **Fit**: refines OR38.a (2) (twin state is evidence to archive, not to lose) — tension: OR38.a (2) calls the twin's FRAM/SCD30 state evidence, this rule treats a corrupt file as disposable · pillar P7
- **Home**: digital_twin/README.md (stays)

### G7.027 Twin persistence is single-instance per chip type
- **Req**: The twin persists only the last-wired SCD30 and FRAM instance (module-global chip handles); devices with several instances (synthetic fixtures only today) are accepted as not persisting the others, and a real multi-instance device makes per-instance persistence required work in the same change.
- **Class**: test-standard
- **Sources**: TWIN.N046, TWIN.N047, TWIN.N083, TWIN.N238, TWIN.N332 · stated at digital_twin/README.md:354-358; digital_twin/machine.py:151-167; SPECIFICATION.md L.4 (:6308-6310); tests/test_digital_twin_machine.py:201-203
- **Provenance**: A — `df2359c` (2026-09-13).
- **Truth**: holds — `_current_scd30_chip` single global; `configure_fram_state_path()`/`flush_fram()` have no dedicated wiring test (TWIN.N238).
- **Fit**: extends OR43.a (extensibility beyond six devices) · pillar P10
- **Home**: digital_twin/README.md + SPEC L.4 (two places) → one

### G7.028 Read twin FRAM logs before clearing them
- **Req**: The FRAM-first rule applies inside the twin: before any `ResetErrors` or state wipe, the twin's persisted error logs are read, since `_fram_chip.py` models the production chunk layout.
- **Class**: process
- **Sources**: TWIN.N334 · stated at CLAUDE.md FRAM-log rule ("The same check applies inside the digital twin")
- **Provenance**: A — CLAUDE.md hard rule born of the 2026-09-08 incident (the base rule is an owner-era hard rule; the twin extension has no owner wording).
- **Truth**: partly — `scripts/run_digital_twin_ci.sh:10` wipes `digital_twin/*.json` and `digital_twin/config/` before every run with no read or archive; OR38.a (2) now requires archiving twin FRAM/SCD30 state instead of deleting it.
- **Fit**: restates CLAUDE.md FRAM rule; refined by OR38.a (2) · pillar P7
- **Home**: CLAUDE.md (stays) + runner archive step

### G7.029 One fault vocabulary, every operation hooked
- **Req**: Twin faults are queued per operation name through one `FaultInjector`, and every chip-fake handler calls both `maybe_raise()` and `maybe_hang()` for every bus operation the driver issues, so the `--fault`/`--hang` vocabulary (shared by `launch.py` and `run_generic_integration.py`) can reach each of them; faults stay bounded (a count, or a hang that ends) so a run cannot block the loop forever unless the hang case is the subject.
- **Class**: test-standard
- **Sources**: TWIN.N034, TWIN.N051, TWIN.N063, TWIN.N064, TWIN.N075, TWIN.N110, TWIN.N111, TWIN.N112, TWIN.N142, TWIN.N161, TWIN.N162, TWIN.N191, TWIN.N193, TWIN.N194, TWIN.N196, TWIN.N201 · stated at digital_twin/_fault_injection.py:2-32; digital_twin/README.md:144-147, :443-446, :673-690; launch.py:22-80, :194-213
- **Provenance**: A — `dfb8975` (2026-08-20).
- **Truth**: partly — BMP3xx and ISL29125 `handle_writeto` (the zero-byte ACK probe) have `maybe_raise` but no `maybe_hang` (TWIN.N142/N162); faults key on op name only (no per-command/address targeting, one exception instance reused); WLAN faults are permanent and WLAN has no hang (deliberate, TWIN.N194); stuck-INT is reachable only programmatically; the twin FRAM fake lacks the mock's WEL knobs.
- **Fit**: refines OR21.a (1) (hardware tests' faults injected in the twin), OR22.a (3) · pillar P5
- **Home**: digital_twin/README.md (stays)

### G7.030 Bounded faults run one driver per process and must recover
- **Req**: A test that needs several drivers to log a chip-healthy error gives each its own process, because three simultaneous driver faults exhaust the supervisor's restart budget and reboot the device; after a bounded fault clears, the reader's recovery (log, recover, readings resume) is asserted, not assumed.
- **Class**: test-standard
- **Sources**: TWIN.N055, TWIN.N269, TWIN.N303, TWIN.N345 · stated at digital_twin/README.md:491-497, :621-624; scripts/_digital_twin_ci_suite.py:840-842; SPECIFICATION.md C.4 (:1660-1662)
- **Provenance**: A — `10ec130` (2026-09-18).
- **Truth**: partly — Run 5c chains one fault per process; README.md:621-624's recovery statement for a manual `--fault` run is not demonstrated there (TWIN.N345); combined multi-driver faults stay untested by design (OR41.a asks for the risky higher-order combinations — a gap to decide).
- **Fit**: refines OR22.a (3), OR41.a (1); tension with OR41.a (three-way pile-ups) · pillar P3, P5
- **Home**: digital_twin/README.md + SPEC C.4 (stays)

### G7.031 The twin fault matrix follows each device's wiring
- **Req**: The twin CI suite's fault matrix is derived from each device's own wiring plan, covers every bus-attached driver and every instance of a multi-instance driver, and names what it leaves out (GPIO-only drivers, `uart_link`); deliberate per-driver asymmetries (SGP40 outside the reset-to-0-with-FRAM-faulted set) are stated where the tables live.
- **Class**: test-standard
- **Sources**: TWIN.N049, TWIN.N206, TWIN.N245, TWIN.N276, TWIN.N287 · stated at digital_twin/README.md:420-422; run_generic_integration.py:194-196; scripts/_digital_twin_ci_suite.py:1264-1266; tests_scripts/test_digital_twin_ci_suite_errcount.py:328-333
- **Provenance**: A — suite commits.
- **Truth**: partly — `--fault`/`--hang` address only the first instance of a multi-instance driver (plain driver name, `run_generic_integration.py:194-196`); `uart_link` instances are never fault-injected.
- **Fit**: refines OR41.a (1), OR43.a (no device-count assumption) · pillar P5
- **Home**: scripts/_digital_twin_ci_suite.py + digital_twin/README.md

### G7.032 Twin suite copies of product values are derived or cross-checked
- **Req**: A value the twin CI suite needs from `src/` or buildgen (errnos, hotspot fallback count, restart budget, the per-driver tables `_BUS_FAULT_OPS`, `_DRIVER_ERRCOUNT_NAME`, `_MEASUREMENT_DRIVERS`, `_NO_PERSIST_WHEN_FRAM_FAULTED`, `_PERSISTED_ERROR_MODULES`) is read from the source or pinned by a test against it, and the outward check covers every bus-attached driver kind, not only i2c/spi.
- **Class**: test-standard
- **Sources**: TWIN.N261, TWIN.N262, TWIN.N263, TWIN.N285, TWIN.N286 · stated at scripts/_digital_twin_ci_suite.py:61-106; tests_scripts/test_digital_twin_ci_suite_errcount.py:100-134
- **Provenance**: A — `5506f44`/`7036dc7` (2026-09-17/19), after ISL29125 was silently absent from every table "from the day it shipped".
- **Truth**: partly — the driver tables are cross-checked (`test_digital_twin_ci_suite_errcount.py`); `_NTP_ERRNO_NO_REPLY = 21` and `_WIFI_SCRIPTED_FAILURES = 5` are hand copies with no pin found (grep in tests_scripts: none).
- **Fit**: refines P8 (generated not copied), OR28.a (3) (errno catalog) · pillar P8
- **Home**: scripts/_digital_twin_ci_suite.py → tests_scripts pins

### G7.033 SPI RX-overrun is modelled in both fakes
- **Req**: The rp2 SPI read-only RX-overrun `OSError(EIO)` (reads of 32 bytes or more) is modelled at the bus level in both `tests/machine.py` and `digital_twin/machine.py`; the twin's chip-level `FaultInjector` must not raise it for reads the real port cannot fail.
- **Class**: platform
- **Sources**: TWIN.N318, TWIN.N319 · stated at SPECIFICATION.md F.5.2 (:3725-3731)
- **Provenance**: F — Part F.5.2 (rp2 source, 1.29 raise site per CLAUDE.md); fakes A (`6bea0b5`).
- **Truth**: partly — both bus fakes carry `rx_overrun`; the twin FRAM `FaultInjector` over-approximates (a 1-byte status read can raise, TWIN.N319); rp2 source not re-read here.
- **Fit**: refines OR17.a (match vs mismatch row) · pillar P5
- **Home**: SPECIFICATION.md F.5.2 (stays)

### G7.034 The twin suite runs at both GC stages
- **Req**: The twin CI suite runs its whole run sequence twice per device, first at `gc.threshold(-1)`, then at 32768, each pass failing on any `MemoryError` or `memory allocation failed` marker; the twin's default threshold equals the generated boot entry's value by derivation or a pinning test, and every statement of the run count is current.
- **Class**: test-standard
- **Sources**: TWIN.N048, TWIN.N203, TWIN.N300, TWIN.N339, TWIN.N343 · stated at digital_twin/README.md:412-420; SPECIFICATION.md A.10 (:677-682); README.md:468-469, :510-514; run_generic_integration.py:36-39, :425-428
- **Provenance**: O — OR40 "all tests to pass with gc threshold set to default, as well as to the agreed value as two full passes"; CLAUDE.md memory rule (owner's standing direction).
- **Truth**: partly — both passes run (`_digital_twin_ci_suite.py`, 14 runs each, verified by the harvest at :633-1111, :1310); `_GC_THRESHOLD_DEFAULT = 32768` (`run_generic_integration.py:39`) is a hand copy of `buildgen/codegen.py:704` with no pinning test found.
- **Fit**: restates OR40.a (2) · pillar P5, P6
- **Home**: digital_twin/README.md + SPEC A.10 + README.md (three statements) → Part E

### G7.035 Assertions read strictly and fail closed
- **Req**: A twin-suite assertion reads device state through a strict reader that raises on an unparseable or missing response, never through the tolerant polling reader; defaults at assertion sites fail closed (`get("counter", -1) == 0`), a "served" verdict requires a parsed JSON body, and recovery notices are logged as `W` so error counts stay meaningful.
- **Class**: test-standard
- **Sources**: TWIN.N052, TWIN.N054, TWIN.N061, TWIN.N266, TWIN.N284, TWIN.N288, TWIN.N289, TWIN.N374, TWIN.N376 · stated at scripts/_digital_twin_ci_suite.py:294-318; tests_scripts/test_digital_twin_ci_suite_errcount.py:140-252; test_digital_twin_ci_suite_ceiling.py:126-127; digital_twin/README.md:453-471, :644-656
- **Provenance**: A — `d370413` (2026-09-17: "the vacuous-pass class is unrepresentable at an assertion site rather than forbidden by a comment").
- **Truth**: partly — strict/tolerant split exists and is used at every assertion found (`:746, :854, :870, :996, :1019, :1025`); README.md:644-649 names SCD30/BMP3XX while the set also holds `isl29125` (`_digital_twin_ci_suite.py:79`); README.md:453-456's "BACKLOG.md" pointer names no item.
- **Fit**: refines OR21 (silent blindness) · pillar P5
- **Home**: digital_twin/README.md "suite-table subtleties" (fix wording)

### G7.036 Retry only a measurement with a known noise source
- **Req**: The twin soak's memory-trend verdict may retry once with a fully independent boot, and its tolerance is derived from each attempt's own observed noise; HTTP, watchdog and shutdown failures are never retried.
- **Class**: test-standard
- **Sources**: TWIN.N265, TWIN.N275 · stated at scripts/_digital_twin_ci_suite.py:134-137, :1176-1182; SPECIFICATION.md E.9 (:3411-3421)
- **Provenance**: A — `846c78b` (2026-09-14).
- **Truth**: holds — E.9 text and `_run_11_soak()`.
- **Fit**: refines OR37.a (2) ("a retry never counts as a race fix") — this retry is a measurement repeat, not a race fix; consistent · pillar P5
- **Home**: SPECIFICATION.md E.9 (stays)

### G7.037 Waits poll for a condition; fault environments are deterministic
- **Req**: Twin-suite checks poll for the observable condition with a deadline instead of sleeping or racing on host speed (the bench Pi4 is ~4x slower than an x86 runner), and each fault environment is deterministic (e.g. NTP unreachability uses RFC 5737 TEST-NET-1 `192.0.2.1`).
- **Class**: test-standard
- **Sources**: TWIN.N053, TWIN.N267, TWIN.N273 · stated at digital_twin/README.md:462-465; scripts/_digital_twin_ci_suite.py:383-385, :1000-1001
- **Provenance**: A — `dfb8975` (TEST-NET-1); F for RFC 5737.
- **Truth**: holds — as stated; not exhaustively checked for remaining fixed sleeps.
- **Fit**: refines OR37.a (3), OR38 · pillar P5
- **Home**: SPECIFICATION.md H.8.1 already states "poll ..., never a fixed sleep" for JS → one Part E rule for all tiers

### G7.038 Twin durations and tolerances are measured tunables
- **Req**: Every twin-tier duration, timeout, warm-up count and tolerance (Run 10 `--duration 15`, boot timeout 30 s, soak warm-up 100, trend tolerance, the timeout just above `outer_cap_s`, the `ResetErrors` client timeout, test tolerances such as BMP3xx's 1e-4 and ISL29125's 4 %) carries its measurement, margin and re-check trigger in the tunables register, and moves with the value it is derived from.
- **Class**: test-standard
- **Sources**: TWIN.N056, TWIN.N059, TWIN.N060, TWIN.N062, TWIN.N066, TWIN.N220, TWIN.N236, TWIN.N264, TWIN.N274, TWIN.N292, TWIN.N308, TWIN.N314, TWIN.N363, TWIN.N373 · stated at digital_twin/README.md:548-666, :705-708; scripts/_digital_twin_ci_suite.py:122-125, :1042-1044; tests_scripts/test_digital_twin_generated_boot.py:31-41; SPECIFICATION.md E.7, E.9; commits b3495d8, 44e39e0/90aece7
- **Provenance**: O — OR30 ("runtime and timeout parameters for test suites ... make a list and document"); the values themselves A (dated single measurements).
- **Truth**: partly — values carry dated rationale in prose; no `@tunable` tags or register exist yet (OR30.a (2)); E.7's figures are against a retired runner (TWIN.N308).
- **Fit**: restates OR30.a (1)-(3) · pillar P8
- **Home**: code comments/README → SPECIFICATION.md tunables section (OR30.a)

### G7.039 A twin soak's wall clock is never bisected
- **Req**: The twin soak's runtime is GC-timing-driven, so it bounds liveness only (budget above the observed range) and is never bisected to a code change; the suite counts operations instead of timing them.
- **Class**: test-standard
- **Sources**: TWIN.N309, TWIN.N348, TWIN.N367 · stated at SPECIFICATION.md E.7 (:3265-3269); BACKLOG.md:677-682; commit 7cf989d
- **Provenance**: A — `7cf989d` (2026-09-11).
- **Truth**: holds — E.7 wording present; E.9 notes the soak no longer measures duration at all.
- **Fit**: refines OR30.a (3) · pillar P5
- **Home**: SPECIFICATION.md E.7 (stays); BACKLOG copy → remove (duplicate)

### G7.040 Test logic runs outside the twin process
- **Req**: Anything that can run outside the twin's MicroPython process without losing coverage runs in the host driver: request driving, history, verdicts and soak loops live host-side, and the DUT emits only values that exist nowhere else (`gc.mem_free()`) through the narrowest channel, a fixed-timer log line; a fix under this rule must end strictly more capable of catching the real case.
- **Class**: test-standard
- **Sources**: TWIN.N039, TWIN.N040, TWIN.N259, TWIN.N290, TWIN.N312, TWIN.N313, TWIN.N341, TWIN.N368, TWIN.N370, TWIN.N377 · stated at SPECIFICATION.md E.9 (:3377-3421); digital_twin/README.md:205-224; README.md:474-478; scripts/run_unix_port_integration.sh:24-26
- **Provenance**: O-confirmed — OR20.a "Owner chose (a) ... SPECIFICATION.md Part E.9 'Driver/DUT process separation' made the rule everywhere and retrofitted"; E.9 text A (`021e733`, 2026-09-14).
- **Truth**: holds — `run_generic_integration.py:288-295` is the only in-DUT sampler; the in-suite L2 integration tier starts only the tasks each test needs, never the full supervisor (TWIN.N040), a scope limit covered by the CI suite.
- **Fit**: restates OR20.a · pillar P5
- **Home**: SPECIFICATION.md E.9 (stays)

### G7.041 The twin's memory sampler settles before measuring
- **Req**: The one in-DUT `gc.collect()` (the memory sampler's baseline) is let to complete and settle before `gc.mem_free()` is read, never measured in the next statement; no other twin or tool code calls `gc.collect()` as a tool, and a threshold change is never accepted as a memory fix.
- **Class**: test-standard
- **Sources**: TWIN.N327, TWIN.N328, TWIN.N369 · stated at SPECIFICATION.md I.4 (:5057-5071); digital_twin/run_generic_integration.py:288-295; commits dfabc76→e75864f
- **Provenance**: O — OR39.a (1) "gc.collect is allowed IN TESTS for getting a clean baseline ... (but it takes time to complete, don't call and measure in the next line of code)"; threshold-not-a-fix is O (`e75864f`: "The project owner rejected that framing").
- **Truth**: partly — `run_generic_integration.py:294-295` calls `gc.collect()` and prints `gc.mem_free()` on the very next line; I.4's "don't re-flag it without new evidence" (A, `2bd5aa5`) predates OR39.a; `segfault_stress_repro.py:92` calls `gc.collect()` before a hammer round with no measurement baseline (a tool use).
- **Fit**: restates OR39.a (1); conflicts SPEC I.4 "don't re-flag" (later owner row wins) · pillar P5
- **Home**: SPECIFICATION.md I.4(e) exception text → reworded per OR39.a

### G7.042 Twin instrumentation buffers are bounded or cleared
- **Req**: Every twin introspection buffer is either bounded (`I2C.log`/`SPI.log`/WDT log/NeoPixel `writes`, keep-last-200) or cleared by its owner (`UARTLink.wire_log`: each unit test clears it, the runner's clearer every 5 s), so harness growth never reads as a product leak; a log-window assertion stays inside the bound.
- **Class**: test-standard
- **Sources**: TWIN.N013, TWIN.N058, TWIN.N078, TWIN.N187, TWIN.N208, TWIN.N227, TWIN.N253, TWIN.N310, TWIN.N371 · stated at digital_twin/machine.py:13-14; neopixel.py:6-9; digital_twin/README.md:41-45, :603-609; run_generic_integration.py:298-308; SPECIFICATION.md E.8 (:3296-3307)
- **Provenance**: A — `8cf7b81` (2026-08-13, after a real MemoryError leak), `dde4b31` (clearer).
- **Truth**: partly — bounds present (`_LOG_MAXLEN`); `tests/test_digital_twin_bus_hazard_concurrency.py:364-370` asserts on a shared bus log during a ~75 s run and only assumes it never wraps; the 200-entry `I2C.log` is described as read by nothing.
- **Fit**: refines OR38.a (1) (no leaks), OR20 (instrumentation distortion) · pillar P5
- **Home**: digital_twin/README.md + SPEC E.8 (stays)

### G7.043 Deliberately narrow L2 scopes are named with their cover
- **Req**: Where an L2 test deliberately boots one device or a subset (wozi-only middle-tier integration, wozi's frozen website under the Unix port, the generated-boot smoke with a stub `frozen_html` and GETs only, `launch.py`'s raw-bus demo), it states why and which other test covers the rest; nothing assumes a fixed device count.
- **Class**: test-standard
- **Sources**: TWIN.N033, TWIN.N192, TWIN.N244, TWIN.N249, TWIN.N291, TWIN.N342 · stated at tests/test_digital_twin_sensortask_integration.py:1-3; test_digital_twin_real_website_integration.py:121-134; tests_scripts/test_digital_twin_generated_boot.py:43-47; digital_twin/README.md:129-133; README.md:504-507
- **Provenance**: A — `a974244`, `6b74a4b` (2026-09-17/19).
- **Truth**: holds — each file carries its reason; whether the named cover exists for each device is for the OR45.a matrix (not checked).
- **Fit**: refines OR43.a (device set derived), OR45.a (4) · pillar P5
- **Home**: test headers (stay)

### G7.044 Manual twin instruments stay outside CI
- **Req**: Manual-only twin tools (`segfault_stress_repro.py`, the frozen 32-bit/64-bit twins, the scratchpad measurement tools) are never a committed build variant or CI gate; each kept one has a written purpose, and each passes the OR33.a necessity verdict (keep, move its idea, or retire with the recipe documented).
- **Class**: tooling
- **Sources**: TWIN.N076, TWIN.N190, TWIN.N214, TWIN.N216, TWIN.N357, TWIN.N358, TWIN.N378 · stated at digital_twin/README.md:886-894; segfault_stress_repro.py:1-6, :79-92; HEAP_FRAGMENTATION_MEASUREMENTS.md:295-311; launch.py:2; commit fef11ad
- **Provenance**: O — HEAP_FRAGMENTATION_MEASUREMENTS.md:295-296 "Both frozen twins are ad-hoc instruments, never a committed build variant or CI gate (owner, 2026-09-23)".
- **Truth**: holds — no CI or `tests/` reference to `segfault_stress_repro.py` (per README); it applies no UDP shim, unwedge or threshold (TWIN.N216) and targets a bug recorded as fixed, so its necessity is open (OR33.a); `launch.py` hand-rolls argument parsing because the Unix-port `argparse` lacks `action="append"`/`choices=` (not verified: micropython-lib not in the scratch checkout).
- **Fit**: restates owner 2026-09-23; feeds OR33.a · pillar P4
- **Home**: digital_twin/README.md + HEAP file

### G7.045 One end-to-end boot per test process
- **Req**: A `tests/` file boots the real supervised task graph at most once per process, because cancelling `main()` leaves its sibling tasks parked (no parent/child tracking in MicroPython asyncio); the runner's forced `sys.exit()` ends the process regardless.
- **Class**: test-standard
- **Sources**: TWIN.N213, TWIN.N246 · stated at tests/test_digital_twin_run_generic_integration.py:227-229; digital_twin/run_generic_integration.py:401-403; CLAUDE.md "Known hang cause #2"
- **Provenance**: A — CLAUDE.md known-cause bullet (session finding).
- **Truth**: holds — single boot in that file; `tests/microtest.py` exits explicitly (per CLAUDE.md, not re-read).
- **Fit**: refines OR38.a (1) · pillar P5
- **Home**: CLAUDE.md (stays)

### G7.046 The twin HTTP client's contract
- **Req**: The twin's HTTP client relies on the server always answering `Connection: close` (no keep-alive support), treats an empty response as a connection-ceiling refusal (`CeilingRefusedError`, an `OSError`), decodes only JSON objects, and `.json()` is never called on a drained (`read_body=False`) response.
- **Class**: test-standard
- **Sources**: TWIN.N030, TWIN.N031, TWIN.N165, TWIN.N166, TWIN.N167, TWIN.N168 · stated at digital_twin/_http_client.py:2-35; digital_twin/README.md:119-124
- **Provenance**: A — `25fc2fe` (2026-08-13).
- **Truth**: partly — every empty response, not only a refusal, is classed as a ceiling refusal by callers' `except OSError` (TWIN.N031, a blindness risk under OR21); `type: ignore[type-var]` at `:35` is a stub gap with no removal trigger.
- **Fit**: refines OR21 · pillar P5
- **Home**: digital_twin/README.md (stays)

### G7.047 Twin tests derive expected values independently
- **Req**: A twin test checks the chip fake against an independently written formula (BMP3xx compensation, Newton inversion), not the driver's own code, and its tolerance states the error budget it covers.
- **Class**: test-standard
- **Sources**: TWIN.N139, TWIN.N197, TWIN.N219 · stated at tests/test_digital_twin_bmp3xx.py:1-57; digital_twin/launch.py:216-219; _bmp3xx_chip.py:62-86
- **Provenance**: A — twin test commits; matches O OR19.a (2) ("Expected values come from an independent source").
- **Truth**: holds — three copies (driver, test, launcher) exist by design; nothing ties any of them to the datasheet's worked example (not checked against `datasheets/bmp3xx`).
- **Fit**: restates OR19.a (2) · pillar P5
- **Home**: test headers (stay)

### G7.048 Twin config and state paths are per run
- **Req**: A twin run's config directory and state files are owned by that run (per-run scratch, or one tier at a time under a lock), not a fixed repo-relative path shared with other suites and runs.
- **Class**: test-standard
- **Sources**: TWIN.N202 · stated at digital_twin/run_generic_integration.py:31, :346
- **Provenance**: O — OR38.a (1) "anything shared between tests or parallel processes (... file names, directories, state files ...)".
- **Truth**: partly — `_CONFIG_DIR = "digital_twin/config/"` is fixed; `scripts/run_digital_twin_ci.sh:10` wipes it per run (sequential only).
- **Fit**: restates OR38.a (1) · pillar P9
- **Home**: digital_twin/run_generic_integration.py

### G7.049 One policy on both sides of the src/↔js/ boundary
- **Req**: Any policy the website encodes on behalf of the device (validation, coercion, bounds, result words, dispatch semantics, masking, envelopes) is identical to `src/`'s and changes in the same change on both sides; there is no backend-only or frontend-only validation or coercion policy. The pairs this binds are listed under G7.050-G7.053, G7.085 and G7.086.
- **Class**: code-style
- **Sources**: WEB.N230, WEB.N231, WEB.N246, WEB.N250, WEB.N258, WEB.N264 · stated at SPECIFICATION.md G.1 (:4161-4172), G.2 (:4246-4248), H.4 (:4372, :4385-4388), H.6 (:4527-4529); CLAUDE.md bird's-eye-scan rule
- **Provenance**: A — `0e94354` (2026-08-25, "Add SPECIFICATION.md Part G").
- **Truth**: partly — most pairs hold (G7.050); divergences found: radio string bounds (G7.086), unknown `/sensors` sub-key (WEB.N082), stale post-write gap (G7.068).
- **Fit**: refines OR24.a (2) (public interfaces change with every consumer) · pillar P8
- **Home**: SPECIFICATION.md G.1/G.2 (stays); CLAUDE.md scan rule points to it

### G7.050 The mock backend matches the real API field for field
- **Req**: `js/mock-server.js` answers exactly as the real server does, bound for bound and quirk for quirk, and every mirrored value is either read from its source or pinned by a test. Mirrored pairs: REST path set ↔ `asy_webserver_service.py` routes; `SYSTEM_CMDS` ↔ `_SYSTEM_CMDS`; `PAUSE_TIME_MAX` ↔ `_PAUSE_TIME_MAX`; `PauseTime` dispatch ↔ `_dispatch_notification_pause()`; `lightCmdLED` bounds and Invalid/Failed split ↔ generated `_FIELD_LED_*`; number/string/enum strictness ↔ `type_or_range_error()`; special values bypass min/max ↔ `_special_bypass()`; malformed body = 200 `res:"ERR"` ↔ `make_response(1)`; 405 code 4 ↔ server; envelope ↔ `api_response.py`; settings GET shape ↔ `_get_settings_flat()`; `ResetErrors` refill ↔ `print_log.py reset()`; `PW` masking ↔ `_mask_pw()` (PW and HotspotPW); `ForceCalRef` reads 400, command-only fields omitted ↔ SCD30/SGP40/ISL29125 schema; `/status` PUT has no per-field result ↔ `_put_status`; unknown field handling ↔ `ConfigManager`.
- **Class**: code-style
- **Sources**: WEB.N071, WEB.N072, WEB.N073, WEB.N075, WEB.N077, WEB.N078, WEB.N080, WEB.N081, WEB.N082, WEB.N083, WEB.N084, WEB.N086, WEB.N087, WEB.N088, WEB.N089, WEB.N093, WEB.N097, WEB.N098, WEB.N099, WEB.N101, WEB.N102, WEB.N103, WEB.N104, WEB.N105, WEB.N106, WEB.N107, WEB.N108, WEB.N213, WEB.N214, WEB.N215, WEB.N217 · stated at js/mock-server.js:7-461; tests_js/mock-server.test.js:247-368; tests_js/render.test.js:418-420; SPECIFICATION.md H.4 (:4385-4388)
- **Provenance**: A — `e5f31f2`/`0e94354` (Part G, 2026-08-25/09-09); constants' comments A.
- **Truth**: partly — spot-checked `SYSTEM_CMDS`/`PAUSE_TIME_MAX`/LED bounds match (`mock-server.js:16-17, :181-184` vs `buildgen/codegen.py:539-542`); pins are comments, not tests; divergences: unknown `/sensors` sub-key silently dropped where the server reports Invalid + errno 10 (WEB.N082/N102), composites never stored or "Unchanged" (N083), action keys stripped on every endpoint (N103), `/status` PUT ignores non-true `ResetErrors` and other keys (N106), mask covers PW only (N108), never "Failed" for sensor dispatch (N088/N214), malformed body throws instead of `res:"ERR"` (N101).
- **Fit**: restates G.2; refines OR43.a (2) ("the mock server and mock data match the real API field for field") · pillar P8
- **Home**: SPECIFICATION.md G.2/H.4 (stays); pins → tests_js/tests_scripts

### G7.051 The website client follows the server's wire contract
- **Req**: The client's hard-coded knowledge of the server wire format matches the server and moves with it: the generated `_gmtimestruct_to_dict()` key names; the errcount history shape (`num`, `type`, no timestamp); the shaped-error `descr` for 400/404/405/413/500; `/sensors` PUT nesting under the sensor key; envelope failure detection (`res:"ERR"` at 200, non-2xx); `/status` PUT's lack of a per-field result; `PauseTime` read from `GET /status`'s `notification`; the `${sensor}_${field}` flattening of `/status` sensors (`UARTLINK_*`, `SGP40_*`); `SystemCmd` never returned by GET; `mempause`'s fixed 300 s in its label.
- **Class**: code-style
- **Sources**: WEB.N048, WEB.N056, WEB.N130, WEB.N139, WEB.N141, WEB.N142, WEB.N150, WEB.N153, WEB.N160, WEB.N192, WEB.N197 · stated at js/field-format.js:27-31; js/definitions.js:48-49; js/render.js:42-45, :211-244, :357-411; js/templates.js:109-114; html/definitions/wozi.json:197; html/definitions/dev.json:964-970
- **Provenance**: A — website sessions (`999aada` onward).
- **Truth**: partly — mirrors are by comment; a key rename on the Python side renders "undefined" silently (WEB.N048); a sensor name containing `_` makes flattened keys ambiguous (N153); `ResetErrors=false` shows Valid though the server treats it as a no-op (N142).
- **Fit**: refines OR24.a (2), OR43.a (1) ("GET returns exactly what PUT accepts") · pillar P8
- **Home**: JS comments → SPECIFICATION.md A.8/H (one wire-contract table)

### G7.052 Hand-kept website duplicates are pinned or derived
- **Req**: Every value duplicated between buildgen/Python and the website (the six-endpoint section skeleton and `pollIntervalMs`, dispatch-group bounds and `SystemCmd` options, `SUPPORTED_SCHEMA_MAJOR` ↔ `SCHEMA_VERSION`, the `decimals` 0-100 bound, `path`-only-on-readonly, the definitions shape check, the TOML `max_connections` reader, errcount keys ↔ live `/status`, `websiteVersion` ↔ `WEBSITE_VERSION`) is derived from one source or pinned by a test that fails on drift in either direction.
- **Class**: tooling
- **Sources**: WEB.N026, WEB.N027, WEB.N028, WEB.N033, WEB.N034, WEB.N036, WEB.N037, WEB.N059, WEB.N062, WEB.N065, WEB.N263 · stated at buildgen/definitions.py:25-85, :373-375; js/definitions.js:59, :70-72, :213-216; tests_scripts/test_buildgen_definitions.py:126-170; test_buildgen_web_tag.py:127-128; test_digital_twin_generated_boot.py:113-133; test_live_twin_ceiling_parser.py:1-3
- **Provenance**: A — buildgen website commits.
- **Truth**: partly — enforced: errcount keys at twin boot, TOML ceiling reader, `path` rule on both sides, decimals bound (`buildgen/web_tag.py:33` and `definitions.test.js`); not enforced: `_shape_problems` vs `validateDefinitions()` (no Node round trip), `SUPPORTED_SCHEMA_MAJOR = 1` vs `SCHEMA_VERSION = "1.0.0"` (no cross-language test found), hand-written `wozi.json`/`dev.json` `"websiteVersion": "2.0b0"` vs `buildgen/version.py:8` (unpinned), bounds mirrored "by cross-reference/comment" (H.5.1).
- **Fit**: refines P8, OR43.a (3) · pillar P8
- **Home**: tests_scripts/tests_js pins

### G7.053 The client gives up exactly when the server does
- **Req**: `js/poll-manager.js`'s `DEFAULT_TIMEOUT_MS` equals `asy_webserver_service.py`'s `outer_cap_s`, pinned structurally by a test, so a slow request surfaces as the server's own abort; the client timeout covers the whole request including the body read.
- **Class**: product
- **Sources**: WEB.N038, WEB.N115, WEB.N116, WEB.N227, WEB.N243 · stated at SPECIFICATION.md H.4 (:4368); js/poll-manager.js:7-13, :63; tests_scripts/test_request_timeout_ceiling.py:62-70
- **Provenance**: A — `fd209b3` (2026-09-17).
- **Truth**: partly — equality pinned (`test_request_timeout_ceiling.py:62-67`); the client clock starts before connect while the server's starts at accept, so the client nearly always aborts first — H.4's stated purpose is not met; the timer is cleared once headers arrive and `response.text()` is unbounded (WEB.N116).
- **Fit**: refines OR30.a (tunable with a dependant) · pillar P2
- **Home**: SPECIFICATION.md H.4 (stays)

### G7.054 The dispatch-only field set comes from the @web tags
- **Req**: Which PUT fields are dispatch-only (re-dispatched on every submit, never persisted, never "Unchanged") is derived from the `@web` `dispatch=true` tags plus the webserver's own action fields, never from a hand list; every consumer (renderer, mock, test matrices, persistence-write gate, docs) reads the derived set, and a command-only toggle whose state GET cannot report (`ContMeas`) is classified the same way everywhere.
- **Class**: code-style
- **Sources**: WEB.N057, WEB.N074, WEB.N090, WEB.N138, WEB.N191, WEB.N193, WEB.N199, WEB.N210, WEB.N247, WEB.N252, WEB.N281 · stated at SPECIFICATION.md H.4 (:4383), H.5 (:4400-4410), H.6 (:4524-4527); js/mock-server.js:22, :244; tests_js/_put_field_cases.js:14; tests_js/mock-server-put-matrix.test.js:26; tests_js/live-backend-put-matrix.test.js:21
- **Provenance**: A — `d370413` (2026-09-17): "the row now says to derive the set from the tags rather than trust the list".
- **Truth**: drifted — at least five disagreeing hand lists: H.4 (4 + tagged `SGPResetVOC`, `ISLCalibrate`), H.6 (adds `ContMeas`, omits `ISLCalibrate`), `mock-server.js:22` `SENSOR_QUIRK_FIELDS` {ForceCalRef, ContMeas, SGPResetVOC, ISLCalibrate} plus `:244`'s own list, `_put_field_cases.js:14` {SystemCmd, PauseTime, lightCmdLED, ResetErrors}, `mock-server-put-matrix.test.js:26` (+PW); `ContMeas` carries no `dispatch` tag (`src/asy_scd30_driver.py:89`) and `defaultValue=true`, so after a reload the toggle shows On even when measurement was stopped (WEB.N191/N199).
- **Fit**: refines P8; relates OR42.c (2) (ContMeas stays a command) · pillar P8
- **Home**: SPECIFICATION.md H.4/H.6 (two lists) → one statement + derived set

### G7.055 Website definitions have one source: the @web tags
- **Req**: Every device's `definitions.json` is generated at build time from the `@web`/`@web-group` tags; no hand-written `html/definitions/<device>.json` exists, `tests_js` loads generated definitions, and adding a driver field needs only its tag.
- **Class**: product
- **Sources**: WEB.N008, WEB.N016, WEB.N032, WEB.N188, WEB.N189, WEB.N190, WEB.N194, WEB.N196, WEB.N253, WEB.N255, WEB.N269, WEB.N270 · stated at scripts/build_website.sh:26-28; SPECIFICATION.md H.5 (:4424-4426), H.5.1 (:4502-4505), K.4 (:5802-5804), C.11 item 9; BACKLOG.md:140, :505-507, :821-832; digital_twin/README.md:838-842
- **Provenance**: O — OR43.a (3) "the hand-written `html/definitions/wozi.json`/`dev.json` are retired, the `@web` tags become the only source, and `tests_js/` loads generated definitions".
- **Truth**: drifted — the two files still exist and are shipped for wozi/dev; their provenance is stated three ways: "hand-written" (`build_website.sh:26`, H.5), "never hand-maintained" (K.4:5804), "generated *and committed*" (BACKLOG.md:507); C.11 item 9 and `digital_twin/README.md:838-842` still instruct a hand update; the golden comparison is order-insensitive (WEB.N032); H.5.1's "done as of Session 6" names an undefined session.
- **Fit**: restates OR43.a (3) · pillar P8
- **Home**: SPECIFICATION.md H.5/H.5.1/K.4/C.11 → H.5.1 only

### G7.056 No website tier assumes a device set
- **Req**: Every website build, test, preview and live tier derives its device set from `devices/*.toml` at run time; no hard-coded device list or count (`KNOWN_DEVICES`, wozi-only `pretest` build, wozi-only live twin, wozi/dev-only fixtures) remains, so a new device is picked up with no edit.
- **Class**: test-standard
- **Sources**: WEB.N005, WEB.N012, WEB.N030, WEB.N040, WEB.N041, WEB.N200, WEB.N209, WEB.N211, WEB.N223, WEB.N266, WEB.N268, WEB.N282, TWIN.N337 · stated at js/app.js:15-16, :29, :46; package.json:10-17; tests_js/_live_twin_command.js:56-62; tests_js/live-backend.test.js:19; tests_js/definitions-mockdata-coverage.test.js:6-7; tests/test_website_build_integration.py:5-7; tests_scripts/test_build_website_sh.py:71-73; README.md:238-260, :430-431
- **Provenance**: O — OR43.a "Extensibility: nothing may assume six devices. Every check, test and build step derives the device set from `devices/*.toml` at run time".
- **Truth**: wrong — `js/app.js:16` `KNOWN_DEVICES = ["wozi", "dev"]` with a silent fallback to wozi; `package.json:10` builds wozi only; the live tier boots `sensortask_wozi` only; README.md:430-431 claims "every real device is fully supported end-to-end via `--device`" though four devices' definitions never pass the JS validator or renderer.
- **Fit**: restates OR43.a · pillar P10
- **Home**: code/tests; README.md claim corrected

### G7.057 Mock fixtures carry exactly the real device's fields
- **Req**: Each `mockdata/<device>.json` carries exactly the measurements, settings and errcount keys the real device publishes (no orphan sensors, no missing wired modules), checked for every field kind by a test.
- **Class**: test-standard
- **Sources**: WEB.N010, WEB.N011, WEB.N013, WEB.N014, WEB.N015, WEB.N278, WEB.N280, WEB.N284 · stated at SPECIFICATION.md H.2 (:4304), K (:5913-5916); mockdata/dev.json; tests_js/definitions-mockdata-coverage.test.js; tests_js/mock-server-put-matrix.test.js:18-21; commits e624018, 12a616a, 699836e
- **Provenance**: O — OR43.a (2) "the mock server and mock data match the real API field for field"; supersedes the owner's 2026-09-12 q5 "leave mockdata's SHTC3/MPRLS placeholders alone" (`12a616a`).
- **Truth**: wrong — `mockdata/dev.json:5-6, :13-14, :65-75` carry SHTC3/MPRLS that `devices/dev.toml` does not wire; no BMP3XX `sensorsConfig` nor `BMP3XX`/`CFGMGR_BMP3XX`/`UART_init`/`UART_resp` errcount rows though dev wires them; the coverage test checks readonly fields only; `mock-server-put-matrix.test.js:18-21` says `DEV_UNIQUE_GROUPS` "matches none of dev's real groups today" while ISL29125 is one; placeholder passwords (`"PW": "testbench42"`) are fixture values.
- **Fit**: restates OR43.a (2); conflicts owner q5 (2026-09-12) — later row wins · pillar P8
- **Home**: mockdata/ + tests_js

### G7.058 The build stages the production site and nothing else
- **Req**: `scripts/build_website.sh` stages only production files: `js/mock-server.js` and every other prototype-only file are never staged (checked against the real `html/`/`js/` contents, not a hand list), the generated `definitions.json` is written to scratch and inlined, never staged as a file, every `<` in the inlined JSON is escaped, and the exact-string contracts with `html/index.html` (stylesheet `<link>`, `</head>`, `id="inlined-definitions"`, the literal `../js/app.js` import) fail the build loudly when they drift.
- **Class**: tooling
- **Sources**: WEB.N017, WEB.N018, WEB.N019, WEB.N029, WEB.N031, WEB.N052, WEB.N068, WEB.N168, WEB.N170, WEB.N203, WEB.N234, WEB.N261 · stated at scripts/build_website.sh:19-67; SPECIFICATION.md H.2 (:4316-4320), H.8 (:4739-4741); html/index.html:29-44; js/main.js:26; js/definitions.js:231-233; tests_scripts/test_build_website_sh.py:49-127
- **Provenance**: A — `15ed282`/`35ba8ac` (2026-08-26/09-22).
- **Truth**: partly — build checks present (`build_website.sh:59-67`); the staging guard is a substring match of each file name anywhere in the script (a comment mention passes; `html/` subdirectories and non-`.js` files under `js/` unchecked, WEB.N029); the prototype-only negative list is four hand-named paths (N031); the inlined path is never exercised by dev/preview (N068).
- **Fit**: refines OR36 (nothing test-only in the image) · pillar P4
- **Home**: SPECIFICATION.md H.2 (stays)

### G7.059 One production entry, one prototype entry, no duplication
- **Req**: `js/main.js` is the only production entry (staged as `app.js`, one build-fixed device, no `?device=` switch); `js/app.js` and `js/mock-server.js` are prototype-only and everything else targets the real API; logic both entries need (`selectSection()`, startup/error-banner) lives once in a shared module.
- **Class**: code-style
- **Sources**: WEB.N039, WEB.N043, WEB.N051, WEB.N070, WEB.N265, WEB.N273 · stated at js/app.js:2-4, :72-84; js/main.js:1-5, :45-57; js/mock-server.js:1-4; README.md:240-242; BACKLOG.md:871-874
- **Provenance**: A — `999aada` (2026-08-21, website session 2).
- **Truth**: partly — entry split holds; `selectSection()` is duplicated (`js/app.js:73`, `js/main.js:46`), open in BACKLOG as low priority.
- **Fit**: refines OR24 (one material), D.10 "one shape for one job" · pillar P4
- **Home**: SPECIFICATION.md H.2 (stays)

### G7.060 The bundle is safe plain concatenation, mechanically checked
- **Req**: Production JS modules use no default exports, dynamic imports or re-exports and only single-line relative imports, so concatenation into one `app.js` is safe; the concatenation list is derived from `js/` (not hand-kept in three places), and the build or a test checks the import order and these constraints mechanically.
- **Class**: tooling
- **Sources**: WEB.N006, WEB.N007, WEB.N020, WEB.N021, WEB.N155, WEB.N235, WEB.N236, WEB.N259 · stated at SPECIFICATION.md H.2 (:4322-4329), H.7 (:4547-4548); scripts/build_website.sh:75-81; js/templates.js:6-8; tests/test_website_build_integration.py:73-137
- **Provenance**: A — `15ed282` (Part H), `582c68a` (re-export rule).
- **Truth**: drifted — H.2:4327-4328 says "every file follows the local files it imports from ... `scripts/build_website.sh` re-checks that mechanically on every build": no check exists (`build_website.sh:73-82` only concatenates), and `templates.js` (3rd) imports from `definitions.js` (4th) — harmless for hoisted functions, but the stated invariant is false; the module list is hand-kept in the banner, the loop and `test_website_build_integration.py`; bundle integrity is checked by string patterns added after manual finds (WEB.N007).
- **Fit**: extends OR16 (silently broken chains) · pillar P4
- **Home**: SPECIFICATION.md H.2 (fix claim) + build_website.sh

### G7.061 A visual redesign never touches mechanics
- **Req**: A purely visual redesign edits only `style.css`, `templates.js` and definitions labelling; `templates.js` builds every DOM element, controllers build none and reach templates only through the listed `data-*` hooks and classes, set only the semantic `data-apply-status`, and non-entry modules never query `document`. In-place poll refresh touches only number/string captions; toggles and enums remount.
- **Class**: code-style
- **Sources**: WEB.N067, WEB.N110, WEB.N126, WEB.N154, WEB.N157, WEB.N159, WEB.N166, WEB.N238, WEB.N239, WEB.N240, WEB.N248 · stated at SPECIFICATION.md H.3 (:4337-4359), H.4 (:4383); js/nav.js:2-3; js/render.js:1-3; js/templates.js:1-3, :54-56, :91, :338-341; js/definitions.js:223-225
- **Provenance**: A — `15ed282` migrated from `WEBSITE_PLAN.md`, whose visual/UX decisions were "resolved interactively with the project owner" (`999aada`); the layering rule itself is not in the owner brief (§1/§3).
- **Truth**: holds — convention only, no lint rule; two stated exceptions (cosmetic self-flip `templates.js:91`, `{grid, errorBanner}` return `:338-341`); `tests_js/templates.test.js:328` guards one hook's ownership.
- **Fit**: extends P4 (website one material) · pillar P4
- **Home**: SPECIFICATION.md H.3 (stays)

### G7.062 Node-context website modules stay DOM-free
- **Req**: A module the Node-context harness imports (`field-format.js`) has no DOM dependency, and a JSDoc type crossing the browser/Node split is a narrow local structural typedef, never an `import()` of the DOM-context type.
- **Class**: code-style
- **Sources**: WEB.N045, WEB.N046, WEB.N262 · stated at js/field-format.js:2-7; SPECIFICATION.md H.8.1 (:4768-4775)
- **Provenance**: A — `7ae2f27` (2026-08-26).
- **Truth**: holds — enforced indirectly by `tsconfig.node.json`'s DOM-free program (per H.8), not by lint.
- **Fit**: extends P4 · pillar P4
- **Home**: SPECIFICATION.md H.8.1 (stays)

### G7.063 Polling is single-flight
- **Req**: All website requests go through one single-flight queue, so live and settings groups are never fetched concurrently and a page never holds more connections than the device budget allows; a stalled request never blocks other sections indefinitely.
- **Class**: product
- **Sources**: WEB.N114, WEB.N122, WEB.N125, WEB.N242 · stated at SPECIFICATION.md H.4 (:4366); js/poll-manager.js:1-3, :11-13, :83-131
- **Provenance**: O-confirmed — `WEBSITE_PLAN.md` §4 (retired, `15ed282^`) records the single-flight poll-manager decision among those "resolved interactively with the project owner" (`999aada`).
- **Truth**: partly — `poll-manager.js:1-3` says every fetch goes through the queue, while the startup loads (`js/app.js:46`, `js/definitions.js:240`) use `fetchWithTimeout()` outside it; one global queue with no abort on section switch, no hidden-tab pause, no backoff.
- **Fit**: refines OR49.a (1) (client side of connection load) · pillar P6
- **Home**: SPECIFICATION.md H.4 (stays)

### G7.064 Every client failure path has a live diagnostic
- **Req**: Every website failure path ends in something a user or developer sees (banner, field status, console error); a diagnostic that no production path can reach is removed or rewired, and docs name where the real one lives.
- **Class**: code-style
- **Sources**: WEB.N124, WEB.N149, WEB.N202, WEB.N260 · stated at js/poll-manager.js:116-117; js/render.js:345-349; eslint.config.js:108-111; SPECIFICATION.md H.8 (:4733-4735)
- **Provenance**: A — H.8 text (`15ed282`); not owner-attributed.
- **Truth**: drifted — H.8 says `js/poll-manager.js`'s "Poll failed:" is "the poll loop's only failure diagnostic and is asserted by a test"; it sits at `poll-manager.js:117` and is tested (`poll-manager.test.js:208`) with a rejecting callback, but in production `render.js`'s `fetchOnce()` never rejects (`:350-368` catches everything), so it is unreachable.
- **Fit**: extends OR16 (silent chains), OR46 (dead code) · pillar P2
- **Home**: SPECIFICATION.md H.8 + eslint.config.js comment

### G7.065 Definitions validation is strict and visible
- **Req**: `validateDefinitions()` rejects every malformed definitions file with a visible error banner: unknown or missing field `kind`/`key`/`label`, non-object groups or fields, duplicate section/group/field keys, and a writable group whose section has no `rest.put`.
- **Class**: product
- **Sources**: WEB.N054, WEB.N063, WEB.N064, WEB.N137, WEB.N245 · stated at js/definitions.js:1-4, :150-225; SPECIFICATION.md H.4 (:4370), H.5
- **Provenance**: A — `999aada`/`15ed282` ("Strict — visible error banner on mismatch").
- **Truth**: partly — checked `definitions.js:150-225`: sections, `pollGroup`, `groups`, `fields` array, `path`/`decimals` hints and `landingSection` are validated; field `kind`/`key`/`label` are not (an unknown kind renders as a text input), a `null` group throws before the check, a null field yields no problem (`:201` comment claims the group loop owns it; it does not), no duplicate-key check, a missing `rest.put` makes Apply a silent no-op (`render.js:197-200`).
- **Fit**: extends OR23.a (3) (errors name what/where) for the browser · pillar P2
- **Home**: SPECIFICATION.md H.4/H.5 (stays)

### G7.066 The website writes only what the user changed, only on Apply
- **Req**: The website sends a PUT only on an explicit user action, never retries or repeats one, and omits every field whose input equals its current baseline (dispatch fields excepted), so a retyped identical value is not sent; the baseline follows what the server reports as stored.
- **Class**: product
- **Sources**: WEB.N058, WEB.N128, WEB.N131, WEB.N136, WEB.N143 · stated at js/render.js:26-30, :61-64, :115-121, :164-169, :247-254; js/definitions.js:55-56
- **Provenance**: O — OR42.a (3) "the website writes only on an explicit user action and never retries or repeats a PUT".
- **Truth**: drifted — `render.js:61-64` says every kind is sparse-omitted when it matches its baseline, but number/string fields are omitted only when the input is blank (`:115-117`), so a retyped identical value is sent (OR42.a records the same); whitespace-only input becomes 0, hex/exponent strings pass as numbers (N128); dispatch toggles move the baseline and are resent (N143); per-field colours persist from earlier Applies (N136).
- **Fit**: restates OR42.a (3) · pillar P9
- **Home**: SPECIFICATION.md H.4 (sparse-PUT row) + js/render.js

### G7.067 An empty string cannot be set from the website: accepted
- **Req**: Under the sparse-PUT "blank means untouched" convention the website cannot set any field to an empty string (e.g. `PW`'s open-network sentinel); this is an accepted gap, and tests treat an empty current value as having no resubmit gesture.
- **Class**: product
- **Sources**: WEB.N212, WEB.N249, WEB.N276 · stated at SPECIFICATION.md H.4 (:4379); tests_js/live-backend-put-matrix.test.js:131-132; commit cc999c1
- **Provenance**: O-confirmed — `cc999c1`: "the sparse-PUT convention can't submit an empty PW) per the project owner's explicit decision to leave both as-is".
- **Truth**: holds — `render.js:115-117` omits blank inputs.
- **Fit**: new (accepted boundary) · pillar P2
- **Home**: SPECIFICATION.md H.4 (stays)

### G7.068 A failed settings-group write reports every field Failed
- **Req**: If a settings group's post-write hook raises, every field that group attempted is reported "Failed" while the envelope still reports success; the website marks a submitted-but-unanswered field Failed as a defensive fallback only, and the mock and tests model the current server behaviour, not a closed gap.
- **Class**: product
- **Sources**: WEB.N091, WEB.N092, WEB.N134, WEB.N216, WEB.N219 · stated at SPECIFICATION.md H.6 (:4529-4531); src/asy_webserver_service.py:464-468; js/mock-server.js:251-266; js/render.js:132-135; tests_js/mock-server.test.js:545-546; tests_js/render.test.js:1016-1018
- **Provenance**: A — server fix and H.6 text (session agents).
- **Truth**: drifted — the server holds (`asy_webserver_service.py:464-468` reports "Failed" for every attempted key); `mock-server.js:251-254`, `render.js:132-135` and `render.test.js:1016-1018` still describe "a known real server-side gap" that drops the group's fields, and the mock's `partial-result` drops only the first key, not a group.
- **Fit**: refines OR26 (no silent drop) · pillar P2
- **Home**: SPECIFICATION.md H.6 (stays); JS comments and mock corrected

### G7.069 Errcount rows match the live logger instances
- **Req**: The Status page's errcount rows are keyed per logger instance exactly as `/status` publishes them (module, `CFGMGR_<name>` except SCD30, `WEBSERVER`), a published source never lacks a row, and a row whose source is absent renders as "no data", never as a reassuring zero; the row colour and the rollup use the same severity rule.
- **Class**: product
- **Sources**: WEB.N147, WEB.N162, WEB.N163, WEB.N256, WEB.N257 · stated at SPECIFICATION.md H.6 (:4514-4520); js/templates.js:215-246, :283; js/render.js:299-312
- **Provenance**: A — H.6 text (per-instance keying added 2026-09-18, session agent).
- **Truth**: partly — keys pinned at twin boot for generated devices (`test_digital_twin_generated_boot.py:113-133`); `templates.js:245-246` still falls back to `?? { counter: 0 }`, the silent-zero H.6 itself names as the failure mode; rollup uses history types while row colour uses `counter > 0` (N162); the rebuilt card loses keyboard focus each poll (N147).
- **Fit**: refines OR16.a (1) (chain reaches the website), P7 · pillar P7
- **Home**: SPECIFICATION.md H.6 (stays)

### G7.070 Errcount history: always visible, unpaginated, type as colour
- **Req**: Error counts are always visible and the full history renders on demand with no pagination (realistic depth well under 20 entries); a history entry's `type` only colours its number and is never shown as text.
- **Class**: product
- **Sources**: WEB.N164, WEB.N165, WEB.N182, WEB.N183, WEB.N184, WEB.N222, WEB.N241 · stated at js/templates.js:292-304; html/style.css:351-353, :411-413, :427-428; tests_js/templates.test.js:399-401; SPECIFICATION.md H.4 (:4365)
- **Provenance**: O — `js/templates.js:293, :304` "(project owner, session 2 follow-up)", "(project owner, session 2)"; `999aada` "history pagination ... resolved interactively with the project owner".
- **Truth**: holds — rendering as stated; depth figures disagree mildly ("15+ modules", "10+ entries" in style.css vs "well under 20"); colour-only information is an accessibility limit (WEB.S16 in the plan).
- **Fit**: new (owner UX decision) · pillar P7
- **Home**: SPECIFICATION.md H.4/H.6 (stays)

### G7.071 Browser floor: current Chromium, Firefox and Safari
- **Req**: The website runs on current Chromium, Firefox and Safari (desktop and mobile); shipped JS/CSS uses only features within that floor, the floor is stated once, and lint/type settings bound shipped `js/` to it.
- **Class**: product
- **Sources**: WEB.N066, WEB.N117, WEB.N167, WEB.N186, WEB.N187, WEB.N201, WEB.N204, WEB.N271 · stated at js/definitions.js:213-214; js/poll-manager.js:38-41; js/templates.js:347; html/style.css:136, :476; eslint.config.js:126-194; tsconfig.json:3-6; BACKLOG.md:833-837
- **Provenance**: O — PQ10 answer (2026-09-26): "browser floor current Chromium, Firefox, Safari"; owner brief "Stable and good-looking on (ideally all) major mobile/desktop browsers" (`WEBSITE_PLAN.md` §3).
- **Truth**: partly — features used (private fields, `replaceChildren`, `toFixed` to 100, range media queries, `min()`) are within current engines; `ecmaVersion: "latest"` bounds nothing; the floor is stated nowhere in SPECIFICATION.md; no real Safari/mobile pass exists (BACKLOG.md:833-837 "needs the project owner directly"; the smoke uses WebKitGTK).
- **Fit**: restates PQ10 · pillar P4
- **Home**: → SPECIFICATION.md H.1 (state the floor)

### G7.072 Cross-browser smoke: one session per engine, unique probes
- **Req**: The cross-browser smoke drives each engine (Chromium, Edge, Firefox, WebKitGTK) in its own single session against a real booted twin, at desktop and mobile widths, with a unique probe value per check and waits on the rendered result, never on an attribute alone; engine limits (no device emulation over WebDriver, minimum window width) are stated.
- **Class**: test-standard
- **Sources**: WEB.N022, WEB.N023, WEB.N024, WEB.N025, WEB.N206, WEB.N267, WEB.N286 · stated at scripts/cross_browser_smoke.mjs:2-44, :304-306, :354-356, :471-473; vitest.config.js:42-46; README.md:255-260; commit 0260f60
- **Provenance**: A — `0260f60` (single-session note, whose home `CONNECTION_SCALING_PLAN.md` is deleted; not re-verified that it migrated).
- **Truth**: holds — `cross_browser_smoke.mjs:2, :31-40`; unit and live tiers run Chromium only (`vitest.config.js:42-46`); "a single Playwright provider can only automate Chromium-family" (README) not verified.
- **Fit**: refines OR45.a (L0 website tier) · pillar P5
- **Home**: scripts/cross_browser_smoke.mjs header (stays)

### G7.073 The theme follows the OS colour scheme only
- **Req**: Light and dark themes switch automatically via `prefers-color-scheme` through CSS tokens, with no manual toggle and no stored preference.
- **Class**: product
- **Sources**: WEB.N175 · stated at html/style.css:3, :29; SPECIFICATION.md H.1
- **Provenance**: O-confirmed — `WEBSITE_PLAN.md` §4 "Dark mode | Automatic only, via `prefers-color-scheme` | No manual toggle" among decisions resolved with the owner (`999aada`).
- **Truth**: holds — `style.css:29`; no `color-scheme` declaration, so native controls stay light in dark mode (a defect candidate inside this rule).
- **Fit**: new (owner UX decision) · pillar P6
- **Home**: SPECIFICATION.md H.1 (stays)

### G7.074 Deliberate website divergences from legacy are recorded
- **Req**: A behaviour the new website changes relative to the legacy site (a failed Apply marks every submitted field Failed instead of only logging; a left accent stripe with legacy's two-level granularity instead of a full recolour) is recorded with its owner decision; one without an owner decision goes to the owner.
- **Class**: process
- **Sources**: WEB.N145, WEB.N181 · stated at js/render.js:264-266; html/style.css:314-316
- **Provenance**: O-confirmed for the card treatment (`999aada`, "card treatment ... resolved interactively with the project owner"); A for the Apply-failure change (`582c68a`, no owner wording).
- **Truth**: partly — both recorded in code comments only; the Apply-failure divergence has no recorded owner decision (OR48.a (2): a session agent's decision does not count).
- **Fit**: restates OR48.a (2) · pillar process
- **Home**: code comments → SPECIFICATION.md H.4

### G7.075 Website security under the trusted-LAN threat model
- **Req**: Under the trusted home-LAN threat model the page needs no CSP, SRI for same-origin resources or security headers; it stays XSS-safe by construction (`textContent` only, never `innerHTML`), and any future cross-origin resource requires SRI.
- **Class**: product
- **Sources**: WEB.N171, WEB.N172, WEB.N207 · stated at html/index.html:3-9; .htmlvalidate.json:7-9; SPECIFICATION.md H.4 (rendering safety)
- **Provenance**: O — OR52.a (3) "Threat model: trusted home LAN"; `textContent`-only is A (`15ed282`).
- **Truth**: holds — no CSP meta (grep count 0); no `innerHTML` in `js/`; `require-sri` narrowed to cross-origin.
- **Fit**: restates OR52.a (3) · pillar P4
- **Home**: → SPECIFICATION.md threat-model statement (CONSOLIDATION §4)

### G7.076 Every REST endpoint is reachable in the GUI
- **Req**: Every REST endpoint's functionality is reachable somewhere in the ordinary GUI (no dedicated API-browser page), with the six sections mirroring the six endpoints; a value a route exposes but no page shows is either placed per OR43.a's placement rule or recorded as a deliberate exception.
- **Class**: product
- **Sources**: WEB.N232, WEB.N233, WEB.N244 · stated at SPECIFICATION.md H.1 (:4281-4289), H.4 (:4369)
- **Provenance**: O — `WEBSITE_PLAN.md` §1 "goals, unchanged from the project owner's brief": "Every REST endpoint's functionality must be reachable somewhere in the GUI — not necessarily as a dedicated API-browser page".
- **Truth**: partly — `/system`'s `build` is not shown (plan WEB.S20, not re-checked here); H.1's "Standing constraints, all met" is a blanket claim with the four generated sites unvalidated (G7.056).
- **Fit**: restates OR43.a (2) · pillar P2
- **Home**: SPECIFICATION.md H.1 (stays)

### G7.077 websiteVersion is provenance; schemaVersion is the wire shape
- **Req**: `websiteVersion` is build provenance only, neither validated nor rendered, and never conflated with `schemaVersion`, whose major version the renderer must support; a bad `websiteVersion` costs provenance, not rendering.
- **Class**: product
- **Sources**: WEB.N060, WEB.N251 · stated at SPECIFICATION.md H.5 (:4393-4396), L.7; js/definitions.js:61-63
- **Provenance**: A — H.5 text (session agent).
- **Truth**: holds — `tests_scripts/test_buildgen_definitions.py:111-112` asserts the two differ.
- **Fit**: new · pillar P7
- **Home**: SPECIFICATION.md H.5 (stays)

### G7.078 freezefs packs gzip files without --compress
- **Req**: The website is frozen with vendored `freezefs` without `--compress` (with `--on-import=mount` it would load files into RAM); website paths stay under freezefs's 255-character limit; build reproducibility accepts the embedded local build time.
- **Class**: tooling
- **Sources**: WEB.N001, WEB.N002, WEB.N003, WEB.N004 · stated at ext/freezefs/archive.py:12-31, :90-171, :295-297
- **Provenance**: F — vendored `ext/freezefs/archive.py` (upstream behaviour); usage decision A (build script).
- **Truth**: holds — `archive.py:295-297` comment; the reproducibility point (local time embedded) is an open question, not checked against SCR's candidates.
- **Fit**: refines P6 · pillar P6
- **Home**: scripts/build_frozen_html.sh (stays)

### G7.079 Display rounding happens only in the renderer
- **Req**: Drivers never round emitted values; the display precision of a field is its `decimals` hint, applied in `formatFieldValue()` as the one rounding point in the stack.
- **Class**: product
- **Sources**: WEB.N049, WEB.N221, WEB.N254, WEB.N283 · stated at js/field-format.js:33-35; SPECIFICATION.md H.5.1 (:4481-4485); tests_js/templates.test.js:48-49; commit 12a616a
- **Provenance**: O-confirmed — `12a616a` (2026-09-12): "put output rounding in the renderer as a decimals hint on the readonly field (q6)" with "the driver rounding nothing".
- **Truth**: holds — grep of `round(` in `src/`: only ISL29125's threshold-count conversion (`asy_isl29125_driver.py:1235`, a register value) and SGP40's sleep delay (`:574`), no emitted reading.
- **Fit**: new (owner decision) · pillar P4
- **Home**: SPECIFICATION.md H.5.1 (stays)

### G7.080 NTP timezone fields render on the System page
- **Req**: `GMTOffset`/`DSTOffset` render on the System page by deliberate cross-file section assignment, not under Networking.
- **Class**: product
- **Sources**: WEB.N035 · stated at tests_scripts/test_buildgen_web_tag.py:500-501
- **Provenance**: A — `b9d59ff` (2026-09-11); no owner wording found.
- **Truth**: holds — pinned by that test.
- **Fit**: conflicts OR43.a (2) placement rule ("Networking = Wi-Fi/NTP/identity, System = system settings") — resolution needs the placement rule's reading of timezone as system vs NTP · pillar P4
- **Home**: test comment → SPECIFICATION.md H placement rule

### G7.081 Poll cadence and per-page connections are tuned values
- **Req**: The website's poll interval (3000 ms) and the connections one page load opens (2, after bundling and inlining) are tunables checked against the device's measured serving throughput and `max_connections`.
- **Class**: tooling
- **Sources**: WEB.N009, WEB.N195 · stated at html/definitions/wozi.json:6, :209; tests/test_digital_twin_real_website_integration.py:304-306; SPECIFICATION.md H.7 (:4548-4552)
- **Provenance**: A — H.7 (session agents); `max_connections = 6` is owner (H.7 "owner decision").
- **Truth**: holds — as stated; "two connections per page load" is a browser-behaviour assumption measured once (not re-checked).
- **Fit**: restates OR30.a (1) · pillar P6
- **Home**: → SPECIFICATION.md tunables section

### G7.082 The shipped page is checked as built
- **Req**: The staged `index.html` (inlined CSS, JSON and bundle) is what html-validate checks, not only the source page; `tsc` not covering the inline bootstrap is an accepted gap only while it stays a thin bootstrap.
- **Class**: tooling
- **Sources**: WEB.N205, WEB.N208, WEB.N237 · stated at SPECIFICATION.md H.2 (:4331-4333); tsconfig.json:28; package.json:19; .htmlvalidate.json
- **Provenance**: A — H.2 "accepted — it's a thin bootstrap" (session agent).
- **Truth**: partly — `lint:html` validates `html/**/*.html` only; the inline bootstrap passes `HTMLElement | null` to `startApp` unchecked (WEB.N205).
- **Fit**: extends OR16 · pillar P5
- **Home**: SPECIFICATION.md H.2/H.8

### G7.083 Rendering and change detection read the same value
- **Req**: Every place that shows or compares a field's current value reads it through `resolveFieldValue()`, so display and change detection never drift apart.
- **Class**: code-style
- **Sources**: WEB.N061, WEB.N148 · stated at js/definitions.js:66-68; js/render.js:321-335
- **Provenance**: A — website session.
- **Truth**: partly — `render.js:327, :331` (in-place caption refresh) read `groupValues[field.key]` directly, bypassing `resolveFieldValue()`; baselines stay frozen at first render.
- **Fit**: refines OR24 (one material) · pillar P4
- **Home**: js/definitions.js comment (stays)

### G7.084 Shipped website code carries no test-only API
- **Req**: The frozen website bundle contains no function, getter or variable that exists only for tests; tests observe the code from outside.
- **Class**: code-style
- **Sources**: WEB.N118 · stated at js/poll-manager.js:43-46
- **Provenance**: O — OR36 ("contains zero artifacts specifically added for testing, neither functions, nor specific variables"); the website is frozen into the firmware image.
- **Truth**: wrong — `pollManager.isBusy` (`poll-manager.js:44`) is used only by `tests_js/poll-manager.test.js:126-142`.
- **Fit**: extends OR36 (to the frozen website) · pillar P4
- **Home**: js/poll-manager.js

### G7.085 Int and float coercion is one policy everywhere
- **Req**: A JSON int is accepted for a float-typed field and coerced; a float is accepted for an int field only without a fractional part; bool is never numeric; the definitions `float` flag matches each schema's type, and the mock, the client and the docs state the same policy.
- **Class**: product
- **Sources**: WEB.N055, WEB.N076, WEB.N140, WEB.N228 · stated at src/config_manager.py:121-142; js/mock-server.js:36-38; js/render.js:216-218; js/definitions.js:44-46; SPECIFICATION.md A.8 (:619)
- **Provenance**: A — `coerce_numeric()` docstring and A.8 (session agents).
- **Truth**: drifted — `config_manager.py:130-134` accepts an int for a float field (blanket accept); README.md:605-607 says "a JSON integer where a `"float"` field is declared (e.g. `60` instead of `60.0`) is correctly rejected as `"Invalid"`".
- **Fit**: refines G.2 numeric primitive · pillar P8
- **Home**: SPECIFICATION.md A.8 (stays); README.md corrected

### G7.086 String bounds follow the device's byte limits
- **Req**: Where the device bounds a string in UTF-8 bytes (SSID, PW, Country, Hostname, HotspotPW), the website hint, client check and mock enforce the same byte bound, not a character or UTF-16 count.
- **Class**: product
- **Sources**: WEB.N079, WEB.N156, WEB.N229 · stated at SPECIFICATION.md C.7.4 (:2014-2027); js/mock-server.js:59-61; js/templates.js:27-28; src/asy_wifi_service.py:71-82
- **Provenance**: F — C.7.4 (pinned `extmod/modnetwork.c` limits, confirmed on silicon 2026-09-25); the mirror obligation is G.1/G.2 (A).
- **Truth**: wrong — `mock-server.js:59-61` compares `value.length` (UTF-16 units) and `templates.js:27-28` says "characters", while `asy_wifi_service.py:82` checks `len(value.encode())`; C.7.4 itself says "the web UI mirrors" the character bound.
- **Fit**: restates G.1 applied to C.7.4 · pillar P8
- **Home**: SPECIFICATION.md C.7.4 (add the website side)

## Conflicts and tensions

1. **Heap-unwedge helper: keep vs retire** (G7.023). SPECIFICATION.md F.6 amendment (:4119-4133) and CLAUDE.md ("stay wired in purely as defense in depth") keep the three call sites; OR52.a (6) (owner, 2026-09-26) retires them once a test proves the override. Resolution: OR52.a (6) wins (later owner row); F.6 keeps the mechanism, CLAUDE.md's bullet shrinks to a pointer.
2. **Memory sampler's `gc.collect()`** (G7.041). SPECIFICATION.md I.4 (:5057-5071, agent, `2bd5aa5`) says "don't re-flag it without new evidence"; OR39.a (1) (owner) forbids measuring in the next line after `gc.collect()`, and `run_generic_integration.py:294-295` does exactly that. Resolution: OR39.a wins; the exception stays (a measurement baseline), its placement changes, and I.4's wording is rewritten.
3. **Mock fixtures' SHTC3/MPRLS placeholders** (G7.057). Owner q5 (`12a616a`, 2026-09-12) "leave mockdata's SHTC3/MPRLS placeholders alone" vs OR43.a (2) (2026-09-26) "the mock server and mock data match the real API field for field". Resolution: OR43.a is later and explicit; remove the orphans.
4. **C.11 item 9 / twin README vs one-source definitions** (G7.004, G7.055). C.11 item 9 and `digital_twin/README.md:838-842` require hand-updating `html/definitions/<device>.json`; K.4 says it is "never hand-maintained"; OR43.a (3) retires the files. Resolution: OR43.a (3); C.11's step becomes "add `@web` tags".
5. **Twin state as evidence vs disposable** (G7.026, G7.028). The twin CI runner wipes `digital_twin/*.json` and `digital_twin/config/` before each run, and a corrupt state file silently loads blank; OR38.a (2) says the twin's FRAM/SCD30 state is evidence to archive (last 3 runs). Resolution: OR38.a (2) for the runner; blank-on-corrupt stays the loader's behaviour (the archived copy is the evidence).
6. **One driver fault per process vs higher-order combinations** (G7.030). The restart budget forces one fault per process in Run 5c; OR41.a (1) asks for risky higher-order combinations. Not contradictory: combined faults are OR18.a hardware-fault escalation (a reboot is the intended outcome). Resolution: a combined-fault run asserts escalation (reboot), not per-driver persistence; unresolved only in which tier owns it — left to OR41.a's matrix.
7. **NTP timezone fields on the System page** (G7.080) vs OR43.a (2)'s placement rule text "Networking = Wi-Fi/NTP/identity". Unresolved by evidence: the fields are time-display settings, not NTP transport; see question 1.
8. **Client timeout equality** (G7.053). H.4 claims equality makes a slow request surface as the server's abort; the client clock starts before connect, the server's at accept, so the client aborts first. Resolution by evidence: keep the pinned equality (a tunable, OR30), correct H.4's rationale or add a stated margin — a tunables-register decision, not an owner question.

## Questions for the owner

1. **Timezone fields: System or Networking page?**
   - (a) System page, as today; the OR43.a placement rule reads "NTP" as the server/transport settings only. Consequence: rule text gets that clarification; no UI change.
   - (b) Networking page with the NTP host. Consequence: `@web` section change in `asy_ntp_client.py`, the pinning test at `test_buildgen_web_tag.py:500` inverted, mock data moved.
2. **Twin fidelity table: where and how complete?**
   - (a) One table in `digital_twin/README.md` covering every LIMIT recorded today (about 150 rows). Consequence: large but complete; OR17.a (2) met literally.
   - (b) One row per fact class (timing, electrical, network, heap, reset, per-chip), linking to the fake's docstring for detail. Consequence: short, readable, per-chip detail stays in code comments.

## No-requirement buckets

- defect-candidate: TWIN.N104, TWIN.N109, TWIN.N209, TWIN.N380, WEB.N044, WEB.N050, WEB.N053, WEB.N094, WEB.N100, WEB.N111, WEB.N112, WEB.N113, WEB.N123, WEB.N133, WEB.N146, WEB.N158, WEB.N161, WEB.N173, WEB.N174, WEB.N176, WEB.N177, WEB.N179, WEB.N198, WEB.N225, WEB.N226, WEB.N272, WEB.N279
- descriptive: TWIN.N044, TWIN.N260, TWIN.N283, WEB.N042, WEB.N069, WEB.N096, WEB.N119, WEB.N120, WEB.N121, WEB.N129, WEB.N132, WEB.N135, WEB.N151, WEB.N152, WEB.N178, WEB.N180, WEB.N224, WEB.N285
- open-item: TWIN.N350, TWIN.N360, TWIN.N379
- suppression-inventory: TWIN.N095, TWIN.N180, TWIN.N204, TWIN.N218, TWIN.N278, WEB.N144, WEB.N185
- drift-only: TWIN.N115, TWIN.N147, TWIN.N272, WEB.N047, WEB.N085, WEB.N095, WEB.N109, WEB.N127, WEB.N169, WEB.N218, WEB.N220
- stale: WEB.N274, WEB.N275, WEB.N277

Notes on selected bucket items (one line each where the reason is not obvious): TWIN.N104 — `digital_twin/machine.py:808-830`: a PERIODIC self-re-`init()` leaves the old `_run()` loop alive (it re-reads `self.mode`), so the callback fires twice per period; TWIN.N109 — `SimulatedRebootError(Exception)` is swallowed by any broad `except Exception` in `src/`; TWIN.N380 — grkizi Run 5c SIGSEGV on reboot, no root cause recorded; WEB.N279 — live tests tolerate "Unchanged" never firing (relates OR42.c); WEB.N272/N225/N226 — `initNav()` keydown listener never removed, `startApp()` has no stop handle.

## Accounting

| Catalog | Items read | In candidates | defect-candidate | descriptive | open-item | suppression-inventory | drift-only | stale | Sum |
|---|---|---|---|---|---|---|---|---|---|
| TWIN.md | 380 | 362 | 4 | 3 | 3 | 5 | 3 | 0 | 380 |
| WEB.md | 286 | 235 | 23 | 15 | 0 | 2 | 8 | 3 | 286 |

Checked by script: every `TWIN.N001-N380` and `WEB.N001-N286` appears exactly once as a primary
placement (a candidate's Sources line or one bucket line); no duplicates, none missing. Suppression
sites whose rule a candidate carries are listed in that candidate's Sources (TWIN.N088 in G7.018,
TWIN.N172/N173 in G7.022, TWIN.N168 in G7.046, TWIN.N211/N217 in G7.024). TWIN.N337 is placed in
the website candidate G7.056.

Candidates: 86 (G7.001-G7.048 twin, G7.049-G7.086 website). Provenance: O 16, O-confirmed 7, A 59,
F 4. Truth: holds 32, partly 38, drifted 11, wrong 5.

Not reachable or not checked: micropython-lib (`argparse` capabilities, G7.044) is absent from the
scratch MicroPython checkout; MicroPython `extmod/asyncio` SIGINT behaviour (G7.024) and the rp2
SPI overrun raise site (G7.033) were not re-read; upstream issue micropython#6924 not fetched.
