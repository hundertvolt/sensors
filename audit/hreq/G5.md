# Harvest requirements G5 — CORE, STOR, XCUT, SEC, UNSORTED (HEAD 2214470)

Verification base: working tree at HEAD `2214470`; MicroPython `v1.29.0` source (scratchpad `mp`);
FRAM datasheet text (scratchpad `dstxt`, MB85RS64V DS501-00015-4v0-E, MB85RS2MTA DS501-00032-3v0-E).
Provenance by `git log -S` on the stating file plus `git show -s` of the introducing commit; every
commit cited as "A" is authored `Claude`. "O" is used only where the text carries "(owner, date)" or
an owner quote, or the owner row OR<n> says it; "O-confirmed" where a Claude commit records an
explicit owner ruling (quoted in the line).

## Candidates

### G5.001 Typing-only imports behind the ImportError guard
- **Req**: Every `src/` module that uses `typing` imports it as `try: from typing import TYPE_CHECKING` / `except ImportError: TYPE_CHECKING = False`, never unconditionally, and puts every typing-only import under `if TYPE_CHECKING:`. Union types are written PEP 604 `X | Y`, never `typing.Union`.
- **Class**: code-style
- **Sources**: CORE.N001, CORE.N013, CORE.N035, CORE.N060, CORE.N077, CORE.N209, XCUT.N002 · stated at SPECIFICATION.md C.10 (:2315-2316), D.6 (:2650-2656), CLAUDE.md "Union type annotations"
- **Provenance**: A — C.10 text from `59f1614` (2026-08-07, spec consolidation); D.6 guard wording likewise agent-authored; PEP 604 half is CLAUDE.md-recorded and ruff `UP007`-enforced
- **Truth**: holds — all 22 `src/` files that import `typing` use the identical guard (grep), the 6 without it (`asy_dns_client`, `asy_i2c_driver`, `crc_checks`, `framing_codecs`, `math_helpers`, `voc_algorithm`) import no `typing`; `_strip_type_checking.py` matches this exact shape
- **Fit**: refines OR24 (one material) · pillar P4
- **Home**: SPECIFICATION.md C.10 + D.6 (stated twice) → one place in the OR44.a checklist's code-style step, D.6 citing C.10

### G5.002 Quote an annotation only for TYPE_CHECKING names
- **Req**: An annotation is quoted only when it names something imported under `TYPE_CHECKING`; every other annotation stays bare. The rule applies to new code and to lines a change touches; existing files are not mass-edited to it.
- **Class**: code-style
- **Sources**: CORE.N212 · stated at SPECIFICATION.md D.6 (:2658-2661)
- **Provenance**: O — "Quoting annotations (owner decision, 2026-09-24)"; applied in `58abf8f` ("SPECIFICATION.md D.6 states the annotation-quoting rule; no mass edit")
- **Truth**: holds as stated rule; mixed style remains by design (not counted)
- **Fit**: conflicts OR24 (one material) — the "not mass-edited" clause leaves two styles; OR24.a authorises harmonising · pillar P4
- **Home**: SPECIFICATION.md D.6 → unchanged, but the "not mass-edited" clause needs the owner's word under OR24 (see Conflicts)

### G5.003 Suppressions in shipped code are coded and justified
- **Req**: A type suppression in `src/` names its error code, and a typing workaround carries the external defect it works around and its removal trigger (e.g. the `ANN401` `**kwargs` exemption lasts until the stubs declare `TypedDict`/`Unpack`). A suppression that no longer fires is removed.
- **Class**: code-style
- **Sources**: CORE.N063, CORE.N082, CORE.N083, CORE.N086, CORE.N185, CORE.N263 · stated at `src/print_log.py:106-108`, `pyproject.toml:253-259`, `src/system_service.py:232,234,363`
- **Provenance**: C — 2 coded `type: ignore[...]` in `src/system_service.py` (:232 `union-attr`, :234 `arg-type`) and 0 uncoded in `src/` (grep); `warn_unused_ignores` in mypy config; removal-trigger wording is agent-authored (`pyproject.toml`)
- **Truth**: holds — both `src/` ignores are coded; the ANN401 removal trigger is stated in pyproject.toml:253-259 but not at `print_log.py:106-108`; whether each still fires is OR50.a's trial-removal check (not run: no mypy here)
- **Fit**: restates OR50.a (1) for suppressions · pillar P4
- **Home**: pyproject.toml comments → unchanged; the rule itself into the OR44.a checklist

### G5.004 No name-mangled methods unless mangling is load-bearing
- **Req**: `src/` uses single-underscore privates; a double-underscore (name-mangled) method is used only where mangling itself is required.
- **Class**: code-style
- **Sources**: CORE.N193 · stated at SPECIFICATION.md C.1-C.2 (:1504-1505)
- **Provenance**: C — 0 non-dunder `__name` methods in `src/` (grep)
- **Truth**: holds — grep over `src/` finds none; no lint rule guards it
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md Part C → OR44.a checklist code-style step

### G5.005 Class members follow the D.15 order
- **Req**: Inside every `src/` class, dunders come first, then private methods, then public ones, each group ordered starters, getters, setters, others; a reorder is a pure AST-verified move in which a method's own comments and a section heading travel with their code while their text does not change.
- **Class**: code-style
- **Sources**: XCUT.N091, XCUT.N094, XCUT.N095, CORE.N213 · stated at SPECIFICATION.md D.15 (:2730-2737); owner ruling in commit `41762dc`
- **Provenance**: O-confirmed — `41762dc` (2026-09-13): "Owner's rulings, 2026-09-13, on the D.15 open question. The order as written stands … comments move with the code they annotate … The reorder itself is a decided task now"
- **Truth**: drifted — D.15 at HEAD still reads "no change to any body/decorator/comment" (the owner's amendment and the high-priority BACKLOG task were dropped by merge `e5d2c43`, BACKLOG has no entry); a crude AST role check at HEAD finds 18 of 51 method-bearing `src/` classes out of order (the project's own classification found 9 at `2a88cc8`); no tool named for the AST comparison
- **Fit**: restates OR24 (naming, ordering: "change, don't flag") · pillar P4
- **Home**: SPECIFICATION.md D.15 → D.15 with the 2026-09-13 amendment restored (rule drift: OR13.a question, see Questions)

### G5.006 Imports are static and AST-scannable
- **Req**: Every import is a plain static `import`/`from … import` statement; `__import__` and `importlib` are never used, so the frozen module set can be derived by static analysis.
- **Class**: code-style
- **Sources**: XCUT.N072, XCUT.N073, XCUT.N083 · stated at SPECIFICATION.md F.1 (:3452-3457)
- **Provenance**: O — "project owner's explicit, standing rule, not just a style preference" (F.1); added in `6aed3c9` (2026-09-09)
- **Truth**: drifted — `src/` holds (0 sites); but `digital_twin/run_generic_integration.py:356`, `tests/_sensortask_scenarios.py:127,365,505,834`, `tests/_webserver_concurrency_scenarios.py:100`, `tests/_digital_twin_construction_scenarios.py:98`, `tests/_boot_contiguity_probe.py:125`, `tests/test_asy_isl29125_driver.py:1301,1306`, `buildgen/validate.py:6,194-197` and `tests_scripts/_script_loader.py:5-18` use them against F.1's "anywhere in this codebase"; SPECIFICATION.md L.4 (:6301-6303) itself describes the twin's `__import__`
- **Fit**: extends P8 (static derivability) · pillar P4
- **Home**: SPECIFICATION.md F.1 → scope to be settled (see Questions: firmware-only or whole repo)

### G5.007 Runtime import graph is a DAG, drivers independent
- **Req**: `src/` modules import each name from its defining module (never through a re-exporting module such as legacy `async_manager`); `print_log.py` never imports `asy_fram_manager` at runtime; the runtime import graph of `src/` stays acyclic and no sensor driver imports another sensor driver.
- **Class**: code-style
- **Sources**: CORE.N061, CORE.N189, XCUT.N043 · stated at SPECIFICATION.md A.4 (:159-161), A.7 (:565-571), `src/print_log.py:38-40`
- **Provenance**: A — agent-authored in A.4/A.7 and the `print_log.py` comment
- **Truth**: holds — AST import graph of `src/` excluding `TYPE_CHECKING` blocks: no cycles, no driver→driver edge, no function-level imports, no `async_manager` reference; nothing enforces it (no lint/test)
- **Fit**: extends OR46.a (1) "imports" level · pillar P4
- **Home**: SPECIFICATION.md A.4/A.7 → OR44.a checklist import step, with a machine check (OR46.a imports level)

### G5.008 bool never passes as int
- **Req**: Integer validation and narrowing use `type(x) is int` (or `is not int`), never `isinstance`, so `bool` is excluded; no CPython-only bool guard is added on top.
- **Class**: code-style
- **Sources**: CORE.N138, CORE.N199, CORE.N215 · stated at SPECIFICATION.md C.5.2 (:1760-1763), F.1 (:3485-3487)
- **Provenance**: A — C.5.2/F.1 agent text; F.1 records a guard "written and removed 2026-09-13" (no owner words); F (Python: `bool` subclasses `int`)
- **Truth**: holds — `config_manager.py` `_special_bypass`/`coerce_numeric` use `type(...) is`; F.1 names the pattern to copy
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.5.2 + F.1 → Part G.2's validation primitive entry (one place)

### G5.009 Guard real-world faults, not type violations
- **Req**: Code does not re-check at runtime what static typing already guarantees for this project's own callers; it does guard NaN/±inf and out-of-domain values of correctly typed inputs, and every input that arrives untyped (REST JSON, TOML, restored FRAM/flash state) is validated at its entry point.
- **Class**: code-style
- **Sources**: XCUT.N068, XCUT.N076, XCUT.N090 · stated at SPECIFICATION.md D.2 (:2617-2622), F.1 (:3584-3590)
- **Provenance**: O-confirmed — `b5502c8` (2026-08-06): "Per project owner feedback: don't guard against inputs violating their already-declared types"
- **Truth**: partly — D.2 states the typed half; the "untyped entry points validated" half is only implied (F.1's driver-gating audit dated 2026-09-24, not re-checked here)
- **Fit**: refines OR26 (handling at the right layer) · pillar P2
- **Home**: SPECIFICATION.md D.2 → D.2 with the entry-point half stated

### G5.010 Module-wide never-raise contracts
- **Req**: `base_classes.py`, `config_manager.py`, `print_log.py`, `system_service.py`, `asy_fram_manager.py` and `asy_fram_driver.py` (outside its one-time `__init__`/`setup()` errors) return a documented sentinel on every path and never raise; raw bus drivers are the one allowed exception, and their only callers catch what they raise.
- **Class**: product
- **Sources**: CORE.N011, CORE.N034, CORE.N059, CORE.N075, STOR.N002, STOR.N036, STOR.N070, STOR.N078, STOR.N118 · stated at each module's docstring line 3; SPECIFICATION.md D.2 (:2615-2628)
- **Provenance**: A — module headers trimmed in `3795d93`/`45bd850`/`b85a056`/`323a1a7` (all Claude); D.2 agent text
- **Truth**: holds by reading of the catches quoted (base_classes `_get_dict_cfg`/`_set_dict_cfg`, print_log `_write`/`_read`); `FRAM_SPI._write()` has no own `try` and relies on `AsyFramManager`'s catch (STOR.N118) — consistent with the bus-driver exception, not re-traced for every path
- **Fit**: restates OR26 · pillar P2
- **Home**: module docstrings + D.2 → unchanged

### G5.011 Caller-supplied callbacks are guarded and persisted
- **Req**: Every call into a caller- or subclass-supplied callback, starter, getter, setter or override is wrapped in `except Exception`, logs through `err_s()`/`wrn_s()` with its own number, and reports `"Failed"` (or its method's sentinel), so one bad callback never ends the caller.
- **Class**: product
- **Sources**: CORE.N017, CORE.N018, CORE.N024, CORE.N025, CORE.N029, CORE.N032, CORE.N081, CORE.N085, CORE.N175, CORE.N223, XCUT.N013, STOR.N057 · stated at SPECIFICATION.md G.2 (:4180-4182); `src/base_classes.py:194-210,309-324,352-354,382-397`; `src/system_service.py:136-139,180-182,335-338`
- **Provenance**: A — G.2 catalog entry (agent); WP8 closed the last print-only site (C.7.1 row SYSTEM errno 7)
- **Truth**: partly — every site listed persists via `err_s`/`wrn_s` (read); the exception is `_timer_sequencer()` (`src/system_service.py:150-154`), a sync Timer-callback context that can only `pr.err()` (see G5.022)
- **Fit**: restates OR26.a (callback dispatch) · pillar P2
- **Home**: SPECIFICATION.md G.2 → unchanged

### G5.012 A swallowing except leaves a trace
- **Req**: An `except` that swallows an exception either logs it through `err_s()`/`wrn_s()` or returns a documented sentinel that its one caller logs; a silent or print-only swallow is allowed only with a stated reason (sync context, logging layer itself, deliberately uncounted).
- **Class**: product
- **Sources**: CORE.N005, CORE.N036, CORE.N038, CORE.N041, CORE.N042, CORE.N073, CORE.N074, CORE.N245, STOR.N052, STOR.N054, STOR.N055, STOR.N059 · stated at SPECIFICATION.md C.7 (:1885-1888 "A per-field get/set forward always logs … never a bare `except Exception: return None`")
- **Provenance**: A — C.7 agent text; CORE.N245's open question (`6f12d22`) is the origin
- **Truth**: partly — `asy_fram_manager.py:594-597` (timestamp unpack → `_TS_UNINIT`, no log), `config_manager.py:79-83,93-97,113-118` (malformed schema → empty, no log) and `print_log.py:247-269` (FRAM I/O → `False`, `_diag` only, silent at level 0) swallow without a persisted trace; the schema ones are authoring-time (G5.041)
- **Fit**: refines OR26.a ("swallowing excepts") · pillar P2, P7
- **Home**: SPECIFICATION.md C.7 → D.2 (the generic rule), C.7 keeps the per-field-forward case

### G5.013 Provably unreachable defensive branches may stay
- **Req**: A defensive branch that is provably unreachable today is kept only where Part E.5.1's precedent names its class (e.g. `Timer.init()`'s `MemoryError` arm, `ConfigManager`'s three type-mismatch catches, `get_log()`'s `_NO_WRN` arm), stays tested through a fake, and is not counted as dead code.
- **Class**: code-style
- **Sources**: CORE.N031, CORE.N049, CORE.N194, CORE.N246, CORE.N257, STOR.N034, STOR.N091 · stated at SPECIFICATION.md C.5 (:1721-1724), E.5.1 (:2985ff), `src/asy_fram_driver.py:423-427`
- **Provenance**: A — `9243049` "Decided: keep all 3 exception-hardening catches" (agent, no owner quote); `acc4993` (agent) for `_NO_WRN`
- **Truth**: holds — branches present, reached only via fakes (`tests/test_asy_fram_manager.py:2120-2122`)
- **Fit**: conflicts OR46.a (2) (confirmed leftovers incl. "unreachable branches" are removed) — see Conflicts · pillar P4
- **Home**: SPECIFICATION.md E.5.1 → E.5.1, with the OR46.a line drawn explicitly

### G5.014 Logger-less teardown returns bool
- **Req**: A teardown or cleanup method on a class that has no logger of its own returns `bool`, never `None`, so its caller can log the failure.
- **Class**: code-style
- **Sources**: CORE.N205 · stated at SPECIFICATION.md C.7 (:1891-1894)
- **Provenance**: A — C.7 agent text
- **Truth**: not checked beyond the three named sites (`AsyUDPSocket.disconnect()`, `WebserverService._close_writer()`, `UART.deinit()`)
- **Fit**: refines OR26 · pillar P2
- **Home**: SPECIFICATION.md C.7 → unchanged

### G5.015 Lock exit tolerates a double release
- **Req**: `Lockable.__aexit__()` and every lock-releasing exit path swallow the `RuntimeError` of an already-released lock and never propagate it; a two-lock entry releases the outer lock when acquiring the inner one fails.
- **Class**: product
- **Sources**: CORE.N014, CORE.N097, CORE.N098, CORE.N112, STOR.N012, STOR.N082 · stated at `src/base_classes.py:47-50`, `src/asy_fram_driver.py:147-148`
- **Provenance**: C — 2 sites swallow (`base_classes.py:49`, `asy_fram_driver.py:147`), pinned by `tests/test_asy_i2c_driver.py:794-797`, `tests/test_asy_uart_driver.py:1296-1299`, `tests/test_asy_fram_driver.py:1321-1322,1378-1379`
- **Truth**: holds (read)
- **Fit**: refines OR38.a (1) (no leaked locks) · pillar P2
- **Home**: code comments only → SPECIFICATION.md C.8 (locking model)

### G5.016 Shared mutable state lives in Locked primitives
- **Req**: State shared across coroutines is held in `Lockable`/`LockedCounter`/`LockedFlag`/`LockedValue`, never a bare module-level variable plus an ad hoc lock; `LockedCounter` clamps to `[0, max_val]` (a negative `max_val` clamps to 0), `LockedValue` stores any value unclamped.
- **Class**: code-style
- **Sources**: CORE.N015, CORE.N117, CORE.N118, CORE.N224 · stated at SPECIFICATION.md G.2 (:4185-4187), `src/base_classes.py:85-88`
- **Provenance**: A — G.2 catalog (agent)
- **Truth**: holds for the primitives (read); project-wide absence of bare module-level mutable state not re-grepped
- **Fit**: restates Part G, OR24 · pillar P4
- **Home**: SPECIFICATION.md G.2 → unchanged

### G5.017 Readiness gate: `initialized`, per-class contract
- **Req**: A class with async setup gates on `self.initialized: bool = False → True` (the one named exception is `ConfigManager.valid`); a call before `setup()` answers exactly as that class's declared raise/never-raise contract says.
- **Class**: code-style
- **Sources**: CORE.N210, XCUT.N062 · stated at SPECIFICATION.md C.13 (:2376-2386)
- **Provenance**: A — C.13 agent text
- **Truth**: not checked per class
- **Fit**: restates OR26.a (C.13 gates honoured) · pillar P2
- **Home**: SPECIFICATION.md C.13 → unchanged

### G5.018 Instance names through `instance_name()`
- **Req**: Every per-instance REST key, config filename and logger name is built by `config_manager.instance_name(base, ext)`, never by hand; an empty extension reproduces the single-instance name byte for byte.
- **Class**: code-style
- **Sources**: CORE.N022, CORE.N211, CORE.N226 · stated at SPECIFICATION.md C.14.1 (:2428-2430), G.2 (:4244-4252), `src/base_classes.py:274-276`
- **Provenance**: A — `6aed3c9` (2026-09-09, build-chain session 1)
- **Truth**: holds — `instance_name()` at `config_manager.py:70-76`; `SensorReaderConfig` builds `config_<self.name>.cfg` from it (read)
- **Fit**: restates OR24 · pillar P4, P10
- **Home**: SPECIFICATION.md C.14.1 → unchanged

### G5.019 Error-source and logger fan-in contract
- **Req**: Every top-level module exposes `get_error_sources() -> list[Any]` and `get_loggers() -> list[PrintLogHistory]` (itself plus any nested logger-owning object such as its `cfgmgr`), and every error-log result is annotated `"ErrorLog"`; aggregation walks these lists, never a hand-enumerated one.
- **Class**: code-style
- **Sources**: CORE.N020, CORE.N080, CORE.N225, STOR.N061, XCUT.N066 · stated at SPECIFICATION.md C.14.3 (:2547-2556), G.2 (:4190-4198)
- **Provenance**: A — `6aed3c9`
- **Truth**: holds for `SensorReader`, `SensorReaderConfig`, `SystemService`, `AsyFramManager` (read); duck-typed, checked only by mypy/tests, no wiring-time Protocol check
- **Fit**: restates OR16.a (1) chain completeness · pillar P10
- **Home**: SPECIFICATION.md C.14.3 → unchanged

### G5.020 Measurement namedtuples: flat scalars, `None` until measured
- **Req**: Every measurement namedtuple has only flat scalar fields (because `make_dict()` parses `repr()`), every reader constructs it with every field `None`, and every consumer treats `None` as the normal "not yet measured" value.
- **Class**: product
- **Sources**: CORE.N202, CORE.N254, XCUT.N064, XCUT.N065, XCUT.N082 · stated at SPECIFICATION.md C.6 (:1810-1814), C.14.2 (:2525-2540), L.2 (:6083-6085)
- **Provenance**: A — `3383dee` restored the landmine note (agent)
- **Truth**: partly — C.6 still calls `make_dict()` "via `repr()`-parsing", but `src/config_manager.py:100-118` now uses `getattr(nt, field)` over an explicit field tuple, so the flat-scalar premise is stale (drift); the `None`-initial half not re-audited
- **Fit**: refines OR26 · pillar P2
- **Home**: SPECIFICATION.md C.6/C.14.2/L.2 → C.6 corrected (drift)

### G5.021 Logger store setup comes first, before any persisted log
- **Req**: Every FRAM-backed logger's `pr.setup()` (and `PrintLogHistoryStore.setup()`) runs first in its module's setup, before anything that may log persistently; `__init__` never calls it, and a log entry made before it lives in RAM only and never overwrites the stored ring. Per OR47.a (2) every such setup runs in the one-time boot `setup()` batch, not lazily inside a task.
- **Class**: product
- **Sources**: CORE.N010, CORE.N012, CORE.N055, CORE.N069, CORE.N094, CORE.N103, CORE.N162, CORE.N229, CORE.N249, STOR.N209 · stated at `src/base_classes.py:5-7`, `src/print_log.py:173-176`, SPECIFICATION.md I.2 (:4894-4898), C.7 (:1830-1834)
- **Provenance**: O-confirmed — I.2 "stays first in its module's own `setup()`" (2026-09-21, owner decision per harvest); `6acc9c0` "§11 item 3 … leave as is, do not defer the per-logger store setup"; OR47.a (2) (owner, 2026-09-26) moves chunk setup into the boot batch
- **Truth**: partly — the RAM-only pre-setup guard holds (`print_log.py:173-176`, pinned by `tests/test_print_log.py:461-466`); but C.7 (:1830-1834) states loggers set up inside their tasks (SGP40/BMP3XX/SCD30 in `_init_*()`, NEOPIXEL, NOTIFY, and SYSTEM in `start_and_check_tasks()`, confirmed at `src/system_service.py:212`), so the OR47.a boot-batch half does not hold yet
- **Fit**: restates OR47.a (2) · pillar P7, P3
- **Home**: SPECIFICATION.md I.2 + C.7 + code comments → A.7 (boot phases) once, C.7 citing it

### G5.022 Two log tiers: persist what counts, print the rest
- **Req**: A failure that should count against `get_error_counter()` logs through the async `err_s()`/`wrn_s()`; the sync `err()`/`wrn()` are used only where no event loop is available (Timer callback), where a bus lock is held, or for a deliberately uncounted routine observation (a streak decrement, a boot-time degraded mode the owner chose not to persist), and each such site says which.
- **Class**: product
- **Sources**: CORE.N064, CORE.N065, CORE.N106, CORE.N158, CORE.N204, STOR.N062, STOR.N063, STOR.N080, STOR.N194, XCUT.N010 · stated at SPECIFICATION.md C.7 (:1866-1869); `src/system_service.py:150-154`; `src/base_classes.py:228-230`
- **Provenance**: A for the tier rule (C.7); O-confirmed for FRAM out-of-memory console-only (`892a591`/`e128331`, owner decision per harvest)
- **Truth**: holds at the sites read (`_error_check` decrement uses `pr.err`, Timer-starter failure uses `pr.err`); UART's sync-`err` repeats are disputed elsewhere (UART.S01, not in this group)
- **Fit**: refines OR35.b (everything still prints to console) · pillar P7
- **Home**: SPECIFICATION.md C.7 → unchanged

### G5.023 Persisted codes fit the one-byte history encoding
- **Req**: Every `errno` and `wrnno` passed to `err_s()`/`wrn_s()` is an integer 1-127 (errors stored as-is, warnings offset by `0x80`); 0 means "nothing to record", and a code outside the range is a defect, never a silently counted, unrecorded entry. Dynamic codes (the supervisor's `wrnno = n + 1`) stay within the range for any task count.
- **Class**: product
- **Sources**: CORE.N062, CORE.N066, CORE.N068, CORE.N070, CORE.N096, CORE.N160, CORE.N161, XCUT.N015, XCUT.N046 · stated at `src/print_log.py:61-65,158-172`; SPECIFICATION.md C.7 (:1876-1878)
- **Provenance**: C — encoding fixed in `print_log.py` constants (`_MAX_ERR = 0x7F`, `_NO_WRN = 0x80`); the "defect, not silent" half is new
- **Truth**: partly — out-of-range and errno 0 are counted but leave no history entry, reported only via `_diag` at level > 0 (`print_log.py:162-172`); `get_log()` reports a raw `0x80` sentinel as `ErrNum` 128 type `N` (`:189-192`); no test checks every call site's number is in range
- **Fit**: refines OR28.a (3) (catalog test) · pillar P7
- **Home**: code only → SPECIFICATION.md C.7.1 (catalog preamble) + OR28.a's catalog test

### G5.024 One global errno/wrnno catalog
- **Req**: Every `errno`/`wrnno` has one meaning project-wide, catalogued in SPECIFICATION.md C.7.1: 1-9 / 1-2 are the base-class meanings, reusable elsewhere only for the identical condition; common conditions share one number; module-specific codes sit in non-overlapping ranges; a retired code is never reused; the numbers readers rely on for diagnosis (e.g. FRAM errno 48 vs 37) are part of the contract; a test checks every call site against the catalog.
- **Class**: product
- **Sources**: CORE.N003, CORE.N206, CORE.N214, CORE.N252, CORE.N264, CORE.N267, CORE.N269, STOR.N009, STOR.N024, STOR.N051, STOR.N056, STOR.N087, STOR.N093, STOR.N163, STOR.N176, XCUT.N001, XCUT.N045, XCUT.N047, XCUT.N048, XCUT.N049, XCUT.N051 · stated at SPECIFICATION.md C.7 (:1871-1880), C.7.1 (:1902-1950)
- **Provenance**: O — OR28/OR28.a (owner, 2026-09-25: "This audit IS the next substantial change … yes to all"); the C.7.1 deferral "(owner decision, 2026-09-24)" is superseded
- **Truth**: drifted — C.7.1 says "No clash is live, since none shares a logger with a `SensorReader`", but `AsyNtpClient`, `AsyConnTime` and `NotificationCoordinator` are `SensorReaderConfig`s and log `wrnno` 1-3/1-5 on the same logger that `base_classes.py` uses for `wrnno` 1/2 (`asy_ntp_client.py:95,157,217`, `asy_wifi_service.py:132,323,360`, `asy_notification_service.py:135,355`); `api_response.py`'s errno 99 lands in the caller's logger and equals `FRAM_SPI`'s own 99; the FRAM row states the manager range two ways (10-88 vs 17-88); no catalog test exists
- **Fit**: restates OR28.a · pillar P7, P8
- **Home**: SPECIFICATION.md C.7/C.7.1 → C.7.1 as the one catalog + catalog test

### G5.025 One central repeat rule in `print_log.py`
- **Req**: A code identical to the newest entry of a logger's history spends no new slot — it is still counted, printed and written through — for every logger, RAM- or FRAM-backed, whatever the fault's origin; the per-module episode flags (`repeat=` in FRAM manager, UART, WIFI, NTP, NOTIFY, SGP40) go where this covers them.
- **Class**: product
- **Sources**: CORE.N019, CORE.N044, CORE.N047, CORE.N067, CORE.N099, CORE.N104, CORE.N116, CORE.N170, CORE.N266, STOR.N018, STOR.N039, STOR.N043, STOR.N044, STOR.N095, XCUT.N050 · stated at SPECIFICATION.md C.7.1 (:1912-1925), `src/print_log.py:167`
- **Provenance**: O — OR35.a/OR35.b (owner, 2026-09-26: "just don't repeat the same error in the slots. Pure and simple")
- **Truth**: drifted — not implemented: `_store_err()` dedupes only on a caller's `repeat=True` (`print_log.py:167`); `repeat=` is set by 6 modules (grep); C.7.1 text says "Three modules therefore dedupe" while its own table rows add NTP, NOTIFY and SGP40; C.7.1 says a chunk "logs into its OWNER's FRAM-backed history", but every chunk is built with `logger=self.pr`, the manager's RAM-only logger (`asy_fram_manager.py:622,672,716`); floods remain possible today (base `_error_check` errno 1 per failed cycle, config read errno 5/7 per read, wrnno 1/2 per GET, NTP-callback errno 1 per second for 120 s)
- **Fit**: restates OR35.b · pillar P1, P7
- **Home**: SPECIFICATION.md C.7.1 repeat-rule paragraph → rewritten to the central rule (OR35.a (4))

### G5.026 Error history stays a lean code-only ring
- **Req**: Each logger's history is a fixed ten-slot ring of codes with one shared, saturating (0xFFFF) error-and-warning counter and no timestamps or persisted episode state; readers filter `ErrType` to tell errors from warnings.
- **Class**: product
- **Sources**: CORE.N187, CORE.N228 · stated at SPECIFICATION.md H.6 (:4517-4518), E.8 (:3343-3345), `tests_scripts/test_digital_twin_ci_suite_errcount.py:24-46`
- **Provenance**: O-confirmed — OR35.a (3): "Protection over reboots is not required … layered limits are overkill on such limited device"; the ring shape itself is C (`print_log.py:132-142`)
- **Truth**: holds — `deque([_NO_ERR] * history_length, history_length)`, `_MAX_CNT = 0xFFFF`, no timestamp field (read)
- **Fit**: refines OR35.a · pillar P6, P7
- **Home**: SPECIFICATION.md H.6 + E.8 → C.7 (one statement), H.6/E.8 citing it

### G5.027 One debug level, pushed through a setter registry
- **Req**: `DebugLevel` (0-5, matching `PrintLog`'s levels) is one persisted system setting, applied to every logger — module and `CFGMGR_*` alike — by calling each registered `set_level()` whenever it changes; there is no shared mutable level object, and an unreadable settings store keeps the constructed level.
- **Class**: product
- **Sources**: CORE.N079, CORE.N084, CORE.N088, CORE.N174, CORE.N191, CORE.N232, CORE.N258, CORE.N259 · stated at SPECIFICATION.md A.7 (:562-564), `src/system_service.py:55-60,292-300,328-335`
- **Provenance**: O-confirmed — `72d658b` (2026-08-11): "Owner feedback on the previous commit's SharedLevel mechanism … explicit direction: … holds a list of" setters; `tests/_sensortask_scenarios.py:690-692` "owner requirement: system-wide, but no shared mutable value"
- **Truth**: holds — `set_level_setters(_collect_level_setters())` emitted at `buildgen/codegen.py:439`, `_collect_level_setters` walks `get_loggers()` (which includes `cfgmgr.pr`); A.7's "no interrupt handler touches logging" is true for hard IRQs only — soft Timer callbacks (`_timer_sequencer`) do call `pr.err/evt/one`, harmless since soft callbacks run in scheduler context
- **Fit**: extends P4 · pillar P4
- **Home**: SPECIFICATION.md A.7 → unchanged

### G5.028 `ResetErrors` clears for good, even in the boot window
- **Req**: `reset()` clears the RAM ring and counter and writes the cleared ring at once, even before `setup()`; it marks the logger initialized only when that write succeeded, so a later `setup()` never restores the old history over it; a module's `reset_error_counter()` also zeroes its consecutive-failure streak. A write the chip acknowledged but did not store is accepted: a clean bus transfer is trusted.
- **Class**: product
- **Sources**: CORE.N071, CORE.N163, CORE.N164, CORE.N165, CORE.N177, CORE.N021, CORE.N203, STOR.N175, XCUT.N019 · stated at SPECIFICATION.md C.7 (:1826-1846), `src/print_log.py:213-223`
- **Provenance**: O — "accepted behaviour, not a defect (owner decision, 2026-09-17)"; the unconditional write is agent-designed (`6cf82a1`, 2026-09-11)
- **Truth**: drifted — code and C.7 hold (`print_log.py:213-223`, `tests/test_print_log.py:477-513`), but `digital_twin/README.md:505-507` still says "a reset arriving before the loggers finish `setup()` is dropped by design and the restore then puts the old history straight back"
- **Fit**: extends P7 · pillar P7
- **Home**: SPECIFICATION.md C.7 → unchanged; `digital_twin/README.md:505-507` corrected (drift)

### G5.029 Expected conditions never log errors or warnings
- **Req**: No error or warning is logged for an expected, routine condition on any boot or in normal operation (startup jitter, a first boot's missing config file, a command-only schema with no stored values); a test for a normal situation never accepts or expects a logged `errno`/`wrnno`.
- **Class**: product
- **Sources**: CORE.N119, CORE.N126, CORE.N153, XCUT.N092 · stated in commit `8a45060`; OR18
- **Provenance**: O-confirmed — `8a45060` (2026-09-12): "Project owner's direction: this is a general principle - no error/warning should ever be logged for expected startup jitter on any boot, on any module - to be addressed, and audited for elsewhere"; OR18 (owner, 2026-09-25) names such tests as a bad-pattern hint
- **Truth**: partly — never written as a standing rule in CLAUDE.md/SPECIFICATION.md (only SGP40's fix at C.14.2); `ConfigManager.setup()` persists `wrnno` 3 "Config file … not found" on every first boot and `wrnno` 6 for a command-only schema (`config_manager.py:426,472`), and `tests/test_base_classes.py:1013-1015,1449-1451`, `tests/test_config_manager.py:2356` accept them
- **Fit**: restates OR18 (hint) and extends P1 · pillar P1, P7
- **Home**: commit message only → SPECIFICATION.md C.7 (new rule)

### G5.030 FRAM logs are evidence: read before clearing
- **Req**: Before any `ResetErrors` or other clearing, the FRAM-persisted error logs are read and saved; before treating one as evidence, what has run against that board is checked, since isolated-driver device scripts and bootloader entry can overwrite or tear production chunks; history survives a reflash.
- **Class**: hardware-safety
- **Sources**: CORE.N261, STOR.N131, STOR.N142, STOR.N144, STOR.N148, STOR.N149, STOR.N201, STOR.N210 · stated at CLAUDE.md "When investigating any unexpected real-hardware error", `tests_hardware/README.md:429-438`, SPECIFICATION.md C.7 (:1818-1822)
- **Provenance**: O-confirmed — `6cf82a1` records owner rulings incl. the FRAM-residue trap; OR28.a (1) and OR38.a (4) (owner, 2026-09-25/26) restate "read before clearing"
- **Truth**: holds as documented; `machine.bootloader()` mid-write tearing (F18) is still an open owner decision in BACKLOG "Real-hardware work still owed" (not checked further)
- **Fit**: restates CLAUDE.md FRAM rule, OR38.a (4) · pillar P7, P9
- **Home**: CLAUDE.md + tests_hardware/README.md → unchanged

### G5.031 Config flash writes: two sites, never retried
- **Req**: `ConfigManager` writes flash at exactly two sites — `setup()` at most once per boot when the file is missing or needs repair, and `_flush_staged()` once per accepted PUT that changed a value — and nothing re-runs either on its own (no timer, sleep, loop or retry); at most one flush per instance is in flight and `flush_pending()` awaits the latest.
- **Class**: product
- **Sources**: CORE.N043, CORE.N050, CORE.N052, CORE.N054, CORE.N142, CORE.N143, CORE.N148, CORE.N149, CORE.N188, CORE.N207 · stated at SPECIFICATION.md C.7.3 (:1994-2002)
- **Provenance**: O — C.7.3 "(owner, 2026-09-24)"
- **Truth**: holds — `tests/test_config_manager.py:2529-2541` pins 2 × `open(self.config_file, "w")`, no Timer/sleep/`while`, one `create_task(`; string counting only, so a renamed call escapes it
- **Fit**: restates OR42.a (1) · pillar P9
- **Home**: SPECIFICATION.md C.7.3 → unchanged

### G5.032 A failed config write costs persistence, never the config
- **Req**: A config write that fails (setup's create/repair, errno 4, or a deferred flush, errno 14) keeps the validated values live and never ends a task or causes a reboot; the client's `"Valid"` means validated and staged, and a later flush failure surfaces only as a logged code.
- **Class**: product
- **Sources**: CORE.N053, CORE.N058, CORE.N095, CORE.N111, CORE.N146, CORE.N147, CORE.N183, CORE.N197, CORE.N233 · stated at SPECIFICATION.md C.7.3 (:1986-1993), C.5.2 (:1749-1756)
- **Provenance**: O — C.7.3 "a reboot loop that also wrote the flash on each pass (owner, 2026-09-24)"
- **Truth**: holds — `_cache` committed in `finally` (`config_manager.py:382-384`), setup failure runs unpersisted (`:476-485`); consequence noted: a flush failing mid-`json.dump()` leaves an unparseable file (open "w" truncates first) that the next boot rebuilds from defaults, losing that change (`tests/test_config_manager.py:2259-2261`); no silicon trigger exists for a write failure
- **Fit**: extends P3 (degrade, never reboot) · pillar P3, P9
- **Home**: SPECIFICATION.md C.7.3 → unchanged

### G5.033 Repair writes are bounded by boots, no cross-boot skip
- **Req**: A config file that keeps failing its repair write is attempted once per boot and no further guard is added (no FRAM "repair failed last boot" flag), since FRAM is optional and only an unrelated reboot loop could repeat it.
- **Class**: product
- **Sources**: CORE.N208, UNSORTED.N001 · stated at SPECIFICATION.md C.7.3 (:2004-2011); commits `0615eba`, `58abf8f`, `12640c2`, `6b47a98`, `6b014dc`
- **Provenance**: O — "Bounded by boots, and deliberately no further - settled (owner, 2026-09-25)"; UNSORTED.N001 bundles further owner decisions (arduino/ out of scope, html_stub retired, four heap gaps not pursued, four consistency decisions — each placed with its topic in other groups)
- **Truth**: holds — no skip flag in `config_manager.py` (read)
- **Fit**: refines OR42.a (1) ("at most once per boot, then stable") · pillar P9
- **Home**: SPECIFICATION.md C.7.3 → unchanged

### G5.034 Flash writes only from user changes or boot repair
- **Req**: The firmware writes its flash filesystem only through `ConfigManager`: after an accepted REST PUT that changed a value, or once per boot in `setup()` repair; nothing else writes it, and docs state both paths wherever they argue safety from "no write in flight".
- **Class**: product
- **Sources**: CORE.N220, CORE.N231, CORE.N240, CORE.N241 · stated at CLAUDE.md:199-201, SPECIFICATION.md F.2 (:3639-3640), BACKLOG.md:167-173
- **Provenance**: O — OR42.a (1) (owner, 2026-09-26); BACKLOG #4 "Decided by the project owner: no — a write is fast enough not to matter" (no long-block coordination)
- **Truth**: drifted — CLAUDE.md:199-201 says "every real flash write is reachable only through the REST PUT path" and BACKLOG.md:168-170 "it never happens on its own", but `ConfigManager.setup()` rewrites files at boot on its own (`config_manager.py:474-481`); SPECIFICATION.md F.2 (:3639) narrows it to "triggered … through the REST PUT path — nothing else ever calls `write_config()`", which is true only of `write_config()`
- **Fit**: restates OR42.a (1) · pillar P9
- **Home**: CLAUDE.md hard rule + F.2 → corrected wording (rule drift: CLAUDE.md is an owner rule → OR13.a question)

### G5.035 Deferred-flush power-loss window is accepted
- **Req**: A power loss between an accepted PUT's response and its deferred flash write may lose that one change; this residual window is accepted, the next boot's `setup()` repairs whatever the interrupted write left, and the port-wide IRQ disable of the flash write stays off the request's own path.
- **Class**: product
- **Sources**: CORE.N089, CORE.N218, CORE.N219, CORE.N221, CORE.N237, XCUT.N026 · stated at SPECIFICATION.md F.2 (:3639-3660), `buildgen/codegen.py:502-504`
- **Provenance**: A — "one narrow, accepted residual window (WP5, 2026-09-16)" (agent); no owner words found
- **Truth**: unverifiable here — the "single scheduler tick" window size is an unmeasured estimate; littlefs power-loss repair not checked on silicon
- **Fit**: extends P3 · pillar P3, P9
- **Home**: SPECIFICATION.md F.2 → unchanged; accepted-limit list of the OR44.a principles Part

### G5.036 The in-RAM config cache is the only truth
- **Req**: After `setup()`, every read is served from the in-RAM cache, never from the file; an out-of-band edit or corruption of the file has no effect until reboot and is silently overwritten by the next accepted write — accepted because the device is the file's only writer.
- **Class**: product
- **Sources**: CORE.N141, CORE.N145, CORE.N190, CORE.N247 · stated at SPECIFICATION.md A.4 (:161-163), `src/config_manager.py:5-7`
- **Provenance**: A — "accepted: this device is the file's only writer" (agent, `83c08ae`)
- **Truth**: holds (read); the "only writer" premise excludes `mpremote`/device-script writes, which the hardware tier does make (config_HWTEST_*.cfg, OR38.a (4))
- **Fit**: extends P2 · pillar P2
- **Home**: SPECIFICATION.md A.4 → C.5 (the ConfigManager contract)

### G5.037 A ConfigManager is built with the full schema
- **Req**: Every `ConfigManager` — firmware, script or test on a real file — is constructed with the complete production schema of that file, because `setup()` drops and rewrites any on-disk key not in it; from the release on, persisted keys and file names change only with a migration, pinned by a golden stored-config fixture.
- **Class**: product
- **Sources**: CORE.N057, CORE.N152, CORE.N195, CORE.N262 · stated at SPECIFICATION.md C.5 (:1726-1729), `src/config_manager.py:461-463`
- **Provenance**: O — OR52.a (2) (owner, 2026-09-26) for the release-stability half; hazard text is A (`6f430c7`)
- **Truth**: partly — the hazard holds (`config_manager.py:446-463` removes unknown keys, wrnno 5); no golden fixture test exists yet; enforcement for scripts is caller discipline only
- **Fit**: restates OR52.a (2) · pillar P8, P9
- **Home**: SPECIFICATION.md C.5 → C.5 plus the config-key stability rule (Part L/C)

### G5.038 int→float coercion gap stays out of every bound
- **Req**: A JSON integer is accepted for a `"float"` field and converted; because single-precision `float` is exact only to 2**24 on the RP2040, no float field's bounds may exceed 2**24, and docs describe the int→float acceptance correctly.
- **Class**: product
- **Sources**: CORE.N040, CORE.N133, CORE.N134, CORE.N192, CORE.N216, CORE.N234, CORE.N260 · stated at SPECIFICATION.md A.8 (:620-622), F.1 (:3499-3502), `src/config_manager.py:130-134`
- **Provenance**: A — `3986be9` (agent) "accepted gap"; F (RP2040 single-precision build)
- **Truth**: drifted — code holds (largest float bound at HEAD `SeaLevelOffs` 5000.0, grep of `src/` schemas) but nothing pins the bound, and README.md:611-612 says an integer for a float field "is correctly rejected as `\"Invalid\"`", the opposite of `coerce_numeric()`
- **Fit**: new (a bound check in buildgen/tests closes the unpinned premise) · pillar P2
- **Home**: SPECIFICATION.md A.8 + F.1 → C.5 schema rules; README.md:611-612 corrected

### G5.039 Validation only through the shared primitives
- **Req**: Every numeric type/range check and coercion — schema-backed or dispatch-only with a synthetic `FieldSchema` — goes through `type_or_range_error()`/`coerce_numeric()`; nothing hand-rolls a cast or range comparison.
- **Class**: code-style
- **Sources**: CORE.N039, CORE.N222 · stated at SPECIFICATION.md G.2 (:4176-4178)
- **Provenance**: A — G.2 catalog (agent)
- **Truth**: holds for the generated `lightCmdLED` dispatch (`buildgen/codegen.py:547-550` calls `type_or_range_error()` with synthetic `_FIELD_LED_*` schemas); drifted comment: `src/config_manager.py:126-127` says that dispatch "has no FieldSchema … so it calls" `coerce_numeric()`
- **Fit**: restates Part G, OR24 · pillar P4
- **Home**: SPECIFICATION.md G.2 → unchanged; comment corrected

### G5.040 Per-field PUT results, one envelope
- **Req**: A config PUT reports each field independently — `"Valid"`, `"Unchanged"`, `"Invalid"` (bad or unknown key) or `"Failed"` (nothing stored: invalid manager or internal error; every requested key then reads `"Failed"`) — while the envelope stays `res: OK, code: 0`; `code == 0` is the only OK outcome, and the returned flag means "no exception", so callers read the per-field results.
- **Class**: product
- **Sources**: CORE.N004, CORE.N026, CORE.N048, CORE.N090, CORE.N093, CORE.N120, CORE.N128, CORE.N129, CORE.N144 · stated at SPECIFICATION.md C.5.3 (:1788-1798), H.6, `src/base_classes.py:326-336`
- **Provenance**: A — "Final project decision" labels in tests come from `04e8f40`/`ca7bffc` (agent, no owner quote)
- **Truth**: partly — holds for `_set_dict_cfg` (read); but one malformed default aborts the whole `write_config()` with `(False, {})`, dropping per-key results already computed (`config_manager.py:323-325`, pinned `tests/test_config_manager.py:1901-1903`), which then reads as all-`"Failed"`
- **Fit**: extends OR43.a (1) ("GET returns exactly what PUT accepts") · pillar P2
- **Home**: SPECIFICATION.md C.5.3 → unchanged

### G5.041 Schema mistakes are caught at authoring time
- **Req**: A config schema is well-formed by construction — six-element records, unique string names, `min <= max`, a `special` of the field's own type, a default that passes its own validation — and this is checked when the schema is written (build or test time), since at runtime a malformed schema only degrades silently (empty names list, every value rejected, duplicate names collapsed).
- **Class**: test-standard
- **Sources**: CORE.N037, CORE.N131, CORE.N132, CORE.N135, CORE.N136, CORE.N137 · stated at `tests/test_config_manager.py:130-132,148-150,477-479,661-663,809-811`, `src/config_manager.py:93`
- **Provenance**: C — only the default self-check exists (`check_cfg_get_default()`, `config_manager.py:190-208`); the rest is documented, not guarded
- **Truth**: partly — swapped bounds, duplicate names, non-string names and a wrong-typed bool special are not detected by any check found (tests pin them as documented limits)
- **Fit**: new (closes the gap between OR26's "incomplete construction" and runtime silence) · pillar P4, P5
- **Home**: test comments → SPECIFICATION.md C.5 + a schema-lint test (tests or buildgen validation)

### G5.042 `handle_set_cmd` keeps its own defense-in-depth catch
- **Req**: `api_response.handle_set_cmd()` wraps the setter dispatch and its post-write hooks in its own `except Exception` on top of Microdot's per-request catch, persists the failure (errno 99) on the dispatching module's logger and answers the generic `ERR`/100 envelope; the post-write hook fires at most once per call and only when a field reported `"Valid"`.
- **Class**: product
- **Sources**: CORE.N006, CORE.N007, CORE.N008, CORE.N009, CORE.N091, CORE.N092, CORE.N184 · stated at SPECIFICATION.md C.5.3 (:1792-1797), `src/api_response.py:89-105`, `pyproject.toml:230-232` (SLF001)
- **Provenance**: A — `ca7bffc` (2026-08-03): "project decision based on prior field experience with Microdot" (agent's wording, no owner quote)
- **Truth**: partly — holds as described; but a hook raising after fields were persisted answers `ERR`/100 with an empty `result` (pinned `tests/test_api_response.py:283-329`), hiding which fields landed; errno 99 collides with `FRAM_SPI`'s 99 (G5.024)
- **Fit**: refines OR26.a (Microdot handlers) · pillar P2
- **Home**: SPECIFICATION.md C.5.3 → unchanged

### G5.043 Persist first, then push; a failed push self-corrects
- **Req**: A setter persists (stages) a changed value first and only then pushes the coerced, persisted value to the hardware, only for fields reported `"Valid"`; a failed push reports `"Failed"` and silently re-persists the best known value (live read-back if schema-valid, else the pre-write snapshot of persisted keys, else the default) through `_set_mgr_cfg` only, never through the push path, so it cannot loop.
- **Class**: product
- **Sources**: CORE.N023, CORE.N027, CORE.N028, CORE.N030, CORE.N033, CORE.N108, CORE.N122, CORE.N123, CORE.N124, CORE.N186, CORE.N198, CORE.N201, CORE.N255 · stated at SPECIFICATION.md C.5.2 (:1744-1760), C.5.2.2 (:1777-1786), `src/base_classes.py:302-397`
- **Provenance**: A — `04e8f40` (2026-08-03) "persist-first then push" (agent), `a2ef9ec` recovery chain (agent); OR42.c (owner, 2026-09-26) extends this path to the SCD30 with the chip as its store
- **Truth**: partly — holds for `SensorReaderConfig` (read); `SCD30_Reader` bypasses it with its own `_set_dict_cfg()` (OR42.b); the recovery write's `(persisted, results)` is discarded, so a non-raising failed recovery is silent (`base_classes.py:394-397`); the recovered value may not match the chip (config↔chip divergence)
- **Fit**: restates OR42.c (1) · pillar P2, P9
- **Home**: SPECIFICATION.md C.5.2 → the OR42.c shared compare-before-write primitive (Part G)

### G5.044 Setters return bool; push callbacks registered once
- **Req**: Every setter returns `bool` (True = applied); push callbacks are registered once per instance at construction, never per call or shared across instances; a wrapper whose setter's `False` means "no-op" reports success once its type check passed, so a no-op is never read as a failed push.
- **Class**: code-style
- **Sources**: CORE.N107, CORE.N114, CORE.N115, CORE.N130 · stated at SPECIFICATION.md C.5.2 (:1763), C.5.2.1 (:1773-1776)
- **Provenance**: A — "Project-wide decision" in `tests/test_asy_wifi_service.py:649-651` from `4c555e0` (agent); per-instance registration from `04e8f40` (agent)
- **Truth**: not checked per setter (convention per driver)
- **Fit**: restates OR24 (uniform behaviour of equivalent methods) · pillar P4
- **Home**: SPECIFICATION.md C.5.2 → unchanged

### G5.045 Command-only fields are never persisted
- **Req**: A command-only field (special-alone: `def=None`, `special` set) is validated and always reported `"Valid"` so a repeated value re-triggers its action, is never stored, is skipped by the failed-push recovery chain, and is excluded from `get_dict_cfg()`'s field list because `ConfigManager.get_dict()` is all-or-nothing.
- **Class**: product
- **Sources**: CORE.N101, CORE.N102, CORE.N109, CORE.N110, CORE.N127, CORE.N140, CORE.N151, CORE.N200 · stated at SPECIFICATION.md C.5.2.1 (:1765-1776)
- **Provenance**: A — replaces legacy `cmd_keys` (agent design); OR42.c (2) (owner) applies the same "always carried out" semantics to SCD30's `ForceCalRef`/`AmbPres`/`ContMeas`
- **Truth**: partly — holds (`base_classes.py:380-383` skips it); a command-only schema still creates an empty `{}` config file on flash (`tests/test_config_manager.py:1119-1121`) and logs wrnno 6 (G5.029)
- **Fit**: refines OR42.c (2) · pillar P9
- **Home**: SPECIFICATION.md C.5.2.1 → unchanged

### G5.046 Lock-free reads only where no await intervenes
- **Req**: Shared state is read without a lock only where no `await` can run between the reads that must agree (the justification is written at the site); every `ConfigManager` serialises its own writes and flush on `config_lock`, and nothing relies on serialisation across two managers.
- **Class**: product
- **Sources**: CORE.N046, CORE.N051, CORE.N113, CORE.N150, CORE.N268 · stated at `src/config_manager.py:268-270,365-367`; PR #40 comment (issuecomment-5344576386)
- **Provenance**: A — code comments and PR #40 comment (agent)
- **Truth**: holds for `_get_values()`'s success path (no await, `config_manager.py:240-251`) and `monitor_loop()`'s three reads (per harvest; not re-traced at HEAD); the PR #40 justification lives only in the PR comment
- **Fit**: refines OR37.a (2) (races proven by analysis) · pillar P2
- **Home**: code comments → SPECIFICATION.md C.8 (concurrency model), with the NOTIFY justification moved from the PR comment

### G5.047 One hard-coded watchdog, created before anything can hang
- **Req**: Each generated boot entry constructs exactly one `WDT(timeout=8000)` — fixed, no injection point, 388 ms under rp2's 8,388 ms cap — as its first statement, before the device-module import (and so before the frozen-HTML `/html` mount, which raises `EEXIST` if `/html` exists on the filesystem); code that runs before it stays minimal.
- **Class**: product
- **Sources**: CORE.N167, CORE.N168, XCUT.N018, XCUT.N022, XCUT.N023, XCUT.N027, XCUT.N035, XCUT.N088 · stated at SPECIFICATION.md A.7 (:381), F.1 (:3445), `buildgen/codegen.py:381,703`
- **Provenance**: O — "must be hardcoded so no error ever can circumvent it" (owner, quoted from FINAL_WIRING_PLAN.md in `eaafc2f`); OR31.a (1) (owner, 2026-09-25) for "first statement … before the device-module import"
- **Truth**: partly — exactly one `WDT(` per generated device (`tests_scripts/test_buildgen_generate.py:48-53`); but it is the first line of `build_system()`, after `from sensortask_<device> import main` (`buildgen/codegen.py:381,703`); `tests/test_reset_call_site_invariant.py:30-45`'s WDT half is vacuous (skips `sensortask_*.py`, none committed) and its docstring still claims "exactly once per sensortask_<device>.py"
- **Fit**: restates OR31.a (1) · pillar P3
- **Home**: SPECIFICATION.md A.7/F.1 → A.7 boot entry; vacuous test half removed or repointed (OR33.a)

### G5.048 Watchdog fed only through `feed_watchdog()`
- **Req**: Every watchdog feed goes through `SystemService.feed_watchdog()`; at boot it is called after every one-time `setup()` in the batch and never inside a loop; at runtime the supervisor loop is the one feed site, never a Timer/IRQ callback, and no caller can keep feeding a hung system.
- **Class**: product
- **Sources**: CORE.N227, XCUT.N007, XCUT.N025, XCUT.N039, XCUT.N086 · stated at SPECIFICATION.md A.7 (:467-472), G.2 (:4253-4259), `buildgen/codegen.py:462-466`
- **Provenance**: O — OR31/OR31.a (2)-(3) (owner, 2026-09-25); WP6 feeds (agent)
- **Truth**: partly — feed after every setup holds (`tests_scripts/test_buildgen_generate.py:56-63`); single runtime feed site not enforced by a test; worst-case inter-feed boot step never computed (OR31.a (2)); `setup()` return values are ignored
- **Fit**: restates OR31.a · pillar P3
- **Home**: SPECIFICATION.md A.7 + G.2 → A.7, G.2 citing it; OR30 register for the margin

### G5.049 Reset delay and supervisor period sit inside the watchdog
- **Req**: `_RESET_DELAY` (4 s) stays below the watchdog timeout and `_TASK_CHECK_TIME` (2 s) well below it, with the relation checked by a test rather than a comment; a commanded reset is deliberate after that delay, not a failure.
- **Class**: product
- **Sources**: CORE.N076, XCUT.N003, XCUT.N004 · stated at `src/system_service.py:4-6,40,44`
- **Provenance**: C — comments "keep < watchdog timeout!"; no test ties them to the 8000 in `buildgen/codegen.py:381`
- **Truth**: holds numerically (4 s, 2 s vs 8 s); unenforced
- **Fit**: restates OR30.a (tunable register with dependants) · pillar P3
- **Home**: code comments → OR30 tunables section with a check

### G5.050 A reset that cannot arm starves the watchdog
- **Req**: When `_reboot()` cannot arm its reset timer, or the task-error budget is exceeded, the supervisor stops feeding the watchdog (one-way `_force_watchdog_starve`) so the hardware watchdog resets the device; docs describe the supervisor's explicit reboot path and this fallback as they are.
- **Class**: product
- **Sources**: CORE.N169, CORE.N180, CORE.N217, CORE.N230, CORE.N239, XCUT.N006, XCUT.N016, XCUT.N030 · stated at `src/system_service.py:95-97,127-130,246-252`, SPECIFICATION.md A.2 (:123-125), I.4 (d)
- **Provenance**: A — `6d730f0`/supervisor work (agent); owner-accepted as backstop (CLAUDE.md watchdog rule)
- **Truth**: drifted — code holds (`_reboot()` sets the flag on `(OSError, MemoryError)`); on budget overrun the supervisor calls `reboot_system()` and returns (stops feeding by leaving the loop), while A.2 says it "stops feeding the watchdog and lets it force a hard reset"; the silicon test exists (`tests_hardware/device_scripts/reboot_fallback_starves_the_watchdog.py`, `79423dd`) yet `tests_hardware/README.md:1280` and BACKLOG.md:89-90 still call it mock-only/open
- **Fit**: restates OR31.a (5) · pillar P3
- **Home**: code + A.2/I.4 → A.2 corrected; tests_hardware/README.md:1280 and BACKLOG.md:90 corrected (drift)

### G5.051 No software timeout races the hardware watchdog
- **Req**: A dropped soft-Timer callback (`start_timers()`'s wait, the one-shot reset timer) is left to the hardware watchdog; no software timeout is added that would race it.
- **Class**: product
- **Sources**: CORE.N250, XCUT.N075 · stated at SPECIFICATION.md F.1 (:3462-3464), commit `f3924e1`
- **Provenance**: O-confirmed — `f3924e1` (2026-07-18): "Owner's call: in the productive system, arming a real machine.WDT is always one of …"; F.1 "don't re-propose without a materially different justification"
- **Truth**: holds — `start_timers()` awaits `timers_running.wait()` unbounded (`src/system_service.py:209-214`)
- **Fit**: restates CLAUDE.md watchdog-backstop rule · pillar P3
- **Home**: SPECIFICATION.md F.1 → unchanged

### G5.052 Resets go only through `_reboot()`, storage paused first
- **Req**: `machine.reset()`/`machine.bootloader()` are called only from `SystemService._reboot()`, which pauses FRAM storage before arming the reset (and before the starve fallback); every new reset path goes through it.
- **Class**: product
- **Sources**: CORE.N166, STOR.N197, XCUT.N008 · stated at SPECIFICATION.md A.4 (:226-239), `tests/test_reset_call_site_invariant.py:14-27`
- **Provenance**: A — `6d730f0` (agent)
- **Truth**: holds — substring scan of `src/` (test) and `_reboot()` order (`system_service.py:118-130`); a repeated reboot request re-arms (postpones) the ONE_SHOT while storage stays paused
- **Fit**: extends P7 · pillar P7, P3
- **Home**: SPECIFICATION.md A.4 → unchanged

### G5.053 Tasks handle routine failures in place
- **Req**: A task ends — and the supervisor restarts it — only when a restart re-initialises something real (a sensor's re-probe, a fresh WLAN object); a failure it can meet again unchanged is handled in place (logged, counted, retried); an unexpected exception it cannot handle may end it. Each restart costs 100 of a 300 budget decaying 1 per clean 2 s pass, so the fourth death inside the window reboots.
- **Class**: product
- **Sources**: CORE.N159, CORE.N179, XCUT.N005, XCUT.N021, XCUT.N044, XCUT.N052, XCUT.N055 · stated at SPECIFICATION.md C.7.2 (:1952-1968), C.4 (:1657-1660), C.9 (:2206-2209)
- **Provenance**: O — C.7.2 "(owner, 2026-09-24)"; OR18/OR31.a (owner, 2026-09-25); budget constants C (`system_service.py:44-46`)
- **Truth**: holds for NTP and notification (C.7.2, confirmed on silicon 2026-09-25 per C.7.2); the hotspot DNS server task is outside supervision by design (C.9); budget numbers restated in C.4, `tests_hardware/README.md:1224-1227` and a test (drift risk, OR30)
- **Fit**: restates OR18, OR31.a · pillar P2, P3
- **Home**: SPECIFICATION.md C.7.2 → unchanged; budget constants into the OR30 register

### G5.054 Inoperational hardware escalates, not contained
- **Req**: A missing, dead or stalled chip, or a device config that does not match the board, is out of scope for containment: its init-failure restarts escalating through the supervisor to a reboot are intended.
- **Class**: product
- **Sources**: XCUT.N053 · stated at SPECIFICATION.md C.7.2 (:1970-1978)
- **Provenance**: O — "Out of scope (owner, 2026-09-24)"; OR18.a (owner, 2026-09-25)
- **Truth**: holds (C.7.2 text)
- **Fit**: restates OR18.a · pillar P3
- **Home**: SPECIFICATION.md C.7.2 → unchanged

### G5.055 Boot signature resolves once and stays for the boot
- **Req**: The boot signature is resolved once per boot (NTP time, else a random value after 120 s) and then never changes until the next boot, so an outside observer detects reboots by its change; a supervisor restart of the uptime task neither resets it nor fakes a reboot.
- **Class**: product
- **Sources**: CORE.N078, CORE.N087, CORE.N171 · stated at `src/system_service.py:42,300-304,375-394`, `tests/test_system_service.py:450-452`
- **Provenance**: A — agent tests/comments
- **Truth**: wrong — `status_counter()` sets `uptime` to 0 and `boot_signature` to `None` on every (re)start (`system_service.py:375-377`), while `start_time_set` stays True, so after a supervisor restart the signature stays `None` for the rest of the boot and uptime restarts from 0 (defect candidate)
- **Fit**: extends P7 (evidence readable) · pillar P7
- **Home**: code/tests → SPECIFICATION.md A.8 (`/status` fields)

### G5.056 Timer arming failures degrade, never reboot
- **Req**: Every `Timer.init()` catches `(OSError, MemoryError)` and degrades locally (skip this cycle, stop sequencing and release `start_timers()`, abort a pause rather than leave storage paused, run without uptime), logging at the level its context allows; none triggers a reboot.
- **Class**: product
- **Sources**: CORE.N172, CORE.N256, XCUT.N009, XCUT.N012, XCUT.N014, XCUT.N017, XCUT.N020, XCUT.N058 · stated at SPECIFICATION.md C.9 (:2219-2222)
- **Provenance**: O-confirmed — `1df8bc4` (2026-07-18): "Owner-confirmed recovery: log and keep running"; F (`Timer.init()` fails only with `OSError(ENOMEM)`, per C.9)
- **Truth**: holds — all 13 `Timer.init()` sites in `src/` catch `(OSError, MemoryError)` (grep); these degrades are print-only (sync context or owner choice), so they leave no FRAM evidence
- **Fit**: restates OR26.a (Timer callbacks) · pillar P3
- **Home**: SPECIFICATION.md C.9 → unchanged

### G5.057 Soft timers only; must-fire timers are PERIODIC
- **Req**: Every `machine.Timer` is soft; a periodic-read callback only sets an `asyncio.ThreadSafeFlag`; a timer whose firing must not be lost is `PERIODIC` (self-healing a dropped callback), and each remaining `ONE_SHOT` states why a dropped fire is acceptable or backstopped.
- **Class**: product
- **Sources**: XCUT.N056, XCUT.N057, XCUT.N061, XCUT.N074 · stated at SPECIFICATION.md C.9 (:2211-2216), F.1 (:3459-3462)
- **Provenance**: A — C.9/F.1 text (agent); F (soft callbacks dropped when the depth-8 scheduler queue is full, F.1)
- **Truth**: drifted — C.9 scopes PERIODIC to "anything that must keep firing", F.1 says "every timer that must fire uses `PERIODIC`"; four `ONE_SHOT` timers remain (`system_service.py:126` reset, `:166` stagger sequencer, `:367` storage unpause; `asy_ntp_client.py:283` retry), and the reset, sequencer and unpause callbacks do real work rather than set a flag; the reset case is backstopped only if the caller also stops feeding
- **Fit**: refines OR26.a, OR31.a · pillar P3
- **Home**: SPECIFICATION.md C.9 + F.1 → C.9 (one wording), F.1 keeps the platform fact

### G5.058 Read-trigger timers staggered across one second
- **Req**: Sensor read-trigger timers start staggered evenly across exactly one second in total, each at `t0 + k·slot` against one shared start so callback latency cannot accumulate, guaranteeing a minimum separation of one measured worst-case read for every combination of whole-second periods, with bus-sharing sensors placed furthest apart; the uptime, WiFi and NTP 1 s timers are outside the stagger.
- **Class**: product
- **Sources**: XCUT.N011, XCUT.N028, XCUT.N029, XCUT.N059, XCUT.N060 · stated at SPECIFICATION.md C.9.1 (:2224-2300), `tests_scripts/test_timer_stagger_no_coincidence.py`
- **Provenance**: O — C.9.1 "Design intent (owner-established)"; OR47/OR47.a (3) (owner, 2026-09-26) sharpens it to shared-start scheduling with a minimum separation
- **Truth**: partly — each one-shot is armed from the previous callback with `delay = 1000 // (N+1)` (`system_service.py:158-168`), so offsets accumulate latency; the proof test replicates the arithmetic instead of running the real sequencer and assumes whole-second periods from one common start
- **Fit**: restates OR47.a (3) · pillar P2, P6
- **Home**: SPECIFICATION.md C.9.1 → corrected per OR47.a (3)

### G5.059 Boot setup order constraints are generated and fixed
- **Req**: The boot `setup()` batch runs in a generated, fixed order in which FRAM precedes `sysfunct` (its `cfgmgr` logger needs FRAM) and `notification.setup()` follows `finalize()`; each ordering constraint is stated where the generator emits it and asserted per generated device.
- **Class**: product
- **Sources**: XCUT.N024, XCUT.N038 · stated at `buildgen/codegen.py:441-450`, SPECIFICATION.md A.7 (:463-464)
- **Provenance**: A — codegen comment (agent), single measurement "0ms against ~170ms"
- **Truth**: holds — `setup_order` puts `fram` first, then `sysfunct`, `conn`, `ntp` (`codegen.py:444-452`); per-device assertion of the order not found beyond the feed test
- **Fit**: restates OR47.a (1) · pillar P2
- **Home**: codegen comment + A.7 → A.7 (boot phases)

### G5.060 One standard timeout mechanism for wrappable calls
- **Req**: Blocking calls that genuinely can be timeout-wrapped (FRAM SPI transactions, `asy_udp_socket.py`'s `ready()`/`write_and_recvfrom()`) use one consistent timeout/cancellation mechanism instead of bespoke ones.
- **Class**: product
- **Sources**: XCUT.N077, XCUT.N089 · stated at SPECIFICATION.md F.2 (:3606-3607), BACKLOG.md:58-67
- **Provenance**: O — "PRIORITIZED (project owner, 2026-09-11): to be done soon" (BACKLOG.md)
- **Truth**: partly — open since 2026-09-11; no plan topic names it (per harvest), not re-checked in code
- **Fit**: extends P4 (one shape per job) · pillar P4
- **Home**: BACKLOG.md + F.2 → done in execution, then F.2 states the mechanism (OR5)

### G5.061 No resurrection of the long-block lock
- **Req**: A new genuinely long-blocking call gets a freshly designed coordination mechanism; the retired `get_long_block_lock()` is not brought back.
- **Class**: product
- **Sources**: XCUT.N078 · stated at SPECIFICATION.md F.3 (:3662-3667)
- **Provenance**: A — F.3 text; the related BACKLOG #4 decision ("no — a write is fast enough") is the owner's
- **Truth**: holds — no `get_long_block_lock` in `src/` (grep)
- **Fit**: restates CLAUDE.md "Long-blocking operations" rule · pillar P6
- **Home**: SPECIFICATION.md F.3 → unchanged

### G5.062 FRAM layout is fixed by generated construction order
- **Req**: FRAM chunks are allocated synchronously at construction by a bump allocator, so the generated construction order is the on-chip layout; every chunk-owning object is constructed unconditionally at top level, exactly once per boot, never in a branch or loop a restart can re-enter; within one firmware build the layout is deterministic and asserted per generated device. Layout need not survive a reflash and carries no fingerprint.
- **Class**: product
- **Sources**: STOR.N035, STOR.N083, STOR.N099, STOR.N104, STOR.N124, STOR.N155, STOR.N183, STOR.N202, XCUT.N032, XCUT.N033, XCUT.N034, XCUT.N036, XCUT.N037, XCUT.N041, XCUT.N063 · stated at SPECIFICATION.md A.4 (:215-226), A.7 (:360-365,379,394-485), C.14.2 (:2514-2518), K.7 (:5899-5901), `devices/dev.toml:96-98`
- **Provenance**: O — OR47.a (2) (owner, 2026-09-26): "there is no requirement for the fram to stay consistent through firmware re-flashes. it only must be rock solid within one individual firmware build"; the determinism rule itself is A
- **Truth**: drifted — SPECIFICATION.md:216-218, :379 and `tests/test_asy_fram_manager.py:88-90` still say the order "must stay identical across firmware versions"; A.7's hand-numbered chunk list is not machine-checked against the generator; per-device layout assertion not found; `devices/dev.toml:97` names an undefined "byte-identity constraint"
- **Fit**: restates OR47.a (2) · pillar P8, P2
- **Home**: SPECIFICATION.md A.4/A.7/K.7 → A.7 stated once (within-build determinism), others citing it

### G5.063 No module insists on FRAM
- **Req**: FRAM is optional hardware: every FRAM-backed logger and store works RAM-only when no FRAM is wired or a chunk cannot be allocated (allocation failure degrades, never raises), SGP40 runs without backup/restore, and `AsyFramManager`'s own log is the one deliberately RAM-only error source.
- **Class**: product
- **Sources**: CORE.N072, STOR.N071, STOR.N072, STOR.N100, STOR.N106, STOR.N107, STOR.N111, STOR.N150, STOR.N152, STOR.N160, XCUT.N042, CORE.N265 · stated at `tests/_sensortask_scenarios.py:422-428,501-503`, `digital_twin/README.md:497-502`, SPECIFICATION.md A.7 (:482-485)
- **Provenance**: O-confirmed — `ec2e803` (2026-08-11): "per the owner's explicit requirement that no module insist on FRAM"; WP Topic 11 (`9ac59cf`, per harvest): "every module shall have optional FRAM logging … FRAM module itself is the sole exception"
- **Truth**: holds — `PrintLogHistoryStore.__init__` falls back to `fram = None` (`print_log.py:236-240`); FRAM exemption pinned in `tests/_sensortask_scenarios.py` and `tests_scripts/test_digital_twin_ci_suite_errcount.py:117-119`; a chip dead at boot yields a non-None but unusable chunk that the None-chunk capacity check does not see (`tests/test_fram_integration.py:132-134`)
- **Fit**: extends P3 · pillar P3
- **Home**: tests + twin README → SPECIFICATION.md C.7 (logger contract)

### G5.064 FRAM capacity is a build-validity check, not a status field
- **Req**: "Does every chunk fit" is decided once per build by a `None` chunk from `get_chunk()`, checked per generated device (mock) and on the board by `mpremote` only; no `/status` field reports it.
- **Class**: test-standard
- **Sources**: STOR.N101, STOR.N138 · stated at `tests_hardware/README.md:1402-1414`
- **Provenance**: O — "mpremote-only by design (owner's own decision)" (`tests_hardware/README.md:1411`, added `a3d43f4`)
- **Truth**: holds (doc and WP4 check); `allocated_size <= size` is tautological and rightly not used
- **Fit**: refines OR16.a (1) · pillar P5
- **Home**: tests_hardware/README.md → unchanged

### G5.065 FRAM history restores all-or-nothing
- **Req**: After an abrupt reset a FRAM-backed history restores either completely or not at all, never partially or garbled, and the chunk still accepts writes afterwards; losing the whole history to a reset mid-write is accepted; a commanded reboot, which pauses storage first, loses nothing; a torn write leaves its trace (E31/W71/W72/W73).
- **Class**: product
- **Sources**: STOR.N103, STOR.N105, STOR.N109, STOR.N110, STOR.N112, STOR.N117, STOR.N121, STOR.N125, STOR.N126, STOR.N130, STOR.N151, STOR.N153, STOR.N154, STOR.N166, STOR.N167, STOR.N168, STOR.N200, STOR.N203, XCUT.N031 · stated at SPECIFICATION.md A.4 (:189-203)
- **Provenance**: O — "(project owner, 2026-09-11)" and "accepted (owner, 2026-09-11)" in A.4, `tests_hardware/flash/test_fram_storage.py:57-59`, `scripts/_digital_twin_ci_suite.py:726-728`
- **Truth**: holds at mock, twin and flash tiers per A.4 (named tests exist); loss rates (~1 in 8 unpaused, 20/20 paused) are single twin measurements, no silicon rate; `PrintLogHistoryStore.setup()` writes the empty ring on any failed read (`print_log.py:273-279`), so a transient read fault at boot also discards a good history (defect candidate, STOR.N066)
- **Fit**: extends P7 · pillar P7
- **Home**: SPECIFICATION.md A.4 → C.3.1 or a FRAM contract section (A.4 is architecture)

### G5.066 Two valid but differing copies are a hard failure
- **Req**: When both chunk copies pass their checks but differ, the read fails (errno 73) rather than guessing, since no generation counter says which is newer; the next successful write heals it.
- **Class**: product
- **Sources**: STOR.N049, STOR.N084, STOR.N170 · stated at SPECIFICATION.md A.4 (:214), `src/asy_fram_manager.py:173-177`
- **Provenance**: A — "Deliberate" (code comment, agent)
- **Truth**: holds (read; pinned `tests/test_asy_fram_manager.py:329-331`)
- **Fit**: extends P7 · pillar P7
- **Home**: SPECIFICATION.md A.4 → FRAM contract section with G5.065

### G5.067 Destructive readout: reads mark busy, WP gates reads
- **Req**: Because the FRAM reads destructively (read-then-restore), `_read_chunk()` writes a busy marker before reading and restores idle after; an interruption leaves the block unreadable until the next write, a write-protected chip refuses reads as well as writes (an access gate, not data loss), and none of this is "fixed".
- **Class**: product
- **Sources**: STOR.N086, STOR.N092, STOR.N120, STOR.N122, STOR.N129, STOR.N133, STOR.N136, STOR.N164, STOR.N165, STOR.N169, STOR.N181, STOR.N195, STOR.N198, STOR.N199 · stated at SPECIFICATION.md A.4 (:178-212), F.5.2 (:3752-3755)
- **Provenance**: O — "Intended, accepted behavior, not a defect (project owner, 2026-09-11)"; F — MB85RS64V p.17 and MB85RS2MTA p.18: "as an FRAM memory operates with destructive readout mechanism" (endurance note; reads count toward 10^12 / 10^13 cycles)
- **Truth**: holds — datasheet text confirmed in `dstxt`; A.4's claim that board capacitance is the primary mitigation (`c9dde56`) is unverifiable here (no schematics in repo)
- **Fit**: extends P7 · pillar P7
- **Home**: SPECIFICATION.md A.4 + F.5.2 → FRAM contract section, F.5.2 citing it

### G5.068 No retry on a transient SPI RX overrun
- **Req**: A 1.29 rp2 `OSError(EIO)` on a 32+ byte SPI read is not retried; the FRAM dual copy absorbs a single overrun and only an overrun hitting both copies degrades the read to `None`; test comments describe SPI's fault surface accordingly.
- **Class**: product
- **Sources**: STOR.N102, STOR.N179, STOR.N180, STOR.N186, STOR.N187 · stated at SPECIFICATION.md F.5.2 (:3724-3760), BACKLOG.md:474-476
- **Provenance**: O — "So no retry is added (owner decision, 2026-09-24)"; "SETTLED, owner, 2026-09-24" (BACKLOG); F (MicroPython v1.29.0 `ports/rp2/machine_spi.c`, per F.5.2 — not re-read here)
- **Truth**: drifted — policy holds; `tests/test_fram_integration.py:3-5` still says "Real RP2040 SPI write()/readinto() cannot raise or report a fault at all", contradicting F.5.2; recovery shown by mock/twin only; a transient read fault at logger `setup()` is not absorbed (G5.065)
- **Fit**: restates owner decision · pillar P3
- **Home**: SPECIFICATION.md F.5.2 + BACKLOG → F.5.2 only (BACKLOG item is settled, OR5); test comment corrected

### G5.069 FRAM lock scope: one whole block, synchronous under the lock
- **Req**: `FRAM_SPI` takes its driver lock and then the bus lock together for one whole block operation (~25 CS cycles), in that order; command bodies under the lock are synchronous and never await or log persistently — the owning coroutine yields between commands and its reporters log; shared scratch and CRC state are used only under these locks; a caller of `get_values()`/`set_values()` already holds the driver lock.
- **Class**: product
- **Sources**: STOR.N008, STOR.N010, STOR.N011, STOR.N022, STOR.N023, STOR.N040, STOR.N041, STOR.N081, STOR.N094, STOR.N128, STOR.N162, STOR.N177, STOR.N178, STOR.N190, STOR.N206, STOR.N207 · stated at SPECIFICATION.md A.4 (:170-178), C.8 (:2036-2047), C.7.1 FRAM row
- **Provenance**: O-confirmed — `8951387`: "The owner's answer to open decision item 6": per block operation, "Lever 3 … stays untaken"; C.8 "(owner's decision, 2026-09-18)"
- **Truth**: partly — scope and yields pinned (`tests/test_asy_fram_driver.py:1161-1162,1231-1233`, `tests/test_asy_fram_manager.py:2477-2511`); the `asy_lock.locked()` guard proves only that someone holds the lock (asyncio.Lock has no owner); `setup()`/`set_write_protected()` touch the shared scratch under the bus lock only
- **Fit**: refines OR41.a (2) (lock interaction cells) · pillar P6
- **Home**: SPECIFICATION.md A.4 + C.8 → C.8 (one place), A.4 citing it

### G5.070 FRAM wire traces are a pinned protocol contract
- **Req**: Every CS cycle, status byte, both copies and the CRC are integrity features, not redundancy: a restructure leaves the FRAM wire traces byte-identical, and a change to them is a protocol change, never an optimisation.
- **Class**: test-standard
- **Sources**: STOR.N013, STOR.N096, STOR.N097, STOR.N098, STOR.N116, STOR.N205 · stated at `tests/test_asy_fram_wire_trace.py:1-2,499-521`
- **Provenance**: O-confirmed — `6811f03`: "integrity features, not redundancy: recorded as the owner gave them"
- **Truth**: holds — trace test present; its header cites "HEAP_FRAGMENTATION_MEASUREMENTS.md archive 3B", which resolves only at `12640c2`; the WRDI after WRITE is datasheet-redundant (WEL resets "At the rising edge of CS after WRITE command recognition", MB85RS64V p.6) yet part of the pinned trace
- **Fit**: extends P7 · pillar P7
- **Home**: test header → SPECIFICATION.md FRAM contract section (archive pointer replaced)

### G5.071 Only the two datasheet-verified FRAM parts
- **Req**: The FRAM driver supports exactly the parts in `_KNOWN_PRODUCT_IDS` — MB85RS64V (8 KB, RDID `04 7F 03 02`, 2-byte addresses) and MB85RS2MTA (256 KB, `04 7F 48 03`, 3-byte addresses) — verifies at `setup()` that the chip's RDID matches the part implied by the configured `max_size` and refuses otherwise; a new size needs its own table entry, and each device TOML names its part next to `max_size`.
- **Class**: hardware-safety
- **Sources**: STOR.N001, STOR.N003, STOR.N004, STOR.N005, STOR.N014, STOR.N020, STOR.N031, STOR.N069, STOR.N073, STOR.N113, STOR.N114, STOR.N115, STOR.N145, STOR.N156, STOR.N157, STOR.N158, STOR.N159, STOR.N174, STOR.N193 · stated at `src/asy_fram_driver.py:30-43,183-188,392-393`, SPECIFICATION.md C.3.1 (:1596-1600)
- **Provenance**: F — RDID tables MB85RS64V p.10 (`04H 7FH 03H 02H`, density 64 kbit) and MB85RS2MTA p.10 (`04H 7FH 48H 03H`, 2 Mbit), confirmed in `dstxt`; dev's MB85RS2MTA confirmed on silicon (`tests/test_asy_fram_driver.py:104-105`)
- **Truth**: partly — IDs, byte order and address widths hold; the driver comment "can tell which … is actually wired" overstates (it only checks the ID expected for the configured size; a mismatch reads "not found"); `arzi`/`klkizi`/`grkizi`/`schlafzi` TOMLs state `max_size = 0x2000` without the part name (dev and wozi do)
- **Fit**: extends P2 · pillar P2
- **Home**: driver + C.3.1 → unchanged; TOML comments completed

### G5.072 Write protection is whole-array and read back from the chip
- **Req**: FRAM write protection sets or clears WPEN, BP1 and BP0 together (whole array, never a sub-range); the cached state is re-synced from the non-volatile status register at `setup()`, not from the constructor's `wp=`; the WP pin is active-low and without a `wp_pin` WP is assumed tied high.
- **Class**: hardware-safety
- **Sources**: STOR.N006, STOR.N017, STOR.N019, STOR.N021, STOR.N030, STOR.N032, STOR.N068, STOR.N075, STOR.N076, STOR.N147 · stated at `src/asy_fram_driver.py:61-65,217-251,387-396`
- **Provenance**: F — MB85RS64V p.6 (WPEN, BP1, BP0 "composed of nonvolatile memories"; WEL reset after power-on), p.11 (BP1/BP0 = 11 protects 0000H-1FFFH; WEL/WPEN/WP table: WP=0 with WPEN=1 protects the status register)
- **Truth**: holds against the datasheet (read); `_wp` only set when `SR & 0x8C == 0x8C` exactly, partial settings read as unprotected
- **Fit**: extends P2 · pillar P2
- **Home**: driver comments → unchanged

### G5.073 WEL is verified, a stuck latch only warns
- **Req**: Before a WRITE or WRSR the driver verifies via RDSR that WREN set the latch (a corrupted WREN is otherwise silently ignored) and aborts with a warning if it did not; a latch still set after the write and one WRDI retry is warned about, not treated as a failed write.
- **Class**: hardware-safety
- **Sources**: STOR.N015, STOR.N016, STOR.N027, STOR.N074 · stated at `src/asy_fram_driver.py:201-210,328-349,371`
- **Provenance**: F — MB85RS64V p.7 "WEL shall be set with the WREN command before" WRITE/WRSR, which "are not performed when WEL is reset"; policy A
- **Truth**: holds (read; pinned `tests/test_asy_fram_driver.py:403-405,695-696`)
- **Fit**: extends P2 · pillar P2
- **Home**: driver comments → unchanged

### G5.074 A clean bus transfer is trusted; integrity is the manager's job
- **Req**: `FRAM_SPI` never verifies payload bytes and trusts the chip when a bus transfer completed without error; payload integrity is `AsyFramManager`'s job (CRC, dual copy, periodic verify); a caller never trusts a buffer after a raise.
- **Class**: product
- **Sources**: STOR.N067, STOR.N079, STOR.N204 · stated at SPECIFICATION.md C.7 (:1836-1846), `tests/_fram_chip_fake.py:37-39`
- **Provenance**: O — "if the bus transfer completed without error, the chip is trusted" (owner decision, 2026-09-17, C.7; `d370413`)
- **Truth**: holds (read)
- **Fit**: extends P7 · pillar P7
- **Home**: SPECIFICATION.md C.7 → FRAM contract section

### G5.075 General-purpose FRAM API stays, with its caller discipline
- **Req**: `FRAM_SPI.verify_present()`, `set_write_protected()`, `get_write_protected()` and `get_size()` stay as general-purpose driver API even with zero station callers; a caller never invokes `verify_present()` or `set_write_protected()` while holding the FRAM lock (asyncio locks are not reentrant) and wraps them in a broad `try/except`.
- **Class**: product
- **Sources**: STOR.N007, STOR.N028, STOR.N033, STOR.N077, STOR.N119, STOR.N185, STOR.N191, STOR.N192 · stated at BACKLOG.md:52-57, `src/asy_fram_driver.py:357-360,401-411`
- **Provenance**: O — BACKLOG "SETTLED, do not re-raise" (repeated owner decision); OR36.a (3) (owner, 2026-09-26)
- **Truth**: holds — API present, zero `src/` callers (grep); caller-discipline half (`dbdb6da`) is not restated in BACKLOG or SPECIFICATION; `verify_present()`'s 1.0 s lock timeout rests on an unmeasured transaction cost (an OR30 tunable)
- **Fit**: restates OR36.a (3) · pillar P4
- **Home**: BACKLOG.md → SPECIFICATION.md C.3.1 (a settled fact leaves BACKLOG, OR5/harmonization 3)

### G5.076 Shipped code carries no test-only hooks
- **Req**: No parameter, attribute or branch in shipped code exists only so a test, the twin or a hardware script can reach inside; such a hook is removed and the test rebuilt from outside, and `src/` is never changed for a test alone.
- **Class**: product
- **Sources**: STOR.N065, STOR.N127, STOR.N137, STOR.N141 · stated at `src/asy_fram_manager.py:96,128,389,433-450`; BACKLOG.md:92-94 ("scratched, each needing a `src/` change for the test alone (owner, 2026-09-25)")
- **Provenance**: O — OR36/OR36.a (1) (owner, 2026-09-26); "src/ is never changed only for a test" (owner, 2026-09-25, G4 scratch `230a8df`)
- **Truth**: partly — `override_pause` on `_write`/`_read`/`clear`/`write`/`write_into` has zero `src/` callers (grep) and is exercised only by tests and `tests_hardware/device_scripts/fram_pause_unpause_and_gating.py`
- **Fit**: restates OR36.a (1) — tension with OR36.a (3) over whether a pause bypass is "a function of the hardware" (see Conflicts) · pillar P4
- **Home**: → SPECIFICATION.md E.9 / principles P4

### G5.077 Timestamped chunk contract
- **Req**: `AsyFramTimestampedChunk.write()`/`write_into()` return `(ntp_synced, utc, success)` in that order; timestamp 0 means "no valid time" (a real epoch-0 time is indistinguishable, accepted); a failed pack or unpack or clock step never raises (logged, then treated as no timestamp); the timestamp format matches `print_log.py`'s explicit little-endian convention; a staleness limit of 0 accepts any age.
- **Class**: product
- **Sources**: STOR.N037, STOR.N038, STOR.N058, STOR.N060, STOR.N088, STOR.N090, STOR.N171, STOR.N184 · stated at SPECIFICATION.md A.4 (:214-215), `src/asy_fram_manager.py:35-36,538-614`, DEVICE_REFERENCE.md:33-35
- **Provenance**: O-confirmed — tuple order "inherited API" recorded with owner decisions in `892a591`/`e128331` (STOR.N194, placed in G5.022); epoch-0 "Inherited from the deployed design" (A)
- **Truth**: partly — holds; the pack guard's message says "Unpacking Timestamp failed" (`asy_fram_manager.py:563`, it packs); negative ages (clock set back) pass a nonzero staleness limit too (`:609-614`), undocumented
- **Fit**: extends P1 (clock steps) · pillar P1
- **Home**: SPECIFICATION.md A.4 → FRAM contract section

### G5.078 Review quirks are pinned to measured behaviour
- **Req**: A genuine quirk found on review that is not a bug gets a comment and a regression test matched to measured behaviour, never a guessed fix; a regression found on the bench gets a test pinning the fixed behaviour.
- **Class**: test-standard
- **Sources**: CORE.N125, CORE.N173, STOR.N089, STOR.N196 · stated at SPECIFICATION.md D.1 (:2612-2613), `tests/test_asy_fram_manager.py:1464-1466`, `tests/test_base_classes.py:1429-1434`, `tests/test_system_service.py:1168-1173`
- **Provenance**: A — D.1 text (agent); the pinned quirks (negative `verify`, epoch-0) from `8eb1894`/`55e4453` (agent)
- **Truth**: holds for the cited pins (read)
- **Fit**: restates OR12, OR12.a (regression tests for proven defects) · pillar P5
- **Home**: SPECIFICATION.md D.1 → Part E test standard (D folds into the one checklist)

### G5.079 Storage pause is bounded and RAM-only
- **Req**: A commanded reboot pauses FRAM storage right before resetting and a REST `mempause` pauses it for a bounded window (300 s fixed, capped at 3,600 s) that ends by itself; a paused chunk drops (does not defer) its writes, reads and clears, and the pause never survives a reboot.
- **Class**: product
- **Sources**: CORE.N178, STOR.N045, STOR.N085, STOR.N123 · stated at `src/system_service.py:41,354-373`, `tests/test_asy_fram_manager.py:392-394`
- **Provenance**: A — code/tests (agent)
- **Truth**: holds — `pause_permanent_storage()` clamps to `_MAX_STORAGE_PAUSE = 3600` and aborts the pause if the unpause timer cannot arm; `_pause` starts False at construction; `clear()`'s wrnno 80 bypasses the episode dedupe (`asy_fram_manager.py:391-393`); auto-unpause is not exercised end to end in the twin
- **Fit**: extends P3 · pillar P3, P7
- **Home**: code → SPECIFICATION.md A.8 (`mempause`) + FRAM contract section

### G5.080 One CRC instance per sequence, finalised before reuse
- **Req**: A `crc_checks.py` instance serves one sequence at a time and every incremental sequence is finalised with `check_inc()` before the next starts; `add()`/`check()` are the documented exception to "never raises" (they propagate `MemoryError`) and every caller guards them; `crc_checks.py` itself is not modified.
- **Class**: product
- **Sources**: CORE.N154, CORE.N155, CORE.N157, STOR.N208 · stated at `tests/test_crc_checks.py:629-712`
- **Provenance**: O-confirmed — `23e5443`: "crc_checks.py is not to be modified" (owner decision per harvest); caller discipline A
- **Truth**: partly — guarded call sites tested in `tests/test_asy_uart_driver.py` only (per harvest, not re-traced); the empty-payload asymmetry (CORE.N156) is still open
- **Fit**: conflicts OR24/OR46.a if the empty-payload fix needs a `crc_checks.py` change — see Conflicts · pillar P2
- **Home**: test comments → SPECIFICATION.md G.2 (CRC primitive entry)

### G5.081 Behaviour changes need proof; improvements keep behaviour
- **Req**: A general improvement or modernisation keeps observable behaviour identical for every valid input with the full suite passing unchanged; a formula or behaviour discrepancy found in verification is not silently changed — it follows the OR12.a path (proof from datasheet or specification, a regression test, logged for the owner).
- **Class**: process
- **Sources**: XCUT.N067, XCUT.N070 · stated at SPECIFICATION.md D.1 (:2604-2607), D.8 (:2670-2674), D.9 (:2682-2684)
- **Provenance**: O — OR12/OR12.a (owner, 2026-09-25); D.1's "flag and ask" is CLAUDE.md's owner rule
- **Truth**: holds as a stated rule (review-only, no tool)
- **Fit**: restates OR12.a · pillar process
- **Home**: SPECIFICATION.md D.1/D.8 → the one ordered checklist (harmonization 4)

### G5.082 Cross-file consistency is kept by one shape per job
- **Req**: Every member of a related set takes the same shape (names, order, optionality, return convention); before writing something new the Part G catalog and the wider tree are searched for an existing primitive, a new primitive joins G.2 in the same change, and the G.3 grep-for-the-shape scan runs whenever a file enters `src/` — generated modules included. Within the audit an inconsistency is changed directly and logged (OR24.a), not flagged and left.
- **Class**: process
- **Sources**: XCUT.N069, XCUT.N071, XCUT.N079, XCUT.N080, XCUT.N081, XCUT.N084, XCUT.N085 · stated at SPECIFICATION.md D.10 (:2685-2693), G.1 (:4161-4172), G.3 (:4266ff), K.1 (:5715-5719), CLAUDE.md "Whenever a new file is added to `src/`"
- **Provenance**: O — OR24/OR24.a (owner, 2026-09-25: "Change, don't flag"); the scan rule in CLAUDE.md is an owner hard rule
- **Truth**: drifted — D.10 ("flag a questionable existing convention rather than silently diverging") and CLAUDE.md ("do not silently fix it. Report it and discuss") still state flag-and-discuss, which OR24.a overrides for consistency changes; no tool runs the scan; generated `sensortask_<device>.py` never "moves into src/", so Part D review never reaches it
- **Fit**: restates OR24.a, OR44.a (3) · pillar P4
- **Home**: SPECIFICATION.md D.10/G.1/G.3/K.1 + CLAUDE.md → the one ordered checklist; CLAUDE.md reworded after the audit (OR24.a (1))

### G5.083 Unrelated failures found on the way are recorded
- **Req**: Any unrelated issue or CI failure met while working is recorded (fixed, or parked with its reason), never passed over; external sources (repos, docs, forums, datasheets) are always fair game for grounding a decision.
- **Class**: process
- **Sources**: XCUT.N096 · stated in commit `9894e85`
- **Provenance**: O-confirmed — `9894e85` (2026-09-16): "Owner-given operating rules for the implementation phase … external sources are always fair game … keep BACKLOG.md updated with any unrelated issues or CI failures found along the way"
- **Truth**: partly — external-sources half is in CLAUDE.md/OR4; the "record unrelated CI failures" half is not stated in CLAUDE.md
- **Fit**: restates OR4, OR6 · pillar process
- **Home**: commit only → CLAUDE.md working agreements (OR51.a (4) close-out practice)

### G5.084 Dev tools are pinned
- **Req**: Every dev tool (ruff, mypy, shellcheck, actionlint, zizmor, pytest …) is pinned in `pyproject.toml`'s dev group, so an upstream release never changes the gate unannounced.
- **Class**: tooling
- **Sources**: UNSORTED.N004 · stated at CLAUDE.md "Code quality tooling"; PR #23 comment
- **Provenance**: A — CLAUDE.md text (agent), after an unpinned mypy 2.3.0 broke CI (PR #23)
- **Truth**: not checked (TOOL group owns pyproject pins)
- **Fit**: restates CLAUDE.md · pillar P5
- **Home**: CLAUDE.md → unchanged

### G5.085 Whole-file suppressions live in `pyproject.toml`
- **Req**: A lint rule suppressed for a whole file is listed in `pyproject.toml`'s per-file-ignores with its reason; an inline `# noqa` covers a single line and carries its reason on that line.
- **Class**: code-style
- **Sources**: UNSORTED.N003 · stated at PR #65 description; `pyproject.toml:314,317`
- **Provenance**: A — PR #65 "One discrepancy found, deliberately not fixed" (agent), later resolved (`454f6a2` per harvest)
- **Truth**: holds — `scripts/_digital_twin_ci_suite.py`'s S603 is now central (`pyproject.toml:317`); the two inline `noqa: B905` in `src/` carry a reason; `scripts/_generate_sensortask_modules.py:21-24` carries four reasonless `noqa: E402`
- **Fit**: refines OR24 · pillar P4
- **Home**: PR text → SPECIFICATION.md B (tooling) or CLAUDE.md "Code quality tooling"

### G5.086 Elapsed time only through `ticks_diff()`
- **Req**: Every elapsed-time computation on `time.ticks_ms()` values uses `ticks_diff()`/`ticks_add()`, never raw subtraction, and holds across the 2**30 ms rp2 wrap; stored-tick sites state why they cannot outlive a wrap.
- **Class**: product
- **Sources**: CORE.N176 · stated at `tests/test_ticks_rollover.py:1-3`, SPECIFICATION.md F.1
- **Provenance**: F — rp2 ticks period 2**30 (F.1); test enforcement C
- **Truth**: partly — raw subtraction is mechanically excluded (test; grep finds none in `src/`); the docstring's "same bounded-short-timeout shape" claim is not checked and stored-tick sites contradict it (DOC.S23 per harvest)
- **Fit**: extends P1 (counter wrap) · pillar P1
- **Home**: test docstring → SPECIFICATION.md F.1

### G5.087 Threat model: trusted home LAN, robust against bad input
- **Req**: The firmware assumes a trusted home LAN: unauthenticated REST writes (config, `SystemCmd` reboot/bootloader/mempause, `ResetErrors`), the webserver bound to 0.0.0.0, UDP services that trust their peers' source address, a 16-bit DNS transaction id and the hotspot's default radio security are accepted, documented limitations; radio-range attackers are out of scope. Malformed, partial or oversized input must never crash or wedge anything — that stays a defect.
- **Class**: product
- **Sources**: SEC.N001, SEC.N002, SEC.N005, SEC.N006, SEC.N011, SEC.N022, SEC.N031, SEC.N032, SEC.N035 · stated at `pyproject.toml:165-167`, `src/asy_udp_socket.py:9-10`, `src/captive_dns.py:66-67`, SPECIFICATION.md A.8 (:607)
- **Provenance**: O — OR52/OR52.a (3) (owner, 2026-09-26): "Threat model: trusted home LAN. Unauthenticated REST writes and `bootloader` are accepted properties, documented as known limitations"; S104 "decided by the project owner" (pyproject comment, `2badee1`)
- **Truth**: partly — the posture holds in code; no threat-model statement exists in SPECIFICATION.md yet (OR52's permanent home); host-side fixed ports (`package.json` preview 8000 on all interfaces, twin 8080 via `--host 0.0.0.0`) are dev conveniences outside the device's model; cyw43 AP default auth unverifiable here (cyw43-driver submodule absent from the `mp` checkout)
- **Fit**: restates OR52.a (3) · pillar P2
- **Home**: scattered comments → SPECIFICATION.md threat-model statement (CONSOLIDATION section 4, OR52)

### G5.088 The shared hotspot password is an accepted risk
- **Req**: The hotspot fallback password `"12345678"` is an accepted risk (exploitable only in radio range of a unit that already lost its WiFi), per-device configurable through each device TOML's `hotspot_password`/`HotspotPW`, falling back to the shared default at boot if out of bounds; it is not rotated or removed without the owner; every file carrying it is listed in `pyproject.toml`'s per-file S105/S106 exemptions so a new credential elsewhere still fails lint, and the docs list its real locations.
- **Class**: product
- **Sources**: SEC.N003, SEC.N008, SEC.N015, SEC.N016, SEC.N017, SEC.N018, SEC.N019, SEC.N020, SEC.N021, SEC.N023, SEC.N024, SEC.N025, SEC.N027, SEC.N028, SEC.N029, SEC.N037, SEC.N038, SEC.N040, SEC.N041, SEC.N043, SEC.N044 · stated at CLAUDE.md "Don't touch `sensors/config.json`…", SPECIFICATION.md L.2 (:6047-6058), `src/asy_wifi_service.py:50-53`
- **Provenance**: O — CLAUDE.md "accepted risk … not something to 'fix' by rotating/removing without the project owner's direction" (from `75f2e11`); OR52.a (3) radio-range out of scope
- **Truth**: drifted — CLAUDE.md names two sites (legacy `async_connect.py`, `src/asy_wifi_service.py`) and pyproject.toml:168-169 "three known, accepted sites", while the literal is in all six `devices/*.toml` (grep) and exempted in eight Python files (`pyproject.toml:252,304-305,340-347`)
- **Fit**: restates owner risk acceptance · pillar P2
- **Home**: CLAUDE.md + pyproject comments → one list in SPECIFICATION.md's threat-model statement, CLAUDE.md citing it

### G5.089 No real credential is ever committed
- **Req**: No real credential is committed; per-device config and host-spilled `config_*.cfg` files stay git-ignored; a generated throwaway credential (such as the bench AP password) is generated at use, never persisted in a tracked file, and not echoed into logs.
- **Class**: process
- **Sources**: SEC.N013, SEC.N014, SEC.N026, SEC.N039, SEC.N042, SEC.N045, SEC.N046 · stated at CLAUDE.md "Don't touch `sensors/config.json`…", `.gitignore:1-3,79-89`
- **Provenance**: O — plan 3.1 (owner, 2026-09-25): "a generated login … persisting one in a file rather is a stylistic (and not safety) breach which will need harmonization"
- **Truth**: partly — `.gitignore:89` covers root `config_*.cfg`; `toolchain/setup_toolchain.py:111` echoes every command's full argv, including `wifi-sec.psk <password>` (`:915-921`), and `:923` prints the password by design; the committed bench PSK (HW.T11, redacted in PR #107) is another group's item; no history secret scan in any gate
- **Fit**: extends OR52.a (7) (public repo) · pillar process, P9
- **Home**: CLAUDE.md → unchanged, plus the "generated, never persisted" clause

### G5.090 Static files served only from the frozen mount
- **Req**: A client-supplied static path is refused if it contains `..` and is opened only inside the frozen `/html` mount by the project's own code (Microdot's `send_file` receives the name only for its content type), falling back to 404 or the captive-portal redirect.
- **Class**: product
- **Sources**: SEC.N007 · stated at `ext/microdot.py:804-806`, `src/asy_webserver_service.py:649-667`
- **Provenance**: F — Microdot v2.6.2 security note ("The filename is assumed to be trusted"); implementation A
- **Truth**: holds — `".." in filename` → `abort(404)` before opening; `open(self._static_mount + "/" + filename + ".gz")` (read)
- **Fit**: refines OR52.a (3) (robustness against malformed input) · pillar P2
- **Home**: code comment → SPECIFICATION.md A.5 (REST layer)

### G5.091 Website renders untrusted text with `textContent` only
- **Req**: The website inserts every server- or user-supplied string with `textContent`, never `innerHTML`, and the build escapes every `<` in inlined JSON so `</script` cannot close the tag; both are enforced by a check, not by review alone.
- **Class**: product
- **Sources**: SEC.N030, SEC.N033, SEC.N034, SEC.N036 · stated at SPECIFICATION.md H.4 (:4371), H.7; `scripts/build_website.sh:54-57`
- **Provenance**: A — H.4 "standing guideline" (agent-authored table)
- **Truth**: partly — 0 `innerHTML` in `js/` (grep) and the escape exists; no eslint rule enforces it (`eslint.config.js` has no `no-restricted-*`), and `tests_js/templates.test.js:502-570` covers hostile labels/values but not keys that reach selectors/ids
- **Fit**: extends OR52.a (3) · pillar P2
- **Home**: SPECIFICATION.md H.4 → unchanged plus a lint rule

### G5.092 Port-binding capability granted fresh per run
- **Req**: Every runner that starts the Unix-port interpreter on a privileged port (the port-53 DNS test) grants `cap_net_bind_service` to that binary on each invocation (via `sudo` when not root), because a cached toolchain archive does not carry xattrs; nothing grants it more broadly.
- **Class**: tooling
- **Sources**: SEC.N009, SEC.N010, SEC.N012 · stated at `scripts/test.sh:280-288`, `scripts/run_digital_twin_ci.sh:39-47`, `scripts/run_unix_port_integration.sh:59-67`
- **Provenance**: A — script comments (agent)
- **Truth**: holds — three identical grant sites (per harvest; not re-read line by line)
- **Fit**: refines OR38.a (3) (fixed product ports) · pillar P5
- **Home**: script comments → SPECIFICATION.md E (runner contract), stated once

### G5.093 Protocol stand-ins track the vendored class by hand
- **Req**: A local `Protocol` standing in for a vendored class (e.g. `_RequestLike` for Microdot's `Request`, which mypy never sees) declares only the members the code uses, and is re-checked against the vendored source whenever the `ext/` pin moves.
- **Class**: code-style
- **Sources**: CORE.N002 · stated at `src/api_response.py:24-29`
- **Provenance**: A — code comment (agent)
- **Truth**: holds — `ext/microdot.py:463-464` still provides `Request.json` as a property (read)
- **Fit**: extends CLAUDE.md "re-check on version bump" practice to the Microdot pin · pillar P8
- **Home**: code comment → SPECIFICATION.md A.5 (Microdot layer) version-bump checklist

### G5.094 A config path that is not a file invalidates the manager
- **Req**: `ConfigManager.setup()` checks the path's stat mode against MicroPython's `MP_S_IFDIR` (0x4000, uniform across VFS backends) and, for a directory, logs `errno` 1 and stays invalid instead of opening it; any other read failure degrades to defaults.
- **Class**: platform
- **Sources**: CORE.N105 · stated at `src/config_manager.py:408-421`, `tests/test_asy_ntp_client.py:115-117`
- **Provenance**: F — MicroPython v1.29.0 `extmod/vfs.h:38` `#define MP_S_IFDIR (0x4000)`
- **Truth**: holds (read both)
- **Fit**: refines OR26 · pillar P2
- **Home**: code comment → unchanged

### G5.095 Schema getters are sync; one schema-less module
- **Req**: A module's schema is exposed as a plain sync `get_cfg_schema()` (no I/O) plus the public `cfg_schema` attribute; `NeopixelDriver` is the one module without a schema.
- **Class**: code-style
- **Sources**: CORE.N196 · stated at SPECIFICATION.md C.5.1 (:1736-1740), A.4 (:164-166)
- **Provenance**: O-confirmed — A.4: "the one deliberate exception to 'every module owns a schema' (no schema at all, confirmed by the project owner)"; sync shape A (`04e8f40`)
- **Truth**: holds — `SensorReaderConfig.get_cfg_schema()` and `SystemService.get_cfg_schema()` are sync (read)
- **Fit**: restates OR24 · pillar P4
- **Home**: SPECIFICATION.md C.5.1 → unchanged

### G5.096 Lock acquisition order is fixed
- **Req**: Locks are always acquired in the fixed order of SPECIFICATION.md C.8 (level 2 before level 1, driver before bus), across every driver, and a new lock takes its place in that order.
- **Class**: product
- **Sources**: XCUT.N054 · stated at SPECIFICATION.md C.8 (:2032-2034)
- **Provenance**: A — "audited across every driver with no violation" (agent)
- **Truth**: not checked — no mechanical guard exists
- **Fit**: refines OR41.a (2) · pillar P2
- **Home**: SPECIFICATION.md C.8 → unchanged

## Conflicts and tensions

1. **FRAM layout across reflashes** — SPECIFICATION.md:216-218 and :379 and `tests/test_asy_fram_manager.py:88-90`: instantiation order "must stay identical across firmware versions"; OR47.a (2) (owner, 2026-09-26): "no requirement for the fram to stay consistent through firmware re-flashes … only … within one individual firmware build". Resolution by provenance: the owner row wins (G5.062); the three texts are corrected in execution. `SPECIFICATION.md` C.7's pitfall "a FRAM-backed history survives … a reflash" stays true as a physical fact and is not a requirement.
2. **Unreachable defensive branches** (G5.013) — E.5.1's precedent and C.5's "don't remove them" (agent-authored, `9243049`, `acc4993`) vs OR46.a (2) (owner): "unreachable branches" are confirmed leftovers to remove. Resolution by provenance: OR46.a governs; a branch stays only where it guards an overridable extension point or a documented runtime failure (OR26.a's "true alternative flow" criterion), and each kept one is listed with its reason in E.5.1.
3. **Test hook vs hardware API** (G5.076 vs G5.075) — `override_pause` could be read as driver API (OR36.a (3)) or as a test hook (OR36.a (1)). Resolution by OR36.a's own line ("a function of the hardware the driver drives" stays): it bypasses the manager's software pause, not a chip function, and has zero `src/` callers, so it is a test artifact.
4. **Flag-and-discuss vs change-don't-flag** (G5.082) — SPECIFICATION.md D.10 and CLAUDE.md's `src/` scan rule say flag and discuss; OR24.a (owner) authorises direct consistency changes and orders CLAUDE.md reworded afterwards. Resolved by OR24.a.
5. **Deferred renumbering** (G5.024) — C.7.1 "renumbered … on its next substantial change, not in one pass (owner decision, 2026-09-24)" vs OR28.a (owner, 2026-09-25) "This audit IS the next substantial change". Resolved: OR28.a, the later row.
6. **Logger setup inside tasks** (G5.021) — SPECIFICATION.md C.7 (:1830-1834) describes per-task `pr.setup()` as the design; OR47.a (2) moves every chunk setup into the boot batch. Resolved: OR47.a; C.7's boot-window explanation is rewritten with it.
7. **WDT position** (G5.047) — A.7/codegen put `WDT()` first in `build_system()` after the module import; OR31.a (1) puts it first in the boot entry, before the import. Resolved: OR31.a.
8. **Stagger guarantee** (G5.058) — C.9.1 claims distinct offsets; OR47.a (3) requires shared-start scheduling and a minimum separation. Resolved: OR47.a.
9. **PERIODIC wording** (G5.057) — C.9 "anything that must keep firing" vs F.1 "every timer that must fire", while four `ONE_SHOT` timers remain. Not a rule conflict between owner statements (both agent text); resolved as drift: C.9's scope is the precise one, F.1 is reworded, and each `ONE_SHOT` states its backstop.
10. **Twin README vs C.7 on boot-window `ResetErrors`** (G5.028) — `digital_twin/README.md:505-507` "dropped by design" vs C.7/`print_log.py` "writes unconditionally … survives setup()". Code and C.7 (fixed 2026-09-11) win; plain factual drift, fixed in place (OR13.a).
11. **Plan's FRAM datasheet citations** — OR47 and `audit/CONSOLIDATION.md` §6 cite MB85RS64V tpu ≥ 0.6 ms at 3.3 V as "p.18" and say MB85RS2MTA's power-on values have no text layer; the extracted text puts MB85RS64V's power-on table on its p.17 and gives MB85RS2MTA's as text on p.18 (tpu ≥ 250 µs, tr ≥ 0.05 ms/V, tf ≥ 0.1 ms/V). Audit-file fact only; correct the citation when OR47.a's FRAM timing step is done.
12. **Boot signature stability** (G5.055) — `tests/test_system_service.py:450-452` pins "never re-picked" while `status_counter()` resets it to `None` on a task restart and never re-resolves it; a defect candidate for the OR12.a path, not an owner-rule conflict.

## Questions for the owner

1. **Quoting annotations: harmonise all, or touched lines only?** — (a) mass-edit every file to D.6 now under OR24 (one material; a large mechanical diff, AST-verifiable); (b) keep D.6's "not mass-edited" (2026-09-24) and accept two styles until each line is touched.
2. **Dynamic-import ban: firmware only or whole repo?** — (a) firmware only (`src/` + generated code; host tooling, tests and twin keep `__import__`/`importlib`, and F.1's "anywhere" is narrowed); (b) whole repo (~12 sites in twin, tests and buildgen rewritten to static imports or per-device dispatch tables).
3. **D.15: restore your lost 2026-09-13 ruling?** — Your ruling (comments move with their code; the reorder is a decided task) was dropped by merge `e5d2c43`; D.15 still says "no change to any comment". (a) restore the amendment and reorder every non-compliant class in the audit; (b) keep D.15 as written now.
4. **CLAUDE.md flash-write rule: name the boot repair path?** — The hard rule says every flash write is reachable only through REST PUT; `ConfigManager.setup()` also repairs files at boot. (a) reword to "an accepted PUT that changed a value, or at most one repair per boot" (meaning of the WiFi-backstop argument unchanged: no write is in flight once boot finished); (b) keep as is.
5. **First-boot missing config file: persisted warning?** — Your 2026-09-12 principle says no warning for expected startup conditions; every first boot persists wrnno 3 (and wrnno 6 for command-only schemas). (a) print-only for a missing file, persisted only for a corrupt one; (b) keep persisting (a later loss of the file is indistinguishable at boot).
6. **CRC empty-payload asymmetry vs "crc_checks.py not modified"?** — `add()` encodes a zero-byte payload no `check*()` can verify (flagged, never decided). (a) fix inside `crc_checks.py` despite the 2026-09 "not to be modified" ruling; (b) forbid zero-length payloads at the callers and leave the module untouched.

## No-requirement buckets

- descriptive: STOR.N025, STOR.N026, STOR.N029, STOR.N046, STOR.N047, STOR.N053, STOR.N132, STOR.N134, STOR.N135, XCUT.N087
- defect-candidate: CORE.N016, CORE.N045, CORE.N056, CORE.N100, CORE.N139, STOR.N042, STOR.N048, STOR.N050, STOR.N064, STOR.N066, STOR.N108, SEC.N004
- open-item: CORE.N156, CORE.N238, CORE.N242, CORE.N243, CORE.N244, STOR.N139, STOR.N182, STOR.N188, STOR.N189
- suppression-inventory: (none — every suppression item's rule is a candidate: G5.001, G5.003, G5.011, G5.012, G5.015, G5.042, G5.056, G5.085, G5.088)
- drift-only: CORE.N121, CORE.N235, STOR.N146, STOR.N161, STOR.N172, STOR.N173, XCUT.N040, UNSORTED.N002
- stale: CORE.N181, CORE.N182, CORE.N236, CORE.N248, CORE.N251, CORE.N253, STOR.N140, STOR.N143, XCUT.N093, UNSORTED.N005

## Accounting

Checked by script (every catalog ID placed exactly once; no ID outside the catalogs).

| Catalog | Items | In candidates | descriptive | defect-candidate | open-item | suppression-inventory | drift-only | stale |
|---|---|---|---|---|---|---|---|---|
| CORE | 269 | 251 | 0 | 5 | 5 | 0 | 2 | 6 |
| STOR | 210 | 185 | 9 | 6 | 4 | 0 | 4 | 2 |
| XCUT | 96 | 93 | 1 | 0 | 0 | 0 | 1 | 1 |
| SEC | 46 | 45 | 0 | 1 | 0 | 0 | 0 | 0 |
| UNSORTED | 5 | 3 | 0 | 0 | 0 | 0 | 1 | 1 |
| **Total** | **626** | **577** | **10** | **12** | **9** | **0** | **8** | **10** |

96 candidates (G5.001-G5.096).
