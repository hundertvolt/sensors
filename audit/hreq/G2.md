# Harvest requirements G2 — TEST (audit/harvest/TEST.md, 767 items) (HEAD 06eff58)
Read-only pass 1. Every TEST item was read in full; checks were run against HEAD `06eff58`, MicroPython `v1.29.0` source (scratchpad `mp`) and GitHub. "SPEC" = SPECIFICATION.md. Candidate IDs `G2.nnn`.
## Candidates
### G2.001 Unix-port runner: one file, fixed path layout
- **Req**: `scripts/test.sh` runs every `tests/test_*.py` from the repo root under the pinned Unix port with `MICROPYPATH=build/generated_src:src:tests:frozen_modules:.frozen`; `ext/` and `digital_twin/` stay off that path and a file needing them inserts them into `sys.path` itself (twin files put `digital_twin/` ahead of `tests/`). `_`-prefixed helper modules are never collected; any tool that re-runs a file (coverage, pytest probes) uses the same layout or states why not.
- **Class**: test-standard
- **Sources**: TEST.N003, TEST.N046, TEST.N299, TEST.N345, TEST.N421, TEST.N428, TEST.N477, TEST.N488, TEST.N513, TEST.N541, TEST.N563, TEST.N569, TEST.N573 · stated at CLAUDE.md Code quality tooling; SPEC E.1, E.3; `tests/test_setter_microdot_integration.py:19-21`; `tests/test_digital_twin_sgp40.py:5-7`
- **Provenance**: A — `7cd363d`/`d49a0f7` (2026-08-12, webserver TDD step: ext/ kept off MICROPYPATH to avoid a scripts/ change needing chroot re-verification); cwd convention is C
- **Truth**: holds — `scripts/test.sh:369` sets the path; mirrors in `tests_scripts/test_coverage_runner.py:35` and `test_digital_twin_boot_contiguity.py:31-32` are pinned by substring; `test_digital_twin_generated_boot.py:153` uses a second layout on purpose; SPEC E.3's single-file example omits `build/generated_src` (explained two paragraphs later)
- **Fit**: extends P8 (one layout, mirrors pinned) · pillar P5
- **Home**: SPEC E.1/E.3 (fine)
### G2.002 One interpreter process per test file
- **Req**: Each `tests/test_*.py` runs in its own Unix-port process, and process-wide swaps (`asy_spi_driver._SPI`, `asyncio.sleep`, `AsyUDPSocket`, `_FALLBACK_DNS_SERVERS`) are allowed only because of that; no runner may batch several test files into one process.
- **Class**: test-standard
- **Sources**: TEST.N181, TEST.N220, TEST.N331, TEST.N380, TEST.N389, TEST.N453 · stated at SPEC E.2.1; `tests/test_asy_fram_driver.py:10-13`; 7 files swap `_SPI` at import
- **Provenance**: C — 7 import-time `_SPI` swaps plus 10+ files with process-wide rebinding all rely on it; no stated owner decision
- **Truth**: holds — `scripts/test.sh:335-390` launches one process per file; SPEC E.2.1 derives the per-device split from it
- **Fit**: new (makes a load-bearing runner property explicit) · pillar P5
- **Home**: SPEC E.2.1 → Part E test standard
### G2.003 No test may hang
- **Req**: Every test is bounded: async test bodies run under `asyncio.wait_for` (a wedge is a fast FAIL), each file runs under a per-file `timeout` with retry and `stdbuf` line buffering, the pytest tier under one suite `timeout`; these backstops stay after any specific hang is fixed and never count as the fix for a race.
- **Class**: test-standard
- **Sources**: TEST.N058, TEST.N087, TEST.N274, TEST.N727 · stated at CLAUDE.md "Standing backstop: hanging tests are never allowed"; `tests/_uart_comm_harness.py:37-39`; `tests/test_asy_uart_driver.py:23-26`
- **Provenance**: O-confirmed — OR37.a (2) names "test.sh's per-file timeout-and-retry (hang backstop)" as one of "the two settled mechanisms"; CLAUDE.md bullet itself is A
- **Truth**: holds — `scripts/test.sh:314` 240 s, `max_attempts=3` (:319), `stdbuf` (:369); tests_scripts 1200 s (:238); per-test bounds exist only in some local `run()` helpers (most files' `run()` is a bare `asyncio.run`, item N337)
- **Fit**: restates OR37.a (2) · pillar P5
- **Home**: CLAUDE.md (fine)
### G2.004 microtest always exits explicitly
- **Req**: `microtest.run()` always ends with `sys.exit()` (0 on all-pass, 1 on any failure) and runs scratch teardown in `finally`, so tasks left parked in the shared queue can never hold a test process open.
- **Class**: test-standard
- **Sources**: TEST.N010, TEST.N147, TEST.N716 · stated at CLAUDE.md "Known hang cause #2"; `tests/microtest.py:30-42`
- **Provenance**: A — added fixing the twin soak exit hang (CLAUDE.md account)
- **Truth**: holds — `tests/microtest.py:42`; `tests/_boot_contiguity_probe.py:161` does the same
- **Fit**: restates CLAUDE.md · pillar P5
- **Home**: CLAUDE.md (fine)
### G2.005 Every test collected, empty files fail
- **Req**: Every test function a file defines must actually run: a file without its `microtest.run(globals())` trailer, a file that collects zero tests, an `async def test_*` that is never awaited, or a check missing from a hand-kept registry (e.g. `_uart_link_contract.ALL_CHECKS`) must fail the run instead of reporting `0/0 passed` or PASS.
- **Class**: test-standard
- **Sources**: TEST.N093, TEST.N144, TEST.N145, TEST.N655, TEST.N740, TEST.N750 · stated at `tests/microtest.py:14-42`; SPEC E.2; commit `c4046a5` (two files ran zero tests); `tests/_uart_link_contract.py:225`
- **Provenance**: O — OR16.a (2) "every test file is collected by some runner and CI job, none is orphaned"; OR15.a (2) summary counts
- **Truth**: partly — all 87 test files carry the trailer at HEAD and no `async def test_` exists, but nothing enforces either: `microtest.py:17-22` counts only callables, prints `0/0 passed` and exits 0; `test.sh:455-470` counts PASS from status files; `ALL_CHECKS` is hand-listed
- **Fit**: refines OR16.a (2) · pillar P5
- **Home**: `tests/microtest.py` + a `tests_scripts/` check → Part E test standard
### G2.006 Coverage and threshold runners are transparent
- **Req**: `tests/_coverage_runner.py` and `tests/_threshold_runner.py` exec a test file exactly as `scripts/test.sh` would, propagate its `SystemExit` code unchanged and never report success for a file that did not complete; a runner that stops applying its threshold or loses its data must fail a pinned test.
- **Class**: test-standard
- **Sources**: TEST.N028, TEST.N029, TEST.N080, TEST.N081, TEST.N082 · stated at `tests/_threshold_runner.py:1-24`; `tests/_coverage_runner.py:50-66`; `tests_scripts/test_threshold_runner.py:1-3`
- **Provenance**: A — introduced with the two-stage GC scheme and `--coverage` (agent sessions)
- **Truth**: partly — threshold runner pinned by `tests_scripts/test_threshold_runner.py`; coverage runner catches only `SystemExit` so any other escape skips the JSON dump (`_coverage_runner.py:50-66`), and a file without a footer returns 0 in both (`_threshold_runner.py:24`)
- **Fit**: refines OR40.a (2), OR16.a (2) · pillar P5
- **Home**: runner files + SPEC E.5 (fine)
### G2.007 Unit runs pinned to TZ=UTC
- **Req**: Every Unix-port test run exports `TZ=UTC`, because the Unix port's `time.mktime()` calls host libc `mktime()` (local time), while the rp2 firmware's is TZ-agnostic; a result obtained under any other TZ is not meaningful.
- **Class**: platform
- **Sources**: TEST.N520 · stated at CLAUDE.md "Local test runs pin $TZ=UTC"; `scripts/test.sh:20-23`
- **Provenance**: F — MicroPython v1.29.0 `ports/unix/modtime.c:191` calls libc `mktime(&time)`; rule text A (`f936c84` doc prune moved it)
- **Truth**: holds — `scripts/test.sh:23`; `tests_scripts/test_coverage_runner.py:35` mirrors it
- **Fit**: new (platform fact the unit tier depends on) · pillar P5
- **Home**: CLAUDE.md → SPEC Part F (platform) + E.3
### G2.008 Plain runs never use the settrace binary
- **Req**: Plain test runs use the Unix port built without `MICROPY_PY_SYS_SETTRACE` (`build-standard`); only `--coverage` uses `build-settrace`, and runners verify the variant by asking the binary (`hasattr(sys, "settrace")`), not by its directory name. Allocation figures taken under `--coverage` are never evidence.
- **Class**: tooling
- **Sources**: TEST.N546, TEST.N640, TEST.N665 · stated at CLAUDE.md Code quality tooling ("owner decision, 2026-09-21"); SPEC E.5.2; `scripts/test.sh:75-110`
- **Provenance**: O-confirmed — CLAUDE.md "Two Unix-port binaries are built, not one (owner decision, 2026-09-21)"
- **Truth**: partly — `scripts/test.sh:97` probes the variant; `tests_scripts/conftest.py:55-60` and `tests_js/_live_twin_command.js:12-13` take `build-standard` by path without probing (item N546, N588); `tests/test_asy_fram_allocation_budget.py:3-5` still says the test interpreter is a settrace build (drift)
- **Fit**: extends OR39.a (3) · pillar P5
- **Home**: CLAUDE.md, SPEC E.5.2 (fine)
### G2.009 Test heap is a measured floor
- **Req**: The unit tier runs every file at one fixed `-X heapsize` (16M) that is a measured floor; it is never raised to make a failing test pass, and per-file heap pressure is relieved at the root (split heavy files per device, smaller object graphs), not by a per-file heap override or a `gc.collect()` prop.
- **Class**: test-standard
- **Sources**: TEST.N038, TEST.N523, TEST.N656, TEST.N658, TEST.N659, TEST.N719, TEST.N735, TEST.N755, TEST.N759 · stated at CLAUDE.md "Known intermittent-MemoryError cause, fixed"; SPEC E.2.1, E.3.1; `scripts/test.sh:311-313`
- **Provenance**: A — `35ba8ac`/SPEC E.3.1 history (sessions 2026-09-17..22); not owner-attributed
- **Truth**: holds — `scripts/test.sh:369` `-X heapsize=16M`; one real-time test is the reason for the floor (SPEC E.3.1, commit `5cd7e30`): the heap value is load-bearing for a timing budget, not only memory
- **Fit**: refines OR39.a (2), OR30.a (1) (heapsize is a tunable) · pillar P6
- **Home**: SPEC E.3.1 → OR30 tunables section
### G2.010 Two full GC stages, threshold set once
- **Req**: Every MicroPython-running tier passes the whole suite at `gc.threshold(-1)` and again at the shipped 32768, each stage checked on its own; no test overrides the file's stage inside its own body except a test whose subject is the threshold itself, and then it restores the value it found.
- **Class**: test-standard
- **Sources**: TEST.N501, TEST.N585, TEST.N694 · stated at CLAUDE.md memory-safety rule; SPEC I.4(e)/(f); `tests/_threshold_runner.py`; `scripts/_digital_twin_ci_suite.py:1310`
- **Provenance**: O — OR40 "all tests to pass with gc threshold set to default, as well as to the agreed value as two full passes"
- **Truth**: partly — unit tier (`ci.yml:454-469`) and twin CI suite (`_digital_twin_ci_suite.py:1310` both values) hold; `tests/test_asy_webserver_service.py` sets `gc.threshold` 58 times inside test bodies (item N307), so its hammer tests run their own stage, not the file's
- **Fit**: restates OR40.a (2) · pillar P6
- **Home**: CLAUDE.md, SPEC I.4 (fine)
### G2.011 gc.collect in tests only as a measurement baseline
- **Req**: `gc.collect()` appears in test code only to set a clean baseline for a memory measurement; it never props up a test that would otherwise hit `MemoryError`, and the rule is machine-checked over `tests/` and `digital_twin/`, not only `src/` and `buildgen/`.
- **Class**: test-standard
- **Sources**: TEST.N119, TEST.N177, TEST.N307, TEST.N312, TEST.N378, TEST.N447, TEST.N690, TEST.N712, TEST.N738, TEST.N751, TEST.N758 · stated at CLAUDE.md memory-safety rule; SPEC I.4(e); `tests/test_fram_integration.py:159-177`; `tests/_webserver_concurrency_scenarios.py:835-861`
- **Provenance**: O — OR39.a (1) "gc.collect is allowed IN TESTS for getting a clean baseline for memory measurement tests"
- **Truth**: drifted — two props remain (`tests/test_fram_integration.py:177`, `tests/_webserver_concurrency_scenarios.py:861`, each justified in its comment as a Unix-port artifact/backstop); `scripts/lint.sh:42-49` and `tests_scripts/test_gc_collect_sites.py` guard only `src/`/`buildgen/`, and `lint.sh`'s comment calls tests/digital_twin "out of scope"; measurement sites (`test_uart_comm_hazard.py:502-515`, `test_asy_fram_allocation_budget.py:47`) read `mem_alloc()` in the very next statement, which OR39.a forbids although `gc.collect()` is synchronous (v1.29.0 `py/modgc.c:34-37`)
- **Fit**: restates OR39.a (1) · pillar P6
- **Home**: CLAUDE.md, SPEC I.4(f.1) + a test over tests/ and digital_twin/
### G2.012 Every MicroPython launch is scanned for allocation failure
- **Req**: Every run of MicroPython code the suite launches (unit, twin CI, flash, bench, and the JS/cross-browser live twins) is scanned for `MemoryError` or `memory allocation failed` and fails on either; all gates read one marker set, and the suite's own deliberate injections never borrow the interpreter's wording.
- **Class**: test-standard
- **Sources**: TEST.N524, TEST.N526, TEST.N528, TEST.N591, TEST.N691, TEST.N692, TEST.N693 · stated at CLAUDE.md "There are FOUR such gates"; SPEC I.4(e); `scripts/test.sh:321-333`; `scripts/_digital_twin_ci_suite.py:184-187`
- **Provenance**: O-confirmed — CLAUDE.md memory-safety rule "Standing rule, every test"; OR49.a (1) "no ... MemoryError marker (CLAUDE.md gates)"
- **Truth**: partly — the four gates agree, pinned by `tests_scripts/test_memory_error_gate_agreement.py`; the JS live twins (`tests_js/_live_twin_command.js:75-78`, `_live_matrix_command.js`) and `scripts/cross_browser_smoke.mjs:102-110` discard twin stdout, so those launches have no gate; the injection-wording walk covers `tests/` and `digital_twin/` only
- **Fit**: extends OR40.a (2) · pillar P5
- **Home**: CLAUDE.md, SPEC I.4(e) (fine)
### G2.013 Leak bounds are rates, soaks are liveness
- **Req**: A memory-leak assertion bounds retention per operation (a rate calibrated against a real and an injected leak), never an absolute heap delta; a soak's wall-clock budget is a liveness backstop set above the whole observed range, not a performance assertion.
- **Class**: test-standard
- **Sources**: TEST.N308, TEST.N670, TEST.N671, TEST.N676, TEST.N753 · stated at SPEC E.7 (:3275), E.8 (:3289); `tests/test_uart_comm_hazard.py` thresholds
- **Provenance**: A — `fd333ce` (2026-09-12, fixing red CI)
- **Truth**: partly — `test_uart_comm_hazard.py` uses per-transaction rates; `tests/test_asy_webserver_service.py:1833` still asserts an absolute 4096 B `mem_free()` drop
- **Fit**: refines OR19.a (1) (tolerances) · pillar P5
- **Home**: SPEC E.7/E.8 (fine)
### G2.014 No brute-force churn on the host
- **Req**: No test generates mass filesystem churn on the host; an invariant is proven structurally (assert the property the current code must hold, e.g. no `TmpScratch` op reads `tests/_tmp`'s shared root), never by reproducing a symptom at scale.
- **Class**: hardware-safety
- **Sources**: TEST.N083, TEST.N440, TEST.N710, TEST.N711 · stated at CLAUDE.md wear rule; `tests/test_tmp_scratch.py:165-178`; `tests/_tmp_scratch.py:1-3`
- **Provenance**: O — CLAUDE.md "(project owner's explicit, standing direction, 2026-09-17)"; OR37.a (1)
- **Truth**: holds — `tests/test_tmp_scratch.py` records the `os` calls of a full lifecycle; no gate measures host I/O generally (convention for new tests)
- **Fit**: restates OR37.a (1) · pillar P9
- **Home**: CLAUDE.md (fine)
### G2.015 Scratch dirs scoped per file key
- **Req**: Test scratch lives only under `tests/_tmp/<key>/`, one module-level `TmpScratch` per file with a key no other file (or concurrent process) uses; construction wipes that key's subtree and `microtest.run()`'s `finally` tears it down; a wipe that fails for any reason other than "absent" is reported, not swallowed.
- **Class**: test-standard
- **Sources**: TEST.N053, TEST.N084, TEST.N085, TEST.N086, TEST.N148, TEST.N441, TEST.N442, TEST.N443 · stated at `tests/_tmp_scratch.py:1-77`; SPEC E.1 "Two per-file resources must stay disjoint"; `tests/microtest.py:30-33`
- **Provenance**: A — `07b1e2b`/`894a84f` (tmp-growth fix, 2026-09-17)
- **Truth**: partly — all 43 `TmpScratch("…")` keys are distinct at HEAD but uniqueness is review-only; eight `except OSError: pass` in `_tmp_scratch.py` swallow every errno (item N085)
- **Fit**: refines OR38.a (1) · pillar P5
- **Home**: SPEC E.1 (fine)
### G2.016 Host tests never write the live tree
- **Req**: The host tier works in `tmp_path`; the only live-tree write allowed is a `devices/zz_test_*.toml` fixture (a reserved namespace no real device may use), removed in `finally`, swept by `scripts/test.sh` before generation and reclaimed at pytest session start; device discovery always filters that prefix, and no test deletes a repo-level state directory another concurrent test may be using.
- **Class**: test-standard
- **Sources**: TEST.N522, TEST.N535, TEST.N542, TEST.N547, TEST.N551, TEST.N555, TEST.N583, TEST.N597 · stated at SPEC E.1 "reserved namespace"; `tests_scripts/conftest.py:22-28`; `tests_scripts/_devices.py:15-18`; `scripts/test.sh:214-231`
- **Provenance**: A — `09c77e2` (2026-09-17, closing test.sh leak/orphan hazards)
- **Truth**: partly — sweep order pinned by `tests_scripts/test_test_sh.py:512-524`; the reserved-prefix guard inspects only `test_build_website_sh.py` (item N583); `test_bench_harness_helpers.py:138-143` globs `devices/` without the filter; both JS live harnesses `rmSync` the shared `digital_twin/config` (item N597, N603)
- **Fit**: refines OR38.a (1)(3) · pillar P5
- **Home**: SPEC E.1 (fine)
### G2.017 Test ports: disjoint bases, OS-chosen, one tier per fixed port
- **Req**: A test that picks its own port asks the OS (port 0); a fixed test port base lies below the ephemeral range (32768-60999), inside SPEC E.1's documented bands, and is disjoint from every other file's; product-fixed ports (53, 123, 18080) are redirected in tests or used by one tier at a time, and two suites that bind real ports never run concurrently.
- **Class**: test-standard
- **Sources**: TEST.N033, TEST.N101, TEST.N169, TEST.N222, TEST.N288, TEST.N325, TEST.N326, TEST.N327, TEST.N356, TEST.N408, TEST.N411, TEST.N415, TEST.N468, TEST.N472, TEST.N483, TEST.N494, TEST.N497, TEST.N527, TEST.N574, TEST.N590, TEST.N653, TEST.N654, TEST.N720, TEST.N721 · stated at SPEC E.1; CLAUDE.md "Two suites that both bind real ports"; per-file comments in `test_asy_dns_client.py:27-35`, `test_captive_dns.py:29-31`, `_webserver_concurrency_scenarios.py:70-77`
- **Provenance**: O — OR38.a (3) "a test that chooses its own port asks the OS for a free one ... product-fixed ports ... used by one tier at a time under a lock"; E.1 band text A (`35ba8ac`)
- **Truth**: partly — Linux default range confirmed (`/proc/sys/net/ipv4/ip_local_port_range` = 32768 60999); bands are review-only and E.1 says TCP bases sit in 17400-19999 while `_webserver_concurrency_scenarios.py:77` assigns 19700+200·i up to 20700; no lock exists for the concurrent-suites rule; `_free_port()` TOCTOU accepted
- **Fit**: restates OR38.a (3) · pillar P5
- **Home**: SPEC E.1 (fine) + a port-map check
### G2.018 Tests restore what they mutate; order-independent
- **Req**: A test that mutates process-wide state (class registries such as `Timer.all_timers`, `raise_on_arm`, RTC/pin/wiring-plan state, module globals, `gc.threshold`, `sys.modules`) restores it in `finally`, and no test depends on the run order of its file, which MicroPython's `globals()` does not preserve.
- **Class**: test-standard
- **Sources**: TEST.N074, TEST.N097, TEST.N138, TEST.N139, TEST.N141, TEST.N146, TEST.N153, TEST.N158, TEST.N189, TEST.N212, TEST.N309, TEST.N375, TEST.N387, TEST.N457, TEST.N463, TEST.N476, TEST.N478, TEST.N504, TEST.N557, TEST.N672, TEST.N747 · stated at `tests/machine.py:608-620,667-670`; `tests/_sensortask_scenarios.py:360-414`; SPEC E.8 (:3314); commit `c5478f0`
- **Provenance**: O — OR38.a (1) "Each test cleans up after itself on every path ... never relying on a later test"; order rule A (`c5478f0`); F: globals map is not ordered (v1.29.0 `py/objdict.c` sets `is_ordered` only for ordered dicts)
- **Truth**: partly — most sites restore by hand; `tests/machine.py:611,630` appends every Timer to a never-cleared class list in files that do not clear it (item N765); `tests_scripts/test_buildgen_twin_wiring.py:26-40` leaves the twin `machine` in `sys.modules`
- **Fit**: restates OR38.a (1) · pillar P5
- **Home**: Part E test standard
### G2.019 No task left parked
- **Req**: A test that starts a background task keeps an explicit reference and cancels and awaits it in its own `finally`; nothing is left parked in the shared task queue for a later test, and JS harnesses clear their own timers and child processes.
- **Class**: test-standard
- **Sources**: TEST.N305, TEST.N509, TEST.N594, TEST.N679 · stated at SPEC F.1 (:3540); `tests/test_asy_webserver_service.py:1244`; `tests/test_digital_twin_sensortask_integration.py:429-431`
- **Provenance**: A — SPEC F.1 text (agent sessions); O-backed by OR38.a (1) "no leaking"
- **Truth**: partly — rule stated and followed in the cited files; convention only (item N679), orphaned tasks once starved the heap (item N509)
- **Fit**: restates OR38.a (1) · pillar P5
- **Home**: SPEC F.1 → Part E
### G2.020 asyncio.run only from synchronous scope
- **Req**: A helper that calls `asyncio.run()` (every file-local `run()`, `hazard_pair()`, `build_pair()`, frame builders using `crc8_byte()`) is called only from synchronous test scope, never from inside a running coroutine, because nesting it crashes the Unix port instead of raising.
- **Class**: test-standard
- **Sources**: TEST.N068, TEST.N242, TEST.N337, TEST.N365, TEST.N393, TEST.N400, TEST.N450, TEST.N508, TEST.N717, TEST.N718, TEST.N742 · stated at CLAUDE.md "Known segfault cause, fixed"; `tests/test_asy_scd30_driver.py:908-910`; `tests/test_uart_comm_hazard.py:527`
- **Provenance**: A — `8850974` (2026-09-11, UART audit pass); first sighting `b686e32`
- **Truth**: partly — rule followed at the cited sites, review-only (item N717); CLAUDE.md's mechanism is wrong at v1.29.0: `extmod/asyncio/core.py:247-248` `run()` is `run_until_complete(create_task(coro))` on the SAME `_task_queue`, it installs no fresh loop (`new_event_loop()` runs only at import, :324), so the crash is re-entrant scheduling, not freed queue pointers
- **Fit**: new (test standard) · pillar P5
- **Home**: CLAUDE.md → SPEC Part F (mechanism) + Part E (rule)
### G2.021 Fake streams never behind a real select.poll
- **Req**: Any test double for a poller or stream (`uart.poller`, fake readers/writers, loopback harnesses) is a bounded pure-Python fake (`_StepPoller`, `LinkPoller`), never a real `select.poll()` over a non-fd object, which never sees readiness on GitHub runners and can crash the Unix port; the rule is asserted by a test for every harness, not only `LinkPoller`.
- **Class**: test-standard
- **Sources**: TEST.N088, TEST.N137, TEST.N264, TEST.N275, TEST.N283, TEST.N301, TEST.N384, TEST.N461, TEST.N462, TEST.N515, TEST.N699, TEST.N713, TEST.N714, TEST.N715, TEST.N743 · stated at CLAUDE.md "Known hang cause, fixed"; SPEC J.7 (:5614-5620); `tests/machine.py:579-601`; `digital_twin/machine.py:756-758`
- **Provenance**: A — `980f76f` (workaround), CLAUDE.md text via `f936c84`
- **Truth**: partly — asserted by `tests/test_machine_uart_link.py:113-119` (LinkPoller) and `tests/test_asy_uart_driver.py:439-445` (that file's two builders); every other fake stream is review-only; root cause on runners never explained
- **Fit**: new (test standard) · pillar P5
- **Home**: CLAUDE.md + SPEC J.7 → Part E
### G2.022 UART fakes count stalls, never take them
- **Req**: UART fakes serve `min(nbytes, queued)` and never block; the real per-byte `timeout_char` stall is counted in `would_have_blocked_bytes` with identical semantics in both fakes, and the never-block invariant is proven by recorded request sizes at the mock tier and by timing only on the bench.
- **Class**: test-standard
- **Sources**: TEST.N096, TEST.N134, TEST.N282, TEST.N682, TEST.N731, TEST.N752 · stated at CLAUDE.md UART never-block rule; SPEC F.5.8; `tests/machine.py:401-413`; `tests/test_asy_uart_driver.py:1464-1472`
- **Provenance**: O — CLAUDE.md "may never block the asyncio loop ... (project owner, 2026-09-11)"; the counting stand-in is A (`fd333ce`/`2b9c997`)
- **Truth**: holds — both fakes held to one count by `tests/_uart_link_contract.py:200-222`; the counter is class-level shared state reset per check
- **Fit**: refines CLAUDE.md UART rule · pillar P2
- **Home**: SPEC F.5.8 (fine)
### G2.023 Mock only at the raw bus transaction
- **Req**: Hardware-touching unit tests mock only the raw I2C/SPI/UART transaction (`tests/machine.py`), so each driver's protocol, CRC, locking and algorithm code runs unmocked; a stand-in above that level models only the one boundary it replaces.
- **Class**: test-standard
- **Sources**: TEST.N123, TEST.N155, TEST.N248 · stated at SPEC E.4; `tests/machine.py:1`; `tests/test_asy_sgp40_driver.py:1`
- **Provenance**: A — SPEC E.4 (agent sessions)
- **Truth**: holds — `tests/machine.py` fakes bus transactions only; SGP40/VOC run unmocked
- **Fit**: restates SPEC E.4 · pillar P5
- **Home**: SPEC E.4 (fine)
### G2.024 Direct tests plus the real chain
- **Req**: Every module has its own direct unit tests, and a module that is one layer in a real call chain is also driven through the actual chain to its real consumer; a shared module exercised only incidentally through a caller's fixtures is not covered.
- **Class**: test-standard
- **Sources**: TEST.N651, TEST.N703 · stated at SPEC D.12 (last sentence), K.6 (:5835-5844)
- **Provenance**: A — D.12 from `2a558f5` (2026-07-14 promotion checklist); K.6 shared-module rule added after the PR #87 gap
- **Truth**: partly — `tests/test_ntp_fram_system_integration.py:504-509` records starters never driven through a real SystemService before; ISL29125/UART starters still outside `_sensor_reader_owners()` (item N055)
- **Fit**: restates OR25.a (1) (four layers) · pillar P5
- **Home**: SPEC D.12/K.6 → the one ordered checklist
### G2.025 Per-parameter bound and float edge tests
- **Req**: Each driver's unit tests cover every parameter alone and in combination: both sides of every bound, the exact bounds accepted, `None`, and `NaN`/`±inf` on every float argument.
- **Class**: test-standard
- **Sources**: TEST.N702 · stated at SPEC D.12, K.6 (:5829-5833)
- **Provenance**: A — `2a558f5` (2026-07-14)
- **Truth**: not checked — per-driver completeness is OR25.a execution work
- **Fit**: refines OR25.a (1) · pillar P5
- **Home**: SPEC D.12/K.6 (fine)
### G2.026 Expected values from an independent source
- **Req**: A test's expected value comes from a source independent of the code under test (datasheet formula, second CRC implementation, hand calculation), never from the function under test or its current output; an exact reference is preferred where one exists, a sanity bound only where none can be derived.
- **Class**: test-standard
- **Sources**: TEST.N160, TEST.N249, TEST.N601, TEST.N650 · stated at `tests/test_asy_bmp3xx_driver.py:100-102`; `tests/test_asy_sgp40_driver.py:51-52`; SPEC D.12 "sanity bound, not an exact reference value"
- **Provenance**: O — OR19.a (2) "Expected values come from an independent source ... never copied from current output"
- **Truth**: partly — BMP3xx and SGP40 oracles are independent; `tests_js/_live_matrix_command.js:9-12` computes expected captions with the `formatFieldValue()` under test; SPEC D.12 asks for a sanity bound instead of an exact value (A, `2a558f5`)
- **Fit**: conflicts SPEC D.12 wording (resolved by OR19.a, see Conflicts) · pillar P5
- **Home**: Part E test standard; D.12 reworded
### G2.027 Every admitted response checked complete
- **Req**: A test of a served response checks that every admitted connection returns a complete, correct, parseable body, not only a status code; counting 200s is never sufficient.
- **Class**: test-standard
- **Sources**: TEST.N685 · stated at SPEC H.7 (:4618-4621); `tests/_webserver_concurrency_scenarios.py:675-678`
- **Provenance**: A — SPEC H.7 (agent sessions)
- **Truth**: partly — `_webserver_concurrency_scenarios.py:675-678` claims "complete and correct" but checks `isinstance(body, dict) and body`; `:817-818` accepts any non-413 status; the digital-twin HTTP client accepts an EOF mid-headers (`digital_twin/_http_client.py:66-72`)
- **Fit**: refines OR19.a (1) · pillar P5
- **Home**: SPEC H.7 → Part E
### G2.028 Emitted JSON checked as strictly as the browser
- **Req**: Every test of emitted JSON (streamed routes included) checks it with the strict RFC 8259 recogniser `tests/_strict_json.py`, because MicroPython's `json.loads()` accepts separators the browser's `JSON.parse()` rejects; the recogniser also rejects duplicate keys.
- **Class**: test-standard
- **Sources**: TEST.N078, TEST.N680, TEST.N764 · stated at SPEC F.1 (:3576); `tests/_strict_json.py:2-3`; commit `cca4fd3`
- **Provenance**: A — `cca4fd3` (gap found when a separator slip passed MicroPython's parser)
- **Truth**: partly — used by streamed-body tests; no check that every emitted-JSON test uses it; duplicate keys pass (`_strict_json.py:83-98`)
- **Fit**: new (closes a parser-oracle gap) · pillar P5
- **Home**: SPEC F.1 → Part E
### G2.029 Growing GET bodies stream, tests prove it
- **Req**: A REST GET whose body grows with configuration streams through `_stream_dict_response()`, and its tests assert the bounded stream shape itself, because a Unix-port heap cannot reproduce the contiguity failure the streaming prevents.
- **Class**: product
- **Sources**: TEST.N313, TEST.N689 · stated at CLAUDE.md memory-safety rule; SPEC I.3 (:4972-4976); `tests/test_asy_webserver_service.py:2615-2617`
- **Provenance**: O-confirmed — CLAUDE.md memory-safety bullet "the canonical example of this 'relieve the pressure' fix" under the owner-established discipline
- **Truth**: holds — `src/asy_webserver_service.py:181,411,418,479`; `_assert_body_is_bounded_stream()` called 7 times
- **Fit**: extends OR39/OR40 (design first) · pillar P6
- **Home**: CLAUDE.md, SPEC I.3 (fine)
### G2.030 A sensor fault never becomes a 500
- **Req**: A sensor read failure degrades `/measurements` to the data the reader already reports for a not-yet-read sensor; it never surfaces as an unhandled exception or a 500.
- **Class**: product
- **Sources**: TEST.N039 · stated at `tests/_digital_twin_construction_scenarios.py:196-199`; SPEC A.5
- **Provenance**: A — twin construction scenarios (agent)
- **Truth**: partly — asserted only as status 200 + key presence (`:196-199`); the injected SGP40 fault may never be consumed by that GET (item N035, not run)
- **Fit**: restates OR26, OR22.a (3) · pillar P2
- **Home**: SPEC A.5/A.7
### G2.031 Fakes model what src relies on, gaps stated
- **Req**: A fake models the real peripheral's semantics wherever `src/` depends on them (internal consistency with `src/`'s comparisons, not bit-for-bit silicon); every known gap (no CS edge, no auto-increment, no wire time, non-singleton buses, returning `reset()`, no WDT cap, AP never reaching GOT_IP) is stated in the fake, and a gap that can hide a real defect is closed or covered at a higher tier.
- **Class**: test-standard
- **Sources**: TEST.N018, TEST.N020, TEST.N041, TEST.N094, TEST.N095, TEST.N126, TEST.N130, TEST.N131, TEST.N132, TEST.N133, TEST.N135, TEST.N136, TEST.N142, TEST.N143, TEST.N149, TEST.N150, TEST.N151, TEST.N152, TEST.N161, TEST.N162, TEST.N193, TEST.N201, TEST.N262, TEST.N279, TEST.N330, TEST.N458 · stated at `tests/network.py:8-9`; `tests/machine.py` limits; `tests/_fram_chip_fake.py`; `digital_twin/machine.py:127-129`
- **Provenance**: A — `tests/network.py:8-9` SETTLED by an agent; O-backed by OR17 ("models the real hardware as close and as correct as possible") for the twin
- **Truth**: partly — most gaps are stated inline; `tests/machine.py:51-54` implies `pull` is honoured (it is not); dev's 256 KB FRAM fake keeps the 8 KB, 2-byte address model (`tests/_sensortask_scenarios.py:65-72`)
- **Fit**: extends OR17 to the unit tier · pillar P5
- **Home**: fake headers + `digital_twin/README.md` fidelity table
### G2.032 Fake semantics cite datasheet or pinned source
- **Req**: Every semantic a fake models on purpose (rp2 UART id/buffer bounds and C check order, `deinit()` no-ops, `write()` returning None on TX timeout, FRAM WEL, asyncio `Stream` behaviour) cites the datasheet page or pinned MicroPython source line it comes from, and is pinned by the fake's own tests; a knob that models non-datasheet behaviour says so.
- **Class**: test-standard
- **Sources**: TEST.N040, TEST.N042, TEST.N043, TEST.N044, TEST.N261, TEST.N270, TEST.N277, TEST.N278, TEST.N303, TEST.N373, TEST.N681 · stated at `tests/_fram_chip_fake.py:1-2,32-36`; `tests/test_asy_uart_driver.py:62-85,196-229`; `tests/test_fake_timer_and_network.py:1-3`; SPEC F.5.1
- **Provenance**: A — agent sessions (`90e8c17` for the deinit facts)
- **Truth**: partly — rp2 constants cited ("not guessed"); FRAM WEL claimed datasheet-verified without a probe; `test_fake_timer_and_network.py` asserts fakes against themselves, never against rp2 source
- **Fit**: refines OR29.a (2) · pillar P5
- **Home**: fake headers (fine)
### G2.033 A stand-in is never richer than the real class
- **Req**: A stand-in for a `src/` class offers no method or result shape the real class lacks, and a fake's signature never forces a non-idiomatic call shape onto `src/`; a conformance check compares each stand-in's surface with the real class.
- **Class**: test-standard
- **Sources**: TEST.N243, TEST.N302, TEST.N455, TEST.N700 · stated at `tests/test_asy_scd30_driver.py:1113-1118`; `tests/test_asy_webserver_service.py:53-55,98-100`; `digital_twin/README.md:225-230`; SPEC K.2 (:5743-5750)
- **Provenance**: A — derived from the masked SCD30 PUT 500 (agent sessions)
- **Truth**: partly — the webserver's one lightweight fake already defined `get_cfg_schema()` and masked a real 500; both machine fakes type `pull` as `int` with a `-1` sentinel, so `src/` cannot pass `pull=None` (K.2)
- **Fit**: new (closes the fidelity-masking class) · pillar P5
- **Home**: Part E test standard
### G2.034 Fakes bound their own allocation
- **Req**: A fake's own bookkeeping (call logs, recorders) is capped with a visible `dropped` count so it cannot surface as a `MemoryError` inside the code under test, and recorders are muted while a test measures `src/`'s allocation.
- **Class**: test-standard
- **Sources**: TEST.N127, TEST.N128, TEST.N385, TEST.N386, TEST.N452 · stated at `tests/machine.py:95-111`; `tests/test_machine_uart_link.py:171-186`; `tests/test_uart_comm_hazard.py:530`
- **Provenance**: A — `8cf7b81` (2026-08-13) introduced `_LOG_MAXLEN`
- **Truth**: holds — logs capped (4096 / `_LOG_MAXLEN`); readers of early entries must check `.dropped` (review-only, item N127)
- **Fit**: refines OR38.a (1) · pillar P5
- **Home**: fake headers (fine)
### G2.035 Mock and twin models share one semantic contract
- **Req**: Where the unit tier and the twin each model the same peripheral (UART link, stall counting, `readfrom_mem_into()` delegation, machine constants a driver uses), both are held to one shared contract suite: they may differ in fidelity, never in semantics, and a new `machine` constant or method goes into both fakes together.
- **Class**: test-standard
- **Sources**: TEST.N092, TEST.N382, TEST.N459, TEST.N490, TEST.N683, TEST.N695, TEST.N701, TEST.N732 · stated at SPEC J.7 (:5574-5578), K.5 (:5823-5825); `tests/_uart_link_contract.py:1-3`; `tests/test_machine_uart_link.py`, `tests/test_digital_twin_machine_uart.py`
- **Provenance**: A — `442a559` (2026-09-11, UART promotion test foundation)
- **Truth**: partly — UART link held by `_uart_link_contract.ALL_CHECKS` run against both; the two fault-injection APIs (`tests/machine.py` `inject_fault()` vs `digital_twin/_fault_injection.py`) are equal by convention only
- **Fit**: extends P8 · pillar P5
- **Home**: SPEC J.7/K.5 → Part E
### G2.036 Twin fakes are independent reimplementations
- **Req**: The twin's peripheral models (`digital_twin/neopixel.py`, the FRAM chip, the fault API) are reimplemented independently of `tests/`'s fakes, not imported from them, and agree with them only through shared contract tests.
- **Class**: test-standard
- **Sources**: TEST.N464, TEST.N469, TEST.N475, TEST.N491 · stated at `tests/test_digital_twin_fram.py:1`; `digital_twin/neopixel.py:1`; `digital_twin/_fault_injection.py:1`
- **Provenance**: A — agent sessions (SETTLED in comments, no owner words)
- **Truth**: holds — separate files; only the UART link has a shared contract
- **Fit**: conflicts P8/OR24 one material (duplicated fakes) — see Conflicts · pillar P5
- **Home**: `digital_twin/README.md`
### G2.037 Test-only hooks in the twin stay minimal
- **Req**: Test-only hooks inside `digital_twin/` (e.g. `Pin.reset_registry()`, `UART.settle()`) stay minimal, are marked as test-only, and never block the event loop in a way the real board could not.
- **Class**: test-standard
- **Sources**: TEST.N460 · stated at `digital_twin/machine.py:59-60,553-559`
- **Provenance**: O — OR20 "Try to avoid putting test code inside the digital twin ... keep tiny anyway"
- **Truth**: partly — hooks are marked; `settle()` busy-waits with `time.sleep_us()` (`digital_twin/machine.py:553-559`)
- **Fit**: restates OR20.a · pillar P4
- **Home**: `digital_twin/README.md`
### G2.038 Tests reach in from outside; product adds no seams
- **Req**: Tests adapt to the product from outside: they may reach private attributes (SLF001 exempt), replace module names (`json`, `deque`, `bytearray`, `asyncio.sleep`), rebind across `sys.modules` for from-import consumers, or use an armed one-shot `_StarvedAlloc`, restoring each in `__exit__`/`finally`; `src/` never gains a parameter, public attribute or non-`const()` value for them.
- **Class**: test-standard
- **Sources**: TEST.N008, TEST.N021, TEST.N037, TEST.N071, TEST.N122, TEST.N163, TEST.N216, TEST.N271, TEST.N361, TEST.N367, TEST.N370, TEST.N418, TEST.N500, TEST.N661, TEST.N733 · stated at SPEC E.4 (:2947-2957); `pyproject.toml:223` SLF001; `HEAP_FRAGMENTATION_MEASUREMENTS.md:154`; `tests/test_print_log.py:331-336`
- **Provenance**: O — OR36 "Testing must adapt to fit the code, never vice versa"; OR36.a (1)
- **Truth**: holds for tests — 15 harvested sites reach privates or substitute names from outside; the product side still has seams OR36 lists (`asy_ntp_client.py` `_NTP_UDP_PORT`, `asy_isl29125_driver.py` `address`), not this group's catalog
- **Fit**: restates OR36.a (1) · pillar P4
- **Home**: Part E test standard
### G2.039 const() values stay const; test mirrors are pinned
- **Req**: A `_`-prefixed `const()` in `src/` stays compiled away even when a test must then wait out a real bound or restate the value; every test literal mirroring one cites its source and is pinned to it by a source-reading check, so drift fails a test.
- **Class**: test-standard
- **Sources**: TEST.N002, TEST.N048, TEST.N157, TEST.N182, TEST.N185, TEST.N188, TEST.N195, TEST.N197, TEST.N214, TEST.N219, TEST.N221, TEST.N232, TEST.N254, TEST.N263, TEST.N310, TEST.N314, TEST.N324, TEST.N377, TEST.N409, TEST.N435, TEST.N496, TEST.N746 · stated at SPEC E.5.1 (leading-underscore fold); `tests/test_asy_bmp3xx_driver.py:21-30`; `tests/test_asy_wifi_service.py:14-23`; `tests/test_asy_ntp_client.py:50-52`
- **Provenance**: F — v1.29.0 `py/parse.c:857` folds only names starting with `_`; keeping them const is O (OR36.a (1): a non-`const()` value for tests is an artifact)
- **Truth**: partly — 22 harvested mirror sites; only the NTP schema is cross-checked (`test_asy_ntp_client.py:2256-2296`); `_PHASE_HOTSPOT = 2` has three hand copies (`tests/test_asy_wifi_service.py:19`, `_sensortask_scenarios.py:31`, `test_digital_twin_real_website_integration.py:27`); UART Class A constants copied into `tests/test_asy_uart_comm.py:33-45`
- **Fit**: extends OR36.a (1), P8 · pillar P5
- **Home**: SPEC E.5.1 + a `tests_scripts` const-mirror check
### G2.040 Duplicated test values come from one source
- **Req**: A value, dataset or call shape used by several test files or tiers (calibration datasets, reserved address ranges, special-field lists, UI strings, binary paths, http-client refusal rules, command signatures) lives in one shared helper or is derived from its source; where a copy is unavoidable a source-reading test pins it.
- **Class**: test-standard
- **Sources**: TEST.N004, TEST.N013, TEST.N014, TEST.N015, TEST.N051, TEST.N061, TEST.N102, TEST.N211, TEST.N295, TEST.N351, TEST.N360, TEST.N392, TEST.N465, TEST.N480, TEST.N540, TEST.N543, TEST.N544, TEST.N565, TEST.N588, TEST.N593, TEST.N595, TEST.N612, TEST.N615, TEST.N626, TEST.N635, TEST.N636, TEST.N652 · stated at SPEC E.1 (source-reading pytest checks); `tests/_bus_hazard_catalog.py:2-3`; `tests_js/definitions-mockdata-coverage.test.js:14-16`
- **Provenance**: O — OR43.a (3) "one source", P8 (OR10.a, OR51.a (4) single source of truth)
- **Truth**: partly — some mirrors pinned (`outer_cap_s`, `test.sh` ordering, `tests_scripts/test_tests_hardware_conftest_constants.py`); BMP calibration `28.460795242070162` still in three files (`_bus_hazard_catalog.py:86`, `test_asy_bmp3xx_driver.py:124`, `test_bus_hazard_multi_device.py:87`); `definitions-mockdata-coverage.test.js:14-16` claims a private copy "goes red" on disagreement, which it cannot
- **Fit**: restates P8 · pillar P8
- **Home**: Part E test standard
### G2.041 One shared helper per test double
- **Req**: A test double or helper used by several files (`_RaiseOnArm`, `_FastAsyncSleep`, `_wlan()`, raising FRAM fakes, frame builders, live-twin harness code) exists once in a shared `tests/_*.py` (or `tests_js/_*.js`) module; no rule requires test files to be self-contained.
- **Class**: code-style
- **Sources**: TEST.N159, TEST.N198, TEST.N237, TEST.N240, TEST.N251, TEST.N318, TEST.N340, TEST.N396, TEST.N407, TEST.N413, TEST.N419, TEST.N602 · stated at SPEC E.2.1 (shared `_*_scenarios.py` libraries); `tests/test_ntp_fram_system_integration.py:56-58` (cites "Part E's per-file self-containment convention")
- **Provenance**: O — OR24 "one material", OR46.b "adopt the solutions at any place they improve code tidyness"
- **Truth**: drifted — `_RaiseOnArm` exists in 7 files and `_FastAsyncSleep` in 10; `test_ntp_fram_system_integration.py:57-58` and `test_setter_microdot_integration.py:5-10` cite a self-containment convention SPEC Part E does not contain (grep), while 13 shared `tests/_*.py` modules exist
- **Fit**: restates OR24 for tests · pillar P4
- **Home**: Part E test standard
### G2.042 Device set derived, never listed
- **Req**: Every device-parametrised test derives its device set and per-device facts (drivers present, FRAM size, bus sharers) from `devices/*.toml` and the generated source at run time; a place that cannot glob (per-device MicroPython wrapper files, CI matrices) is checked against that derivation so a new device fails loudly, and no scenario assumes a driver every current device happens to have.
- **Class**: test-standard
- **Sources**: TEST.N032, TEST.N049, TEST.N054, TEST.N057, TEST.N065, TEST.N422, TEST.N423, TEST.N424, TEST.N425, TEST.N426, TEST.N427, TEST.N474, TEST.N518, TEST.N534, TEST.N536, TEST.N553, TEST.N556, TEST.N566, TEST.N567, TEST.N620, TEST.N657, TEST.N684 · stated at `tests_scripts/_devices.py:1-20`; `tests_scripts/test_device_tomls.py:247-269`; SPEC E.2.1
- **Provenance**: O — OR43.a (3) "Every check, test and build step derives the device set from devices/*.toml at run time ... never a hard-coded list or count"
- **Truth**: partly — host tier derives (`_devices.py`); the three `_DEVICES` tuples and 18 wrapper files are hand-kept but pinned by `test_device_tomls.py:247-269`; `tests_scripts/test_build_firmware.py:90-160` hardcodes wozi/dev; `test_buildgen_definitions.py:19-20`/`test_device_tomls.py:18-40` hand-keep per-driver sets; SCD30/SGP40 scenarios lack `_has()` guards (item N057); live JS PUT matrix and heavy twin tests are wozi-only
- **Fit**: restates OR43.a (3) · pillar P10
- **Home**: Part E test standard
### G2.043 Generated code is fresh before it is tested
- **Req**: A test that imports or boots generated modules runs only after buildgen regenerated `build/generated_src/` in the same run, never against a stale or missing tree.
- **Class**: test-standard
- **Sources**: TEST.N346, TEST.N589 · stated at SPEC E.3; `tests/test_bus_hazard_generated.py:48-50`; `tests_js/_live_twin_command.js:14-17`
- **Provenance**: A — SPEC L/E.3 (agent sessions)
- **Truth**: partly — `scripts/test.sh` and npm `pretest` regenerate; a bare `vitest run` or a direct Unix-port run tests whatever exists
- **Fit**: refines OR22.a (1) · pillar P8
- **Home**: SPEC E.3 (fine)
### G2.044 Four-tier bus-hazard coverage for every bus device
- **Req**: Every bus-facing device gets same-device read-vs-write concurrency, cross-device interleaving if it shares a bus, and an address/command sweep, at every tier that applies: sensor-specific hazards in the driver's own test file, generic shapes in the permanent `tests/test_bus_hazard_multi_device.py`, TOML-driven per-bus coverage in `test_bus_hazard_generated.py`; a new occupant without a catalog adapter or health field aborts with an actionable message, never skips.
- **Class**: test-standard
- **Sources**: TEST.N016, TEST.N023, TEST.N024, TEST.N245, TEST.N258, TEST.N347, TEST.N348, TEST.N349, TEST.N350, TEST.N352, TEST.N353, TEST.N473, TEST.N641, TEST.N642, TEST.N643, TEST.N644, TEST.N645, TEST.N646, TEST.N704, TEST.N709 · stated at CLAUDE.md bus-hazard hard rule; SPEC C.8 (:2070-2130), K.6 (:5871-5889); `tests/_bus_hazard_catalog.py:126-127`; `tests/test_digital_twin_bus_hazard_concurrency.py:81-86`
- **Provenance**: O — CLAUDE.md "(project owner's explicit, standing direction)"; SPEC C.8 "(project owner's own reframing, 2026-09-15)" for the two collections and never retiring the generic file
- **Truth**: partly — fail-loud adapters hold (KeyError at `_bus_hazard_catalog.py:358,531`; assert in twin file); CLAUDE.md names only `test_bus_hazard_multi_device.py` for the mock tier while SPEC C.8 places sensor-specific hazards in driver files (drifted); generated scheme is I2C-only; only the first writer is driven (`_bus_hazard_catalog.py:388-392`)
- **Fit**: refines CLAUDE.md rule; extended by OR41.a (3) to every shared resource · pillar P5
- **Home**: CLAUDE.md + SPEC C.8 (one text)
### G2.045 Interleavings forced, not hoped for
- **Req**: A concurrency test forces the interleaving it proves (scripted schedule, fake clock, barrier, a fake that suspends mid-transaction); a test that relies on a number of `sleep(0)` yields or on scheduler timing states that as a limit and does not claim the interleaving.
- **Class**: test-standard
- **Sources**: TEST.N164, TEST.N230, TEST.N366, TEST.N434 · stated at `tests/test_system_service.py:77-79`; `tests/test_asy_bmp3xx_driver.py:1224-1230`; `tests/test_config_manager.py:1750-1752`
- **Provenance**: O — OR37.a (3) "deterministic tests that force the interleaving (scripted schedule, fake clock, barrier)"
- **Truth**: partly — `_pump()` uses 5 yields per tick; BMP torn-read test proves the lock, not the interleaving; `test_asy_ntp_client.py:945-948` relies on one yield before the peer binds
- **Fit**: restates OR37.a (3) · pillar P5
- **Home**: Part E test standard
### G2.046 Unit tests drive time, not wait for it
- **Req**: Unit tests drive time through the fakes (`Timer.trigger()`/`drop()`, patched `asyncio.sleep`, synthetic ticks via `ticks_add()`, since `time` attributes cannot be monkeypatched); a real wall-clock wait is used only where a `const()` or the platform leaves no other way, and it is stated as such.
- **Class**: test-standard
- **Sources**: TEST.N006, TEST.N059, TEST.N140, TEST.N203, TEST.N208, TEST.N213, TEST.N238, TEST.N241, TEST.N255, TEST.N320, TEST.N390, TEST.N394, TEST.N398, TEST.N401, TEST.N404, TEST.N678 · stated at `tests/machine.py:646-660`; SPEC F.1 (:3528-3530); `tests/test_asy_isl29125_driver.py:806-808`
- **Provenance**: C — ~10 files patch `asyncio.sleep`, `Timer.trigger()` everywhere; counter-examples: neopixel, notification and notification-integration suites run on real sleeps (item N208, N213, N390, N394, N401)
- **Truth**: partly — pattern dominant; real-time suites remain and are timing-sensitive under 4× oversubscription
- **Fit**: refines OR39.a (3) (speed), OR37.a (3) · pillar P5
- **Home**: Part E test standard
### G2.047 Readiness waits: poll where a probe is harmless
- **Req**: A test waits for readiness by polling for the exact expected state with a bound; a fixed sleep is used only where a probe would disturb the property under test (e.g. a connection probe occupying a `max_connections` slot), and then its value is a measured tunable with its margin.
- **Class**: test-standard
- **Sources**: TEST.N034, TEST.N104, TEST.N498, TEST.N505, TEST.N512, TEST.N688, TEST.N756 · stated at SPEC H.8.1 (:4770-4772) JS rule; `tests/_webserver_concurrency_scenarios.py:129-140`; commit `a335923`
- **Provenance**: A — JS rule `7ae2f27` (2026-08-26); fixed-sleep choice `a335923` (polling probes broke the max_connections exactness tests)
- **Truth**: partly — JS tier polls; Python twin tests use 0.5-2.5 s fixed readiness sleeps sized from single ~400 ms measurements (item N104, N498, N505, N512)
- **Fit**: refines OR30.a (1) · pillar P5
- **Home**: SPEC H.8.1 + Part E
### G2.048 Every test timing value is a recorded tunable
- **Req**: Every timing value a test or runner depends on (sleeps, bounds such as `elapsed < 200`, loopback budgets, per-file/suite timeouts, vitest backstop, speed-probe thresholds) is a tunable with its measurement, margin and re-check trigger, sized so host load cannot fail a healthy test; accepted blind spots (e.g. no latency regression below 240 ms) are stated with the value.
- **Class**: test-standard
- **Sources**: TEST.N090, TEST.N112, TEST.N173, TEST.N215, TEST.N235, TEST.N269, TEST.N293, TEST.N294, TEST.N306, TEST.N359, TEST.N362, TEST.N371, TEST.N403, TEST.N416, TEST.N445, TEST.N446, TEST.N486, TEST.N487, TEST.N489, TEST.N511, TEST.N521, TEST.N550, TEST.N560, TEST.N561, TEST.N570, TEST.N582, TEST.N586, TEST.N611, TEST.N669, TEST.N696, TEST.N697, TEST.N698, TEST.N726, TEST.N760 · stated at SPEC J.7 (:5580-5612), E.7; README.md:146-149; `scripts/test.sh:129-131`
- **Provenance**: O — OR30/OR30.a (1) "per-file timeouts and retries ... twin run and soak durations"
- **Truth**: partly — values are commented with single dated measurements; no `@tunable` tag or register exists (grep); CPU-starvation hazard for twin budgets stays open (`test.sh:129-131`); one UART test sat at 1.25× margin until a CI failure
- **Fit**: restates OR30.a · pillar P5
- **Home**: OR30 tunables section
### G2.049 Tick arithmetic wrap-safe, proven by scan and argument
- **Req**: All deadline and interval arithmetic in firmware uses `ticks_diff()`/`ticks_add()`; because the Unix port's tick period (2**62) never reaches rp2's 2**30 wrap, wrap safety is proven by a source scan over every tick user (src and generated code) plus period-parametric code identity, and a test that names a rollover must actually cross one.
- **Class**: platform
- **Sources**: TEST.N202, TEST.N268, TEST.N438, TEST.N439, TEST.N649, TEST.N677 · stated at SPEC F.1 (:3528-3536); `tests/test_ticks_rollover.py:8-18,98-126`
- **Provenance**: F — v1.29.0 `py/mpconfig.h:1925` period = `MP_SMALL_INT_POSITIVE_MASK + 1`, `extmod/modtime.c:152`
- **Truth**: partly — `_KNOWN_TICKS_USERS` enforced both ways; the subtraction scan is per-line over `src/` only (not generated modules or `digital_twin/`); `test_asy_uart_comm.py:748-756` names the 2**30 rollover but only sets a deadline 1 ms in the past
- **Fit**: refines P1 (OR44.a counter wrap) · pillar P1
- **Home**: SPEC F.1 (fine)
### G2.050 Test code runs on MicroPython's subset
- **Req**: Unit-tier test code uses only what the pinned MicroPython provides: no `zip(strict=)` (ruff B905 suppressed with that reason), no `await` inside a comprehension (PERF401), no `dataclasses`; such a lint suppression names the platform fact and is re-checked on a version bump.
- **Class**: platform
- **Sources**: TEST.N026, TEST.N199, TEST.N227, TEST.N368, TEST.N485, TEST.N517 · stated at `tests/test_asy_isl29125_driver.py:174-176`; `tests/test_digital_twin_uart_link.py:411-413`; `tests/_bus_hazard_catalog.py:132-134`
- **Provenance**: F — v1.29.0 `py/objzip.c:39-40` `mp_arg_check_num(..., false)` rejects keywords; SPEC F.1 (:3542) records the comprehension limit
- **Truth**: holds — 6 harvested sites carry the reason
- **Fit**: extends CLAUDE.md version-bump practice to tests · pillar P5
- **Home**: SPEC F.1 (fine)
### G2.051 Unix-port quirks shimmed in tests only
- **Req**: Unix-port behaviour that differs from rp2 (socket `bind`/`sendto`/`connect` reject a `(host, port)` tuple, micropython#6924; `gmtime()` returns 9 elements; `settimeout` may be missing; opaque sockaddrs; leaked poll registrations) is worked around only in test code, recorded in SPEC Part F with its source, and each shim names its removal trigger; the rp2 path the shim bypasses is covered at the twin or hardware tier.
- **Class**: platform
- **Sources**: TEST.N170, TEST.N205, TEST.N223, TEST.N224, TEST.N233, TEST.N234, TEST.N289, TEST.N328, TEST.N329, TEST.N357, TEST.N358, TEST.N406, TEST.N414, TEST.N506, TEST.N739, TEST.N745 · stated at `tests/test_asy_dns_client.py:41-56`; `tests/test_asy_ntp_client.py:1422-1435,2051-2061`; `tests/test_captive_dns.py:41-44`; `tests/test_asy_isl29125_driver.py:2334-2347`
- **Provenance**: F — v1.29.0 `ports/unix/modsocket.c:231,376` take the address as a raw buffer; `ports/unix/modtime.c:138` builds a 9-tuple vs `extmod/modtime.c:65` 8; github.com/micropython/micropython/issues/6924 closed with the port unchanged
- **Truth**: partly — shims are test-side only; none names a removal trigger; SPEC Part F records neither #6924 nor the gmtime length (grep), so the rp2 tuple path is unexercised below the bench
- **Fit**: refines OR29.a (3) · pillar P5
- **Home**: test files → SPEC Part F
### G2.052 Every workaround names its trigger
- **Req**: Every workaround in the test tiers names the external defect it avoids (stub gap, mypy narrowing limit, Vitest API gap, filename length) and the event that removes it; `type: ignore` is preferred over mistyping an annotation to match a wrong stub.
- **Class**: process
- **Sources**: TEST.N252, TEST.N267, TEST.N355, TEST.N437, TEST.N587, TEST.N623 · stated at `tests/test_ticks_rollover.py:32-35`; `tests/test_asy_sgp40_driver.py:720-722`; `tests_js/_live_twin_command.js:1-3`; commit `0d68b8d`
- **Provenance**: A — audit method (CONSOLIDATION section 7: WORKAROUND = requirement plus removal trigger); stub-ignore choice A (`0d68b8d`)
- **Truth**: partly — `pyproject.toml:409-413` names one ("Revisit if Microdot ships hints"); the harvested test workarounds state none
- **Fit**: new (closes untracked workarounds) · process
- **Home**: Part E test standard
### G2.053 Tests never swallow what they should check
- **Req**: A test swallows an exception only for a named expected outcome (cancellation during cleanup, an address sweep that counts touched addresses, a readiness poll) and then asserts something the swallow cannot fake, e.g. touched-set equality; a bare `except: pass` or `except Exception` that can turn a failing run into a pass is not allowed.
- **Class**: test-standard
- **Sources**: TEST.N019, TEST.N106, TEST.N116, TEST.N156, TEST.N167, TEST.N172, TEST.N206, TEST.N246, TEST.N259, TEST.N336, TEST.N388, TEST.N592, TEST.N600, TEST.N607 · stated at `tests/_bus_hazard_catalog.py:196-538`; `tests/test_asy_scd30_driver.py:1520-1523`; `tests/_webserver_concurrency_scenarios.py:183-184,736-739`
- **Provenance**: O — OR19.a (1) red flag "`except: pass` in the test"
- **Truth**: partly — SCD30 sweep guarded by `touched == {_ADDR}`; SGP40 sweep asserts only a subset (`test_asy_sgp40_driver.py:2379-2405`); `_still_serving()` and connect loops swallow every `OSError`/`Exception`
- **Fit**: restates OR19.a (1) · pillar P5
- **Home**: Part E test standard
### G2.054 A check that exercised nothing does not pass
- **Req**: A scenario or check that finds nothing to exercise (no writer on a bus, no broadcaster, device lacks the module, fake knob not armed, transaction timed out before the corruption check, empty section list) reports skipped or fails; it never counts as PASS.
- **Class**: test-standard
- **Sources**: TEST.N025, TEST.N056, TEST.N383, TEST.N449, TEST.N554, TEST.N614, TEST.N638 · stated at `tests/_bus_hazard_catalog.py:415-416,507-515`; `tests/_sensortask_scenarios.py:289,890`; `tests/test_machine_uart_link.py:103`; `tests/test_uart_comm_hazard.py:745-767`
- **Provenance**: O — OR21.a (3) "a test that passes with nothing actually checked is reported as such"
- **Truth**: partly — several scenarios `return` early as PASS; `tests/test_reset_call_site_invariant.py` keeps a permanently vacuous `WDT()` half (SPEC A.4 :230-236); UART corruption tests guard by budget choice
- **Fit**: restates OR21.a (3) · pillar P5
- **Home**: Part E test standard
### G2.055 Skips visible, never passes
- **Req**: Every skip, deselect and opt-in gate is listed with a still-valid reason and counted in the run's summary; a skip caused by a missing build (Unix port, settrace variant, generated tree, toolchain) is never reported as a pass, and a hard prerequisite (e.g. `node`) fails rather than skips consistently across tiers.
- **Class**: test-standard
- **Sources**: TEST.N539, TEST.N545, TEST.N559, TEST.N562, TEST.N568, TEST.N580, TEST.N584, TEST.N596, TEST.N616, TEST.N621, TEST.N749 · stated at `tests_scripts/conftest.py:55-60`; `tests_scripts/test_coverage_runner.py:20-28`; `tests_js/live-backend.test.js:1-3`; `tests_js/live-backend-put-matrix.test.js:65-66`
- **Provenance**: O — OR16.a (2) "every skip, deselect and opt-in gate is inventoried with a still-valid reason"; OR21.a (3)
- **Truth**: partly — pytest skips when binaries are absent (bare `pytest tests_scripts` passes); JS live tests turn a skip into a pass via `console.warn` + `return`; `test_live_twin_ceiling_parser.py:20-25` fails without `node` (inconsistent policy); one fixture-writer skip remains (`test_buildgen_validate.py:1203-1208`)
- **Fit**: restates OR16.a (2) · pillar P5
- **Home**: runner summary block (OR15.a (2))
### G2.056 Log assertions check code and type
- **Req**: A test that expects an error or warning asserts its number and its type (errno vs wrnno share one history, discriminated by `ErrType`), not only that the count rose.
- **Class**: test-standard
- **Sources**: TEST.N200, TEST.N448 · stated at `tests/test_asy_isl29125_driver.py:170-172`; `tests/test_uart_comm_hazard.py:539-541`
- **Provenance**: O — OR19.a (1) red flag "asserting a log line exists but not its content"
- **Truth**: partly — ISL tests discriminate `ErrType`; `test_uart_comm_hazard.py` asserts `ErrCount` only outside its later catalog section
- **Fit**: refines OR19.a (1), OR28.a (3) · pillar P5
- **Home**: Part E test standard
### G2.057 Unreachable defensive branches: tested via double or registered
- **Req**: A defensive branch that normal flow cannot reach keeps its handler; it is exercised through a double (a raising Protocol fake, a substituted name) or, if untestable, named in SPEC E.5.1's register with the reason, never left silently uncovered.
- **Class**: test-standard
- **Sources**: TEST.N001, TEST.N281, TEST.N338, TEST.N342, TEST.N343, TEST.N364, TEST.N381, TEST.N660, TEST.N663, TEST.N664, TEST.N744, TEST.N762 · stated at SPEC E.4 (:2940-2943), E.5.1 (:2992-3025); commit `500851c`; `tests/test_base_classes.py:54-56`
- **Provenance**: O-confirmed — `500851c`: "the general 'leave the catch, skip an untestable test, note why' convention ... confirmed directly by the project owner"; E.5.1 print_log sentinel "confirmed intentional by the project owner"
- **Truth**: partly — E.5.1 register exists; the convention's own home (BACKLOG) is gone and not restated in Part D/E (item N744); `voc_algorithm.py`'s `_FIX16_OVERFLOW` unreachability is disputed (ALGO.S01)
- **Fit**: refines OR25.a (3), OR26.a · pillar P2
- **Home**: SPEC E.5.1 (+ the convention sentence)
### G2.058 Line coverage reported, never gating
- **Req**: `scripts/test.sh --coverage` reports line coverage and never gates on a threshold; its test result gates. The traced set covers `src/`, `digital_twin/` and the generated device modules, and the report knows the tracer's false-negative classes (folded `_` constants, decorator lines, `while True:` headers, pass-through `finally`).
- **Class**: tooling
- **Sources**: TEST.N027, TEST.N322, TEST.N525, TEST.N564, TEST.N662 · stated at CLAUDE.md "never gates anything"; SPEC E.5, E.5.3; `tests/_coverage_runner.py:1-24`
- **Provenance**: O — OR23.a (2) "report-only and never gating, like src/"; E.5.3 test-result gating "owner decision, 2026-09-22" (CLAUDE.md)
- **Truth**: partly — generated `sensortask_*.py` untraced (`_coverage_runner.py:24`, `tests_scripts/test_coverage_runner.py:77-86`); `_render_coverage.py` relies on unpinned coverage.py internals
- **Fit**: refines OR22.a (2) · pillar P5
- **Home**: SPEC E.5 (fine)
### G2.059 Tests proven by planted faults
- **Req**: A test's power is proven by planting the fault it exists to catch (scripted sweep in a throwaway copy, file restored in `finally`) and recording which test turns red; a guard no planted fault can fail is blind and is rebuilt.
- **Class**: test-standard
- **Sources**: TEST.N673, TEST.N674, TEST.N767 · stated at SPEC E.8 (:3347-3365); `tests/test_asy_sgp40_driver.py:1068-1128` (W13, commit `83c9920`)
- **Provenance**: O — OR16.a (4), OR19.a (3)
- **Truth**: partly — E.8 records four of seven UART read-path regressions caught; no committed sweep script; W13 tests each failed a matching mutation
- **Fit**: restates OR16.a (4) · pillar P5
- **Home**: Part E test standard
### G2.060 Boot-contiguity bounds twin-only, with a control arm
- **Req**: `tests_scripts/test_digital_twin_boot_contiguity.py` keeps its suppressed control arm, which must violate every bound or the guard is not a guard; its bounds are twin units, never copied to the board, and bounds mirrored from the hardware script are pinned.
- **Class**: test-standard
- **Sources**: TEST.N005, TEST.N007, TEST.N009, TEST.N734 · stated at `HEAP_FRAGMENTATION_MEASUREMENTS.md:244-247`; `tests/_boot_contiguity_probe.py:34-38`; CLAUDE.md gc exception
- **Provenance**: O-confirmed — CLAUDE.md "added 2026-09-18 with the owner's approval"; OR21.a (2) "existing control arms ... are left as they are"
- **Truth**: holds — control arm present (`test_digital_twin_boot_contiguity.py:37-47`); probe constants equal the hardware script's (`heap_layout_after_full_boot_sequence.py:32-35`) but are unpinned
- **Fit**: restates OR21.a (2) · pillar P6
- **Home**: SPEC I.4(f.1) (fine)
### G2.061 Shared scenarios run on every backend that applies
- **Req**: A behaviour tested on several backends lives once in a backend-agnostic helper (importable under MicroPython and CPython) whose docstring states the backends it applies to; genuinely backend-specific coverage is not forced into sharing, and every twin scenario has a flash/bench counterpart or a listed exception.
- **Class**: test-standard
- **Sources**: TEST.N075, TEST.N285, TEST.N379, TEST.N666, TEST.N667, TEST.N668 · stated at SPEC E.6/E.6.2 (:3100-3153); `tests/_shared_rest_roundtrip.py:1-3`; `tests/test_fram_integration.py:304-305`
- **Provenance**: O — OR45.a (2)(b) "every twin scenario has a real-hardware counterpart, adapted"; sharing scope A
- **Truth**: partly — shared helpers exist; the applicable-backend docstring rule is review-only; containment is unmeasured (OR45.a)
- **Fit**: restates OR45.a (2)(b) · pillar P5
- **Home**: SPEC E.6 (fine)
### G2.062 Test logic outside the DUT process
- **Req**: Twin and hardware tests keep driving and asserting logic in a host process outside the DUT, with at most a tiny stub inside; a fix under this rule must catch the real case strictly better, never merely lighten the DUT.
- **Class**: test-standard
- **Sources**: TEST.N105, TEST.N675 · stated at SPEC E.9 (:3404-3409); `tests/_webserver_concurrency_scenarios.py:153-155`
- **Provenance**: O — OR20.a
- **Truth**: partly — `_webserver_concurrency_scenarios.py` still runs its client in the DUT's heap (in-process twin tier)
- **Fit**: restates OR20.a · pillar P5
- **Home**: SPEC E.9 (fine)
### G2.063 Connection-ceiling tests: admitted or cleanly refused
- **Req**: Real-socket concurrency tests treat a refusal only as the documented reject-when-full outcome (reset or EOF, which may also stand in for a 413 over an unread body), assert that at least one attempt is refused only where the burst exceeds the ceiling by a full ceiling, and require `max_connections >= 4`; per-source-IP behaviour is out of scope because `_serve()` has none.
- **Class**: test-standard
- **Sources**: TEST.N099, TEST.N103, TEST.N115, TEST.N117, TEST.N481 · stated at `tests/_webserver_concurrency_scenarios.py:15-21,114-116,299-313,503-506,719-721,812`
- **Provenance**: A — agent sessions; F: Linux SYN retry ≈1 s drives the backlog scenario
- **Truth**: partly — any `OSError`, not only a reset, counts as a clean rejection (item N108); `:311-313` calls rejection timing-dependent while `:503-506` asserts it
- **Fit**: refines OR49.a (1) · pillar P5
- **Home**: Part E / SPEC H.7
### G2.064 Every bus chip gets a conformance probe
- **Req**: Every bus-facing chip gets a conformance probe that checks its fake's modelled behaviour against the real chip, with a pytest gate on the probe script.
- **Class**: hardware-safety
- **Sources**: TEST.N648, TEST.N705 · stated at SPEC C.11.1 (:2354-2357), K.9 (:5934-5936)
- **Provenance**: A — `5b97af3` (2026-09-24, ISL29125 Part M move); first probe from the ISL promotion
- **Truth**: partly — only ISL29125 has one; SCD30, SGP40, BMP3xx and FRAM none; K.9 records no pytest gate for the probe script
- **Fit**: refines OR17.a (3), OR29.a (4) · pillar P5
- **Home**: SPEC C.11.1 → the one ordered checklist
### G2.065 Browser floor covered by a narrow smoke
- **Req**: The browser tier runs its full matrix on Chromium through Vitest, and a deliberately narrow single-session smoke covers Firefox, WebKit and Edge so the current Chromium, Firefox and Safari floor is exercised.
- **Class**: test-standard
- **Sources**: TEST.N686, TEST.N687 · stated at SPEC H.7 (:4678-4688); `scripts/cross_browser_smoke.mjs:2,29-35`; `ci.yml:254-309`
- **Provenance**: O — PQ10 answer "browser floor current Chromium, Firefox, Safari"; narrow scope A
- **Truth**: holds — WebKitGTK, Firefox, Edge and Chromium drivers in the smoke; CI job `web-cross-browser-smoke`
- **Fit**: restates PQ10 · pillar P5
- **Home**: SPEC H.7 (fine)
### G2.066 Tag-family tests: full accept, single-dimension reject
- **Req**: Each buildgen tag family's unit tests cover the accept side in full dimensionality and the reject side one dimension at a time without recombining.
- **Class**: test-standard
- **Sources**: TEST.N707 · stated at SPEC L.5 (:6372-6413); `tests_scripts/test_buildgen_tag_comments.py`, `test_buildgen_requires_tag.py`
- **Provenance**: A — `df2359c` (2026-09-13, buildgen docs rewrite)
- **Truth**: not checked — reference files named; per-family completeness is OR23 work
- **Fit**: refines OR23.a · pillar P5
- **Home**: SPEC L.5 (fine)
### G2.067 Every buildgen abort has its own fixture test
- **Req**: Every buildgen abort condition has its own test driven by a deliberately malformed fixture definition file.
- **Class**: test-standard
- **Sources**: TEST.N708 · stated at SPEC L.5 (:6424-6430)
- **Provenance**: O — OR23.a (3) "every error path has a test asserting the exit code and that the message carries what, where and the fix"
- **Truth**: partly — malformed fixtures exist; the fixture writer cannot emit inline tables, so the dict-valued `led_target` case always skips (`test_buildgen_validate.py:1203-1208`)
- **Fit**: restates OR23.a (3) · pillar P5
- **Home**: SPEC L.5 (fine)
### G2.068 No test reaches the public internet
- **Req**: No test in any software tier contacts a public internet host; real fallback targets (8.8.8.8/1.1.1.1, pool.ntp.org) are overridden to loopback or unroutable addresses for the whole process.
- **Class**: test-standard
- **Sources**: TEST.N171 · stated at `tests/test_asy_dns_client.py:70-78`
- **Provenance**: A — agent sessions
- **Truth**: partly — `test_asy_dns_client.py` and `test_ntp_wifi_dns_integration.py` override `_FALLBACK_DNS_SERVERS`; other resolver users monkeypatch `resolve_ipv4`; whether twin boots resolve `pool.ntp.org` through `src/asy_dns_client.py:20,110`'s public fallback was not checked
- **Fit**: new (hermetic tests; OR37.a third-party services) · pillar P9
- **Home**: Part E test standard
### G2.069 mypy resolves one bare module per run
- **Req**: Because mypy resolves each bare module name to one file per run, `tests/machine.py` stands in for `machine` in the main `src tests` pass, `digital_twin/` and host tooling get separate passes, and name collisions (a bare `conftest`) are split into separately named modules; a stub gap masked by this is recorded.
- **Class**: tooling
- **Sources**: TEST.N124, TEST.N315, TEST.N537, TEST.N724, TEST.N725 · stated at CLAUDE.md Code quality tooling; `tests/machine.py:2`; `tests_scripts/_script_loader.py:2-3`
- **Provenance**: A — agent sessions (`confirmed directly` in CLAUDE.md)
- **Truth**: unverifiable here (mypy not run in this read-only pass) — layout matches CLAUDE.md; a `src`-only run shows 12 `call-overload` errors from the stub's missing zero-arg `Timer()` (CLAUDE.md)
- **Fit**: extends OR46.a (imports) · pillar P5
- **Home**: CLAUDE.md → SPEC B.15
### G2.070 Suppressions per site; none in src for method-assign
- **Req**: Test and twin code suppress type findings per site with a bracketed error code (`method-assign`, `assignment`, `arg-type` for deliberate wrong-type inputs, `misc` for module swaps), never through a scope-wide override; `src/` never carries a `method-assign` suppression.
- **Class**: code-style
- **Sources**: TEST.N011, TEST.N022, TEST.N045, TEST.N047, TEST.N073, TEST.N089, TEST.N100, TEST.N121, TEST.N129, TEST.N168, TEST.N175, TEST.N180, TEST.N184, TEST.N187, TEST.N191, TEST.N196, TEST.N207, TEST.N210, TEST.N217, TEST.N218, TEST.N228, TEST.N239, TEST.N250, TEST.N260, TEST.N265, TEST.N276, TEST.N284, TEST.N290, TEST.N300, TEST.N316, TEST.N332, TEST.N333, TEST.N334, TEST.N335, TEST.N344, TEST.N354, TEST.N363, TEST.N369, TEST.N372, TEST.N376, TEST.N395, TEST.N399, TEST.N402, TEST.N410, TEST.N417, TEST.N420, TEST.N429, TEST.N436, TEST.N444, TEST.N454, TEST.N484, TEST.N492, TEST.N499, TEST.N510, TEST.N516, TEST.N549, TEST.N558, TEST.N577, TEST.N722, TEST.N741 · stated at CLAUDE.md "`method-assign` stays globally enabled"; `scripts/lint.sh:31-34`
- **Provenance**: O-confirmed — CLAUDE.md "(project owner's direction)"
- **Truth**: holds for the rule (`lint.sh:31-34`); drifted count — CLAUDE.md says ~157 sites, HEAD has 201 lines in 23 files under `tests/` and `digital_twin/`
- **Fit**: restates CLAUDE.md · pillar P4
- **Home**: CLAUDE.md (drop the count)
### G2.071 noqa E402 only after a real pre-import statement
- **Req**: `# noqa: E402` appears only in files where a statement other than a `sys.path` change precedes imports; a `sys.path`-only file carries none, since RUF100 fails an unused suppression.
- **Class**: code-style
- **Sources**: TEST.N031, TEST.N471, TEST.N495, TEST.N503, TEST.N514, TEST.N519, TEST.N548, TEST.N552, TEST.N576, TEST.N723 · stated at CLAUDE.md "`# noqa: E402` belongs only on files ..."
- **Provenance**: A — `dd38176` (2026-09-10, "verified directly")
- **Truth**: holds — enforced by ruff E402 + RUF100 (`select = ["ALL"]`); 29 sites across tests/, tests_scripts/, digital_twin/
- **Fit**: restates CLAUDE.md · pillar P4
- **Home**: CLAUDE.md (fine)
### G2.072 Test lint exemptions central and reasoned
- **Req**: Test-tier lint exemptions live centrally in `pyproject.toml` per-file-ignores with a reason (`S101`, `SLF001`, `PLC0415`, `PLR2004`, `ARG00x` for faithful doubles; fakes' `A002`/`FBT`/`ANN401`; the Microdot `disallow_untyped_decorators` override with its revisit trigger), and the docs describe exactly which scopes carry them.
- **Class**: tooling
- **Sources**: TEST.N529, TEST.N530, TEST.N531, TEST.N532, TEST.N533 · stated at `pyproject.toml:216-276,408-413`; CLAUDE.md Code quality tooling
- **Provenance**: A — `def319b` (2026-09-24, retro audit config sweep)
- **Truth**: drifted — CLAUDE.md says `tests_scripts/`, `scripts/` and `toolchain/` carry the same block `tests/` does; `pyproject.toml` has it for `tests_scripts/**` and `tests_hardware/**` only, `scripts/`/`toolchain/` get per-file `S603` only
- **Fit**: refines OR46.b (3) (central exemptions) · pillar P4
- **Home**: `pyproject.toml` (fine); CLAUDE.md corrected
### G2.073 Doubles keep the real signature
- **Req**: A test double that replaces a real method, Protocol member or constructor keeps its exact signature and parameter names (mypy checks them; `print_log.py` calls `get_chunk(size, crc=...)` by keyword), with the needed unused-argument or builtin-shadow exemptions.
- **Class**: code-style
- **Sources**: TEST.N183, TEST.N186, TEST.N339, TEST.N571 · stated at `pyproject.toml:220-222,237-247`; `tests/test_base_classes.py:61-63`; `tests/test_asy_fram_manager.py:924-926`
- **Provenance**: A — pyproject rationale (`def319b`)
- **Truth**: holds — exemptions carry that reason; mypy strict checks signatures
- **Fit**: refines OR46.b (3) · pillar P4
- **Home**: `pyproject.toml` (fine)
### G2.074 Every JS lint disable carries a reason
- **Req**: Every `eslint-disable-next-line` in `tests_js/` names its rule and a reason (`-- reason` or the line above), like the Python `noqa` convention.
- **Class**: code-style
- **Sources**: TEST.N599, TEST.N604, TEST.N605, TEST.N606, TEST.N629, TEST.N631 · stated at `tests_js/_live_twin_command.js:38-247`; `tests_js/_live_matrix_command.js:159-269`
- **Provenance**: C — 20 harvested disables all carry a reason; no lint rule requires it
- **Truth**: holds (harvested sites); unenforced
- **Fit**: extends OR27.a (one short reason) · pillar P4
- **Home**: `eslint.config.js` (a require-description rule)
## Conflicts and tensions

1. **gc.collect() before a measurement: OR39.a vs the MicroPython fact** (G2.011). OR39.a (1) (owner): a test's `gc.collect()` must never be "followed by the measurement in the very next statement". v1.29.0 `py/modgc.c:34-37`: `gc.collect()` returns only after `gc_collect()` finished, so reading `gc.mem_alloc()` on the next line is exact; `tests/test_uart_comm_hazard.py:502-515` and `tests/test_asy_fram_allocation_budget.py:47-49` do exactly that on purpose (a collection-free window). Resolution: unresolved — the owner's wording is a rule, the fact says it buys nothing; see Question 1.
2. **Test-side gc.collect() props vs CLAUDE.md/OR39.a** (G2.011). CLAUDE.md and SPEC I.4(e) forbid `gc.collect()` propping a test; `tests/test_fram_integration.py:159-177` and `tests/_webserver_concurrency_scenarios.py:835-861` keep one each, justified in their comments as a Unix-port artifact / defence in depth, and `scripts/lint.sh:37-38` declares tests/ and digital_twin/ "out of scope". Resolution by provenance: the owner rule (OR39.a) wins; these are defects to fix at the root (OR39.a (1)), and the lint comment is drift.
3. **Oracle policy: SPEC D.12 vs OR19.a (2)** (G2.026). D.12 (agent, `2a558f5`): "a valid typical input against a sanity bound, not an exact reference value". OR19.a (2) (owner): expected values from an independent source (datasheet, spec, hand calculation), and a red flag for "tolerances wide enough to pass anything". Resolution: OR19.a wins; D.12 is reworded to "an independent reference value, or a sanity bound only where none can be derived".
4. **CLAUDE.md's nested-asyncio.run mechanism vs v1.29.0 source** (G2.020). CLAUDE.md: "run() installs a fresh event loop and task queue". `extmod/asyncio/core.py:247-248`: `run()` = `run_until_complete(create_task(coro))` on the same queue; `new_event_loop()` runs only at import (:324). The rule stands (the crash is empirical, `b686e32`); the stated mechanism is wrong and is fixed as a factual doc error (OR13.a: facts, not a recorded decision).
5. **"Per-file self-containment convention" vs shared helpers** (G2.041). `tests/test_ntp_fram_system_integration.py:57-58` cites a Part E convention that "no test file in this suite imports another"; SPEC Part E contains no such rule (grep "self-contain": none in Part E) and E.2.1 itself prescribes shared `_*_scenarios.py` libraries. OR24/OR46.b (owner) favour one shape per job. Resolution: the cited convention is drift; duplicated doubles (`_RaiseOnArm` ×7, `_FastAsyncSleep` ×10) consolidate into shared helpers.
6. **Twin fakes independent of unit fakes vs one material** (G2.036 vs G2.041, P8/OR24). `tests/test_digital_twin_fram.py:1`, `digital_twin/neopixel.py:1` say "independently reimplemented, not shared" (agent). OR24 "one material" and P8 argue for one source; the argument for independence (a shared bug cannot hide in both tiers) is not written down. Unresolved — Question 2.
7. **Hand-written device lists vs OR43.a** (G2.042). OR43.a (3) (owner): never a hard-coded list. `tests_scripts/test_device_tomls.py:251-252` (agent): the MicroPython `_DEVICES` tuples stay hand-written because their order assigns twin port bases, pinned by that test. Resolution by evidence: the MicroPython tier cannot glob into new files (one process per file), so the pinned list plus a fail-loud check meets OR43.a's intent ("a new device TOML is picked up ... or fails"); port bases can be derived from the sorted device list instead of tuple order. `test_build_firmware.py`'s literal `["wozi","dev"]` and the hand-kept per-driver sets do not have that excuse and follow OR43.a.
8. **Heavy twin tests wozi-only (SPEC E.2.1) vs OR22.a (2) "all six"** (G2.042). E.2.1 (agent): a ×6 parametrisation "would buy nothing". OR22.a (2) (owner): the same bar "per device, all six". Resolution: OR22.a wins for generated-code resilience; E.2.1's device-independence argument may justify which *scenarios* stay single-device only if the intent map (OR25.a) shows the device-specific paths covered elsewhere — logged, not decided here.
9. **Readiness: poll vs fixed sleep** (G2.047). SPEC H.8.1 (JS): "never a fixed sleep". Python twin tests use fixed sleeps on purpose (`a335923`: a probe connection occupies a `max_connections` slot and broke the exactness tests); OR36.a forbids adding a product "init done" flag (`test_digital_twin_sensortask_integration.py:663-666`). Resolution: both rules hold in their scope — poll where the probe is harmless, otherwise a fixed sleep registered as an OR30 tunable.
10. **CLAUDE.md bus-hazard file list vs SPEC C.8** (G2.044). CLAUDE.md names only `tests/test_bus_hazard_multi_device.py` for the mock tier; SPEC C.8 (owner reframing 2026-09-15) splits sensor-specific hazards into driver files and adds the generated tier. Both owner-sourced; C.8 is the later, more specific statement — CLAUDE.md is drift to align (OR13.a: rule text, logged).
11. **Absolute heap bound vs "a leak is a rate"** (G2.013): `tests/test_asy_webserver_service.py:1833` vs SPEC E.8 (:3289). Both agent; E.8 is the stated rule — the absolute bound is a defect candidate.
12. **Port band text vs code** (G2.017): SPEC E.1 "TCP bases sit in 17400-19999" vs `tests/_webserver_concurrency_scenarios.py:77` (up to 20700). Factual drift; both agent.
13. **Per-file-ignores scope** (G2.072): CLAUDE.md vs `pyproject.toml:216-276` — factual drift, pyproject is the truth.

## Questions for the owner

1. **gc.collect then measure on the next line?** Options: (a) keep OR39.a literally — insert a settle step between `gc.collect()` and every baseline read; (b) amend OR39.a: `gc.collect()` is synchronous in MicroPython (`py/modgc.c:34-37`), so reading right after it is exact, and only "never inside a timed window" stays. Consequences: (a) adds waits to ~10 measurement sites and no accuracy; (b) keeps the collection-free windows the allocation-budget tests rely on.
2. **Twin fakes: independent copies or shared code?** Options: (a) keep independent reimplementations held equal by shared contract tests (as the UART link already is); (b) share one implementation per peripheral between `tests/` and `digital_twin/`. Consequences: (a) keeps two copies but a bug cannot hide in both tiers, and needs a contract suite per peripheral; (b) one material, less code, but one fake bug blinds both tiers.

## No-requirement buckets
- `descriptive` (46): TEST.N036, TEST.N052, TEST.N066, TEST.N069, TEST.N072, TEST.N077, TEST.N098, TEST.N109, TEST.N174, TEST.N179, TEST.N190, TEST.N192, TEST.N229, TEST.N244, TEST.N247, TEST.N257, TEST.N273, TEST.N280, TEST.N292, TEST.N311, TEST.N341, TEST.N374, TEST.N391, TEST.N412, TEST.N430, TEST.N433, TEST.N451, TEST.N479, TEST.N482, TEST.N493, TEST.N507, TEST.N538, TEST.N572, TEST.N575, TEST.N578, TEST.N609, TEST.N613, TEST.N617, TEST.N618, TEST.N628, TEST.N630, TEST.N633, TEST.N634, TEST.N639, TEST.N737, TEST.N763
- `defect-candidate` (48): TEST.N050, TEST.N055, TEST.N062, TEST.N064, TEST.N067, TEST.N070, TEST.N076, TEST.N079, TEST.N091, TEST.N107, TEST.N108, TEST.N111, TEST.N114, TEST.N118, TEST.N125, TEST.N154, TEST.N165, TEST.N178, TEST.N204, TEST.N231, TEST.N272, TEST.N286, TEST.N287, TEST.N291, TEST.N304, TEST.N319, TEST.N397, TEST.N405, TEST.N431, TEST.N432, TEST.N456, TEST.N466, TEST.N467, TEST.N470, TEST.N502, TEST.N598, TEST.N603, TEST.N608, TEST.N610, TEST.N619, TEST.N622, TEST.N624, TEST.N637, TEST.N647, TEST.N706, TEST.N730, TEST.N754, TEST.N765
- `open-item` (4): TEST.N728, TEST.N729, TEST.N748, TEST.N766
- `suppression-inventory` (3): TEST.N226, TEST.N317, TEST.N581
- `drift-only` (27): TEST.N012, TEST.N017, TEST.N030, TEST.N035, TEST.N060, TEST.N063, TEST.N110, TEST.N113, TEST.N120, TEST.N166, TEST.N176, TEST.N194, TEST.N209, TEST.N225, TEST.N236, TEST.N253, TEST.N256, TEST.N266, TEST.N296, TEST.N297, TEST.N298, TEST.N321, TEST.N323, TEST.N579, TEST.N625, TEST.N627, TEST.N632
- `stale` (3): TEST.N736, TEST.N757, TEST.N761

## Accounting

| Catalog | Items read | In candidates | descriptive | defect-candidate | open-item | suppression-inventory | drift-only | stale | Sum |
|---|---|---|---|---|---|---|---|---|---|
| TEST.md | 767 | 636 | 46 | 48 | 4 | 3 | 27 | 3 | 767 |

74 candidates. Every catalog ID (N001-N767) appears exactly once (primary placement). Suppression sites whose rule a candidate carries are listed as that candidate's sources (mostly G2.070, G2.071, G2.053, G2.072, G2.055).
