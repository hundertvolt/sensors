# Harvest requirements G3 — SENS, ALGO, BUS, LED (HEAD 06eff58)

Sources read in full: `audit/harvest/SENS.md` (412), `ALGO.md` (81), `BUS.md` (141), `LED.md` (64).
Verification used the working tree at HEAD, MicroPython `v1.29.0` source (scratchpad `mp`), Sensirion
`gas-index-algorithm` (scratchpad `gia`, fixpoint variant) and the extracted datasheet text (scratchpad
`dstxt`; line numbers below are lines of those text files, page given where it matters).

## Candidates

### G3.001 CRC algorithm identities are fixed
- **Req**: CRC8 is Sensirion's CRC-8 (poly 0x31, init 0xFF, no reflection, no final XOR), CRC16 is CRC-16/CCITT-FALSE and CRC32 is CRC-32/MPEG-2; these identities never change, since the chips and the UART C peer depend on them. Test vectors come from the datasheets.
- **Class**: product
- **Sources**: ALGO.N001, SENS.N250 · stated at `src/crc_checks.py:1-4`, `tests/test_crc_checks.py:26-41`
- **Provenance**: F — SGP40 datasheet v1.2 Table 7 (dstxt sgp40 lines 469-495: poly 0x31, init 0xFF, example CRC(0xBE 0xEF)=0x92); SCD30 Interface Description lines 167-168 (same)
- **Truth**: holds — code parameters match both datasheets; small drift: the test cites "Table 10" (it holds CRC-annotated conversion values) and calls 0xBEEF→0x92 an "SHT3x" vector, though it is the SGP40 datasheet's own Table 7 example
- **Fit**: refines OR4/OR19.a (2) (independent oracle) · pillar P4
- **Home**: module header → SPECIFICATION.md Part G catalog entry for `crc_checks`

### G3.002 CRC and codec primitives never raise
- **Req**: Every public CRC and framing-codec method returns None/False on invalid input or failed allocation and never raises, except CRC `add()`/`check()`, whose MemoryError is a documented controlled raise; every caller of those two catches MemoryError itself. Pass-through mode (poly None) returns the caller's object unchanged.
- **Class**: product
- **Sources**: ALGO.N002, N006, N007, N009, N043, N081 · stated at `src/crc_checks.py:4-6`, `src/framing_codecs.py:3`, commit `500851c`
- **Provenance**: A — rule text since `b85a056`/`323a1a7`; the controlled-raise wording from `500851c` ("raising is allowed, but only in a documented, controlled manner")
- **Truth**: holds — `asy_uart_driver.py:337-344, 431-432` catch MemoryError around `check()`/`add()`; the FRAM manager's CRC32 path (`asy_fram_manager.py:651, 693`) not traced
- **Fit**: refines OR26 (handled at the right layer) · pillar P2
- **Home**: module headers → Part G catalog

### G3.003 Stateful CRC and codec instances are never shared
- **Req**: Incremental CRC state (`run_inc`/`check_inc`) and a delimited codec's scratch belong to one instance and one sequence; every construction site makes its own instance, and a caller finishes with a returned scratch view before the next `encode_into()`.
- **Class**: code-style
- **Sources**: ALGO.N003, N011, N072, N080 · `src/crc_checks.py:6`, `src/framing_codecs.py:53-55`, BACKLOG.md "Framing_COBS instance shared", commit `32ceee8`
- **Provenance**: A — `32ceee8` ("instance-local by design, not concurrency-safe by accident")
- **Truth**: holds by convention — `asy_uart_driver.py:76` builds its own `CRC_Pass()`; no mechanical guard exists, and a shared instance would fail silently
- **Fit**: extends OR41.a (shared-resource matrix) · pillar P2
- **Home**: module comments → Part G catalog

### G3.004 CRC callers supply exact lengths
- **Req**: A caller of `check()`/`check_from()`/`check_inc()` passes the buffer's true length, because a zero register stays zero through trailing 0x00 bytes and zero-padding past the true end is undetectable.
- **Class**: product
- **Sources**: ALGO.N004, N079 · `src/crc_checks.py:8-10`, commits `93b809b`, `c086605`
- **Provenance**: A — `93b809b` flagged it "to the project owner for confirmation rather than treated as settled"; no owner answer found; `c086605` removed the three pinning tests
- **Truth**: partly — the comment stands; no test pins the blind spot (`tests/test_crc_checks.py` covers leading padding only, :193)
- **Fit**: new (caller obligation with no test) · OR25.a · pillar P5
- **Home**: `crc_checks.py` header → Part G catalog

### G3.005 Framing codecs are pluggable with pass-through default
- **Req**: The UART driver takes a pluggable framing codec with pass-through as default; a delimited codec receives one frame without its delimiter, and the caller's buffer holds the worst-case encoded frame (else None). COBS's 0x00 delimiter cannot collide with a UID rollover.
- **Class**: product
- **Sources**: ALGO.N010, N013, N074, BUS.N041, N128 · `src/framing_codecs.py:19-21, 127-128`, `src/asy_uart_driver.py:366-367`, UART_C_PORT_CHANGELOG.md B14 and :107-124
- **Provenance**: A — changelog B14; the UID check "Checked against 20,010 cases (2026-09-11)"
- **Truth**: holds — `asy_uart_driver.py:366-367` returns None for an undersized buffer; `devices/dev.toml` wires no COBS
- **Fit**: restates CLAUDE.md UART contract · pillar P4
- **Home**: code + changelog → SPECIFICATION.md Part J (changelog is temporary)

### G3.006 Math helpers return None, never raise
- **Req**: Every `math_helpers` function returns None for a None, out-of-domain or NaN input and for any residual math error (ValueError, ArithmeticError), relying on a domain gate before computation; it does not defend against wrong types, which mypy-checked callers exclude.
- **Class**: product
- **Sources**: ALGO.N014, N019, N021, N023, N025, N027, N046 · `src/math_helpers.py:3`, `:60-61, 82-83, 97-98, 115-116, 135-136`
- **Provenance**: A — rule text since `b85a056`
- **Truth**: holds — gates of the form `not (lo <= x <= hi)` reject NaN; `ZeroDivisionError` is an `ArithmeticError` subclass (`py/objexcept.c:317`), so the catches are complete (and ALGO.N075's reverted explicit catch was redundant)
- **Fit**: restates SPEC D.2; refines OR26 · pillar P2
- **Home**: module header → SPECIFICATION.md Part D.2 (already)

### G3.007 Out-of-domain derived values are rejected, not clamped
- **Req**: A derived quantity whose input or result leaves its validity domain returns None; it is never clamped, because a clamped edge value looks like a real reading (colour chain inputs outside 0-1, CCT outside 2000-12500 K).
- **Class**: product
- **Sources**: ALGO.N015, N026, N029, N049, N071 · `src/math_helpers.py:31-33, 189-191`, SPEC M.1.3
- **Provenance**: A — `b95a0d7` (ISL29125 driver and colour maths)
- **Truth**: partly — holds for the colour chain and CCT; `rel_humidity()` clamps to 0-100 (`math_helpers.py:137`), the one helper on the opposite policy (no production caller)
- **Fit**: OR24 (one material: one policy for equivalent helpers) · pillar P4
- **Home**: code comment + SPEC M.1.3 → SPEC Part G/D.2 (policy is not ISL-specific)

### G3.008 Formula validity domains come from their source
- **Req**: Each empirical formula is gated to the domain its source validates: Stull wet bulb −20..50 °C and 5..99 %RH, McCamy CCT 2000..12500 K, barometric reduction 300..1250 hPa and −40..85 °C from the BMP3xx datasheet.
- **Class**: product
- **Sources**: ALGO.N017, N018, N044, N066 · `src/math_helpers.py:13-43, 46-47`, `tests/test_math_helpers.py:40`, SPEC D (~2603)
- **Provenance**: F — BMP390 datasheet "Operating range -40 ‒ +85 °C, 300‒1250 hPa" (dstxt bmp390 line 86); Stull (2011) and McCamy (1992) papers not in the repo
- **Truth**: partly — code bounds equal the stated domains; the BMP bound is confirmed; the Stull and McCamy domains are unverifiable here (papers not reachable)
- **Fit**: refines OR4/OR51.a (2) (primary-source recheck) · pillar P4
- **Home**: code comments → unchanged

### G3.009 Pinned colour constants are never "corrected"
- **Req**: The sRGB/Rec.709 D65 matrix stays pinned as its literals and the McCamy form stays as written; another published rounding or algebraically equal form is not substituted. The test pins the literals by exact equality.
- **Class**: code-style
- **Sources**: ALGO.N028, N047, N048, N070 · `src/math_helpers.py:163-164`, `tests/test_math_helpers.py:429-434`, SPEC M.1.3 (~6718)
- **Provenance**: A — `b95a0d7`
- **Truth**: holds — literals and exact-equality test present; the test hand-copies the const-folded matrix (a by-hand mirror), and pins source text on the double-precision Unix port, not float32 device values (see G3.011)
- **Fit**: refines OR13 (do-not-fix markers) · pillar P4
- **Home**: SPEC M.1.3 → SPEC Part G (colour helpers are general)

### G3.010 Dew-point branch discontinuity is accepted
- **Req**: `dew_point()` keeps two independently fitted Magnus branches with a ~1 °C step at 0 °C; the step is a documented limit, guarded by a test against growth, not a bug.
- **Class**: product
- **Sources**: ALGO.N020, N045, N077 · `src/math_helpers.py:65-67`, `tests/test_math_helpers.py:95-100`, commit `d0abd60`
- **Provenance**: A — `d0abd60`
- **Truth**: holds — both coefficient pairs present; test tolerates 1.5 °C
- **Fit**: extends OR27 (accepted limit stated once) · pillar P4
- **Home**: code + test → unchanged

### G3.011 Numeric code is correct in rp2 single precision
- **Req**: Every numeric threshold, constant and formula in firmware code is valid in the rp2 port's single-precision float, not only in the Unix port's double; reasoning in comments and test expectations states which precision it assumes.
- **Class**: platform
- **Sources**: ALGO.N016, N040, SENS.N030, N033 · `src/math_helpers.py:36-39`, `src/voc_algorithm.py:199-202`, `src/asy_bmp3xx_driver.py:451-477, 517-521`
- **Provenance**: F — `ports/rp2/mpconfigport.h:129` `MICROPY_FLOAT_IMPL_FLOAT`; SPEC F.1 (~3500) states the Unix test rig is double
- **Truth**: partly — comments reason in double ("~1e300" chromaticity, unrepresentable in float32); `_f16()` converts at runtime in float32 where the C reference folds in double; BMP compensation uses 2**-65 scales; no test tier runs float32
- **Fit**: new (gap: the unit and twin tiers cannot observe float32 effects) · OR17/OR29.a · pillar P6
- **Home**: scattered comments → SPEC Part F.1 platform fact + Part D checklist item

### G3.012 VOC algorithm port stays literal to Sensirion
- **Req**: `voc_algorithm.py` traces Sensirion's reference 1:1 — constants, field names, operation order and casing — so it stays diffable against the reference; a stylistic rewrite is not allowed, and unused reference API (tuning setters) is kept.
- **Class**: code-style
- **Sources**: ALGO.N030, N033, N039, N055, N057, N058, N065, N067, N068 · `src/voc_algorithm.py:5-6, 47-50, 184-185`, SPEC C.2 (~1511), F.4 (~3672)
- **Provenance**: A — SPEC F.4 text, no owner attribution found
- **Truth**: partly — constants checked against `gia` fixpoint `sensirion_gas_index_algorithm.c`/`.h` (INITIAL_BLACKOUT 45 `.h:73`, SAMPLING_INTERVAL 1 `.h:72`, guards 10.3972/−11.7835 `.c:259-261`, FIX16_MINIMUM/OVERFLOW 0x80000000 `.c:37,40`) and match; the docstring names the archived `embedded-sgp` via DFRobot, while the reachable reference is `gas-index-algorithm` with `GasIndexAlgorithm_*` names, so "diffable" needs a name map
- **Fit**: restates OR4.a (source now reachable) · pillar P4
- **Home**: module docstring + SPEC F.4 → unchanged, reference name updated

### G3.013 Fixed-point arithmetic reproduces C int32 semantics
- **Req**: The Q16.16 helpers reproduce the C reference's int32 behaviour exactly — overflow detection, FIX16_MINIMUM/OVERFLOW results and wrap — and no loop in them can run unbounded.
- **Class**: product
- **Sources**: ALGO.N031, N032, N041, N059 · `src/voc_algorithm.py:7, 41-43, 236-247`, `tests/test_voc_algorithm.py:368-381`
- **Provenance**: F — `gia` fixpoint `.c:120-150` (`while (divider < remainder)` on uint32, then `if (!bit) return FIX16_OVERFLOW`)
- **Truth**: partly — Python shifts unbounded ints (`voc_algorithm.py:245-248`), so `bit` never becomes 0 and the reference's overflow return is unreachable; `_FIX16_MINIMUM` is positive in Python, INT32_MIN in C; the unbounded-loop case needs a divider masked to 0, not reachable from int32 inputs. Feeds the defect scan (ALGO.S01/S06)
- **Fit**: refines OR12.a (defect proven by the reference) · pillar P2
- **Home**: code → unchanged; test against reference vectors

### G3.014 Persisted VOC state layout is fixed
- **Req**: SGP40 reboot survival dumps and restores the whole 32-field algorithm state (not Sensirion's mean/std pair); field order and the 256-byte size are the FRAM chunk layout, fixed within a build, packed with an explicit `"<"` byte order; a pack/unpack failure returns False.
- **Class**: product
- **Sources**: ALGO.N034, N035, N036, N037, N038, N042, N056 · `src/voc_algorithm.py:52, 56-57, 98, 134-143`, `tests/test_voc_algorithm.py:238-244`
- **Provenance**: A — module docstring and tests; the `"<"` rule is SPEC F.1 (~3506) "Pin any wire or on-chip layout with an explicit `\"<\"` prefix"
- **Truth**: partly — layout and size hold; `"32q"` has no byte-order prefix (`voc_algorithm.py:98, 141`) against F.1's rule (no live bug: `q` is 8-byte little-endian on both hosts); restored state is not validated
- **Fit**: refines OR47.a (2) (fixed within one build, no fingerprint) · pillar P7
- **Home**: code → SPEC A.7 FRAM layout

### G3.015 VOC sampling and index semantics follow Sensirion
- **Req**: The SGP40 raw signal is fed to the algorithm at a fixed 1 s period; the index is 0 during the 45-sample blackout, then 1..500 with 100 as the 24 h average; a higher raw count means cleaner air. Tests and docs use these facts from the datasheet and reference, not from current output.
- **Class**: platform
- **Sources**: ALGO.N052, N053, N054, N062, N063, N064, SENS.N172, N179 · `src/asy_sgp40_driver.py:467-476, 645-647`, SPEC A.4 (~247)
- **Provenance**: F — SGP40 datasheet v1.2 p9 §3.2 ("1 to 500 VOC Index points", "100 refers to the average indoor gas composition over the past 24 h", "sampling rate of 1 s"); `gia` `.h:72-73`
- **Truth**: holds — 1 Hz PERIODIC soft timer at `asy_sgp40_driver.py:472`; blackout `<=` so real values from call 46 (`voc_algorithm.py:761`); soft-timer ticks can merge in a stall (SENS.T11) — the 1 s period is nominal
- **Fit**: extends OR29.a · pillar P5
- **Home**: SPEC A.4 → unchanged

### G3.016 CRC per-byte yield stays; no table CRC
- **Req**: `crc_checks._crc()` keeps its `await asyncio.sleep(0)` after every byte, accepting the wall-clock cost; a table-driven CRC is not introduced (RAM cost not justified by the small buffers).
- **Class**: product
- **Sources**: ALGO.N069, N073, N078 · SPEC I.2 (~4895), commit `b1059bd`, archive `12640c2` HFM:2666
- **Provenance**: O — SPEC I.2: "2026-09-18: \"pure wall clock time is not such an issue, don't touch\"" (commit `23e5443`, "Record the owner's three decisions"); the table decline is A (`b1059bd`), not restated in SPEC
- **Truth**: holds — `crc_checks.py:49`; the silicon wall-time factor stays unmeasured (informational)
- **Fit**: restates owner decision; relevant to OR39 · pillar P6
- **Home**: SPEC I.2 → unchanged; table decline added there

### G3.017 General-purpose driver API with no caller stays
- **Req**: API that completes a module as a general-purpose driver for its hardware or algorithm stays even with zero callers in the station firmware (BMP `get_altitude()`, standalone getters, optional `setup()` parameters, SCD30 reader getters, CRC16/CRC32, COBS, the VOC tuning setter, the async SPI transfers).
- **Class**: product
- **Sources**: ALGO.N008, N012, N060, SENS.N034, N131, N187, N189, N191, N410, BUS.N013 · `src/asy_bmp3xx_driver.py:570-577`, `src/asy_spi_driver.py:2`, PR #30
- **Provenance**: O — OR36.a (3) ("These are functions of the hardware items … They remain as they are.")
- **Truth**: holds — CRC16 has no production user (grep: only `CRC_Pass`, `CRC8`, `CRC32` are imported), COBS unused, `get_altitude()` has no `src/` caller
- **Fit**: restates OR36.a (3); bounds OR46.a (2) dead-code removal · pillar P4
- **Home**: plan OR36.a → one ordered checklist (Part K) and Part C

### G3.018 No runtime defence against out-of-contract input
- **Req**: A function does not check at runtime what static typing already enforces (types, address format); it does check NaN/inf and domain, which real sensors can produce.
- **Class**: code-style
- **Sources**: ALGO.N076, BUS.N056 · SPEC D.2 (~2619), `tests/test_asy_i2c_driver.py:914-916`, commit `bd00a8d`
- **Provenance**: A — `2a558f5` (src/README checklist), now SPEC D.2
- **Truth**: holds — `asy_i2c_driver.py` does not range-check `address`; `math_helpers` has no type checks
- **Fit**: restates SPEC D.2 · pillar P4
- **Home**: SPEC D.2 → unchanged

### G3.019 Unreachable defensive branches are labelled and tested
- **Req**: A defensive branch no real input can reach is kept only as labelled defence in depth in a module contracted never to raise, and a test drives it through a fake; SPEC E.5.1 lists such branches so no pass re-chases them.
- **Class**: test-standard
- **Sources**: BUS.N051, N074, SENS.N196, N210, N226, N239, N244, N245, N246, N405 · SPEC E.5.1 (~3008-3020), `tests/test_asy_isl29125_driver.py:152`
- **Provenance**: A — `defb282` ("Defence in depth (SPECIFICATION.md Part E.4)")
- **Truth**: holds — SPEC E.5.1 enumerates the classes; tests drive them via fake `struct`, fake CRC and monkeypatches
- **Fit**: refines OR16.a (3) ("justified" line) · pillar P5
- **Home**: SPEC E.5.1 → unchanged

### G3.020 Validate before any lossy numeric conversion
- **Req**: A value is range- and NaN-checked before `int()`, `struct.pack()`/`pack_into()` or `to_bytes()`, and before any shared buffer is touched, because `int()` truncates toward zero and MicroPython's pack silently truncates; truncation that remains (TempOffs 0.01 ticks) is stated as the driver's, not the chip's.
- **Class**: product
- **Sources**: BUS.N101, N137, SENS.N087, N140, N215, N216, N217, N310, N314 · SPEC F.1 (~3510), `tests/test_asy_scd30_driver.py:282-364`, SPEC A.4 (~243-245)
- **Provenance**: F — SPEC F.1 (pack truncation at 1.29, `py/binary.c` checks preview-gated; not re-read here); the rule is A (regression tests for `set_ambient_pressure(-0.5)`)
- **Truth**: drifted — SCD30 validates before `int()`; SPEC:243-245 says TempOffs is "silently truncated by the chip", but the truncation is the driver's `int(offset * 100)` (`asy_scd30_driver.py:568-570`); legacy raised OverflowError where src now truncates (BUS.N137)
- **Fit**: refines OR26 · pillar P2
- **Home**: SPEC F.1 + A.4 → Part D checklist item

### G3.021 Non-finite values are never published
- **Req**: A NaN or ±inf reading is never published (MicroPython `json.dumps` emits bare `nan`/`inf`, breaking the page) and never poisons filter state; every comparison that gates on a value handles NaN explicitly, since NaN compares False.
- **Class**: product
- **Sources**: ALGO.N051, LED.N008, N042, SENS.N143, N218, N282, N340 · `src/asy_scd30_driver.py:624-627`, `src/math_helpers.py:208-215`, SPEC F.1 (~3589)
- **Provenance**: A — `d8da5b3` ("Gate the SCD30's raw floats on isfinite, closing BACKLOG 47")
- **Truth**: holds — `ema_step()` resets on non-finite state; SCD30 non-finite → failed read; notification NaN never triggers, ±inf triggers one side (tested); the silicon trigger does not exist (SENS.N282); `json.dumps` behaviour not re-checked
- **Fit**: refines OR26/OR49.a · pillar P2
- **Home**: SPEC F.1 → Part D checklist

### G3.022 Readings pass a datasheet plausibility gate
- **Req**: Layer 2 of every sensor driver rejects (raises) a decoded or compensated value outside the chip's datasheet operating range, before user offsets are applied; the gate is the integrity check on buses without CRC.
- **Class**: product
- **Sources**: SENS.N004, N031, N190, N264, N267, N323 · SPEC C.3 (~1573), `src/asy_bmp3xx_driver.py:56-60, 483-487`
- **Provenance**: A — SPEC C.3 ("reject and raise rather than return an implausible value silently")
- **Truth**: partly — BMP3xx gates 300-1250 hPa/−40..85 °C (confirmed against the BMP390 datasheet, dstxt line 86); SCD30 has a finiteness gate only, no range gate (SENS.S10); in-range corruption passes on BMP
- **Fit**: OR24 (one material across drivers) · pillar P2
- **Home**: SPEC C.3 → unchanged

### G3.023 Layered raise contract for drivers
- **Req**: Layer 1 (bus) returns None only for a non-hardware failure and lets a real OSError propagate, with no logging; layer 2 (protocol) raises on any real failure; layer 3 (reader) never raises, wrapping each layer-2 call in its own `try/except Exception` (never bare) and logging it.
- **Class**: product
- **Sources**: BUS.N002, N006, N063, N093, N100, LED.N019, N021, SENS.N221, N253, N321, N324 · `src/asy_i2c_driver.py:4-6`, SPEC C.3 (~1557), C.4 (~1635), C.7.1 (~1948), F.5.2
- **Provenance**: A — text since `b85a056`/`e5f31f2`; C.7.1 row from `59d6b7b`
- **Truth**: holds — i2c/spi/udp/dns-client carry no logger; SPI RX-overrun EIO propagates (`tests/test_asy_spi_driver.py:708-710`); readers catch per call
- **Fit**: restates SPEC C/D.2; refines OR26 · pillar P2
- **Home**: SPEC C → unchanged

### G3.024 Bus raise surface follows rp2 at the pinned tag
- **Req**: Code and fakes model the rp2 raise surface of the pinned MicroPython: hardware I2C raises EIO or ETIMEDOUT, a zero-length I2C write (probe, `scan()`) goes through soft I2C and raises ENODEV on NAK; SPI `write()` never raises and a ≥32-byte DMA read can raise EIO; UART transfers never raise, `write()` may short-write or return None.
- **Class**: platform
- **Sources**: BUS.N031, N085, N091, N092, N094, N095, N108 · SPEC C.3 (~1556), C.3.2 (~1622), C.12 (~2373), D (~2620), F.5.8
- **Provenance**: F — `ports/rp2/machine_i2c.c:127-154`; `extmod/machine_i2c.c:203` (`return -MP_ENODEV` on NAK); `ports/rp2/machine_spi.c:267, 341`; `ports/rp2/machine_uart.c:640-660`
- **Truth**: wrong in one statement — SPEC C.12 "real hardware only raises `OSError(EIO)` or `OSError(ETIMEDOUT)` — never `ENODEV`" does not hold for zero-length writes, which the presence probe (`asy_i2c_driver.py:266`) uses; behaviour is safe (the probe catches every OSError), the spec and the fakes' model are not. SPI and UART statements hold
- **Fit**: refines OR29.a (2) (corrected fact) · pillar P5
- **Home**: SPEC C.12/D/F.5 → Part F only, cited from C/D

### G3.025 Shared I2C scratch is safe by construction
- **Req**: The per-bus 32-byte scratch is filled and decoded with no `await` in between, no Timer/IRQ callback touches I2C, and no returned value aliases it; a caller keeping a returned view copies it. A read larger than the scratch falls back to allocation.
- **Class**: product
- **Sources**: BUS.N003, N004, N005, N047, N057, N109, N112, N113, N124, N125 · `src/asy_i2c_driver.py:17-19, 33-36, 59-61`, SPEC G.2 (~4221), I.2 (~4881-4887)
- **Provenance**: A — `323a1a7`
- **Truth**: holds by review — every `callback=`/`.irq(` in `src/` only sets an Event (grep); no mechanical guard; the allocating fallback is unexercised (largest read 21 B)
- **Fit**: extends OR41.a (proven independent with reason) · pillar P6
- **Home**: code + SPEC G.2/I.2 → one place (G.2), I.2 cites it

### G3.026 Device sessions hold the lock across sequences
- **Req**: Locking is the caller's job: every I2CDevice operation runs inside `async with device:`, a multi-transaction sequence (RMW, snapshot, burst) holds the session lock for its whole length, helpers that document "caller holds the lock" take none, and nested sessions on one device are forbidden (plain non-reentrant asyncio.Lock).
- **Class**: product
- **Sources**: BUS.N054, N055, N073, N080, N087, SENS.N028, N098, N135, N225 · SPEC C.3 (~1564), `tests/test_asy_i2c_driver.py:662-664, 831-833`
- **Provenance**: A — SPEC C.3/C.8
- **Truth**: holds — tests pin non-reentrancy and caller locking; SCD30 snapshot holds the lock across six reads (≥6×50 ms)
- **Fit**: refines OR41.a · pillar P2
- **Home**: SPEC C.8 → unchanged

### G3.027 SPI chip-select window never yields
- **Req**: While CS is asserted nothing awaits: the CS settle is a blocking `sleep_us(2)`, the whole CS cycle runs under the bus lock, and exactly one scheduling point follows each session, after deassert and release; each CS cycle is framed by one `SPI.init()` at the fixed bus config.
- **Class**: product
- **Sources**: BUS.N014, N021, N022, N049, N062, N079, N090, N126, N139 · `src/asy_spi_driver.py:17-20, 126-150, 173-178`, SPEC C.3.1 (~1586-1593), HEAP_FRAGMENTATION_MEASUREMENTS.md:369-372
- **Provenance**: F — MB85RS64V AC table tCSU/tCSH 10 ns, tD 60 ns (dstxt fram64 lines 453-458); A for the rule (`f6a182d`, `04ef56a`)
- **Truth**: holds — settle and trailing yield present; wire-trace goldens pin one init per CS cycle
- **Fit**: refines OR41.a; CLAUDE.md long-block principle · pillar P6
- **Home**: SPEC C.3.1 → unchanged

### G3.028 Synchronous SPI sessions: caller owns lock and deassert
- **Req**: `session_begin()`/`session_end()` never take the bus lock; the caller holds it (enforced by `configure()`'s guard) and deasserts CS in its own `try/finally`; `__aenter__` releases the lock itself if `configure()` raises, and `__aexit__` tolerates a double release.
- **Class**: product
- **Sources**: BUS.N016, N020, N060, N061, N064, N065 · `src/asy_spi_driver.py:61-67, 126-148`, `tests/test_asy_spi_driver.py:386-389, 467-472, 841-868, 1056-1057`
- **Provenance**: A — tests and code comments
- **Truth**: drifted — `asy_spi_driver.py:61-67` says the guard is "only ever called from SPIDevice.__aenter__", but `session_begin()` (FRAM's sync path) calls it too (BUS.S03)
- **Fit**: OR13 (comment vs code) · pillar P2
- **Home**: code → SPEC C.3.1

### G3.029 Bus-layer asymmetries are deliberate
- **Req**: SPI has a synchronous session and I2C does not; UART infers "read in flight" from its lock rather than a flag; I2C session bursts carry no between-session yield. These differences follow each bus's semantics and are not harmonised away.
- **Class**: code-style
- **Sources**: BUS.N029, N110, N122, N140, N141 · BACKLOG.md:613-624, SPEC G.2 (~4234), `src/asy_uart_driver.py:110-112`, archive `12640c2` HFM:5227
- **Provenance**: A — `0c54c6f`, `9415902`/`88245b2` ("flagged-deliberate"); no owner attribution
- **Truth**: partly — the SPI/I2C and UART asymmetries are recorded; the missing I2C between-session yield (BUS.N140) survives only in the archive, dropped by `17b4354`
- **Fit**: tension with OR24.a (see Conflicts) · pillar P4
- **Home**: BACKLOG deferred list → SPEC G.2 (a rule, not a backlog item)

### G3.030 UART driver never blocks the loop
- **Req**: `asy_uart_driver.py`/`asy_uart_comm.py` never hold the asyncio loop, not even waiting: every counted read clamps to `uart.any()`, `ready()` yields before returning True, delimited reads yield every 16 bytes, a zero-length round sleeps, and a wait with no deadline polls at the slower `poll_idle_ms`, kept well under the peer's reply timeout.
- **Class**: product
- **Sources**: BUS.N026, N027, N032, N039, N040, N042, N045, N072, N075, N076, N077, N102, N103, N104, N105, N116, N117, N118, N119, N130, N131 · CLAUDE.md hard rule, SPEC F.5.8-F.5.9 (~3914-3994), changelog B24/B25
- **Provenance**: O — CLAUDE.md "may not wait synchronously (project owner, 2026-09-11)"; measurements A (4.4 ms per 53-byte frame)
- **Truth**: partly — `ready()` yields at `asy_uart_driver.py:294`, `_read_delimited` at `:168-169`, `_buffered()` clamps counted reads; `readline()`/`readline_until_complete()` gate on one buffered byte, then call `uart.readline()` unclamped (`:395, :410`), holding for the buffered line plus one `timeout_char` (rp2 `machine_uart.c` inter-char wait) — documented in F.5.8, contested by BUS.S06; the write path waits for POLLOUT with no deadline at the idle rate
- **Fit**: restates CLAUDE.md; OR30 (`poll_idle_ms`, 16-byte yield are tunables) · pillar P6
- **Home**: CLAUDE.md + SPEC F.5.8 → unchanged

### G3.031 Wedged I2C/SPI bus: watchdog is the backstop
- **Req**: A wedged I2C bus or sensor is recovered by task restart escalating to reboot and the hardware watchdog, not by an I2C-level timeout mechanism; the UART clamp-and-yield technique is not generalised to I2C/SPI, which have no partial-read API; a task respawn does not rebuild the bus peripheral.
- **Class**: product
- **Sources**: BUS.N096, N097, N106, N115, N120, N134 · CLAUDE.md hard rule, SPEC F.2 (~3601-3620), F.5.8 (~3993)
- **Provenance**: O — CLAUDE.md "settled, don't re-propose"; OR18.a ("a stalled chip may recover by a reboot")
- **Truth**: holds — no I2C timeout wrapper in `src/`; SPEC F.2:3620 "only a full reboot does that"; hot-reconnect completeness never confirmed on hardware (BUS.N134)
- **Fit**: restates OR18.a · pillar P3
- **Home**: CLAUDE.md + SPEC F.2 → unchanged

### G3.032 UART cancel handshake is terminating and latched
- **Req**: `cancel_read_timeout()` is bounded (1000 ms), infers an in-flight read from the lock, is acknowledged by every holder leaving via `__aexit__`, and counts a never-acknowledging holder in `cancel_unacknowledged` while the request stays latched; a True result forbids the caller to take the lock and drain; `deinit()`/`init()` are not called while a session is held.
- **Class**: product
- **Sources**: BUS.N025, N028, N035, N036, N114, N123, N129 · `src/asy_uart_driver.py:30-33, 90-98, 241-256`, SPEC J.5 (~5484), BACKLOG.md:705-710, changelog B15
- **Provenance**: A — changelog B15
- **Truth**: holds — code matches; the 1000 ms bound assumes every poll round (dev `poll_idle_ms` 50) is far below it (OR30 tunable); a latched cancel can abort the next unrelated read (BUS.N036, accepted)
- **Fit**: refines OR41.a · pillar P2
- **Home**: SPEC J.5 → unchanged

### G3.033 UART driver never raises; its owner logs
- **Req**: The UART driver never raises: poll, `any()`, unregister and caller type errors collapse to a return value (False/None/0), `ready()` re-checks `_uart`/`poller` every iteration against a concurrent `deinit()`, and the one upstream owner (`asy_uart_comm.py`) logs. A caller's buffer belongs to the call until it returns.
- **Class**: product
- **Sources**: BUS.N030, N034, N038, N043, N069, N070, N071, N111 · `src/asy_uart_driver.py:124-125, 234-235, 287-290, 442-443`, SPEC C.7.1 (~1950), I.2 (~4869)
- **Provenance**: A — module docstring and tests
- **Truth**: holds — catches present; accumulation loops are MemoryError-wrapped and one is documented unbounded-and-accepted (`:415`)
- **Fit**: refines OR26 · pillar P2
- **Home**: code + SPEC C.3.2 → unchanged

### G3.034 An uninitialised bus is a silent no-op
- **Req**: On a deinitialised or never-initialised bus, I2C/SPI/UART operations return None/no-op without raising (the "non-hardware failure" carve-out), including mid-session; a failed re-init leaves the bus deinitialised.
- **Class**: product
- **Sources**: BUS.N007, N009, N017, N018, N048, N050, N058, N068, N086, SENS.N188 · `src/asy_i2c_driver.py:155-160, 204-205`, `src/asy_spi_driver.py:70-96`, SPEC C.3 (~1559)
- **Provenance**: A — tests pin it (`tests/test_asy_i2c_driver.py:76-96`)
- **Truth**: holds as pinned — but a skipped write and a success are indistinguishable to the caller, and BMP's poll then logs a misleading "timeout"; contested by BUS.S01/S04 (defect scan)
- **Fit**: tension with OR26 ("no missing handling") · pillar P2
- **Home**: SPEC C.3 → unchanged

### G3.035 rp2 bus deinit semantics stated once, consistently
- **Req**: On rp2 `machine.I2C.deinit()` and `machine.SPI.deinit()` are no-ops and I2C's `deinit()` needs ≥1.29; bus objects are per-bus singletons, so no code may depend on bus identity; `asy_i2c_driver.py`, `asy_spi_driver.py`, `tests/machine.py` and `digital_twin/machine.py` state the same semantics.
- **Class**: platform
- **Sources**: BUS.N098, N099, N121 · SPEC F.5.1 (~3709-3722), CLAUDE.md typecheck note
- **Provenance**: F — CLAUDE.md "audited against upstream source at both tags, 2026-09-10"
- **Truth**: holds — SPEC F.5.1 present; per-bus singleton reconfigured by a later construction stays a plan item (HW.S18); the four files not re-read here
- **Fit**: refines OR29.a · pillar P5
- **Home**: SPEC F.5.1 → unchanged

### G3.036 Wireless-owned GPIOs are never assigned
- **Req**: No bus, UART or IRQ wiring uses GPIO23/24/25/29, which the Pico W hands to the wireless chip, even where the RP2040 mux would allow it.
- **Class**: hardware-safety
- **Sources**: BUS.N023 · `src/asy_uart_driver.py:4-5`, SPEC A.6 (~350)
- **Provenance**: F — Pico W datasheet p.8 (dstxt pico-w lines 192-204: GPIO23/24/25/29 wireless)
- **Truth**: holds — dev wires UART on GPIO0/1, 8/9
- **Fit**: restates OR53 (1) ("wireless-reserved GPIOs stay excluded") · pillar P9
- **Home**: code comment → SPEC L.6.5 (`pico_gpio.py`)

### G3.037 Chip bus limits are machine-enforced
- **Req**: A driver declares its datasheet bus limits as `@requires` tags that buildgen enforces, a shared bus taking the strictest: SCD30 needs `timeout ≥ 200000` µs (150 ms daily clock stretch vs rp2's 50 ms default) and `frequency ≤ 100 kHz`; SGP40 `≤ 400 kHz`; BMP3xx has no limit.
- **Class**: product
- **Sources**: BUS.N082, N083, N084, SENS.N120, N121, N152, N299, N301, N342 · `src/asy_scd30_driver.py:105-111`, `src/asy_sgp40_driver.py:87-90`, SPEC L.6.4 (~6504), A.7 (~382)
- **Provenance**: F — SCD30 Interface Description lines 49-54 ("Maximal I2C speed is 100 kHz", "recommends … 50 kHz or smaller", "maximal clock stretching of 150 ms may occur once per day"); SGP40 datasheet Table 3 fSCL 400 kHz; `ports/rp2/machine_i2c.c:38` `DEFAULT_I2C_TIMEOUT (50000)`
- **Truth**: holds — tags present; all six TOMLs run 50 kHz and set 200000 on the SCD30 bus; the 50 kHz recommendation and ISL29125's own limit are not tagged; a 150 ms stretch still blocks the loop inside one call
- **Fit**: extends OR30 (bus timeout is a tunable) · pillar P5
- **Home**: code tags + SPEC L.6.4 → unchanged

### G3.038 SGP40 general-call reset is an accepted risk
- **Req**: SGP40 `_reset()` broadcasts the I2C general call (0x00, 0x06), tolerating the NAK; this resets every general-call listener on the bus, is accepted as a low risk, and has a real-hardware regression test; no device address may be 0x00 or in the reserved ranges.
- **Class**: product
- **Sources**: BUS.N044, N078, N135, N136, SENS.N176, N231, N249, N279, N290, N332, N333 · SPEC C.8 (~2060-2068), `src/asy_sgp40_driver.py:586-593`, `tests/test_bus_hazard_multi_device.py:68-70, 282`
- **Provenance**: F — SGP40 datasheet Table 17 ("a general call resetting all devices connected to the same I2C bus"); acceptance A (`f5c9f90`); the quiesce mechanism was "flagged for a project-owner decision", never decided (BUS.N136)
- **Truth**: holds — no BMP/SCD30/ISL datasheet text mentions general call (grep of all extracted texts); drift: the test comment "BMP3xx doesn't ack general calls (datasheet-confirmed)" overstates a datasheet that is silent; no bench-tier trigger exists (structural exception)
- **Fit**: extends OR41.a/OR45.a (listed exception) · pillar P2
- **Home**: SPEC C.8 → unchanged

### G3.039 Chip identity is verified at setup
- **Req**: Every driver's layer-2 `setup()` verifies the chip's identity from documented content and raises if it does not answer; SPI devices, having no ACK, verify by content only. The strength of each check follows the datasheet.
- **Class**: product
- **Sources**: BUS.N059, N089, SENS.N141, N148, N175, N180, N228, N229, N230, N268, N322, N338, N383, N385 · SPEC C.3 (~1566), C.3.1 (~1580), commits `e960d44`, `5ff8c0b`
- **Provenance**: A — `eff19bf` (DRIVER_SPEC), `e960d44`, `5ff8c0b`
- **Truth**: partly — SGP40 self-test compares the 0xD4 high byte (datasheet Table 13: "0xD4 0xXX … ignore 0xXX"); the serial read fetches 1 of the 3 words the datasheet specifies (§3.4) and checks an undocumented `word[0] == 0` inherited from Adafruit; SCD30 accepts any CRC-valid firmware version
- **Fit**: refines OR48.a (feature-set check dropped vs legacy, recorded) · pillar P2
- **Home**: SPEC C.3 → unchanged

### G3.040 BMP3xx family is one driver
- **Req**: `asy_bmp3xx_driver.py` supports BMP384, BMP388 (chip ID 0x50) and BMP390 (0x60) with one register map, and rejects any other ID at setup; its docs cite all three datasheets.
- **Class**: product
- **Sources**: SENS.N001, N002, N185, N316 · SPEC A.6 (~354-356), `src/asy_bmp3xx_driver.py:5-7, 36-37`
- **Provenance**: O — SPEC A.6 "The project owner has confirmed the whole family shares the same register map/protocol"; F — BMP390 datasheet Register 0x00 CHIP_ID 0x60 (dstxt bmp390 line 1328)
- **Truth**: drifted — the BMP390 PDF is now in `datasheets/bmp3xx/`, but the module docstring still says "Verified against BST-BMP388-DS001/BST-BMP384-DS003" only; the bus-hazard catalog seeds 0x50 only
- **Fit**: restates OR4.a (source now present) · pillar P4
- **Home**: code docstring → unchanged, updated

### G3.041 BMP3xx register semantics and timings from the datasheet
- **Req**: BMP3xx register bits, encodings (OSR 0-5, IIR 2^i−1), forced-mode value, soft-reset sequence with 2 ms settle and the measurement timeout come from the cited datasheet sections and Bosch's reference; reserved OSR codes raise; every timing constant cites its source.
- **Class**: platform
- **Sources**: SENS.N003, N005, N006, N007, N025, N026, N027, N035, N192 · `src/asy_bmp3xx_driver.py:49-69, 408, 419-436, 635-642`
- **Provenance**: F — BMP388/384 datasheet sections as cited; not every section re-read
- **Truth**: partly — IIR table and OSR set match the code; `_CMD_RDY_TIMEOUT_MS = 50` has no datasheet source ("near-instantly"), and the 2 ms `_wait_time` is an unused Adafruit tuning hint; IIR field has no reserved-code check (all 8 codes mapped)
- **Fit**: OR30 (timeouts are tunables with sources) · pillar P4
- **Home**: code → OR30 register

### G3.042 BMP3xx trim data is plausibility-checked
- **Req**: BMP3xx setup rejects a calibration block that reads all-0x00 or all-0xFF (stuck bus); exact per-coefficient bounds are used where the application note provides them.
- **Class**: product
- **Sources**: SENS.N032, N186, N386 · `src/asy_bmp3xx_driver.py:512-516`, commit `6ee7182`
- **Provenance**: A — `6ee7182` ("exact per-coefficient bounds live in a header file this codebase doesn't have")
- **Truth**: partly — the stuck-byte check holds; BST-MPS-AN006 is in `datasheets/bmp3xx/`, so "doesn't have the exact values" may no longer be true (AN006 contents not checked for bounds)
- **Fit**: OR4 (resolve with the source now present) · pillar P2
- **Home**: code → unchanged

### G3.043 Trigger-interval setters never raise
- **Req**: `set_trigger_secs()` never raises: an invalid interval (also a stale stored one at init) logs its errno and keeps the previous value, so a bad stored interval never fails init.
- **Class**: product
- **Sources**: SENS.N017, N023, N064, N193 · `src/asy_bmp3xx_driver.py:214-216, 355-357`, `src/asy_isl29125_driver.py:319-320`
- **Provenance**: A — tests `tests/test_asy_bmp3xx_driver.py:936-938`
- **Truth**: holds — BMP errno 21, ISL errno 25 plus derived-persist reapply (errno 24)
- **Fit**: refines OR26 · pillar P3
- **Home**: code → SPEC C.9

### G3.044 Config-read failure in a cycle publishes a defaulted sample
- **Req**: When a reader cannot read its own config during a cycle it logs the errno and still publishes a fresh sample computed with fixed fallback values (BMP3xx: offsets 0, mean temperature 15 °C; ISL29125: filter off).
- **Class**: product
- **Sources**: SENS.N019, N075, N195 · `src/asy_bmp3xx_driver.py:231-234`, `src/asy_isl29125_driver.py:503-506`
- **Provenance**: A — "documented fallback" in tests
- **Truth**: holds as coded — conflicts with SPEC M.1.1 requirement 16 for the ISL29125 (see Conflicts); contested by SENS.S25
- **Fit**: conflicts G3.060 · pillar P3
- **Home**: code → decided rule in SPEC C.4

### G3.045 I2C addresses follow each chip's pins
- **Req**: A driver exposes a TOML `address` only where the chip has an address-select pin: BMP3xx 0x76/0x77 by SDO (a `@limits` set); ISL29125 (0x44), SGP40 and SCD30 are hard-wired, so a second instance needs another bus, and no address parameter exists for test injection.
- **Class**: product
- **Sources**: SENS.N009, N099, N183, N248, N292, N293, N296, N297, N298 · `src/asy_bmp3xx_driver.py:118-119`, `src/asy_isl29125_driver.py:1065-1066`, `buildgen/buildspec.py:29-31, 61-63`
- **Provenance**: F — ISL29125 datasheet p7/p15 "internally hard-wired as 1000100" (dstxt isl lines 477, 1139); O — harmonization 10 / OR36.a (1) (ISL's test-only address parameter goes)
- **Truth**: drifted — `asy_isl29125_driver.py:1065-1066` says the parameter "exists only for test injection, exactly as BMP3XX_I2C's does", but BMP's is a real hardware choice; dev and wozi both wire BMP at 0x77 (`dev.toml:91`, `wozi.toml:70`)
- **Fit**: restates OR36.a (1) + harmonization 10 · pillar P4
- **Home**: code + buildspec → Part C config schema rule
