# Harvest requirements G4 — PLAT (400), MEM (175), PERF (55) (HEAD 06eff58)

Sources checked: MicroPython `v1.29.0` checkout (scratchpad `mp`; submodules `lib/cyw43-driver`, `lib/lwip`,
`lib/pico-sdk` are absent, so facts inside those three are "unverifiable here"), datasheet text (`dstxt`:
RP2040 DS-1, Pico W DS-2, SCD30 Interface Description), the repo at HEAD. `F` citations give file:line in
the pinned MicroPython tree unless a repo path is named.

## Candidates

### G4.001 Full platform re-check on every pin move
- **Req**: `toolchain/versions.toml` `[micropython] ref` is the refactor's only MicroPython pin. Every move of it re-verifies every Part F fact, every upstream file:line citation, the override anchors (Part B.14) and the stub version against the new tag's own source, and asks for each construct whether a newer or better way now exists; ruled-out items (e.g. `getaddrinfo()` timeout support) are re-checked, not carried.
- **Class**: process
- **Sources**: PLAT.N258, N286, N288, N290, N293, N303, N345, N346, N348, N359, N363, N365, N366 · stated at CLAUDE.md "Platform target" (2nd bullet), SPEC F.5 intro, B.14 (:1081-1084), B.14.1 (:1206-1217), versions.toml:9-10
- **Provenance**: A — added by `f3f25fe` (2026-08-06, "Add MicroPython-currency re-check as standing practice"), no owner attribution; OR29.a (2) (O) requires the same cross-check for hardware facts
- **Truth**: partly — pin is `v1.29.0` (versions.toml:10; CONSOLIDATION 6: still latest); nothing enforces the re-check: `setup_toolchain.py --latest`/`write_micropython_ref()` move the pin without prompting it (TOOL.T12); citations such as `modlwip.c:802`, `runtime.c:1692/1696`, `gc.c:891` hold at v1.29.0 (checked)
- **Fit**: refines OR29.a (2) · pillar P1
- **Home**: CLAUDE.md "Platform target" → CLAUDE.md working agreements (practice) + SPEC Part F (checklist)

### G4.002 Upstream source before any API claim
- **Req**: A session asserts how a MicroPython or Microdot API behaves only after reading the pinned source or current upstream docs (GitHub `docs/` when the docs hosts are blocked), never from memory, and cites what it read.
- **Class**: process
- **Sources**: PLAT.N364, N376 · stated at CLAUDE.md "Platform target" (1st bullet), BACKLOG.md:900-902
- **Provenance**: A — `cd4f07a` (2026-07-13), migrating the owner's initial `PROJECT_NOTES.md` (commit `8c4a73d`, Nico = owner); OR2/OR4 (O) demand the same grounding in external sources
- **Truth**: holds as practice (convention only); BACKLOG's `readfrom_mem_into()` verification is one honoured instance
- **Fit**: restates OR4 · process
- **Home**: CLAUDE.md "Platform target" → CLAUDE.md working agreements (sources rule, OR2/OR4)

### G4.003 One home and a primary source per platform fact
- **Req**: Every platform fact the code relies on is stated once in SPECIFICATION.md Part F with its primary source at the pinned tag (source file:line or datasheet page) and the tier that confirmed it (Unix port, twin, silicon); code comments point to Part F rather than restating it, and a fact confirmed on an older version is re-stamped or flagged at the current pin.
- **Class**: docs
- **Sources**: PLAT.N053, N115, N386, N388 · stated at SPEC Part F intro; CLAUDE.md "Platform target"
- **Provenance**: O — OR29.a (3) ("platform facts in SPECIFICATION.md Part F ... each with its source, cross-linked rather than duplicated")
- **Truth**: partly — the `mktime` "~2037" fact appears in six `src/` comments with no Part F entry and is wrong (G4.038); `print_log.py:136-139` points to CLAUDE.md for boundaries that live in SPEC F.1; `test_asy_neopixel_driver.py:543` stamps a 1.28.0 confirmation; the json omitted-value leniency (`b087dc7`) and cancel-before-first-step (`d92da47`) facts are in no Part
- **Fit**: restates OR29.a (3) · pillar P8
- **Home**: code comments + SPEC F → SPEC Part F only

### G4.004 Deployed units stay on 1.26 until a reflash
- **Req**: Fielded units keep MicroPython 1.26 and the legacy firmware until a deliberate reflash campaign; the refactor alone targets the pinned version, and no claim verified at 1.28/1.29 is extrapolated to 1.26 behaviour (the `import sensortask.py` rule stands on that basis).
- **Class**: platform
- **Sources**: PLAT.N307, N374 · stated at BACKLOG.md:160-166 (#3), SPEC F.1 (:3434-3438), CLAUDE.md `_boot.py` hard rule
- **Provenance**: A — BACKLOG #3 "Decided" with no owner name (`50d86c3`); OR52.a (1) (O) confirms reflash with fresh setup, no migration
- **Truth**: holds — SPEC F.1 and BACKLOG #3 agree; CLAUDE.md `_boot.py` rule applies it
- **Fit**: refines OR52.a (1) · pillar P3
- **Home**: BACKLOG #3 + SPEC F.1 → SPEC F.1 (fact) + the reflash runbook (OR52.a (1)); BACKLOG item goes when settled

### G4.005 Watchdog timeout below the RP2040 cap
- **Req**: The hardware watchdog is armed with `WDT(timeout=8000)`, under the RP2040's 8,388 ms cap (24-bit counter decremented twice per 1 µs tick, erratum RP2040-E1; a larger value raises `ValueError` at arm time), and the timeout is not raised; every inter-feed stretch (boot steps, device scripts, twin WDT model) is budgeted against 8,000 ms.
- **Class**: platform
- **Sources**: PLAT.N207, N221, N308, N347 · stated at SPEC F.1 (:3440-3441), `digital_twin/machine.py:835-838`, `tests/test_digital_twin_machine.py:319-330`
- **Provenance**: F — `ports/rp2/machine_wdt.c:32-38,56-57` (`WDT_TIMEOUT_MAX 8388`, RP2350 16777), RP2040 datasheet p.546 §4.7.3 (double decrement); the "don't casually increase" norm is A (`cd4f07a`)
- **Truth**: holds — `buildgen/codegen.py:381` emits `WDT(timeout=8000)`; twin mirrors the cap and id-0-only
- **Fit**: refines OR31.a (2) · pillar P3
- **Home**: SPEC F.1 (good) + OR30 tunables register for the 8000

### G4.006 Long device scripts feed or re-arm the watchdog
- **Req**: Every device script or hardware test that can run longer than the armed watchdog window feeds the watchdog or re-arms it deliberately, because an mpremote soft reset never disarms an rp2 watchdog and a re-`WDT(timeout=…)` takes the new (shorter) timeout.
- **Class**: test-standard
- **Sources**: PLAT.N227, N230, N235, N239 · stated at `tests_hardware/device_scripts/fram_pause_unpause_and_gating.py:26-28`, `bmp3xx_plausibility_read.py:29-30`, `watchdog_starvation_reset.py:2-9`, `tests_hardware/flash/test_watchdog_starvation.py:1-3`
- **Provenance**: F — `ports/rp2/main.c:265-300` (soft-reset exit never touches the watchdog), `machine_wdt.c:47-59` (`watchdog_enable()` per construction); silicon observation (bare serial EIO) recorded in the scripts
- **Truth**: holds for the four scripts named; not audited across all device scripts (OR33.a names an orphan script "without watchdog feeding")
- **Fit**: refines OR29.a (4) · pillar P9
- **Home**: script headers → `tests_hardware/README.md` (bench fact) with SPEC F cross-link

### G4.007 Soft reset keeps state; only a hard reset resumes
- **Req**: Hardware tooling assumes that raw-REPL entry soft-resets the board without re-running `main.py` (only a hard reset resumes the live system), and that the watchdog, the GC threshold, the free-running `ticks_ms()` counter and GPIO pin muxing survive that soft reset; every heap figure names the threshold in force.
- **Class**: hardware-safety
- **Sources**: PLAT.N226, N228, N243, N245, N276, N375 · stated at `tests_hardware/README.md:488-493,962-975`, BACKLOG #12 (:214-226), `tests_scripts/test_device_script_gc_threshold.py:2-3`
- **Provenance**: F — `ports/rp2/main.c:157-166` (`gc_init()` outside the soft-reset loop, so `gc_alloc_threshold` persists, `py/gc.c:264`), `main.c:246-247` (`main.py` only under `FRIENDLY_REPL`); BACKLOG #12 "Do not re-investigate" is A
- **Truth**: holds — source confirms threshold and main.py behaviour; `ticks_ms` survival is a silicon observation (unverifiable here)
- **Fit**: refines OR29.a (1) · pillar P7
- **Home**: `tests_hardware/README.md` (good), BACKLOG #12 → README only

### G4.008 Boot entry frozen as `main.py`; VFS cannot pre-empt it
- **Req**: Each device's boot entry is frozen under the literal name `main.py`, never run from a custom frozen `_boot.py` (USB comes up only after `_boot.py` and `boot.py` return); a reflashed unit's filesystem holds no `boot.py` and no `.py`/`.mpy` whose name matches a frozen module, because a VFS `boot.py` still runs before the frozen `main.py` and the VFS root precedes `.frozen` on `sys.path`.
- **Class**: platform
- **Sources**: PLAT.N249, N256, N287, N309, N392 · stated at SPEC B.11 (:954-962), F.1 (:3441-3443), `scripts/build_firmware.py:96-97`, `dev_legacy/README.md:602-614`
- **Provenance**: F — `ports/rp2/main.c:229-247` (`_boot.py` → `boot.py` → `mp_usbd_init()` → `main.py`), `shared/runtime/pyexec.c:743-752` (a frozen `main.py` wins over a VFS one), `py/runtime.c:147-150` (`sys.path = ['', '.frozen']`); design A (`e554073`)
- **Truth**: holds for the build; new finding: the legacy-residue risk (`PAR.T08`) is real for `boot.py` and for same-named modules, not for `main.py`
- **Fit**: refines OR52.a (1) (reflash runbook, `PAR.T08`) · pillar P3
- **Home**: SPEC B.11/F.1 → SPEC F.1 + the reflash runbook

### G4.009 Frozen-import sentinel and import order
- **Req**: Build and test tooling treat `.frozen` as MicroPython's hard-coded import sentinel (never a real directory on an import path) and never rely on a frozen module winning over a same-named file earlier on `sys.path`/`MICROPYPATH`; code imported outside the frozen image is precompiled with the pinned `mpy-cross` (on-device compilation can exhaust the heap mid-import).
- **Class**: tooling
- **Sources**: PLAT.N246, N271, N285, N304, N382, N391 · stated at `.gitignore:35-38`, SPEC A.9 (:638-641), E.3 (:2849-2854), HEAP_FRAGMENTATION_MEASUREMENTS.md:254-258
- **Provenance**: F — `py/builtinimport.c:54-65` (`MP_FROZEN_PATH_PREFIX ".frozen/"`), `py/runtime.c:147-150`; A (`fef4b54`)
- **Truth**: holds — `frozen_modules/` naming and MICROPYPATH handling follow it
- **Fit**: new (tooling fact behind OR45 twin/unit tiers) · pillar P8
- **Home**: SPEC A.9/E.3 + `.gitignore` comment → SPEC F (one fact) with pointers

### G4.010 Soft callbacks may be dropped or coalesced
- **Req**: No code relies on every soft `Timer` or `Pin.irq` callback being delivered: MicroPython's scheduler queue (depth 8 on rp2) drops a callback silently when full, and a `ThreadSafeFlag` set twice before a wait coalesces, so callbacks only set a flag or event whose loss or coalescing the consumer tolerates (next period, timeout or retry).
- **Class**: platform
- **Sources**: PLAT.N010, N152, N236, N312 · stated at SPEC F.1 (:3454-3458), `src/asy_isl29125_driver.py:230-232,359-362`, `tests_hardware/device_scripts/scheduler_saturation_drop.py:1-3`
- **Provenance**: F — `ports/rp2/mpconfigport.h:131` (`MICROPY_SCHEDULER_DEPTH 8`), `shared/runtime/mpirq.c:103-105` (return of `mp_sched_schedule()` ignored), `py/scheduler.c:163-176`, `extmod/asyncio/event.py:45-63`
- **Truth**: holds for the documented pattern; per-consumer tolerance not checked here (OR41.a matrix)
- **Fit**: refines OR49.a (1) (8-deep soft-IRQ queue) · pillar P2
- **Home**: SPEC F.1 (good)

### G4.011 Every Timer stays referenced
- **Req**: Every `machine.Timer` is created once and held by a long-lived attribute for as long as it may fire; a Timer built per step and not stored is collectable, and its `__del__` cancels the alarm (a one-shot chain then hangs).
- **Class**: code-style
- **Sources**: PLAT.N055, N182 · stated at `src/system_service.py:86-89,161-163`, SPEC F.1 (soft-Timer gotcha), `tests/test_system_service.py:527-532`
- **Provenance**: F — `ports/rp2/machine_timer.c:115` (`mp_obj_malloc_with_finaliser`), `:160` (`__del__` → `deinit`); A (`3bb015b`, found fixing a Timer-GC hang)
- **Truth**: holds — all 13 Timers in `src/` are `self.*` attributes created in `__init__` (grep `Timer()`); regression test present
- **Fit**: new (P2 construction rule) · pillar P2
- **Home**: SPEC F.1 + Part C → Part C pattern with F.1 pointer

### G4.012 Every Timer arm site handles ENOMEM and MemoryError
- **Req**: Every `Timer.init()`/`Timer(period=…)` arm site catches `(OSError, MemoryError)` (alarm-pool exhaustion raises `OSError(ENOMEM)`) and degrades locally with a logged error instead of ending its task; a bare `Timer()` is the allocate-now, arm-later form and never reaches the pool.
- **Class**: code-style
- **Sources**: PLAT.N083, N095, N123, N179, N237, N316 · stated at SPEC F.1 (:3489-3490), CLAUDE.md "Platform target", `tests/machine.py:613-614`
- **Provenance**: F — `ports/rp2/machine_timer.c:107-110` (`mp_raise_OSError(MP_ENOMEM)`), `:131-135` (init helper only with arguments); silicon-confirmed (`timer_alarm_pool_exhaustion.py`); rule A (`71dc6c5`)
- **Truth**: holds — 13/13 arm sites in `src/` catch `(OSError, MemoryError)` (bmp3xx:290, isl29125:849, ntp:281/332/343, scd30:214, sgp40:469, wifi:413/662, system_service:126/164/204/365)
- **Fit**: refines OR26.a (raising calls mapped from source) · pillar P2
- **Home**: SPEC F.1 (good)

### G4.013 Periodic timers are drift-free; one-shot chains are not
- **Req**: Timing designs may rely on `Timer.PERIODIC` being drift-free (the alarm re-arms at `-delta_us` from its own schedule) but not on a chain of one-shots armed from callbacks, whose dispatch latency accumulates.
- **Class**: platform
- **Sources**: PLAT.N300 · stated at SPEC C.9.1 (:2268-2280)
- **Provenance**: F — `ports/rp2/machine_timer.c:50-60` (`return -self->delta_us`)
- **Truth**: holds — source confirms; the accumulation half is OR47.a (3)'s open gap (a)
- **Fit**: refines OR47.a (3) · pillar P1
- **Home**: SPEC C.9.1 (good)

### G4.014 I2C/SPI `deinit()` releases nothing; UART/Timer/WLAN do
- **Req**: Code never relies on `machine.I2C.deinit()`/`machine.SPI.deinit()` to stop or release a bus: on rp2 both are silent no-ops (the protocol's `deinit` slot is NULL; I2C's only exists from 1.29) and each bus id is a static singleton that re-construction reconfigures; only `UART.deinit()`, `Timer.deinit()` and `WLAN.deinit()` really release hardware. A wrapper makes a bus "unavailable" by dropping its own reference.
- **Class**: platform
- **Sources**: PLAT.N007, N013, N024, N034, N076, N105, N125, N198, N201, N225, N231, N333, N334, N335, N372 · stated at SPEC F.5.1 (:3689-3712), CLAUDE.md "Code quality tooling" (I2C.deinit note)
- **Provenance**: F — `ports/rp2/machine_i2c.c:161-164`, `machine_spi.c:357-360` (no `.deinit`), `extmod/machine_i2c.c:331-337` (NULL-slot no-op), `machine_i2c.c:50` (static `machine_i2c_obj[]`), `machine_uart.c:483-497`, `machine_timer.c:152-158`; A (`90e8c17`)
- **Truth**: holds at source level; silicon confirmation owed (`bus_deinit_is_a_noop_on_real_hardware.py` exists, run status not checked)
- **Fit**: refines OR29.a (2) · pillar P2
- **Home**: SPEC F.5.1 (good), CLAUDE.md copy → pointer only

### G4.015 I2C raises only EIO or ETIMEDOUT
- **Req**: I2C bus wrappers handle exactly the rp2 raise surface — `OSError(EIO)` for a NAK or any non-timeout failure, `OSError(ETIMEDOUT)` for a stalled or stretched bus — and fakes model that surface (never `ENODEV`, which is SoftI2C's).
- **Class**: platform
- **Sources**: PLAT.N074, N108, N346 · stated at `tests/machine.py:115-117`, `tests/test_asy_i2c_driver.py:446-461`, SPEC F.5.6 (:3853-3857)
- **Provenance**: F — `ports/rp2/machine_i2c.c:150-155`; zero-length writes go through the soft-I2C path (`:127-144`); `WRITE1` fix inactive on rp2 (source SPEC-cited, not re-checked)
- **Truth**: holds
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC Part C.3/F → F.1 (fact), C.3 (contract)

### G4.016 I2C timeouts cover the slowest chip's clock stretching
- **Req**: A bus's I2C timeout covers the longest clock stretching any chip on it may do — at least 150 ms on a bus carrying an SCD30 — and otherwise keeps rp2's 50 ms default; a wrapper omits `addrsize`/`timeout` it does not set instead of duplicating `machine.I2C`'s defaults.
- **Class**: hardware-safety
- **Sources**: PLAT.N004, N065, N075 · stated at `tests/_sensortask_scenarios.py:284-293`, `src/asy_i2c_driver.py:61,172-173`, `tests/machine.py:118`
- **Provenance**: F — SCD30 Interface Description p.2 ("maximal clock stretching of 150 ms may occur once per day"), `ports/rp2/machine_i2c.c:38` (`DEFAULT_I2C_TIMEOUT 50000`)
- **Truth**: holds — all six `devices/*.toml` set `timeout = 200000` on the SCD30 bus
- **Fit**: new (datasheet-derived bus rule) · pillar P2
- **Home**: test comment → SPEC Part C (bus config) + `buildgen` validation

### G4.017 SPI reads of 32+ bytes can raise EIO
- **Req**: Every SPI read of 32 bytes or more (the DMA path) is treated as able to raise `OSError(EIO)` on an RX overrun since 1.29, and callers of block reads (FRAM) handle it at their layer; writes and shorter transfers never raise; a transient overrun is not retried (owner, 2026-09-24).
- **Class**: platform
- **Sources**: PLAT.N023, N025, N064, N077, N098, N103, N127, N130, N161, N199, N202, N336, N337, N367, N396 · stated at SPEC F.5.2 (:3721-3736), CLAUDE.md "Platform target", `src/asy_spi_driver.py:4-6,72-90`
- **Provenance**: F — `ports/rp2/machine_spi.c:267-270` (`dma_min_size_threshold = 32`), `:305-317,339-341`; no-retry is O-confirmed (BACKLOG "SETTLED, owner, 2026-09-24", per PLAT.N396)
- **Truth**: holds
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC F.5.2 (good)

### G4.018 SPI is MSB-first; CS only after setup
- **Req**: SPI code uses MSB-first only (LSB raises `NotImplementedError` on rp2), passes equal-length buffers to `write_readinto()`, and never drives a CS pin before its `setup()` has made it an output, since `Pin.value()` writes the output register regardless of direction and reads back the pad level.
- **Class**: platform
- **Sources**: PLAT.N026, N073, N078, N079, N126, N128, N129, N200, N233 · stated at `src/asy_spi_driver.py:153-155`, `tests/machine.py:57-58,255-261,316-319`
- **Provenance**: F — `ports/rp2/machine_spi.c:41,203,256`; `machine_pin.c:327-351` (`gpio_get()` read, `gpio_put()` write); `extmod/machine_spi.c` length check (SPEC-cited)
- **Truth**: holds — `asy_spi_driver.py` guards CS with `RuntimeError`; minor fidelity gap: twin `SPI.__init__` accepts `firstbit=LSB` unchecked (`digital_twin/machine.py:384`)
- **Fit**: refines OR29.a (2) · pillar P2
- **Home**: code comments → SPEC F (fact) + C.3 (contract)

### G4.019 UART code never blocks the loop
- **Req**: `asy_uart_driver.py` and `asy_uart_comm.py` never block the asyncio loop, not even in a wait: every read is clamped to `uart.any()` and a real yield separates rounds (in `ready()`), because rp2 `UART.read()/readinto()` wait out `timeout_char` per missing byte inside `mp_event_handle_nowait()`, which never yields, and `POLLIN` fires on one byte.
- **Class**: product
- **Sources**: PLAT.N032, N139, N238, N350, N351, N368; PERF.N004, N037, N038 · stated at CLAUDE.md hard rule, SPEC F.5.8 (:3891-3965)
- **Provenance**: O — CLAUDE.md "(project owner, 2026-09-11)"; F — `ports/rp2/machine_uart.c:593-622`
- **Truth**: holds — source confirms the per-byte wait; dated bench figures (4.4 ms per 53-byte frame, clamped 137-278 µs) are evidence only
- **Fit**: refines OR31.a (tasks never stall), P6 · pillar P6
- **Home**: CLAUDE.md + SPEC F.5.8 → SPEC F.5.8 (rule text) with CLAUDE.md pointer

### G4.020 UART faults arrive as sentinels, never exceptions
- **Req**: UART code treats every rp2 UART fault as a sentinel, not an exception: framing, parity and overrun errors never raise (the byte is delivered or lost), break bytes are dropped, `read()` answers `None` on an empty ring (also after `any()` said otherwise), `write()` may accept fewer bytes or `None`, and `any()` has no raise site; every read or write loop ends on those sentinels, and the driver caches its own construction values because `machine.UART` has no getters.
- **Class**: platform
- **Sources**: PLAT.N031, N035, N069, N082, N137, N138, N140, N141, N186, N389 · stated at SPEC C.3.2, `src/asy_uart_driver.py:6,417-418`, `tests/machine.py:450-452`
- **Provenance**: F — `ports/rp2/machine_uart.c:160-189` (OE/PE/FE ignored, BE skipped), `:504-508`, `:593-622` (`MP_EAGAIN`); A (`59d6b7b`)
- **Truth**: holds
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC C.3.2 (good)

### G4.021 UART re-init only by construction
- **Req**: A UART is re-initialised only by constructing it anew, never `deinit()` followed by `init()`, because `deinit()` unroots the RX/TX ring buffers while a later `init()` of the same size reuses them unrooted (IRQ writes into collectable memory); moving a UART to other pins needs a hard reset, since `deinit()` leaves the GPIO function select in place.
- **Class**: platform
- **Sources**: PLAT.N033, N242, N349, N398 · stated at SPEC F.5.7 (:3867-3876), `src/asy_uart_driver.py:199-200`, `tests_hardware/README.md:472-480`
- **Provenance**: F — `ports/rp2/machine_uart.c:436-443` (alloc only when `buf == NULL`), `:483-497` (roots cleared, `buf` kept, no `gpio_set_function` reset)
- **Truth**: holds
- **Fit**: refines OR29.a (2) · pillar P2
- **Home**: SPEC F.5.7 + README (good)

### G4.022 Waits without a deadline poll at an idle rate
- **Req**: A task waiting for traffic that may never come polls at a slow idle interval (`poll_idle_ms`), never at the transaction rate, and no poll interval defaults to 0; the idle rate is a tuned value with its measurement.
- **Class**: product
- **Sources**: PERF.N001, N005, N041, N042, N043 · stated at SPEC F.5.9 (:4026-4047), CLAUDE.md UART hard rule ("mirror-image failure"), `src/asy_udp_socket.py:126-129`
- **Provenance**: O-confirmed — SPEC F.5.9 "same owner rule behind it" as F.5.8 (CLAUDE.md, owner 2026-09-11); the UDP default is A
- **Truth**: partly — UART honours it (`asy_uart_driver.py:68,267`); `asy_udp_socket.ready()` polls every 20 ms forever for captive DNS (50 Hz, NET.S11)
- **Fit**: extends OR49.a (CPU time) · pillar P6
- **Home**: SPEC F.5.9 → Part C/G (one pattern for every poller) + OR30 register

### G4.023 Catch MicroPython's exception classes, not CPython's
- **Req**: `except` clauses name the classes MicroPython actually raises: `MemoryError` is not an `OSError` subclass (catch `(OSError, MemoryError)` where both are plausible), `asyncio.TimeoutError` is a plain `Exception`, `CancelledError` is a `BaseException` that still runs `__aexit__`, and `struct` raises `ValueError`, not `struct.error`.
- **Class**: code-style
- **Sources**: PLAT.N021, N096, N109, N143, N146, N169, N317, N325 · stated at SPEC F.1 (:3490-3492,3549-3550), CLAUDE.md memory rule
- **Provenance**: F — `extmod/asyncio/core.py:18-22`, `py/modstruct.c:146-155` (`mp_raise_ValueError`); A (`71dc6c5`)
- **Truth**: holds — every Timer/UDP/config site checked catches both (G4.012; `asy_udp_socket.py:178`, `config_manager.py:356`)
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC F.1 (good)

### G4.024 Numeric coercion rejects NaN and infinities by both exceptions
- **Req**: Every `int(float)` coercion of external input catches both `ValueError` (NaN) and `OverflowError` (±inf), and a numeric validator never lets a non-finite or complex result through; facts verified only on the Unix port (e.g. `round()` returning `int`) are marked as such.
- **Class**: code-style
- **Sources**: PLAT.N002, N016, N047, N092, N093, N112, N124, N163, N284 · stated at SPEC A.8 (:617-619), `src/config_manager.py:137-138`, `src/asy_bmp3xx_driver.py:349-351`
- **Provenance**: F — `py/objint.c:149-151` ("can't convert inf/NaN to int")
- **Truth**: partly — config and BMP3XX paths hold; SGP40 compensation values are validated only for "not None" (PLAT.N124, SENS.S03)
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC A.8/F.1 → F.1 + Part G numeric primitive

### G4.025 Persisted and wire binary formats pin byte order
- **Req**: Every `struct` format that describes persisted or wire bytes carries an explicit `"<"` (a bare format is native `"@"` with padding, and native `"L"` is 4 bytes on rp2 but 8 on the 64-bit test rig).
- **Class**: code-style
- **Sources**: PLAT.N054, N135, N176, N319 · stated at SPEC F.1 (:3499-3503), `src/print_log.py:227`
- **Provenance**: F — `py/modstruct.c:55-67` (default `'@'`)
- **Truth**: partly — `print_log.py:234` (`"B" * n`) and `voc_algorithm.py:141` (`"32q"`) are bare; both are size-neutral, so no defect, only a rule gap
- **Fit**: new (convention) · pillar P4
- **Home**: SPEC F.1 → Part G/D.10 pattern

### G4.026 Range-check before packing or storing a byte
- **Req**: Code range-checks every value before `struct.pack()`/`pack_into()` or a `bytearray` item store, because MicroPython silently truncates or zero-pads (0x101 becomes 0x01, missing arguments become zeros, NaN packs as a float) until the `MICROPY_PREVIEW_VERSION_2` overflow checks ship; it also handles `pack_into()`'s `ValueError` on a negative offset and an empty-tuple result from a zero-field format.
- **Class**: code-style
- **Sources**: PLAT.N005, N006, N015, N028, N029, N106, N107, N111, N132, N133, N187, N320, N384 · stated at SPEC F.1 (:3505-3508), CLAUDE.md "Platform target", `src/asy_uart_comm.py:371-373,409-410`
- **Provenance**: F — `py/binary.c:47` (`OVERFLOW_CHECKS MICROPY_PREVIEW_VERSION_2`), `py/modstruct.c:192,213`; A (`5dbf4df`)
- **Truth**: holds — UART header and command id are guarded; `asy_i2c_driver.set_register_struct` catches only malformed formats, by design (overflow is the caller's range check)
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC F.1 (good); re-check trigger at MicroPython 2.0 (G4.001)

### G4.027 Bound-check spans before slice assignment
- **Req**: Code copying a computed span into a buffer bound-checks the span itself, because a `bytearray` destination silently resizes on a length mismatch while a `memoryview` raises `ValueError`; writability of a `memoryview` is probed by a zero-length slice assignment (there is no `.readonly`), and out-of-range slices clamp rather than raise.
- **Class**: code-style
- **Sources**: PLAT.N027, N110, N321; MEM.N103 · stated at SPEC F.1 (:3510-3519), `src/asy_uart_comm.py:151-153`
- **Provenance**: A — `9f4c084` (2026-09-11, "close a partial-allocation gap"); measured on the Unix port only
- **Truth**: holds at the named instance (`asy_uart_comm.py` `written + size > len(dest)`); rp2 behaviour of the three slice facts not silicon-checked (unverifiable here)
- **Fit**: new · pillar P2
- **Home**: SPEC F.1 (good)

### G4.028 Exclude `bool` by exact type, not by subclass
- **Req**: Numeric validators reject `bool` with an explicit type test (`type(x) is int` or an `isinstance(x, bool)` guard), because on MicroPython `bool` is not an `int` subclass; tests and comments state that fact the same way.
- **Class**: code-style
- **Sources**: PLAT.N094, N113, N114, N134, N306, N315 · stated at SPEC F.1 (:3478-3487), E.8 (:3363-3364)
- **Provenance**: F — `py/objbool.c:87-96` (`mp_type_bool` has no `parent` slot); A (`839c31e`)
- **Truth**: drifted — `tests/test_asy_isl29125_driver.py:2835` ("bool is an int subclass in MicroPython as in CPython") and `tests/test_asy_neopixel_driver.py:541` contradict SPEC and source
- **Fit**: new · pillar P4
- **Home**: SPEC F.1 (good); two test comments corrected

### G4.029 Emitted JSON is checked by a strict oracle
- **Req**: Every test asserting that output is valid JSON parses it with the strict oracle `tests/_strict_json.py`, never with MicroPython's `json.loads()`, which treats `,` and `:` as whitespace and so accepts missing, doubled or stray separators; duplicate keys resolve last-wins and a non-`str` key is emitted as its own JSON text, and code relying on either states it.
- **Class**: test-standard
- **Sources**: PLAT.N036, N067, N145, N177, N178, N189, N327 · stated at SPEC F.1 (:3569-3571), `digital_twin/README.md:125-126`, `tests/_strict_json.py:1-3`
- **Provenance**: F — `extmod/modjson.c:170-177`; A (`cca4fd3`, closing coverage-audit gaps)
- **Truth**: holds — oracle and its own tests exist (`tests/test_strict_json.py`)
- **Fit**: refines OR19.a (checks that can fail) · pillar P5
- **Home**: SPEC F.1 + Part E (test standard)

### G4.030 Never emit non-finite JSON; tolerate a lenient read
- **Req**: Product code never passes NaN or ±inf to `json.dumps()`, which never raises and writes bare `nan`/`inf` (unreadable by browsers and by MicroPython's own `json.load()`), so every float that can become non-finite is gated before serialisation; a config read survives a leniently mangled parse through per-key validation with default fallback.
- **Class**: product
- **Sources**: PLAT.N122, N166, N167, N328, N386 · stated at SPEC F.1 (:3573-3583), `tests/test_config_manager.py:912-935`, commit `b087dc7`
- **Provenance**: A — `83b727e` (2026-09-24, "Close two conditions the merged code leaves unhandled"); measured on the Unix port
- **Truth**: partly — SCD30 gates per word (`test_asy_scd30_driver.py:415`); a repo-wide non-finite gate is XCUT.T23's open topic; the omitted-value leniency is fixed upstream after 1.29 (commit `9ef16b466` per `test_config_manager.py:929`), so the loader must keep working either way
- **Fit**: refines OR26.a, OR52.a (2) · pillar P2
- **Home**: SPEC F.1 → F.1 fact + Part C/G rule

### G4.031 Product code uses the pinned language subset
- **Req**: Code that runs on MicroPython avoids syntax the pinned parser rejects or mishandles — iterable unpacking in a display (`[*a, b]`), `await` in a comprehension, a plain literal containing `{` implicitly concatenated with an f-string — and relies on annotations never being evaluated; each such gap is enforced by a named lint rule where one exists (RUF005 and the other justified ignores), and code not run by the unit tier (device scripts) is proven by running it on the target interpreter, since host tools cannot see target-only syntax errors.
- **Class**: code-style
- **Sources**: PLAT.N039, N044, N120, N172, N220, N234, N267, N268, N313, N324, N371, N395 · stated at SPEC F.1 (:3464-3470,3534-3543), `pyproject.toml:46-49,116-129`, CLAUDE.md union-annotation bullet
- **Provenance**: A — `6aed3c9` (2026-09-09); suppressions "verified on the pinned interpreter" (`pyproject.toml`)
- **Truth**: holds — ruff ignores present (`pyproject.toml:117-129`); `fram_error_log_reset_race_verify.py` shows the device-script gap was real (caught on the board, 2026-09-11)
- **Fit**: new (P4 + OR16.a chain liveness for device scripts) · pillar P4
- **Home**: SPEC F.1 + pyproject comments (good)

### G4.032 Product code uses the pinned builtin and library subset
- **Req**: Code avoids builtins and modules the rp2 build lacks — `zip(strict=)`, `bytearray` slice deletion, `deque.clear()` and keyword arguments, `file.writelines()`, `itertools`, `str.removeprefix()`, `random.Random`, `inspect`, `namedtuple._asdict()/_fields`, user-class `__del__` and `weakref` — and never relies on dict or `globals()` order.
- **Class**: code-style
- **Sources**: PLAT.N014, N046, N081, N104, N117, N131, N160, N173, N174, N203, N216, N217, N218, N240, N269, N270, N299 · stated at SPEC F.1, C.6 (:1806), `pyproject.toml:150-153,348-351`, `tests_hardware/README.md:316-319`
- **Provenance**: F — `py/mpconfig.h:1662-1663` (`_asdict` at ROM level EVERYTHING; rp2 builds EXTRA_FEATURES, `ports/rp2/mpconfigport.h:81`), `py/mpconfig.h:1951-1952` (WEAKREF at EVERYTHING), `py/objtype.c` (no `__del__` support), `py/objstr.c` (no `removeprefix`); A for the rest (Unix-port checks)
- **Truth**: holds — FURB122/RUF007/RUF037 ignores in `pyproject.toml`; `str.removeprefix()` is not "unproven" but absent (0 hits in `py/objstr.c`)
- **Fit**: new · pillar P4
- **Home**: SPEC F.1 (good)

### G4.033 asyncio semantics relied on are MicroPython's
- **Req**: Code uses MicroPython's asyncio as it is: an uncontended `Lock.acquire()` never yields, lock hand-off order is not relied on, a finished `Task` has no `exception()/result()/cancelled()` (re-await it; use `done()`), `create_task()` needs a coroutine object, a task cannot cancel itself, `gather()` returns a list, each accepted connection is its own task, a task cancelled before its first step raises `CancelledError` to its awaiter, and a coroutine is detected by its `send` attribute.
- **Class**: platform
- **Sources**: PLAT.N019, N030, N049, N057, N102, N150, N153, N183, N206, N282, N388 · stated at SPEC A.5 (:318-322), `src/asy_ntp_client.py:194-196`, `src/system_service.py:185-187`
- **Provenance**: F — `extmod/asyncio/lock.py:33-48`, `extmod/asyncio/stream.py:174`, `funcs.py:65`; A (`d92da47` for cancel-before-first-step)
- **Truth**: holds — confirmed for Lock and per-connection task; the cancel-before-first-step fact is in no Part (G4.003)
- **Fit**: refines OR41.a (interleavings) · pillar P2
- **Home**: code comments → SPEC F.1 (one asyncio list)

### G4.034 No async generators, no nested `asyncio.run()`
- **Req**: Code never defines an `async def … yield` generator (a broken object that segfaults when driven past an await) — streaming bodies are synchronous iterators over prepared pieces — and never calls `asyncio.run()` while other tasks are parked (the Unix port segfaults); test helpers that call it run only from synchronous test scope.
- **Class**: code-style
- **Sources**: PLAT.N037, N148, N358, N369, N394 · stated at SPEC F.1 (:3534-3543), I.1 (:4805-4808), CLAUDE.md "Known segfault cause"
- **Provenance**: A — `6d08fdb`; segfaults reproduced on the pinned Unix port (not checked here)
- **Truth**: holds — `asy_webserver_service.py:197-199` streams a sync iterator; CLAUDE.md audit of `asyncio.run()` call sites recorded
- **Fit**: refines OR26.a · pillar P2
- **Home**: SPEC F.1 + CLAUDE.md (good)

### G4.035 `const()` rules as the pinned compiler applies them
- **Req**: Constants that must cost no RAM are `_`-prefixed `const()` names (inlined at compile time and absent as module attributes, so never imported or read by `getattr`); a non-underscore `const()` name stays an importable global; `const()` accepts int, str, float, `None/True/False` and tuples of those at 1.29, and a `const()` referenced inside another `const()` tuple is itself `const()`; float constants are frozen as single-precision values.
- **Class**: code-style
- **Sources**: PLAT.N052, N059, N247, N265, N297, N298, N322, N343, N380, N393 · stated at SPEC F.1 (:3521-3524), C.1 (:1520-1521), C.5 (:1710-1712), F.5.6 (:3837-3840), HEAP_FRAGMENTATION_MEASUREMENTS.md:155-157
- **Provenance**: F — `py/parse.c:832-869` (underscore names become `pass`, others stored), `py/parse.c:369-392` (str/token/const-object/tuple accepted), `py/mpconfig.h:582-605` (`COMP_CONST_TUPLE/FLOAT` on); A (`6f430c7`, `e5f31f2`)
- **Truth**: wrong in part — SPEC F.1 (:3521-3524) "a `const()`-wrapped value does not survive as an importable module attribute in a frozen build" holds only for `_`-prefixed names; `parse.c:864-869` keeps a non-underscore `const()` as a module global. Float `const()` precision at freeze time not checked (ALGO.T06)
- **Fit**: new · pillar P6
- **Home**: SPEC F.1 (corrected), C.1/C.5 pointers

### G4.036 Single-precision float, no FPU
- **Req**: Numeric code assumes the target's single-precision float (24-bit mantissa, software float from the boot ROM, no FPU) and uses integer arithmetic where precision or speed matters; float edge cases proven on the double-precision Unix port are re-proven on the target (device script) before they are relied on.
- **Class**: platform
- **Sources**: PLAT.N051, N162, N232, N318, N339 · stated at SPEC F.1 (:3494-3497), F.5.3 (:3792-3794), `src/framing_codecs.py:41`
- **Provenance**: F — `ports/rp2/mpconfigport.h:128-129` (`MICROPY_FLOAT_IMPL_FLOAT`), RP2040 datasheet p.137 §2.8.3.2 (boot-ROM float library), p.9 ("Dual ARM Cortex-M0+ @ 133MHz"); A (`3986be9`)
- **Truth**: drifted — SPEC F.5.3 (:3792-3793) says `-fno-math-errno` makes `math.sqrt` "compile to the hardware instruction"; the Cortex-M0+ has no FPU or sqrt instruction (the flag is in `ports/rp2/CMakeLists.txt:547`, but the effect claimed is wrong)
- **Fit**: refines OR29.a (2) · pillar P6
- **Home**: SPEC F.1/F.5.3 (F.5.3 corrected)

### G4.037 Tick arithmetic wraps every 2^30 ms
- **Req**: Every comparison of `ticks_ms()` values goes through `ticks_diff()`/`ticks_add()`, a stored tick is never trusted after 2^29 ms (~6.2 days), and the ~12.4-day rp2 wrap is proven by a test that uses the target's period, since the unit rig's period is 2^62.
- **Class**: platform
- **Sources**: PLAT.N012, N184, N185, N254, N323, N373 · stated at SPEC F.1 (:3526-3528), `tests/test_ticks_rollover.py`, README.md:394-396
- **Provenance**: F — `py/mpconfig.h:1925` (`TICKS_PERIOD = MP_SMALL_INT_POSITIVE_MASK+1`, 2^30 on 32-bit), `extmod/modtime.c:178-196`
- **Truth**: partly — rule holds; `tests/test_ticks_rollover.py:25-27` calls the accepted `ticks_add()` range `[-period/2, period/2)` while the source excludes `-period/2` too (`modtime.c:187-191`, minor comment drift); the rp2-period proof is a deferred hardware measurement (PLAT.N399)
- **Fit**: refines OR44.a P1 (counter wrap) · pillar P1
- **Home**: SPEC F.1 (good)

### G4.038 Wall-clock conversions: valid after NTP, unsigned to 2106
- **Req**: A wall-clock timestamp is used only after NTP sync, and the reason recorded for handling `OverflowError`/`OSError` from `time.mktime()`/`gmtime()` matches the pinned port: on rp2 at 1.29 timestamps are unsigned 32-bit seconds since 1970 (valid to 2106), so no "~2037" boundary exists and persisted timestamps keep a width that holds them.
- **Class**: platform
- **Sources**: PLAT.N009, N018, N022, N042, N056, N099, N116 · stated at `src/asy_fram_manager.py:549,613`, `asy_ntp_client.py:147,408`, `asy_notification_service.py:184`, `asy_wifi_service.py:194`, `system_service.py:144`
- **Provenance**: F — `py/mpconfig.h:1061-1083` (32-bit: no Y1969/Y2100 support → `MICROPY_TIMESTAMP_IMPL_UINT`), `ports/rp2/mpconfigport.h:147` (`EPOCH_IS_1970`), `shared/timeutils/timeutils.h:38-42,75-91` (`mp_obj_new_int_from_uint`), `extmod/modtime.c:83-95`; the "~2037" claim is A (`69d85b7`)
- **Truth**: wrong — all seven `src/` comments (and `tests/test_asy_fram_manager.py:678-680`, `test_asy_notification_service.py:1468-1470`) claim rp2 raises `OverflowError` past ~2037; source shows the range ends in 2106. The handlers are harmless; FRAM stores timestamps as `"<Q"` (`asy_fram_manager.py:35`)
- **Fit**: refines OR44.a P1 (clock) · pillar P1
- **Home**: seven code comments → SPEC F.1 (one corrected fact) + pointers

### G4.039 Unix-port time differs from rp2; tests pin UTC
- **Req**: Unit and twin runs export `TZ=UTC` (the Unix port's `mktime()` uses host libc local time; rp2's is TZ-agnostic) and normalise `gmtime()` to rp2's 8-element tuple (the Unix port returns 9); a code path reachable only on one shape is tested on that shape, and `sleep(0)` never advances wall-clock time in tests.
- **Class**: test-standard
- **Sources**: PLAT.N119, N121, N171, N251, N370 · stated at CLAUDE.md "Local test runs pin `$TZ=UTC`", `scripts/test.sh:20-21`, `tests/test_asy_ntp_client.py:1418-1424,1545-1547`
- **Provenance**: F — `ports/rp2/datetime_patch.c` (mktime via `timeutils_mktime`), `extmod/modtime.c:44-71` (8-tuple on rp2); A
- **Truth**: holds — `scripts/test.sh` exports `TZ`; note `cettime()`'s `len(cet) == 8` branch is unreachable on rp2 and only faked
- **Fit**: refines OR45.a (4) (tier scope) · pillar P5
- **Home**: CLAUDE.md + SPEC E.3 (good)

### G4.040 RTC tuple layout is rp2's
- **Req**: Code setting `machine.RTC().datetime()` passes rp2's 8-tuple `(year, month, mday, weekday, hour, minute, second, subseconds)`, knowing the setter ignores the weekday and subseconds fields; fakes return what the real getter returns, not the tuple as set.
- **Class**: platform
- **Sources**: PLAT.N020, N084 · stated at `src/asy_ntp_client.py:262-263`, `tests/machine.py:664-666`
- **Provenance**: F — `ports/rp2/machine_rtc.c:75,86-91` (setter reads indices 0,1,2,4,5,6)
- **Truth**: partly — `src/` call holds; the fake returns the tuple as set (weekday not recomputed, TEST.T05)
- **Fit**: refines OR17 (twin fidelity) · pillar P5
- **Home**: code comment → SPEC F.1

### G4.041 Per-boot uniqueness from hardware entropy
- **Req**: Code that needs a value unique per boot relies on rp2 seeding `random` from `get_rand_32()` (ring-oscillator entropy) and never re-seeds it with a fixed value; unit and twin tiers inject their own seed through a seam outside the product (`random.Random` does not exist).
- **Class**: platform
- **Sources**: PLAT.N058, N387 · stated at `src/system_service.py:390-391`, commit `9712cea`
- **Provenance**: F — `ports/rp2/mpconfigport.h:163` (`MICROPY_PY_RANDOM_SEED_INIT_FUNC (get_rand_32())`)
- **Truth**: holds
- **Fit**: refines OR36.a (seams outside the code) · pillar P1
- **Home**: code comment → SPEC F.1

### G4.042 WLAN arguments validated against the port's limits
- **Req**: Configuration that reaches `network` is validated before the call against the port limits — hostname at most 32 bytes, country exactly 2 bytes, WPA2 passphrase 8-64 bytes, SSID at most 32 bytes — in bytes, not characters; fakes enforce the same limits with the same exception types.
- **Class**: product
- **Sources**: PLAT.N091, N149, N170, N215, N222, N266, N272 · stated at `buildgen/validate.py:112-113`, `tests/network.py:27,35`, `tests/test_asy_wifi_service.py:439-441`
- **Provenance**: F — `extmod/modnetwork.h:58-60` (`HOSTNAME_MAX_LEN 32`), `extmod/modnetwork.c:134-146` (country `len != 2` → ValueError); SSID/key limits live in `lib/cyw43-driver` (unverifiable here: submodule absent)
- **Truth**: partly — hostname and country confirmed; key/SSID exception types unverified at source
- **Fit**: refines OR43.a (1) (ranges from datasheets) · pillar P2
- **Home**: test comments → SPEC F (fact) + schema limits

### G4.043 CYW43 magic values are named with their source
- **Req**: A CYW43 value MicroPython does not name — link status 2 ("joined, no IP yet": `STAT_*` has no constant for it) and the power-management word `0xA11140` (no-power-save mode with explicit listen fields, not equal to `WLAN.PM_NONE`) — is a named constant with its source, re-checked at every pin move.
- **Class**: code-style
- **Sources**: PLAT.N041, N043, N090 · stated at `src/asy_wifi_service.py:128-129,386,452`, `tests/network.py:4-6`
- **Provenance**: F — `extmod/modnetwork.c:197-202` (no `CYW43_LINK_NOIP` entry), `extmod/network_cyw43.c:43-52` (`PM_NONE = 0x10`; `0xA11140` decodes to mode 0, 200 ms sleep-return, listen fields 1/1/10)
- **Truth**: partly — values are magic literals with comments (NET.T12)
- **Fit**: refines OR24 (one material) · pillar P4
- **Home**: code → named constants + SPEC F

### G4.044 CYW43 behaviour the Wi-Fi service must respect
- **Req**: The Wi-Fi service treats an RSSI query outside station mode as an expected `ValueError("STA required")`, never re-applies `essid/password/active(True)` to a running access point (the CYW43 can drop its beacon), and the bench access point disables PMF and offers WPA2/CCMP only (the CYW43439 handshake fails silently otherwise).
- **Class**: product
- **Sources**: PLAT.N151, N154, N248 · stated at `tests/test_asy_wifi_service.py:1153-1154,2792-2794`, `dev_legacy/README.md:581-586`
- **Provenance**: F — `extmod/network_cyw43.c:365`; the beacon and PMF facts are silicon findings (unverifiable here)
- **Truth**: holds for RSSI (source); AP idempotency guard exists per the test; PMF fact is bench-only
- **Fit**: refines OR29.a · pillar P2
- **Home**: `dev_legacy/README.md` → `tests_hardware/README.md` (OR32.a (3)); rest SPEC F

### G4.045 lwIP pools serve every admitted connection
- **Req**: The lwIP build override sizes every shared pool for `max_connections` admitted connections plus three spare TCP PCBs (a FIN_WAIT pcb outlives its slot and is never reclaimed at equal priority), gives `MEM_SIZE` at least 2,000 B per connection (every `tcp_write()` copies), keeps `TCP_MSS` when it sets `MEM_SIZE`, and reads the values back from the built image; settings the port ignores (`MEMP_NUM_NETCONN`) are not set.
- **Class**: tooling
- **Sources**: PLAT.N259, N260, N261, N262, N273, N279, N291, N292, N295, N354, N383, N400; PERF.N020, N021, N023, N029 · stated at SPEC B.14.2 (:1215-1379), H.7 (:4557-4561), `toolchain/micropython_overrides.py:175-271`, `tests_scripts/test_micropython_overrides.py:627-647`
- **Provenance**: F — `extmod/lwip-include/lwipopts_common.h:50-52,70,80-107` (NETCONN/SOCKET 0; MEM_SIZE block also defines TCP_MSS), `extmod/modlwip.c:802` (`TCP_WRITE_FLAG_COPY`), `ports/rp2/lwip_inc/lwipopts.h:8` (IPv6 on); lwIP-internal facts (`tcp_alloc()` reclaim order, `PBUF_IP_HLEN`) unverifiable here (`lib/lwip` absent); the 2,000 B floor is A (a relationship to the fielded design)
- **Truth**: holds — override tests pin the shipped pattern (PCB 9, SEG 48, MEM_SIZE 12000 at 6)
- **Fit**: refines OR49.a (1) (sockets/connections) · pillar P6
- **Home**: SPEC B.14.2 (good), OR30 register for the tuned floors

### G4.046 Socket I/O follows modlwip's semantics
- **Req**: The webserver never writes to a connection after its read side saw a reset (modlwip frees the pcb yet accepts writes that reach `tcp_write(NULL)`), always passes an explicit listen backlog (asyncio's default is 5), and treats refusals at the ceiling (reset inside lwIP, FIN after ~6 ms, or RST when request bytes arrived) as invisible to `src/` and expected by clients.
- **Class**: product
- **Sources**: PLAT.N038, N040, N147, N355, N356 · stated at `src/asy_webserver_service.py:256-260,736-737`, SPEC H.7 (:4569-4602), `tests/test_asy_webserver_service.py:1648-1658`
- **Provenance**: F — `extmod/asyncio/stream.py:179-188` (`backlog=5`), `extmod/modlwip.c:1589,1692-1693` (abort paths); the write-after-reset behaviour is a silicon finding (1.29)
- **Truth**: holds — `peer_gone` guard and explicit backlog present
- **Fit**: refines OR26.a (response-writing gap) · pillar P2
- **Home**: SPEC H.7/A.5 (good)

### G4.047 Accepted: modlwip can stall the VM on arena exhaustion
- **Req**: A `tcp_write()` that finds the lwIP arena full retries up to 200 × 50 ms (10 s) inside modlwip, blocking the whole VM beyond the watchdog window; this is accepted only because G4.045's sizing keeps the arena from filling for admitted connections, and the firmware keeps no lwIP pool statistics in the field.
- **Class**: platform
- **Sources**: PLAT.N294, N296 · stated at SPEC B.14.2 (:1313-1320,1380-1383)
- **Provenance**: F — `extmod/modlwip.c:801-811`; accepted status is A ("never observed on silicon")
- **Truth**: holds (source); a 10 s stall exceeds the 8 s watchdog, so this would end in a WDT reset — see Conflicts
- **Fit**: conflicts OR49.a (1) (no WDT reset under load) unless G4.045 proves no exhaustion · pillar P3
- **Home**: SPEC B.14.2 (good)

### G4.048 Uninterruptible calls fall to the watchdog backstop
- **Req**: A call MicroPython cannot interrupt — any `machine.I2C`/`machine.SPI` transfer (no await point, no partial-read API), `socket.getaddrinfo()` (also inside `asyncio.start_server()`) — is not wrapped in an asyncio timeout; a genuinely wedged bus or resolver is left to the hardware watchdog, and the upstream status is re-checked at every pin move.
- **Class**: platform
- **Sources**: PLAT.N190, N208, N329, N330, N344, N352 · stated at CLAUDE.md hard rule (wedged I2C), SPEC F.2 (:3603-3623), F.5.8 (:3991-3993), `digital_twin/README.md:534-540`
- **Provenance**: F — `extmod/asyncio/stream.py:183` (`getaddrinfo(host, port)[0]  # TODO this is blocking!`), `ports/rp2/machine_i2c.c:120-158` (synchronous SDK calls); settled rule A (`75f2e11`), consistent with OR18.a (O)
- **Truth**: holds
- **Fit**: restates OR18.a · pillar P3
- **Home**: CLAUDE.md + SPEC F.2 (good)

### G4.049 UDP error and poll semantics differ by port
- **Req**: UDP code handles `POLLERR` defensively but does not depend on it (rp2/lwIP does not propagate ICMP errors to a connected UDP socket's poll state), passes addresses as plain tuples (modlwip accepts them), treats `addr[0]` as possibly non-`str`, and keeps payloads under the IPv4 UDP maximum (oversize raises `OSError` on the Unix port).
- **Class**: platform
- **Sources**: PLAT.N045, N142, N144, N193, N244 · stated at `tests_hardware/README.md:676-680`, `digital_twin/README.md:775-780`, `src/captive_dns.py:102-106`
- **Provenance**: F — silicon observation (zero POLLERR/POLLHUP in 6 cycles) and Unix-port runs; lwIP behaviour unverifiable here
- **Truth**: holds; the POLLERR branch is effectively dead on rp2 (coverage question, OR25.a)
- **Fit**: refines OR26.a · pillar P2
- **Home**: `tests_hardware/README.md` + SPEC F

### G4.050 Flash writes stay off the request path
- **Req**: A write to the RP2040 flash filesystem never runs inline in a request: `ConfigManager` stages the change and flushes it from its own task after the response, because a flash program/erase disables interrupts port-wide and inline it reset the triggering HTTP connection.
- **Class**: product
- **Sources**: PLAT.N048, N168, N331 · stated at SPEC F.2 (:3638-3640), `src/config_manager.py:302-304`
- **Provenance**: F — `ports/rp2/rp2_flash.c:171-197` (`save_and_disable_interrupts()` around program/erase); A (`81d6a36`, WP5)
- **Truth**: holds — `_flush_staged()` deferred path, regression test `test_config_manager.py:1730-1732`
- **Fit**: refines OR42.a (write sites) · pillar P9
- **Home**: SPEC F.2 (good)

### G4.051 Wireless-reserved GPIOs are never assignable
- **Req**: buildgen never accepts GPIO 23, 24, 25 or 29 for a device pin on the Pico W, because the CYW43 wireless interface owns them.
- **Class**: tooling
- **Sources**: PLAT.N264 · stated at `buildgen/pico_gpio.py:5,36-37`
- **Provenance**: F — Pico W datasheet (DS-2) p.8 ("A few RP2040 GPIO pins are used for internal board functions": GPIO23 power-on, GPIO24 data/IRQ, GPIO25 CS, GPIO29 CLK/ADC3); O — OR53 (1) "wireless-reserved GPIOs stay excluded"
- **Truth**: holds
- **Fit**: restates OR53 (1) · pillar P2
- **Home**: `buildgen/pico_gpio.py` (good)

### G4.052 Budgets are sized for the RP2040 on a Pico W
- **Req**: Design budgets assume the RP2040 on a Pico W: two Cortex-M0+ cores up to 133 MHz with MicroPython on one, 264 KB SRAM of which the GC heap is what the linker leaves after the SRAM-resident interpreter and the CYW43 driver, 2 MB QSPI flash with the littlefs partition behind the firmware image.
- **Class**: platform
- **Sources**: PLAT.N301, N310 · stated at SPEC D (:2633), F.1 (:3443-3445)
- **Provenance**: F — RP2040 datasheet p.9 ("Dual ARM Cortex-M0+ @ 133MHz", "264kB on-chip SRAM"); partition size depends on the build (not checked)
- **Truth**: holds (silicon facts); the ~848 KB partition figure unverifiable here
- **Fit**: restates OR44 (low-end hardware) · pillar P6
- **Home**: SPEC F.1 (good); D copy → pointer

### G4.053 Stub defects are repaired, never suppressed in `src/`
- **Req**: A defect in the MicroPython stub packages is handled where it lives — repaired in the installed stub tree (conditionally, so it no-ops once upstream ships the fix), or modelled by the test fake — never by `type: ignore` in `src/` or `digital_twin/`; each workaround names its removal trigger (the upstream fix).
- **Class**: tooling
- **Sources**: PLAT.N001, N003, N011, N017, N188, N253 · stated at CLAUDE.md "scripts/typecheck.sh repairs two verified defects", `scripts/typecheck.sh:83-97`, `src/asy_bmp3xx_driver.py:152-154`
- **Provenance**: A — CLAUDE.md "(added with the 1.29 bump)"; stub gaps F against source (`machine_timer.c:118-127` bare `Timer()` valid)
- **Truth**: partly — `typecheck.sh` repairs are conditional; `Timer()`, `const()`-as-`Any`, private `_mpy_shed` import and `DeflateIO` workarounds state no removal trigger; `tests/test_asy_fram_manager.py:1599-1602` uses `type: ignore[return-value]` for `gather()` (tests, allowed)
- **Fit**: new · pillar P4
- **Home**: CLAUDE.md → SPEC B.15 (tooling) with CLAUDE.md pointer

### G4.054 `TYPE_CHECKING` blocks never reach the frozen image
- **Req**: Every `from typing import …` in MicroPython-target code sits under a guarded `TYPE_CHECKING` import, and the build strips those blocks before `mpy-cross`, which does not dead-code-eliminate them.
- **Class**: tooling
- **Sources**: PLAT.N155, N255, N302 · stated at SPEC D (:2646-2651), B.11, `scripts/_strip_type_checking.py:1-3`
- **Provenance**: A — Unix-port and mpy-cross facts (not re-checked here)
- **Truth**: holds — stripper exists and runs in the build (SCR.T03)
- **Fit**: refines OR36 (lean image) · pillar P6
- **Home**: SPEC B.11/D (good)

### G4.055 Test harness accounts for Unix-port mechanics
- **Req**: The unit-tier harness works with the Unix port as it is: the executed script is not `sys.modules["__main__"]` (tests pass a namespace dict), `SystemExit` carries `.args` not `.code`, an uncaught traceback goes to stdout (gates merge `2>&1`), stdout is block-buffered when not a tty (`stdbuf -oL`), there is no `unittest`, `typing`, `glob`, `os.path`, `network` or `neopixel` (fakes on `MICROPYPATH`), and builtin `time` is read-only (patch the importing module's name).
- **Class**: test-standard
- **Sources**: PLAT.N062, N063, N068, N085, N086, N087, N089, N100, N159, N181, N274, N275, N281, N305 · stated at `tests/microtest.py:7-13`, `tests/_coverage_runner.py:54-61`, SPEC E.3 (:2905-2906)
- **Provenance**: A — "confirmed directly against the built interpreter" in each file (not re-run here)
- **Truth**: holds (harness follows each); not re-executed
- **Fit**: refines OR20.a (unit tier on the Unix port) · pillar P5
- **Home**: harness comments → SPEC E.3

### G4.056 Unit-tier divergences from rp2 are proven on the target
- **Req**: A property that depends on where the Unix-port rig differs from rp2 — 64-bit words and allocation boundaries, double floats, the tick period, `select.poll()`/`ipoll()` readiness on Python streams and UDP sockets, socket API gaps (`getsockname()`, packed `getaddrinfo()`), bound-method identity, private `asyncio.core` internals — is proven on the twin or silicon, or the test states the divergence it works around.
- **Class**: test-standard
- **Sources**: PLAT.N066, N070, N097, N118, N136, N164, N165, N180, N194, N195, N212, N213, N214, N223 · stated at CLAUDE.md "Known hang cause" (poll), `digital_twin/README.md:869-906`, `tests/test_asy_ntp_client.py:928-930`
- **Provenance**: F — `py/mpconfig.h:1892-1893` + `ports/unix/variants/mpconfigvariant_common.h:110` (`SELECT_POSIX_OPTIMISATIONS` Unix-only, so the modselect dangling pointer cannot hit rp2); `STR_UNICODE` on rp2 (`py/mpconfig.h:1369-1370`) so codepoint bounds transfer; A for the rest
- **Truth**: holds for the cited cases; the prewarm helper depends on private `_io_queue` and a never-shrinking pollfds array (TWIN.T07)
- **Fit**: refines OR45.a (2)(b) · pillar P5
- **Home**: `digital_twin/README.md` + test comments → SPEC E (rig limits list)

### G4.057 Fakes and the twin track the pinned source
- **Req**: Every port constant or behaviour copied into `tests/machine.py`, `tests/network.py` or `digital_twin/` (pin constants, `DMA_MIN_SIZE_THRESHOLD`, UART buffer limits, stream poll code, Timer init-helper rule, WDT cap, `WDT.feed()` allocating nothing, NeoPixel busy-wait) names its source file at the pinned tag and is re-checked at every pin move; a behaviour not modelled is listed in the twin fidelity table.
- **Class**: test-standard
- **Sources**: PLAT.N071, N072, N080, N088, N196, N197, N204, N205 · stated at `tests/machine.py:21-39,327-333`, `digital_twin/machine.py:12,48-50,604-606,792-794`, `tests/neopixel.py:4-6`
- **Provenance**: A — per-file "confirmed against ports/rp2/…"
- **Truth**: partly — `_MAX_BUFFER_SIZE = 32766` (confirmed: `machine_uart.c:69`) and `_UART_INVERT_MASK = 3` cite no source; NeoPixel blocking time and colour order not modelled; the twin `Timer` rejects `freq=`
- **Fit**: restates OR17.a (2) · pillar P5
- **Home**: code comments → `digital_twin/README.md` fidelity table (OR17.a)

### G4.058 Stream clients retry `None` and bound their reads
- **Req**: Host-side and twin HTTP clients retry a `readinto()` that returns `None` (only `0` means the peer closed), close through `wait_closed()`, and read bodies with bounded reads into a preallocated buffer rather than `readexactly()`/`read(-1)`, which grow by `r += r2`.
- **Class**: test-standard
- **Sources**: PLAT.N191, N192, N209, N210, N219; MEM.N039, N041 · stated at `digital_twin/README.md:712-739`, `digital_twin/_http_client.py:75-82,150-152`
- **Provenance**: F — `extmod/asyncio/stream.py:24-50` (`r += r2`), `:37-40`; A
- **Truth**: holds — `_read_exact()`/`_read_until_close()` in `_http_client.py`
- **Fit**: refines OR38.a (no leaks, no contention) · pillar P5
- **Home**: `digital_twin/README.md` (good)

### G4.059 The Unix-port SIGINT override replaces the heap unwedge
- **Req**: Every Unix-port build applies the `unix_kbd_intr` override (safe deferred SIGINT, `MICROPY_ASYNC_KBD_INTR=0`); once a test proves it is applied to every build, `digital_twin/unix_port_gc_unwedge.py`, its test and call sites are retired, and SPEC F.6 keeps the mechanism and its recovery (`gc.collect()`, not `heap_unlock()`). `src/` needs no SIGINT path (rp2 has no signals).
- **Class**: tooling
- **Sources**: PLAT.N211, N224, N289, N353; MEM.N043 · stated at SPEC B.14.1 (:1140-1142), F.6 (:4087-4088), CLAUDE.md "Known intermittent `digital-twin-e2e` shutdown flake"
- **Provenance**: O — OR52.a (6) ("retire if it's no longer needed but document how it was done")
- **Truth**: holds for the override (`py/mpconfig.h:848` default 0 with the Unix `!GIL` rule); retirement pending (execution)
- **Fit**: restates OR52.a (6) · pillar P4
- **Home**: SPEC B.14.1/F.6 (good)

### G4.060 Settrace lives only in the coverage binary
- **Req**: `MICROPY_PY_SYS_SETTRACE` is compiled only into the `build-settrace` coverage binary, never into the plain test rig or the firmware, because it allocates a frame and code object per call and inflates allocation 4-5x non-uniformly; allocation constants calibrated on one binary are re-derived on the other.
- **Class**: tooling
- **Sources**: PLAT.N263, N381; MEM.N098 · stated at CLAUDE.md "Code quality tooling" (owner decision, 2026-09-21), SPEC E.5.2 (:3038-3051), HEAP_FRAGMENTATION_MEASUREMENTS.md:165-170
- **Provenance**: O-confirmed — CLAUDE.md "(owner decision, 2026-09-21)"; F — `py/mpconfig.h:1845-1846` (`#ifndef` guard makes the `-D` variant work)
- **Truth**: holds — `setup_toolchain.py:353-375` builds both; `scripts/test.sh` checks `hasattr(sys, "settrace")`
- **Fit**: refines OR39.a (2) · pillar P5
- **Home**: CLAUDE.md + SPEC E.5.2 (good)
