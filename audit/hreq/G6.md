# Harvest requirements G6 — UART.md, NET.md, REST.md (HEAD 06eff58)

Read in full: `audit/harvest/UART.md` (282), `NET.md` (258), `REST.md` (186). Verified against HEAD's
tree, MicroPython `v1.29.0` source (scratchpad `mp`), Microdot upstream `v2.6.2`/`v2.7.0` (scratchpad
`microdot`, `ext/microdot.py` confirmed byte-identical to `v2.6.2` today) and git history. `arduino/`
not read (out of scope). Retired UART requirements file read from history (`4b85426^:UART_PROMOTION_REQUIREMENTS.md`,
its §3 "Owner decisions, 2026-09-11").

## Candidates

### G6.001 UART_Comm never raises
- **Req**: Every `UART_Comm` entry point returns its documented sentinel and never raises; a link fault, a failed allocation or a misbehaving callback is never an exception. The listen loop's broad catch and the callback dispatcher's catch are the runtime backstop, not the mechanism.
- **Class**: product
- **Sources**: UART.N001, N169, N057, N067 · stated at `src/asy_uart_comm.py:3`, SPEC Part J (5173), J.9
- **Provenance**: O-confirmed — Part J "confirmed by the project owner (2026-09-11)" (SPEC:5169); UART requirements file: "same never-raise/never-wedge contracts"
- **Truth**: holds — no `raise` statement in `asy_uart_comm.py`; catches at `:676` (`_call`) and `:1123` (`_listen_loop`); the exerciser loop (`asy_uart_link_driver.py:111-121`) relies on it without its own try
- **Fit**: refines OR26 · pillar P2
- **Home**: module docstring + SPEC J.9 → unchanged

### G6.002 Every UART protocol change gets a changelog entry
- **Req**: Every change to `asy_uart_comm.py`'s wire constants, accept/reject rules or recovery timings is recorded in `UART_C_PORT_CHANGELOG.md` as Class A (mirror in C) or Class B (no C impact), both logged. A change to any `const()` wire constant or recovery timing is Class A by definition.
- **Class**: process
- **Sources**: UART.N002, N213, N221, N170, N175, N233 · stated at CLAUDE.md hard rules (UART bullet), SPEC J.1, `src/asy_uart_comm.py:4-5`, changelog :27
- **Provenance**: O-confirmed — CLAUDE.md hard rule; changelog :34-46 "the project owner has decided (2026-09-11)"
- **Truth**: drifted — `UART_C_PORT_CHANGELOG.md:27` scopes it to "every change to the module during its `src/` promotion", CLAUDE.md to every change; no test or hook checks that a `const()` change has an entry (only three files name the changelog)
- **Fit**: extends OR24.a (2) (UART wire format untouched in the audit) · pillar P8
- **Home**: CLAUDE.md + SPEC J.1 + changelog header → SPEC J.1 only, CLAUDE.md pointing (OR51.a (4))

### G6.003 Part J is the normative UART specification
- **Req**: SPECIFICATION.md Part J is the one normative contract for both UART implementations; it describes the promoted module, and its differences from legacy are enumerated in the changelog.
- **Class**: docs
- **Sources**: UART.N168, N222 · stated at SPEC:5166-5176, README.md:680-684
- **Provenance**: O-confirmed — "confirmed by the project owner (2026-09-11)" (SPEC:5169)
- **Truth**: holds — Part J present; README points to it; note Part J's intro is split by a misplaced `## I.6` heading inside Part J (SPEC:5183), a placement defect
- **Fit**: restates OR51.a (4) single source · pillar P8
- **Home**: SPEC Part J → unchanged; I.6 moved back into Part I

### G6.004 Emitted-bytes changes are owner flag days
- **Req**: A UART protocol change should only tighten receiver validation; a change to emitted bytes (enabling a CRC, a delimited codec, a shorter ACK) is a coordinated flag day that needs an explicit owner decision, and the Python side may lead it once recorded. The pass-through codec stays the default so the fixed frame size stays the framing.
- **Class**: process
- **Sources**: UART.N176, N215, N234, N182, N071 · stated at SPEC J.1/J.3, CLAUDE.md, changelog :34-46, `src/framing_codecs.py:4-6`
- **Provenance**: O-confirmed — changelog "the project owner has decided (2026-09-11) that the Python side may lead it"
- **Truth**: holds — `dev.toml` wires no `crc=`/codec; `Framing_Pass` default; buildgen emits neither (`validate.py:256-258`)
- **Fit**: restates OR24.a (2) (UART wire format not touched during the audit) · pillar P8
- **Home**: SPEC J.1 → unchanged

### G6.005 C implementation out of scope until after the audit
- **Req**: The Arduino C implementation is out of scope; no audit work reads, verifies or reconciles `arduino/`. C-side conformance is unverified and never assumed; each Class A entry states what the reconciliation must check. With no C device in the field, a changelog entry is an obligation to record, not a deployment risk. Hardware tests prove Python↔Python interoperation only.
- **Class**: process
- **Sources**: UART.N174, N216, N231, N177, N226, N146, N281, N282, N130, N217, N232 · stated at SPEC J.1, CLAUDE.md, BACKLOG:480-482, changelog :10-23
- **Provenance**: O — "outside this project's scope (owner, 2026-09-24)"; "prototypical ... (owner, 2026-09-11)"; OR5.a (3)
- **Truth**: holds — no `src/`/test file reads `arduino/`; N177's per-change C re-verification is deferred to the reconciliation by these rows
- **Fit**: restates OR5.a (3) · process
- **Home**: CLAUDE.md, SPEC J.1, BACKLOG → unchanged

### G6.006 Changelog stays until the C reconciliation
- **Req**: `UART_C_PORT_CHANGELOG.md` stays in the repo past the audit and is deleted only once the C side is reconciled.
- **Class**: docs
- **Sources**: UART.N214, N223, N230 · stated at CLAUDE.md, README.md:694-699, changelog :3-4
- **Provenance**: O — OR5.a (1) "`UART_C_PORT_CHANGELOG.md` stays until the C side is reconciled"; OR11.a (1)
- **Truth**: holds — file present (128 lines)
- **Fit**: restates OR5.a (1), OR11.a · process
- **Home**: CLAUDE.md → unchanged; the "Temporary file" wording (changelog :3) reads as a stray-file candidate and should say "kept until reconciliation"

### G6.007 UART parameters agreed out of band
- **Req**: `payload_size`, `timeout` and baud are fixed by out-of-band agreement and never negotiated, now or at reconciliation; a mismatched pair is diagnosed (`errno` 32 naming CRC, baud and `payload_size`), never recovered.
- **Class**: product
- **Sources**: UART.N034, N112, N128, N131, N134, N196, N218, N256 · stated at CLAUDE.md, SPEC J.6, changelog B18
- **Provenance**: O — CLAUDE.md "owner decision, 2026-09-11: no version or capability negotiation is to be added"
- **Truth**: holds — `_ERR_LINK_UNINTELLIGIBLE = 32` (`asy_uart_comm.py:129`), `uart_crossover_recovery.py:139-141` pins it on silicon
- **Fit**: new (UART-specific) · pillar P2
- **Home**: CLAUDE.md + SPEC J.6 → SPEC J.6, CLAUDE.md pointing

### G6.008 errno 32 blind spot accepted
- **Req**: A speak-when-spoken-to peer with mismatched parameters is accepted to present as repeated `errno` 22 plus `wrnno` 10, not `errno` 32; the test pins current behaviour and inverts if it is ever changed.
- **Class**: product
- **Sources**: UART.N113, N197, N198, N262, N277 · stated at SPEC J.6, changelog B26, `tests/test_uart_comm_hazard.py:854-856`
- **Provenance**: O-confirmed — "accepted rather than fixed (owner decision, 2026-09-12)" (SPEC J.6; commit dc970fb)
- **Truth**: holds — pinning test present
- **Fit**: new (accepted boundary) · pillar P2
- **Home**: SPEC J.6 → unchanged

### G6.009 Strict initiator/responder, no arbitration
- **Req**: One side is constructed initiator, the other responder; the role is enforced structurally (initiation entry points refuse on a responder, only a responder exposes a listen task, the responder's GET answer uses the internal path). Simultaneous initiation is out of contract: detected and logged as the peer's violation (`errno` 31), never arbitrated.
- **Class**: product
- **Sources**: UART.N039, N058, N080, N109, N178, N179, N219, N250 · stated at CLAUDE.md, SPEC J.2, changelog B8
- **Provenance**: O-confirmed — CLAUDE.md hard rule; Part J confirmed by owner 2026-09-11
- **Truth**: holds — `get_task_starters()` returns `[]` for an initiator (`asy_uart_comm.py:1128-1133`), `_gate()` role check
- **Fit**: new (UART-specific) · pillar P2
- **Home**: CLAUDE.md + SPEC J.2 → SPEC J.2

### G6.010 UART module standalone, BSEC constrains nothing
- **Req**: `asy_uart_comm.py` is a standalone message transport with no application semantics; the BME688/BSEC use case constrains nothing, though a faithful BSEC port must be expressible (and needs `rxbuf` above the per-poll floor, the legacy 32 is refused).
- **Class**: product
- **Sources**: UART.N103, N171, N172, N173, N229, N279 · stated at CLAUDE.md, SPEC J.1, BACKLOG:838-846
- **Provenance**: O — CLAUDE.md "standalone/self-contained"; BACKLOG "owner-confirmed this stays as-is"
- **Truth**: holds — two conformance tests in `tests/test_asy_uart_comm.py` (SPEC J.1)
- **Fit**: new · pillar P10
- **Home**: CLAUDE.md + SPEC J.1 → SPEC J.1

### G6.011 UART classes are plain classes
- **Req**: `UART_Comm` and `UartLinkExerciser` are plain classes, never `SensorReader`/`SensorReaderConfig` subclasses; one merged class per point-to-point wrapper (not a session+protocol pair); per-instance names allow several instances per device.
- **Class**: code-style
- **Sources**: UART.N062, N209, N156, N252 · stated at SPEC J.9, C.3.2, `asy_uart_link_driver.py:4-5`, changelog B11
- **Provenance**: O-confirmed — "owner-delegated decision, 2026-09-11, resolved against precedent" (SPEC J.9; requirements §3 item 2)
- **Truth**: holds — resolved through `buildgen/driver_registry.py` `_OVERRIDES`, not a reader base
- **Fit**: new · pillar P4
- **Home**: SPEC J.9/C.3.2 → unchanged

### G6.012 UID space never contains 0xFF
- **Req**: The UID counter runs `0 … 0xFE` and wraps to 0, never emitting `0xFF`; any code predicting the next UID uses the one controlled wrap, never a bare `+1`; CMD is validated by exact match. Do not "fix" the wrap to `0xFF`.
- **Class**: product
- **Sources**: UART.N005, N015, N027, N084, N185, N186, N187, N240, N269 · stated at SPEC J.3, `asy_uart_comm.py:81, 160-164`, changelog A1/A8
- **Provenance**: O-confirmed — "author-confirmed rationale, 2026-09-11" (SPEC J.3)
- **Truth**: holds — `_UID_MAX = const(0xFE)`, `_next_uid()` (`:160-164`)
- **Fit**: new (wire contract) · pillar P2
- **Home**: SPEC J.3 → unchanged

### G6.013 Fixed-size frames and chunk-train rules
- **Req**: Every frame is exactly `5 + payload_size` bytes, the payload zero-filled from a preallocated zero buffer (no remnant of an earlier frame is ever transmitted); stop-and-wait per frame, no windowing; SET trains carry `ceil(len/payload_size)+1` chunks floored at 2, a GET is one chunk, a streamed send declares its total up front; every received frame is validated for exact CMD, CHUNKS constancy, UID and per-position SIZE. The 43.6 % asymptotic wire efficiency is accepted for the intended traffic.
- **Class**: product
- **Sources**: UART.N026, N028, N048, N079, N096, N180, N188, N189, N190, N200, N208, N235, N236, N237, N245 · stated at SPEC J.3/J.4/J.6/J.8, changelog A1-A3/A12
- **Provenance**: O-confirmed — Part J confirmed 2026-09-11; padding rule A (added `4b85426` while retiring the promotion requirements file)
- **Truth**: holds — `_allocate()` zero buffer (`:273-298`); the receiver does not enforce GET `CHUNKS=1` (N190, sender-side rule only); the hazard tests copy the wire constants independently (G6.087)
- **Fit**: new (wire contract) · pillar P2
- **Home**: SPEC J.3-J.4 → unchanged

### G6.014 Rejection by withheld ACK, no NAK
- **Req**: A receiver rejects by withholding the ACK, checks size before copying (a desynced or hostile peer cannot overrun), and defers the final ACK until the total-size check passes; there is no NAK, so a declined command costs the peer a timeout plus resync.
- **Class**: product
- **Sources**: UART.N046, N052, N066, N086, N191 · stated at SPEC J.4, `asy_uart_comm.py:765, 784, 1026-1027`
- **Provenance**: O-confirmed — Part J confirmed 2026-09-11
- **Truth**: holds — checks at `:765`/`:784`; NAK exists only as proposal A9 (open item)
- **Fit**: new · pillar P2
- **Home**: SPEC J.4 → unchanged

### G6.015 Quiesce-and-resync recovery, never retransmission
- **Req**: On any fault, local mid-train aborts included, the noticing side drains until the line is quiet for `1.5 × timeout`, then holds off initiating for a further `1.5 × timeout`; both constants are Class A. Every reported fault resyncs through the one `_fault()` helper; the drain is bounded at a multiple of the quiet window, so it is never shorter than the peer's drain; a half-received frame is abandoned after `timeout` (anti-desync).
- **Class**: product
- **Sources**: UART.N006, N007, N045, N089, N102, N192, N193, N238, N239, N265, N266 · stated at SPEC J.5, changelog A4/A5/B29/B30, `asy_uart_comm.py:84-92`
- **Provenance**: O-confirmed — Part J confirmed 2026-09-11; mid-train-abort half A (fixed 2026-09-13)
- **Truth**: holds — `_RESYNC_NUM/_DEN = 3/2`, `_DRAIN_BOUND_MULT = 4` (`:86-92`), 32 `await self._fault(` sites
- **Fit**: new (wire contract) · pillar P3
- **Home**: SPEC J.5 → unchanged

### G6.016 Write hold-off gates initiation only
- **Req**: The write hold-off gates only initiating transmissions and never ACKs (both sides gating ACKs would stall a healthy link); it waits out its deadline rather than refusing, so no caller is told "failed" about a link that is merely quiescing.
- **Class**: product
- **Sources**: UART.N003, N029, N085, N194 · stated at SPEC J.5, `asy_uart_comm.py:5-6, 507-525`
- **Provenance**: A — introduced `c63ef97` (UART promotion), backed by Part J's owner confirmation
- **Truth**: holds — `_send_ack()` never calls `_await_write_gate()` (`:507-514`)
- **Fit**: new · pillar P3
- **Home**: SPEC J.5 → unchanged

### G6.017 UART modules use no machine.Timer
- **Req**: `asy_uart_comm.py` and `asy_uart_link_driver.py` create no `machine.Timer`; waits are `ticks_ms()` deadlines, because a soft Timer callback can be dropped and a deadline cannot.
- **Class**: product
- **Sources**: UART.N030, N059, N069, N081, N248 · stated at `asy_uart_comm.py:517-519, 1136`, changelog B2
- **Provenance**: A — `c63ef97` ("removes the legacy dropped-soft-callback hang"); F — SPEC F.1 soft-callback drop
- **Truth**: holds — `tests/test_asy_uart_comm.py:398` `test_the_module_constructs_no_timer_at_all`; link driver `get_timer_starters()` empty (`:142-143`)
- **Fit**: refines OR31.a (never feed or rely on Timer callbacks) · pillar P3
- **Home**: code + changelog → SPEC J.5 (one line)

### G6.018 Stored ticks deadlines respect the ticks_diff horizon
- **Req**: A stored `ticks_ms()` deadline is compared only within `ticks_diff()`'s `2**29` ms horizon or cleared before it can age past it; a flag that keeps a deadline alive until some later event must also expire by elapsed time.
- **Class**: platform
- **Sources**: UART.N031, N050 · stated at SPEC F.1 (3531-3533), `asy_uart_comm.py:203, 520-529, 988-990`
- **Provenance**: F — SPEC F.1 ticks period `2**30` ms on rp2 (unverified here at source line); norm A
- **Truth**: partly — `_holdoff_active` is cleared only by a write reaching `_await_write_gate()` (`:523`) or a listen (`:990`); an initiator idle for over ~6.2 days after a resync compares a stale deadline, which `ticks_diff` can read as up to `2**29` ms in the future. SPEC F.1 claims "every real use in `src/` is a short bounded timeout" (drifted); defect candidate
- **Fit**: extends P1 (counter wrap) · pillar P1
- **Home**: SPEC F.1 → unchanged, corrected

### G6.019 UART frame path allocates nothing
- **Req**: Steady-state frame traffic allocates nothing: two long-lived frame buffers, a preallocated ACK, zero and command scratch per instance; one allocation per logical message; drains use `readinto` into the RX scratch; construction is refused unless every buffer allocated; an allocation failure degrades to the failure sentinel plus resync. A responder whose `set_callback` answers "don't care" lets the peer size one allocation (up to ~64 kB at `payload_size` 255), accepted because it is caught and degraded; new responders declare their size.
- **Class**: product
- **Sources**: UART.N014, N091, N205, N206, N207, N227 · stated at SPEC J.8/J.9, `asy_uart_comm.py:143-145, 262-306`, BACKLOG:698-705
- **Provenance**: O-confirmed — requirements §3 item 8 (owner, 2026-09-11); N227 A ("deliberately left", audit pass 2026-09-11)
- **Truth**: holds — `_allocate()`/`_buffers_ready()` (`:273-310`); `_drain()` uses `device.readinto` (`:557`); per-transaction retained bytes pinned by `tests/test_uart_comm_hazard.py`
- **Fit**: refines OR26.a (MemoryError with a real alternative flow) · pillar P6
- **Home**: SPEC J.8 → unchanged

### G6.020 Lost final ACK folds into failure
- **Req**: No frame is ever re-sent. A lost final ACK, or a post-ACK right-sizing `MemoryError`, is reported as failure on one side while the other side delivered; a caller that retries a failed `uart_set()` must tolerate the peer seeing the message twice.
- **Class**: product
- **Sources**: UART.N053, N087, N088, N099, N114, N212 · stated at SPEC J.9, `tests/test_uart_comm_hazard.py:1108-1110`
- **Provenance**: O-confirmed — requirements §3 item 10 "Lost final ACK folds into failure" (owner, 2026-09-11)
- **Truth**: holds — `:1078-1080`; idempotency is a caller obligation nothing enforces (the only caller, the exerciser, only issues GETs)
- **Fit**: new (accepted boundary) · pillar P2
- **Home**: SPEC J.9 → unchanged

### G6.021 UART result shapes: None is failure
- **Req**: `None` means failure and a zero-length result means a genuinely empty payload, at every result shape, and the two never collapse; `uart_listen()` returns a `ListenResult(cmd_id, cmd, payload)` on every path; `deliver`/`message_callback` are optional for a GET-only responder.
- **Class**: product
- **Sources**: UART.N049, N094, N098, N210, N211, N247, N253, N259 · stated at SPEC J.9, changelog B1/B13/B21
- **Provenance**: O-confirmed — requirements §3 items 8-9 (owner, 2026-09-11)
- **Truth**: holds — `:1068-1075`; `_LISTEN_FAILED` preallocated (`:147`)
- **Fit**: new · pillar P2
- **Home**: SPEC J.9 → unchanged

### G6.022 UART fault-episode logging budget
- **Req**: A UART fault episode persists its `errno` once and exactly one warning (`wrnno` 11 outranks 10, 14 at most once per command id until a reset); the closing `wrnno` 12 is persisted only if the fault repeated; a boot drain persists nothing; `wrnno` 13 fires only on a rise of the driver's cumulative cancel counter.
- **Class**: product
- **Sources**: UART.N023, N024, N032, N036, N038, N055, N077, N082, N090, N093, N101, N158, N159, N195, N254, N258, N264, N268, N278 · stated at SPEC C.7.1 (UART row), J.5, `asy_uart_comm.py:312-341`, changelog B16/B20/B28/B32
- **Provenance**: O for W11-over-W10 ("owner decision, 2026-09-18", SPEC C.7.1); A for the rest (`c63ef97`, "exactly two persisted entries per episode")
- **Truth**: drifted — C.7.1's repeat rule (SPEC:1917-1920) says repeats are passed `repeat=True`, "which still counts it and still writes the updated count through to FRAM", but UART repeats go to print-only `pr.err()`/`pr.wrn()` (`:321, :339`), neither counted nor written; C.7.1 says "three modules dedupe", six use `repeat=` or an episode flag; comments `:315-317` ("exactly two entries") and `:326-328` ("a single transient leaves one entry") disagree
- **Fit**: conflicts OR35.b (one central rule in `print_log.py`, no per-module episode cases; see Conflicts) · pillar P7
- **Home**: SPEC C.7.1 → rewritten to the OR35.b central rule; UART-specific remainder in J.5

### G6.023 UART reset is total and allocation-free
- **Req**: `reset_error_counter()` clears everything the module counts (history, streak, episode state, declined-id map) in place without allocating; `UartLinkExerciser` holds no error state of its own and forwards counter and reset to its inner `UART_Comm`, also clearing its transfer/failure counters.
- **Class**: product
- **Sources**: UART.N060, N070, N104 · stated at `asy_uart_comm.py:1141-1149`, `asy_uart_link_driver.py:150-160`
- **Provenance**: A — `c63ef97`; forwarding pinned by `tests/test_asy_uart_link_driver.py:248-249`
- **Truth**: holds — code as stated
- **Fit**: refines CLAUDE.md FRAM rule (ResetErrors) · pillar P7
- **Home**: code → unchanged

### G6.024 UART code never blocks the asyncio loop
- **Req**: `asy_uart_driver.py` and `asy_uart_comm.py` never wait synchronously, not even in a wait state: every read is clamped to `uart.any()` and `ready()` yields between rounds. The one unbounded wait is the responder's idle listen, which polls at the slower `poll_idle_ms`, kept well under the peer's timeout.
- **Class**: product
- **Sources**: UART.N051, N132, N135, N136, N137, N138, N157, N163, N165 · stated at CLAUDE.md hard rule, SPEC F.5.8/F.5.9
- **Provenance**: O — "project owner, 2026-09-11" (CLAUDE.md, SPEC F.5.8)
- **Truth**: holds — `ready()` idle rate `asy_uart_driver.py:267`; shipped-driver silicon script `tests_hardware/device_scripts/uart_driver_read_never_blocks_the_loop.py` exists (closed 2026-09-25); SPEC F.5.9 notes `_write_all`'s `POLLOUT` wait also runs at the idle rate (BUS.S05)
- **Fit**: refines F.3 / P6 · pillar P6
- **Home**: CLAUDE.md + SPEC F.5.8 → SPEC F.5.8, CLAUDE.md pointing

### G6.025 UART timeout and poll floors
- **Req**: A `UART` driving the protocol uses a single-digit `poll_wait_ms`; `timeout` is refused at construction below `2 × poll_wait_ms + poll_idle_ms +` the measured worst-case GC pause, and buildgen refuses the same device config at build time with the same arithmetic read from source.
- **Class**: product
- **Sources**: UART.N020, N021, N075, N148, N164, N166, N201, N202, N203, N263 · stated at SPEC J.6, C.7.2, `asy_uart_comm.py:245-260`, `buildgen/validate.py:256-276`
- **Provenance**: A — `c63ef97`/changelog B27; build-time half follows owner's C.7.2 scope (2026-09-24)
- **Truth**: partly — timeout floor enforced at `asy_uart_comm.py:245-246` and `validate.py:270-273`; the single-digit `poll_wait_ms` rule is enforced nowhere (driver default stays 20 ms, `asy_uart_driver.py:56`; only `dev.toml:47,56` honor it)
- **Fit**: extends OR23.a (3) (build aborts with what/where/fix) · pillar P2
- **Home**: SPEC J.6 → unchanged

### G6.026 payload_size and rxbuf refused, never clamped
- **Req**: `payload_size` must be `1 … 255` and identical on both ends, and `rxbuf` must hold one whole framed frame and one poll interval's arrivals; an out-of-range value is a readiness-gate refusal with its own `errno`, never a silent clamp.
- **Class**: product
- **Sources**: UART.N019, N073, N076, N199, N204, N246, N255 · stated at SPEC J.6, changelog A6/B17, `asy_uart_comm.py:231-232, 262-270`
- **Provenance**: A — `c63ef97` ("never clamped: a clamped payload_size turns a loud configuration error into a link that desyncs")
- **Truth**: holds — `_validate_config()` (`:230-252`), `_min_rxbuf()`; build-time mirror `validate.py:274-276`
- **Fit**: refines C.13 readiness gate · pillar P2
- **Home**: SPEC J.6 → unchanged

### G6.027 Sync allocate-only constructor, persisted at setup
- **Req**: A module's `__init__` only allocates and validates; a construction refusal is printed at once and persisted by `setup()`, which also calls the logger's own `setup()` first; validation stops at the first finding. A driver `init()` whose remaining raise surface is a controlled path documents that its callers handle it. Caller arguments are validated before use (a non-integer or negative size, an out-of-byte id, a non-buffer).
- **Class**: code-style
- **Sources**: UART.N017, N018, N054, N260, N271 · stated at SPEC C.13, `asy_uart_comm.py:224-226, 1085`, changelog B19/B22, commit 9e418ed
- **Provenance**: A — `c63ef97`, `9e418ed`
- **Truth**: holds — `setup()` `:1084-1087`
- **Fit**: restates C.13; refines OR26.a (construction completeness) · pillar P2
- **Home**: SPEC C.13 → unchanged

### G6.028 Validate before storing into bytes
- **Req**: Code writing into a `bytearray` or through `struct.pack()` validates value range and slice length first, because MicroPython truncates an index store to 8 bits silently and resizes a `bytearray` on a length-mismatched slice assignment instead of raising.
- **Class**: platform
- **Sources**: UART.N257, N261, N273 · stated at changelog B19/B23, SPEC F.1, commit 8850974
- **Provenance**: F — `py/objarray.c:505-540` (slice `len_adj` grow/shrink) at v1.29.0; CLAUDE.md platform list
- **Truth**: holds — resize path confirmed in source; `asy_uart_comm.py` `_buffers_ready()` and header writes range-check
- **Fit**: restates CLAUDE.md Part F pointer · pillar P2
- **Home**: SPEC F.1 → unchanged

### G6.029 Reentry refused and unsticking from outside
- **Req**: A reentrant call on a `UART_Comm` is refused with a sentinel (the session lock is not reentrant), the busy flag is cleared in `finally`; `clear()` is the only safe way to free a parked listener and cancels the in-flight read before taking the lock; a caller's buffer is borrowed for the call only. `setup()` is never called twice while the listen loop runs.
- **Class**: product
- **Sources**: UART.N016, N037, N047, N092, N095, N110, N133, N228 · stated at SPEC J.5, `asy_uart_comm.py:200, 613-615, 815-816`, BACKLOG:711-716
- **Provenance**: A — `c63ef97`; "no guard invented" for double `setup()` (BACKLOG, audit pass 2026-09-11)
- **Truth**: holds — supervisor re-calls starters, never `setup()` (BACKLOG re-check 2026-09-12)
- **Fit**: refines OR41.a (concurrency) · pillar P2
- **Home**: SPEC J.5 → unchanged

### G6.030 UART hazard tests run in both CRC modes
- **Req**: Every UART hazard check is registered both without a CRC and with CRC16, except the two mode-specific payload-corruption checks.
- **Class**: test-standard
- **Sources**: UART.N106, N161 · stated at `tests/test_uart_comm_hazard.py:48-50`, SPEC E.8
- **Provenance**: O-confirmed — "Standing rule (project owner, 2026-09-12)" (added `dc970fb`)
- **Truth**: holds — `_register_both_crc_modes()` (`tests/test_uart_comm_hazard.py:1280-1298`)
- **Fit**: refines OR19/OR41 · pillar P5
- **Home**: test file + SPEC E.8 → SPEC E.8

### G6.031 dev link runs without CRC until reconciliation
- **Req**: The deployed `dev` link runs `CRC_Pass` (no CRC on the wire) as the supported interim; payload corruption is then undetected and accepted. The CRC algorithm and byte order are specified, never inherited: `src/crc_checks.py`'s MSB-first big-endian CRC16 is not wire-compatible with the legacy/C native-order variant, and selecting it is changelog A7's flag day. A CRC instance is never shared between link ends.
- **Class**: product
- **Sources**: UART.N111, N115, N116, N181, N183, N184, N241, N272 · stated at SPEC J.3, changelog A7, commits 5f76c4e/6a2d43e
- **Provenance**: O-confirmed — commit 6a2d43e "A7 owner-decided (Python leads; CRC_Pass is the supported interim)"
- **Truth**: holds — codegen passes no `crc=`; test pins big-endian (`tests/test_uart_comm_hazard.py:1223-1225`)
- **Fit**: new (accepted boundary) · pillar P2
- **Home**: SPEC J.3 → unchanged

### G6.032 Twin UART and network fakes match silicon
- **Req**: The twin's UART and network fakes model the rp2/CYW43 behaviour the code relies on (crossover topology, `any()` draining the FIFO, overrun counted not raised, re-construction superseding an instance, radio byte bounds), and every fact they leave unmodelled (baud mismatch, TX backpressure, the `timeout_char` stall, lwIP, repeated flapping) is listed in the twin fidelity table with its evidence.
- **Class**: test-standard
- **Sources**: UART.N119, N120, N121, N122, N123, N124, N125, N126, N153; NET.N193, N195, N198 · stated at `digital_twin/machine.py:436-750`, `digital_twin/_unix_port_udp_addr_shim.py`, `digital_twin/README.md`
- **Provenance**: O — OR17/OR17.a
- **Truth**: unverifiable here — silicon half needs the board; `rp2 machine_uart.c` read for re-init only (G6.034); `run_generic_integration.py` reaches private `_uart`/`poller` (N125)
- **Fit**: restates OR17.a · pillar P5
- **Home**: code comments → `digital_twin/README.md` fidelity table (OR17.a (2))

### G6.033 Nothing in src/ exists for a test
- **Req**: No parameter, non-`const()` value, public attribute or code path exists in shipped code only so a test, the twin or a hardware script can reach it; tests adapt from outside, and a test that would need a `src/` change alone is dropped or redesigned.
- **Class**: code-style
- **Sources**: UART.N140, N144, N225; NET.N006, N018, N142 · stated at `asy_dns_client.py:21`, `asy_ntp_client.py:44-45`, `asy_udp_socket.py:48`, tests_hardware/README.md:1265-1269, BACKLOG:92-94
- **Provenance**: O — OR36/OR36.a; "Scratched (owner, 2026-09-25): ... changes `src/` for the test alone"
- **Truth**: drifted — seams remain: `_FALLBACK_DNS_SERVERS` non-const "so tests can monkeypatch it" (`asy_dns_client.py:20-21`), `_NTP_UDP_PORT` (`asy_ntp_client.py:44`), `self.uart` public for the twin (`asy_uart_link_driver.py:57`), `AsyUDPSocket` accepting a non-tuple addr for the test host (N142)
- **Fit**: restates OR36.a (1) · pillar P4
- **Home**: → SPEC E.9 / principles Part

### G6.034 UART re-init constructs a fresh machine.UART
- **Req**: `asy_uart_driver.UART.init()` constructs a new `machine.UART(...)` and never calls `self._uart.init(...)`, because on rp2 `deinit()` unroots the RX/TX ring buffers and a same-size `init()` does not re-root them.
- **Class**: platform
- **Sources**: UART.N162 · stated at SPEC F.5.7, `src/asy_uart_driver.py:199-207`
- **Provenance**: F — `ports/rp2/machine_uart.c:436-438, 471, 492` at v1.29.0
- **Truth**: holds — source confirmed; guarded only by a comment and the silicon injector, no unit test names it
- **Fit**: restates Part F · pillar P2
- **Home**: SPEC F.5.7 → unchanged

### G6.035 UART fault injection stays mock-only
- **Req**: The ~20-scenario UART fault-injection catalog is an E.6.6 structural exception and stays mock-only until injection hardware exists; it is not improvised with a second raw UART.
- **Class**: test-standard
- **Sources**: UART.N141, N160 · stated at SPEC E.6.6, tests_hardware/README.md:1276-1279
- **Provenance**: O — "Answered 2026-09-22 (owner): it is a structural exception, for now"
- **Truth**: holds
- **Fit**: refines OR45.a (3) (listed exception, reviewed at audit close) · pillar P5
- **Home**: SPEC E.6.6 → unchanged

### G6.036 No UART hardware flow control
- **Req**: `asy_uart_driver.py` exposes no RTS/CTS hardware flow control, with no plan to revisit.
- **Class**: product
- **Sources**: UART.N270 · stated at SPEC:279-280, commit c9006dc
- **Provenance**: O-confirmed — c9006dc "record owner decision ... Confirmed directly"
- **Truth**: holds — no flow parameters in the driver
- **Fit**: new · pillar P4
- **Home**: SPEC A.4 list → unchanged

### G6.037 dev carries the UART pair, wozi none
- **Req**: `dev` wires exactly one initiator and one responder `uart_link`, UART0 on GP0/GP1 and UART1 on GP8/GP9 across the permanent jumper, each with its own FRAM-backed history; `wozi` never gets a UART instance. Only the initiator reports transfer/failure counts, as a maintenance sensor. Its command ids are bench application semantics. GPIO16/17 stays free for a future BSEC UART0.
- **Class**: hardware-safety
- **Sources**: UART.N064, N068, N118, N147, N149, N150, N154, N155, N220, N267, N275 · stated at CLAUDE.md, SPEC A.3/A.7/J intro, `devices/dev.toml:38-57,150-172`, `buildgen/codegen.py:631-633`
- **Provenance**: O — CLAUDE.md hard rules (wozi never flashed); changelog B31 reverses an earlier agent "never"
- **Truth**: holds — `dev.toml` two `uart_link` entries; `wozi.toml` none
- **Fit**: restates CLAUDE.md wozi/dev rules · pillar P10
- **Home**: CLAUDE.md + SPEC A.7 → SPEC A.7

### G6.038 Frame layer order is fixed
- **Req**: Write order is build → CRC → encode → delimiter and read order the exact reverse; a pass-through codec emits byte for byte what the driver emitted before; skipping empty frames on the wire is the read loop's job, not the codec's.
- **Class**: product
- **Sources**: UART.N061, N105, N167 · stated at SPEC G.2, `asy_uart_driver.py:77-79`, `tests/test_framing_codecs.py:157-158`
- **Provenance**: O-confirmed — requirements §3 item 11 "CRC/framing ownership" (owner, 2026-09-11)
- **Truth**: holds — not re-traced line by line (quote matches)
- **Fit**: new · pillar P4
- **Home**: SPEC G.2 → unchanged

### G6.039 typing import fallback idiom
- **Req**: A `src/` module that needs `typing` imports it under `try`/`except ImportError` with a `TYPE_CHECKING = False` fallback, since `typing` has no runtime presence on MicroPython.
- **Class**: code-style
- **Sources**: UART.N004, N063; NET.N046, N061, N113; REST.N003 · stated at each module's import block
- **Provenance**: C — 22 of 28 `src/*.py` carry the identical comment; no `src/` file uses `TYPE_CHECKING` without it
- **Truth**: holds
- **Fit**: restates OR24 (one material) · pillar P4
- **Home**: code → SPEC G.2 catalog entry

### G6.040 Suppressions in shipped code are coded and reasoned
- **Req**: Every `# type: ignore`/`# noqa` in shipped code names its error code and carries a one-line reason (a verified stub defect, an unannotated vendored import, a MicroPython builtin limit such as `zip()` rejecting `strict=`); a stub defect that can be repaired at install is repaired rather than suppressed.
- **Class**: code-style
- **Sources**: UART.N040, N042, N043, N044; NET.N039, N048, N052, N075, N100, N102, N228; REST.N002, N028, N049 · stated at CLAUDE.md typecheck bullets, SPEC F.5.5
- **Provenance**: C — 21 `type: ignore` in 12 `src/` files, all coded; ruff `select=["ALL"]` (PGH) and mypy `strict` (`warn_unused_ignores`) enforce codes and liveness
- **Truth**: drifted — SPEC F.5.5 (3833) calls `asy_udp_socket.py`'s `recvfrom()` "the one place a `type: ignore` *is* right", while 20 others exist, two in the same file (`:48`, `:136`); vendored `ext/microdot.py`'s own suppressions stay untouched (G6.060)
- **Fit**: extends OR50.a (1) (suppressions re-checked on trial) · pillar P4
- **Home**: CLAUDE.md + F.5.5 → SPEC D checklist

### G6.041 Broad catches only at boundaries, always logged
- **Req**: A broad `except Exception` in `src/` appears only around caller-supplied callbacks, an unsupervised task's top, or a platform call with an undocumented raise surface; every one logs (persisted for attempts and callbacks, print-only for routine observations) and none is a silent `pass`.
- **Class**: code-style
- **Sources**: UART.N041; NET.N028, N033, N076, N079, N086, N089; REST.N030, N031, N032, N033, N040, N045 · stated at inline comments ("caller-supplied callback, could legitimately misbehave")
- **Provenance**: C — 119 `except Exception` in `src/`
- **Truth**: partly — two do not log: `config_manager.py:185-186` (`pass`) and `asy_webserver_service.py:790-791` (JSON parse → code 1 reply, not silent to the client)
- **Fit**: refines OR26.a (swallowing excepts) · pillar P2
- **Home**: → SPEC C.7 / D.2

### G6.042 Observation failures degrade silently, attempts persist
- **Req**: A WiFi "attempt" failure persists an `errno` and sets `hw_op_failed`; a routine state observation (status, isconnected, ifconfig, stations, LED) degrades print-only to a safe sentinel. Such observation failures therefore leave no FRAM evidence.
- **Class**: product
- **Sources**: NET.N060, N071, N072, N074, N090, N098, N164, N188 · stated at `src/asy_wifi_service.py:4-6, 204-210`
- **Provenance**: A — introduced with the WiFi exception audit (`asy_wifi_service.py` docstring)
- **Truth**: holds — pinned by `tests/test_asy_wifi_service.py:1004-1005`
- **Fit**: tension with OR18/OR26 (a failure class invisible to FRAM forensics, P7) · pillar P2
- **Home**: module docstring → SPEC C.7

### G6.043 UDP socket and DNS never raise; callers log disconnect
- **Req**: `AsyUDPSocket` I/O, `resolve_ipv4()` and the captive DNS return their sentinels and never raise (`AsyUDPSocket.__init__` excepted); `AsyUDPSocket` owns no logger, so every caller checks `disconnect()`'s bool and logs a failure, leaving a trail for socket or poll-slot leaks.
- **Class**: product
- **Sources**: NET.N002, N012, N045, N049, N051, N053, N054, N055, N058, N121, N149, N182 · stated at `src/asy_udp_socket.py:8, 204-206`, SPEC C.7 bool rule
- **Provenance**: A — `a6abe13` (resolver) and the UDP exception audit; C.7 bool rule
- **Truth**: partly — only `captive_dns.py:139-153` checks and logs; `asy_ntp_client.py:188` and `asy_dns_client.py:120` discard the result, and NTP's fetch has no `finally` around `disconnect()` (N030, a cancel mid-fetch skips it); C.7.1 row "every failure surfaces to exactly one upstream owner" does not hold for disconnect
- **Fit**: refines OR38.a (1) (no leaked sockets) · pillar P1
- **Home**: module docstring → SPEC C.7

### G6.044 NTP never ends its task on network failure
- **Req**: An unreachable NTP server is routine: the task keeps no failure streak and never ends to be restarted; while unsynced it retries on a backoff from `retry_s` (10 s) doubling to `retry_max_s` (600 s), rounded up to the 10 s tick and checked by buildgen as a pair; a synced device's failed resync has its own short retry loop.
- **Class**: product
- **Sources**: NET.N024, N025, N029, N140, N141, N215, N216, N220, N258 · stated at SPEC C.7.2, `asy_ntp_client.py:101-128`, `buildgen/validate.py:231-243`
- **Provenance**: O — "the NTP task used to end after six failed syncs ... (owner, 2026-09-24)" (SPEC C.7.2)
- **Truth**: holds — constructor passes streak 0 (`:115`); 50-attempt test; confirmed on silicon 2026-09-25 (one `E21`, no task ended)
- **Fit**: restates OR18 · pillar P2
- **Home**: SPEC C.7.2 → unchanged

### G6.045 Log repeats spend no new slot
- **Req**: A condition that fails on every attempt must not refill a ten-slot error ring: repeats are counted and written through but spend no slot. Today six modules do this with their own episode flags while NTP 16/17/18, captive DNS `wrnno` 1/2, the webserver's reclaim warnings and the WiFi byte-bound refusal persist per occurrence.
- **Class**: product
- **Sources**: NET.N035, N036, N043, N096, N097, N118, N168, N187; REST.N014 · stated at SPEC C.7.1 repeat rule, `asy_ntp_client.py:295-304`, `asy_wifi_service.py:624-630`
- **Provenance**: O — OR35/OR35.b (central rule); per-module flags A
- **Truth**: partly — NTP `errno` 18 persists on every 10 s tick while its config read fails (`:450-453`); captive DNS `wrn_s` without `repeat=` (`:119, :123`); C.7.1 says "three modules dedupe", six do
- **Fit**: restates OR35.b · pillar P7
- **Home**: SPEC C.7.1 → rewritten to the central `print_log.py` rule

### G6.046 wifi_mode_lock contract
- **Req**: `wifi_mode_lock` protects WLAN state: the NTP task looks up the DNS server before acquiring it; `network_available()` requires the caller to hold it while every other getter checks `.locked()` and degrades to a "don't know yet" sentinel; the lock is acquired unbounded (no timeout), accepting the 60 s STA-retry stall as a priority-inversion cost; double releases are absorbed by one helper.
- **Class**: product
- **Sources**: NET.N026, N027, N042, N077, N087, N092, N104, N105, N139, N161, N165, N184, N185, N222, N223, N237, N246 · stated at SPEC C.8, `asy_wifi_service.py:735-780`, `asy_ntp_client.py:123-153, 431-440`
- **Provenance**: O-confirmed for unbounded acquire — commit 5ddbcd3 "reverted per direction"; A for the rest (C.8 "left as-is")
- **Truth**: holds — `network_available()` comment `:780`; `_safe_get_dns_server()` before acquire; C.8's two lock shapes are a recorded inconsistency (OR24 one material candidate)
- **Fit**: tension OR24 (one shape per job) · pillar P2
- **Home**: SPEC C.8 → unchanged

### G6.047 Unlocked read-modify-write has no await inside
- **Req**: A read-then-write of shared state without a lock contains no `await` between read and write; a function requiring a caller-held lock says so where it is defined.
- **Class**: code-style
- **Sources**: NET.N050, N133 · stated at `tests/test_asy_ntp_client.py:393-394`, `asy_udp_socket.py:105-107`
- **Provenance**: A — test and code comments
- **Truth**: holds for the cited sites — not mechanically enforced
- **Fit**: refines OR41.a (2) · pillar P2
- **Home**: → SPEC C.8

### G6.048 AsyUDPSocket contract
- **Req**: `AsyUDPSocket` is IPv4 `AF_INET`/`SOCK_DGRAM` only and content-agnostic; it validates `mode` eagerly; a datagram longer than the buffer is truncated silently (documented); `(b"", addr)` stays distinct from the `(None, None)` sentinel; `connect`/`disconnect` are serialized by a per-instance lock; its widened excepts never include `CancelledError`; one instance is used from one coroutine at a time.
- **Class**: product
- **Sources**: NET.N143, N145, N146, N147, N150, N151, N153, N154, N155, N232, N239, N148 · stated at `tests/test_asy_udp_socket.py`, BACKLOG #5
- **Provenance**: A — UDP audit (`6438bdc`), confirmed on hardware (BACKLOG #5 closed 2026-09-08)
- **Truth**: holds — POLLERR/POLLHUP branch unreachable on rp2/lwIP (N232, dead-code candidate)
- **Fit**: new · pillar P2
- **Home**: tests → SPEC G.2 entry

### G6.049 Project networking is IPv4-only
- **Req**: The project is IPv4-only end to end: resolver A records, UDP sockets and address tuples; an IPv6-shaped resolver result degrades to "no reply", never a crash.
- **Class**: product
- **Sources**: NET.N127, N134, N144, N194 · stated at `a6abe13`, `tests/test_asy_dns_client.py:45-46`
- **Provenance**: A — `a6abe13` ("this project is IPv4-only")
- **Truth**: holds — `TypeError` from a 4-tuple caught (`tests/test_asy_ntp_client.py:896-900`)
- **Fit**: new · pillar P6
- **Home**: → SPEC A (one line)

### G6.050 DNS resolver is minimal and never raises
- **Req**: `resolve_ipv4()` returns a literal IPv4 host unchanged, otherwise resolves one A record over UDP: 512-byte replies, bare-pointer answer names only, query refused before sending when a label exceeds 63 octets, a placeholder or malformed server skipped, DHCP server first then public fallbacks; any unparseable reply is `None`. Callers pass their real timeout.
- **Class**: product
- **Sources**: NET.N001, N003, N004, N005, N007, N008, N009, N010, N011, N013, N128, N131, N129, N130, N132, N248, N255 · stated at `src/asy_dns_client.py`, PR #41
- **Provenance**: A — `a6abe13` (from-scratch resolver, public fallback added there)
- **Truth**: partly — no 255-octet QNAME check and no empty-label check (`_build_query()`, `asy_dns_client.py:40-51`), reachable through `NTP_Host`'s 1,024 bound; the public fallback `8.8.8.8`/`1.1.1.1` is new vs legacy `getaddrinfo()` (question below)
- **Fit**: refines OR52.a (3) (robust to malformed input) · pillar P2
- **Home**: module docstring → unchanged

### G6.051 NTP_Host keeps its 1,024-character bound
- **Req**: `NTP_Host` keeps its 1,024-character bound (do not re-raise); the bound is mirrored in `_VAL_NH`, the generated definitions and the tests, which change together; it sets the largest legitimate PUT body and the one over-cap response piece.
- **Class**: product
- **Sources**: NET.N229, N230, N235, N236 · stated at BACKLOG:483-488, SPEC I.3, I.6
- **Provenance**: O — "SETTLED, owner, 2026-09-21: 'keep it'. Do not re-raise."
- **Truth**: holds — measured on silicon 2026-09-25 (`/networking` complete at 1,182 B)
- **Fit**: new · pillar P6
- **Home**: BACKLOG → SPEC I.6 (settled facts leave BACKLOG, harmonization 3)

### G6.052 WiFi state machine is intentional
- **Req**: An empty SSID goes straight to the hotspot; a link that was established once never escalates to the hotspot, it retries silently every 60 s; a second failed STA streak after a hotspot phase deactivates WLAN permanently, a terminal state that task restarts keep and only a power cycle clears; repeated hardware failure hands the task to the supervisor.
- **Class**: product
- **Sources**: NET.N062, N069, N078, N080, N081, N088, N109, N110, N158, N166, N171, N202, N203, N206, N213, N245, N249 · stated at SPEC A.4 (269-275), `asy_wifi_service.py:126, 304-323, 469-493`
- **Provenance**: O-confirmed — "Functional behaviors confirmed intentional by the project owner" (SPEC:269-275); commit 8696c1d
- **Truth**: holds — code as stated; a failed `LedWifiOn` config read at task start also deactivates (N080) and a missing hotspot config sets `hotspot_started_once` (N081), both reachable only on a broken config file; stale AP station entry after an unclean reset is a field risk (N206, N249)
- **Fit**: tension P1 (a terminal no-network state needs a person) — resolved by owner's words · pillar P3
- **Home**: SPEC A.4 → principles Part names it as a deliberate exception to P1

### G6.053 CYW43 false positive recovered by power cycle
- **Req**: A WiFi link stuck in a CYW43 `isconnected()` false positive is recovered by a power cycle or `hard_reset()`, a deliberate recovery feature; no independent reachability probe is added.
- **Class**: product
- **Sources**: NET.N169, N186, N207, N225, N226, N231, N233, N244, N250 · stated at CLAUDE.md hard rule, SPEC F.2 (3620-3631), BACKLOG #6
- **Provenance**: O — CLAUDE.md; commit 655e4f9 "the project owner judged a new reachability-probe mechanism's complexity not worth it"
- **Truth**: drifted — `tests_hardware/README.md:635-641` still calls it "a real architectural question for the project owner, not decided here"
- **Fit**: restates CLAUDE.md; refines OR18.a · pillar P3
- **Home**: CLAUDE.md + SPEC F.2 → SPEC F.2, README line removed

### G6.054 wrnno 4 is not proof of a wrong password
- **Req**: WiFi `wrnno` 4 reports "authentication or handshake failed", cyw43's catch-all including an AP dropping mid-association; a `W4` beside a `W5` on one outage is benign and survives dedupe.
- **Class**: platform
- **Sources**: NET.N095, N219, N234 · stated at SPEC C.7.1 (WIFI row), BACKLOG #29
- **Provenance**: F — answered from cyw43 source (commit e1dff54); not re-read here (cyw43-driver not in the local checkout)
- **Truth**: unverifiable here — cyw43-driver submodule absent from the scratchpad checkout
- **Fit**: new · pillar P7
- **Home**: SPEC C.7.1 → unchanged

### G6.055 Radio strings are bounded in bytes
- **Req**: SSID, PW, Country, Hostname and HotspotPW are bounded in UTF-8 bytes at write and at use (SSID ≤ 32, PW 8-63 or "" for an open network, country exactly 2, hostname ≤ 32): a PUT over the byte bound is "Invalid" (`errno` 20), a stored over-bound value runs on its default (`wrnno` 8), and an SSID over 32 bytes never reaches `connect()`. buildgen checks injected defaults; runtime keeps a default rather than failing to boot.
- **Class**: platform
- **Sources**: NET.N063, N064, N066, N067, N091, N101, N103, N126, N157, N174, N176, N196, N197, N201, N214, N221 · stated at SPEC C.7.4
- **Provenance**: F — `extmod/modnetwork.c:140` (country `len != 2`), `:157` (hostname > `MICROPY_PY_NETWORK_HOSTNAME_MAX_LEN` = 32, `modnetwork.h:60`); SSID overflow claim from cyw43 (not re-read)
- **Truth**: holds — confirmed on silicon 2026-09-25 (SPEC C.7.4); HotspotPW is build-time only, no REST group (N174)
- **Fit**: refines OR18 (a config value never treated as a hardware fault) · pillar P2
- **Home**: SPEC C.7.4 → unchanged

### G6.056 GET never discloses the WiFi password
- **Req**: `GET` returns a fixed mask for `PW`, never the stored password, for an open network too.
- **Class**: product
- **Sources**: NET.N159, N217, N218 · stated at SPEC C.4, `asy_wifi_service.py` `_mask_pw`
- **Provenance**: A — setter-dispatch work (`4c555e0` era)
- **Truth**: partly — the mask holds, but a `PUT` of the mask string is stored as a new password (`tests_js/live-backend-put-matrix.test.js:134-137`), defect candidate (NET.S16)
- **Fit**: new · pillar P2
- **Home**: SPEC C.4 → unchanged

### G6.057 Timer arm failure degrades, dropped callbacks self-heal
- **Req**: A `Timer.init()` failing with `OSError(ENOMEM)`/`MemoryError` degrades gracefully and never crashes its caller; periodic timers are preferred where a dropped soft callback must self-heal one period later.
- **Class**: platform
- **Sources**: NET.N037, N084, N085, N099, N167 · stated at SPEC C.9, F.1, `asy_ntp_client.py:330-349`, `asy_wifi_service.py:410-422, 667-669`
- **Provenance**: F — SPEC F.1/CLAUDE.md (`Timer.init()` ENOMEM, soft-callback drop); A for the handling
- **Truth**: partly — NTP's check timer and both uptime counters fail print-only and never retry until a supervisor restart (`asy_ntp_client.py:343-349`, `asy_wifi_service.py:667-669`), silently losing NTP refresh or uptime counting (OR18/OR26 candidate)
- **Fit**: refines OR26.a · pillar P3
- **Home**: SPEC C.9 → unchanged

### G6.058 Captive DNS never raises and never ends
- **Req**: The captive DNS answers every on-subnet query with one A record for the AP's own address (TTL 60 s) and drops malformed, truncated, off-subnet or root queries; because no supervisor restarts it, it catches everything at its task top with a bounded backoff and persists a code; one instance is reused across hotspot activations, relying on `disconnect()` fully resetting the socket; each request's `DNSQuery` shares the `DNSSRV` logger.
- **Class**: product
- **Sources**: NET.N083, N112, N117, N119, N120, N122, N123, N124, N125, N152, N177, N178, N180, N183, N199, N241 · stated at `src/captive_dns.py`, SPEC C.7.1 (DNSSRV row)
- **Provenance**: A — derived from p-doyle's captive portal; hardened in the exception audits
- **Truth**: holds — catches at `:105, :133, :187`; reply-send failure discarded silently (N152); root query never answered (N241, accepted)
- **Fit**: refines OR52.a (3) · pillar P2
- **Home**: module docstring → unchanged

### G6.059 Hotspot redirects unmatched static paths
- **Req**: While the hotspot is active, an unmatched static GET answers `302` to `/` so phones raise the captive-portal sign-in; never for a `..` path or a non-GET, and no bespoke try/except is needed around it.
- **Class**: product
- **Sources**: NET.N059, N204, N205; REST.N088 · stated at SPEC A.5, tests_hardware/README.md:539-573
- **Provenance**: A — verified on hardware 2026-09-03; Android no-prompt case blamed phone-side, unconfirmed (N205)
- **Truth**: holds — `_serve_static()` (`asy_webserver_service.py:649-660`)
- **Fit**: new · pillar P3
- **Home**: SPEC A.5 → unchanged

### G6.060 NTP reply validation and time window
- **Req**: An NTP reply is accepted only with a synchronized leap indicator, a non-zero stratum and a transmit time inside the plausibility window 2025-01-01 … 2100-01-01 after NTP-era reinterpretation (2036 wrap); the window is reviewed on a stated trigger.
- **Class**: product
- **Sources**: NET.N019, N020, N021, N034, N135, N243 · stated at `asy_ntp_client.py:47-58, 247-257`
- **Provenance**: A — commit c7b9431
- **Truth**: partly — no re-check trigger exists ("bump both forward occasionally", `:50-51`); OR30 register entry needed
- **Fit**: refines OR30 · pillar P1
- **Home**: code → OR30 tunables section

### G6.061 Local time follows legacy's CET/CEST rule
- **Req**: `cettime()` applies the EU last-Sunday switch dates to the configured GMT/DST offsets, copied verbatim from deployed legacy; `NTP_Offset_S` shifts the RTC and every stored timestamp.
- **Class**: product
- **Sources**: NET.N015, N022, N137 · stated at `asy_ntp_client.py:5, 73, 384-414`
- **Provenance**: C — legacy parity (formula copied verbatim, tests check branch choice only)
- **Truth**: holds — not independently verified against a calendar (N137)
- **Fit**: restates CLAUDE.md "verify against legacy field behaviour" · pillar P4
- **Home**: code → unchanged

### G6.062 Tuned values are registered tunables
- **Req**: Every tuned (not derived) value that carries the design — UART GC-pause, jitter, gate-step and diagnostic-streak constants, UART test timeouts, NTP retry/timeout constants, WiFi settle and poll delays, UDP backoff, webserver timeouts and body cap — has a register entry with how it was determined and its re-check trigger.
- **Class**: tooling
- **Sources**: UART.N008, N009, N010, N012, N074, N108, N129; NET.N017, N047, N068, N093; REST.N178 · stated at the constants' comments, SPEC I (21 ms GC pause)
- **Provenance**: O — OR30/OR30.a
- **Truth**: partly — no `@tunable` register exists yet; `_GC_PAUSE_WORST_MS = 21` is one dated host measurement; the WiFi 5 s/3 s settle sleeps have no stated reason (`asy_wifi_service.py:551, 556`)
- **Fit**: restates OR30.a · pillar P8
- **Home**: → new SPECIFICATION.md tunables section

### G6.063 Status handling accounts for AP-mode STAT_GOT_IP
- **Req**: WiFi status handling accounts for `network.STAT_GOT_IP` being reported by an AP interface too, for status 2 ("obtaining IP") having no named constant, and for `rssi` raising `ValueError("STA required")` outside STA mode.
- **Class**: platform
- **Sources**: NET.N082, N094, N106, N170, N173, N211, N227 · stated at SPEC F.2 (3630-3631), `asy_wifi_service.py:129, 378-380, 570, 760-765`
- **Provenance**: F — SPEC F.2 and `extmod/network_cyw43.c` (per code comment; not re-read)
- **Truth**: partly — `_run_hotspot_mode()`'s `status != STAT_GOT_IP` branch is live only on the first tick; `rssi` error prints on every `/status` in AP mode (N106)
- **Fit**: restates Part F · pillar P2
- **Home**: SPEC F.2 → unchanged

### G6.064 errno/wrnno catalog is one record
- **Req**: SPEC C.7.1 is the one record of every `errno`/`wrnno`, checked mechanically against the source per module; a retired number is never reused; no two conditions share a number within one module.
- **Class**: product
- **Sources**: UART.N013, N078, N251; NET.N014; REST.N120 · stated at SPEC C.7.1, `tests/test_asy_uart_comm.py:277-316`
- **Provenance**: O — OR28/OR28.a; UART's own range owner-directed (requirements §3 item 5)
- **Truth**: drifted — `asy_ntp_client.py:9-10` says "numbering starts at 11, clear of base_classes.py's reservation" while it uses `wrnno` 1-3 (C.7.1 records this); the webserver uses `wrnno` 2 for both the per-call and the outer-cap timeout (`asy_webserver_service.py:254, 720`); only UART has a range-sweep test
- **Fit**: restates OR28.a · pillar P7
- **Home**: SPEC C.7.1 → the global catalog of OR28.a

### G6.065 @web-group declared once
- **Req**: Each `@web-group` is declared by exactly one source file; other modules only add fields to it, and buildgen fails the build on a missing or duplicate declaration.
- **Class**: tooling
- **Sources**: NET.N023 · stated at `asy_ntp_client.py:76-80`
- **Provenance**: C — convention; enforced by `buildgen/definitions.py:283-292`, `web_tag.py:254-259`
- **Truth**: holds
- **Fit**: refines OR43.a (3) · pillar P8
- **Home**: SPEC L.6.4 → unchanged

### G6.066 Retry loops back off, never spin
- **Req**: A retry loop that fails without raising gets its own capped exponential backoff (captive DNS 0.5 s → 5 s, UART listen `timeout/2` → `5 × timeout`), never a zero-delay retry, resets on the first success, and never ends its task to "retry by restart".
- **Class**: product
- **Sources**: UART.N011, N056, N083; NET.N114, N181, N210, N224 · stated at SPEC C.9 (2300-2306), `captive_dns.py:30-35`, `asy_uart_comm.py:97, 1105-1126`
- **Provenance**: A — C.9 convention after a measured ~5 lines/s spin
- **Truth**: holds — backoff code at both sites; captive DNS growth branch unexercised by the bench tier (N210)
- **Fit**: restates OR18 · pillar P2
- **Home**: SPEC C.9 → unchanged

### G6.067 Cancellation leaves state consistent
- **Req**: Every task path leaves shared state consistent when cancelled: locks, busy flags and indicators restored in `finally`, `CancelledError` re-raised and never swallowed by a widened except.
- **Class**: code-style
- **Sources**: UART.N100; NET.N156, N160, N162 · stated at `asy_uart_comm.py` `_busy` finally, `tests/test_asy_udp_socket.py:1596-1598`, `tests/test_asy_wifi_service.py:658-660, 852-853`
- **Provenance**: A — test comments
- **Truth**: holds — `_serve()` re-raises `CancelledError` (`asy_webserver_service.py:709-710`)
- **Fit**: restates OR38.a (1) (cleanup on every path, cancellation included) · pillar P2
- **Home**: → SPEC C.8

### G6.068 Stored hostname wins over TOML default
- **Req**: A device TOML's hostname is only a default: a name stored through the web UI survives a reflash whose TOML says otherwise.
- **Class**: product
- **Sources**: NET.N175 · stated at `tests/test_asy_wifi_service.py:2851-2853`
- **Provenance**: A — hostname follow-up (PR #61)
- **Truth**: holds — pinned by test
- **Fit**: refines OR52.a (2) (stored config is a public interface) · pillar P1
- **Home**: → SPEC L config-key stability rule

### G6.069 Network test scope excludes DHCP faults
- **Req**: Network-robustness tests treat "WiFi up, no internet" as NTP/DNS unreachable and deliberately do not cover DHCP flakiness, which lives in lwIP and could strand the DUT.
- **Class**: test-standard
- **Sources**: NET.N208, N209 · stated at tests_hardware/README.md:796-837
- **Provenance**: A — hardware test design
- **Truth**: holds
- **Fit**: refines OR49.a (no destructive or uncontrollable tests) · pillar P9
- **Home**: tests_hardware/README.md → unchanged

### G6.070 Vendored Microdot is never edited
- **Req**: `ext/microdot.py` stays byte-identical to upstream `v2.6.2`; every behaviour change and every Microdot gap (the write-phase exception gap among them) is handled by wrapping or calling it from `asy_webserver_service.py`.
- **Class**: code-style
- **Sources**: REST.N001, N052, N166 · stated at CLAUDE.md hard rule, SPEC A.5, `asy_webserver_service.py:2`
- **Provenance**: O — OR24.a (2) "vendored `ext/` ... never touched"
- **Truth**: holds — `git -C microdot show v2.6.2:src/microdot/microdot.py | diff - ext/microdot.py` identical (2026-09-26); no test, script or CI step re-verifies it
- **Fit**: restates OR24.a (2) · pillar P4
- **Home**: CLAUDE.md → unchanged

### G6.071 Relied-upon Microdot behaviour is listed and rechecked
- **Req**: Every Microdot behaviour the REST layer relies on — dispatch semantics and handler return shapes, the body read before the 413 check, hook order (`after_request` on the happy path only), error-handler lookup keys, per-header writes ending in a separate blank line, the `send_file` short-read loop, the muted socket errnos, HTTP/1.0 without keep-alive — is stated with its source line and rechecked whenever `ext/microdot.py` moves.
- **Class**: tooling
- **Sources**: REST.N006, N021, N022, N043, N051, N053, N054, N055, N060, N062, N070, N090, N123, N124, N127, N129, N159, N177 · stated at SPEC A.5, I.3, I.6, H.7
- **Provenance**: O — CLAUDE.md "Always check current MicroPython and Microdot documentation before asserting how an API behaves"
- **Truth**: holds — rechecked at v2.6.2 (`ext/microdot.py:56-61, 403-430, 672-681, 746-751, 1441-1447`) and v2.7.0 (same body order, 1,024 B buffer, blank-line write); `MUTED_SOCKET_ERRORS` lists 54 and 128, which are not rp2 errnos (rp2 uses internal errno, `mpconfigport.h:134`: EPIPE 32, ECONNRESET 104, ENOTCONN 107), so an rp2 `ENOTCONN` reaches `_serve()`'s `OSError` branch (`wrnno` 3)
- **Fit**: restates CLAUDE.md standing practice · pillar P4
- **Home**: SPEC A.5 → unchanged

### G6.072 Static content served pre-gzipped
- **Req**: The static site is served only from pre-gzipped files with `Content-Encoding: gzip`, regardless of `Accept-Encoding`, with no `Cache-Control`; freezefs path containment plus the `..` guard bound what can be served; unknown extensions go out as `application/octet-stream`.
- **Class**: product
- **Sources**: REST.N035, N037, N061, N065, N066, N071 · stated at `asy_webserver_service.py:649-667`, SPEC A.9
- **Provenance**: A — frozen-HTML pipeline design
- **Truth**: holds — every current browser in the owner's floor (PQ10) accepts gzip; any `open()` error reads as "no such file"
- **Fit**: new · pillar P6
- **Home**: SPEC A.9 → unchanged

### G6.073 Every caught exception reaches the FRAM history
- **Req**: Every exception Microdot catches in a handler or hook is persisted through the registered `errorhandler(Exception)` into `pr.err_s()`; Microdot's own console `print_exception` is accepted output, and an exception raised while writing the response only costs that one connection.
- **Class**: product
- **Sources**: REST.N023, N069, N089, N125, N126, N168 · stated at SPEC A.5
- **Provenance**: A — Step 2 webserver design
- **Truth**: holds — `app.errorhandler(Exception)(self._handle_unhandled_exception)` (`asy_webserver_service.py:393`), persisted as `errno` 4
- **Fit**: refines OR26.a (Microdot handlers, response-writing gap) · pillar P7
- **Home**: SPEC A.5 → unchanged

### G6.074 Both Microdot request limits move together
- **Req**: `Request.max_content_length` and `Request.max_body_length` are both set from the one constructor parameter (2,048 B) so no oversized body is ever buffered; the cap serves every body the API accepts, checked per device and per PUT route with the largest legitimate body pinned; the limits are process-global class state.
- **Class**: product
- **Sources**: REST.N016, N020, N056, N057, N059, N077, N078, N082, N083, N114, N115, N116, N117, N119, N128, N160, N161, N162, N163, N173, N181 · stated at SPEC I.6, A.5, `asy_webserver_service.py:363-370`
- **Provenance**: A — commit c304b70 (upstream issue filed)
- **Truth**: partly — enforced by `tests_scripts/test_request_body_cap_headroom.py` and the F.2b test section; the headroom counts `maxLength` in characters and only `@web`-tagged fields (N116); a negative `Content-Length` reaches `readexactly(negative)` (`ext/microdot.py:421-426`), untested (defect candidate); `max_readline` stays at Microdot's 2 KB default
- **Fit**: refines OR52.a (3) · pillar P6
- **Home**: SPEC I.6 (move out of Part J, G6.003) → unchanged

### G6.075 Connection ceiling and slot accounting
- **Req**: `max_connections` is 6; `MEMP_NUM_TCP_PCB` is at least `max_connections + 3`, a build error otherwise; `backlog` defaults to `max_connections + 1` and buildgen refuses one outside `[max, max + 1]` (runtime only clamps up); a connection over the ceiling is accepted and closed with no response; a slot counts until its close completes and is released even if the close's warning raises `MemoryError`; no open connection is ever evicted; keep-alive is not implemented.
- **Class**: product
- **Sources**: REST.N018, N019, N042, N046, N074, N075, N076, N081, N084, N092, N101, N102, N107, N108, N110, N111, N143, N144, N145, N146, N147, N148, N150, N164, N174, N180; NET.N252, N253, N254 · stated at SPEC H.7/H.7.1, `asy_webserver_service.py:315-353, 697-731`, `toolchain/micropython_overrides.py:182, 260-262`
- **Provenance**: O-confirmed — `max_connections` 6 "Owner's decision on the peak-load evidence" (db3bf52); reject-when-full and no restart backstop "owner decisions on the 10-question round" (ee5310c); A for slot-after-close ("settled 2026-09-24", 58b14ac) and keep-alive (8328943)
- **Truth**: holds — `SPARE_TCP_PCBS = 3`, `MEMP_NUM_TCP_PCB = 9` (`toolchain/versions.toml:42`), all six TOMLs `max_connections = 6`; ceiling rejections leave no log (N102); readiness-probe slot timing worked around, not root-caused (open item REST.N073)
- **Fit**: refines OR49.a (graceful degradation under load) · pillar P6
- **Home**: SPEC H.7 → unchanged

### G6.076 A cut response never reads as success
- **Req**: A connection is bounded by `per_call_timeout_s` (5 s) per stream call and `outer_cap_s` (15 s) per request; a read-phase timeout is logged by the stream proxy because Microdot swallows it; every body that can fail after its status line carries `Content-Length`, the header block goes out as one write, and writes after a peer reset are dropped.
- **Class**: product
- **Sources**: REST.N013, N015, N038, N094, N100, N149, N151, N157, N176, N182 · stated at SPEC H.7.1, I.3, `asy_webserver_service.py:230-289, 660-667`
- **Provenance**: O-confirmed — layered hardening "owner-decided" after the arzi/neu incident (PR #27, e72936e); A for header coalescing (58b14ac)
- **Truth**: holds — bench timings 5.12-5.16 s and 15.08-15.13 s (2026-09-23); the modlwip freed-pcb workaround has no removal trigger (G6.080)
- **Fit**: refines OR26.a (response-writing gap) · pillar P3
- **Home**: SPEC H.7.1/I.3 → unchanged

### G6.077 Static routes registered after every API route
- **Req**: The static `/` and `/<path:filename>` routes are registered after every exact API route, since Microdot's first match wins.
- **Class**: product
- **Sources**: REST.N024, N036, N067, N086, N109, N130 · stated at SPEC A.7, `asy_webserver_service.py:396-401`
- **Provenance**: A — Step 2 webserver design
- **Truth**: holds — pinned by `tests/test_asy_webserver_service.py:1925-1927`; the `assert self._static_mount is not None` in shipped code exists only for mypy (per-file `S101` ignore, `pyproject.toml:306-310`)
- **Fit**: new · pillar P4
- **Home**: SPEC A.7/A.8 → unchanged

### G6.078 GET aggregation merges module dicts
- **Req**: GET routes build their result with `.update()` over each module's own `{name: {...}}`, never `result[name] = ...`; duplicate names resolve last-registration-wins with no dedup code; every registered error source appears in `/status` errcount under a unique logger name, including `DNSSRV` and the webserver's own entry.
- **Class**: product
- **Sources**: REST.N009, N012, N025, N048, N072, N079, N112, N132; NET.N179 · stated at SPEC A.8, H.6, `asy_webserver_service.py:118-124, 406-418`
- **Provenance**: O-confirmed — "Duplicate registration resolves to last-wins by construction" (owner decisions round, ee5310c); A for `.update()` (production bug fix)
- **Truth**: holds — key collisions between modules overwrite silently (N025, N012); a wrong fan-in list drops a logger silently, caught only by `tests/test_captive_dns.py:279-281`
- **Fit**: extends OR16.a (1) (chains complete) · pillar P8
- **Home**: SPEC A.8 → unchanged

### G6.079 PUT result-word contract
- **Req**: PUT bodies are sparse JSON with no `cmd` envelope; each field reports "Valid", "Unchanged", "Invalid" or "Failed" and an unknown key is "Invalid"; a per-field failure never demotes the envelope below `"OK"`/0; a failing post-write hook marks every field of its group "Failed"; dispatch-only fields report "Valid" on every well-formed repeat; malformed JSON answers HTTP 200 with code 1; setters return a uniform `True`; toggling the WiFi LED never reconnects.
- **Class**: product
- **Sources**: REST.N026, N029, N050, N097, N103, N136, N141, N142; NET.N107, N189 · stated at SPEC A.8, C.5, H.6
- **Provenance**: A — "Final project decision" (130cef2) and H.6; setter contract "project-wide decision" (4c555e0)
- **Truth**: drifted — the `_put_sensors` comment cites a per-key "Invalid" but emits nothing for an unknown sensor key (`asy_webserver_service.py:427-430`); `lightCmdLED` reports "Failed" for a malformed payload where H.6 says "Invalid" (REST.N122 mechanisms differ)
- **Fit**: refines OR43.a (1) (GET returns what PUT accepts) · pillar P4
- **Home**: SPEC H.6/A.8 → unchanged

### G6.080 Workarounds name their defect and removal trigger
- **Req**: Every workaround of an external defect (stub gaps, a cyw43 quirk, a modlwip freed-pcb write, Microdot's 1,024 B `send_file` default) names the defect and the condition under which it is removed, as the install-time stub repairs already do.
- **Class**: code-style
- **Sources**: NET.N056, N073; REST.N005, N041 · stated at `asy_udp_socket.py:174-177`, `asy_wifi_service.py:235-236`, `asy_webserver_service.py:55-57, 693-695`, SPEC I.3
- **Provenance**: C — stub repairs in `scripts/typecheck.sh` are conditional (CLAUDE.md); these five sites state no trigger
- **Truth**: partly — none of the five states a removal trigger; the `stations` 100 ms sleep is unverified
- **Fit**: extends OR50.a (1) (every entry has a live reason) · pillar P4
- **Home**: code → SPEC F.5.5 pattern

### G6.081 Tests take product values from source
- **Req**: A test or build check that needs a product constant reads it from source (AST or import) or states why an independent copy is the oracle; a test of production wiring drives the real generated wiring rather than a local re-implementation.
- **Class**: test-standard
- **Sources**: UART.N107, N151, N152; NET.N190, N192; REST.N095, N096, N113, N118 · stated at `tests/test_uart_comm_hazard.py:33-45`, `tests/test_setter_microdot_integration.py:8-10`, `tests_scripts/test_persistence_write_marker_completeness.py:19-22`
- **Provenance**: C — `buildgen/validate.py` and `test_request_body_cap_headroom.py` read via AST; the cited sites hand-copy
- **Truth**: partly — hand copies remain undetected on drift (dispatch field list, route shape, wire offsets, NTP window, twin README's 5 s)
- **Fit**: extends OR16/OR22.a (silently broken chains), P8 · pillar P8
- **Home**: → SPEC E test standard

### G6.082 SystemCmd and PauseTime are fixed server-side
- **Req**: `SystemCmd` accepts only `reboot`, `bootloader`, `mempause`, and `mempause`'s duration (300 s) is fixed server-side, never client-supplied; `PauseTime` is range-checked 0-3600 and rejected, not clamped.
- **Class**: product
- **Sources**: REST.N007, N008, N133 · stated at SPEC A.8, `asy_webserver_service.py:95-101`
- **Provenance**: A — Step 2 webserver design; 3600 mirrors legacy `pauseAutoLED`
- **Truth**: holds — `_PAUSE_TIME_MAX` is a copy of `asy_notification_service.py:48`'s ceiling (P8 copy)
- **Fit**: refines OR52.a (3) (unauthenticated `bootloader` accepted) · pillar P2
- **Home**: SPEC A.8 → unchanged

### G6.083 Envelope only via make_response
- **Req**: Every REST envelope is built by `api_response.make_response()`, never a hand-built `{"res", "code", "descr", "result"}` dict.
- **Class**: code-style
- **Sources**: REST.N139 · stated at SPEC G.2 (4177-4178)
- **Provenance**: A — Part G catalog
- **Truth**: holds — the only literal `"res":` in `src/` is inside `make_response()` (`api_response.py:56`); `js/` and generated code not checked
- **Fit**: restates OR24 · pillar P4
- **Home**: SPEC G.2 → unchanged

### G6.084 Build info is a nested system sub-entry
- **Req**: `GET /system` carries one nested, never-flattened `"build"` entry (`firmwareVersion`, `websiteVersion`, `buildDate`) from `build_info=`, never through a `SettingsGroup`.
- **Class**: product
- **Sources**: REST.N165 · stated at SPEC L.7
- **Provenance**: A — Part L session 7
- **Truth**: holds — `asy_webserver_service.py:490-491`
- **Fit**: new · pillar P8
- **Home**: SPEC L.7 → unchanged

### G6.085 GET responses are bounded streams
- **Req**: Every GET route whose response grows with device configuration streams its JSON through `_PieceWriter` in pieces of at most `chunk_bytes` (default 256, the same parameter that sizes static reads, clamped to ≥ 1), with an exact `Content-Length`, byte-identical to MicroPython's `json.dumps()`; the largest allocation is the largest fragment, and the one over-cap fragment is `NTP_Host`'s value.
- **Class**: product
- **Sources**: REST.N010, N011, N017, N064, N087, N091, N093, N137, N140, N152, N153, N154, N155, N156, N158, N167, N186 · stated at CLAUDE.md memory rule, SPEC I.3, G.2
- **Provenance**: O-confirmed — CLAUDE.md memory-safety rule names `_stream_dict_response()` as the canonical fix (owner's standing direction)
- **Truth**: partly — all six GET routes stream (`asy_webserver_service.py:411-418` and siblings); `_PieceWriter` counts characters, not bytes (`:147-151`), so a non-ASCII value can exceed the byte bound; the byte-parity mirror must be rechecked at every MicroPython bump
- **Fit**: restates CLAUDE.md memory rule; refines OR40/P6 · pillar P6
- **Home**: CLAUDE.md + SPEC I.3 → SPEC I.3, CLAUDE.md pointing

### G6.086 Responses are strict JSON
- **Req**: No route lets a non-finite float into a response, since MicroPython's `json.dumps()` emits `inf`/`nan` text no JSON parser accepts; shaping values is the caller's obligation.
- **Class**: product
- **Sources**: REST.N099, N138 · stated at SPEC F.1, `tests/test_strict_json.py:86-88`
- **Provenance**: F — pinned against the interpreter by `test_strict_json.py`
- **Truth**: partly — the emit behaviour is pinned; no mechanical check that every route's values are finite
- **Fit**: new · pillar P2
- **Home**: SPEC F.1 → unchanged

### G6.087 ResetErrors is global and destroys evidence
- **Req**: `PUT /status {"ResetErrors": true}` resets every registered module, not one; because it erases FRAM evidence, FRAM logs are read before it; its sweep time under load is fixed at the source, never by raising a client timeout.
- **Class**: process
- **Sources**: REST.N034, N080, N169, N170, N185 · stated at SPEC H.6, CLAUDE.md FRAM rule, BACKLOG #24
- **Provenance**: A — decision in `tests/test_asy_webserver_service.py:1052-1054` (7cd363d); O for FRAM-first (CLAUDE.md)
- **Truth**: partly — the sweep reaches 88-98 % of the 15 s cap at three readers (BACKLOG #24 open, owner decision pending)
- **Fit**: refines CLAUDE.md FRAM rule; OR49.a · pillar P7
- **Home**: CLAUDE.md + BACKLOG → unchanged

### G6.088 A PUT answers on validated values
- **Req**: A PUT answers on validated values; a deferred flash write that later fails costs persistence only, is logged (`errno` 14) and is never reported back to the client, and never retried.
- **Class**: product
- **Sources**: REST.N098 · stated at SPEC C.7.3
- **Provenance**: O — "(owner, 2026-09-24)", "settled (owner, 2026-09-25)" (SPEC C.7.3)
- **Truth**: holds — pinned by `tests/test_setter_microdot_integration.py:660-669`
- **Fit**: refines OR42.a (bounded writes) · pillar P9
- **Home**: SPEC C.7.3 → unchanged

## Conflicts and tensions

1. **UART episode dedupe vs OR35.b's single central rule** (G6.022, G6.045). OR35.b: "just don't
   repeat the same error in the slots ... No per-origin, per-module or per-episode special cases".
   UART pairs every fault's `errno` with a resync `wrnno` in the same ring (`print_log.py` keeps one
   `history` deque for both), so a repeated fault alternates E/W and the newest-entry rule alone
   would spend two slots per occurrence — the flood UART's own flags prevent today
   (`asy_uart_comm.py:312-341`). OR35.a (4) removes the flags only "where it covers them".
   Unresolved: owner question 1.
2. **C.7.1 per-distinct-code rule vs OR35.b** (G6.045). C.7.1 argues WiFi/FRAM keep one slot per
   distinct code so "a W4 beside a W5" survives; OR35.b's newest-entry rule keeps that pair but lets
   an alternation W4/W5/W4 through. Resolved by provenance: OR35.a (3) (owner) puts alternating codes
   out of scope; C.7.1 is rewritten with OR35.b.
3. **UART `errno` 10-34 "disjoint from the owner's range" vs OR28.a** (G6.011, G6.064). C.7.1 says
   overlap between modules is expected; OR28.a wants one meaning per number project-wide with
   non-overlapping per-module ranges. Resolved: OR28.a (owner, later) wins; UART's range moves in the
   one-pass renumbering. The UART numbers are not wire content, so G6.004's flag-day rule does not
   apply.
4. **SPEC F.1 "every real use in `src/` is a short bounded timeout" vs the UART hold-off**
   (G6.018). Code wins; F.1 is corrected and the hold-off gains an elapsed-time expiry (execution
   fix, OR12.a path: proven by the platform fact).
5. **Permanent WLAN deactivation vs P1** (G6.052). A terminal no-network state that only a power
   cycle clears needs a person. Resolved by provenance: owner-confirmed intentional (SPEC:269-275);
   the principles Part names it as a deliberate exception.
6. **Observation-tier silence vs P7/OR18** (G6.042). WiFi observation failures leave no FRAM trace.
   The two-tier convention is agent-introduced (A), not an owner decision; it stays (conservative,
   reversible) and is reviewed under OR26.a's "right layer" check in B2. Resolved for now.
7. **CLAUDE.md "every change" vs changelog "during promotion"** (G6.002). CLAUDE.md is the rule
   text; the changelog's scope line is factual drift, fixed in place (OR13.a).
8. **tests_hardware/README "not decided here" vs CLAUDE.md settled probe rule** (G6.053). CLAUDE.md
   and owner commit 655e4f9 win; the README line is factual drift, fixed in place.

## Questions for the owner

1. **UART fault pairs under the central log rule**
   Options: (a) keep UART's own episode rule as the one exception to OR35.b; (b) the central rule
   compares a new code with the newest entry *of the same kind* (E with E, W with W); (c) central
   rule only, accept two slots per repeated UART fault.
   Consequences: (a) keeps today's two-entry budget but breaks "no special cases"; (b) needs no
   exception and covers every module that pairs an error with a warning, changes the rule's wording;
   (c) a dead link fills the ten-slot ring in five faults and evicts other evidence.
2. **Public DNS fallback (8.8.8.8, 1.1.1.1)**
   Options: (a) keep it; (b) remove it, DHCP-assigned server only as legacy did.
   Consequences: (a) NTP still resolves when the router's DNS is broken, but the device sends queries
   outside the home LAN, a behaviour legacy never had (agent-added, `a6abe13`); (b) exact legacy
   parity, NTP fails with the router's DNS.

## No-requirement buckets

descriptive: UART.N033, N072, N097, N117, N127, N249; NET.N032, N138, N163, N172; REST.N004, N047, N068, N085, N131
defect-candidate: UART.N025, N065, N274, N280; NET.N016, N030, N031, N038, N040, N044, N065, N111, N115, N136, N191, N242, N256; REST.N027, N039, N058, N063, N104
open-item: UART.N142, N143, N242, N243, N244; NET.N200, N247; REST.N073, N106, N134
suppression-inventory: (none — every suppression item is listed under G6.039, G6.040 or G6.041)
drift-only: UART.N022, N035; NET.N041, N057, N070, N108, N116; REST.N044, N121, N122, N135
stale: UART.N139, N145, N224, N276; NET.N212, N238, N240, N251, N257; REST.N105, N171, N172, N175, N179, N183, N184

## Accounting

Every item read in full; each ID placed exactly once (checked by script: no missing, no duplicate, no
foreign ID). 88 candidates: provenance O 19, O-confirmed 24, A 30, F 8, C 7; truth holds 62, partly 17,
drifted 7, unverifiable here 2.


| Catalog | Items | In candidates | descriptive | defect-candidate | open-item | suppression-inventory | drift-only | stale | Sum |
|---|---|---|---|---|---|---|---|---|---|
| UART.md | 282 | 261 | 6 | 4 | 5 | 0 | 2 | 4 | 282 |
| NET.md | 258 | 229 | 4 | 13 | 2 | 0 | 5 | 5 | 258 |
| REST.md | 186 | 162 | 5 | 5 | 3 | 0 | 4 | 7 | 186 |
