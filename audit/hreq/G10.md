# Harvest requirements G10 — implicit conventions, code scan (no catalog) (HEAD 06eff58)

Source: the code itself (`src/`, `js/`, `html/`, `tests/`, `tests_js/`, `digital_twin/`, `buildgen/`,
`devices/`, `scripts/`, `toolchain/`, `tests_scripts/`, `tests_hardware/`), `pyproject.toml`,
`eslint.config.js`, `.github/workflows/ci.yml` and `git log`; the legacy tree only as a comparison.
Counts are exact greps/AST walks at HEAD unless marked "heuristic". "Written" means stated in
SPECIFICATION.md / CLAUDE.md; every candidate is `C` unless the text it matches carries an
attribution. A convention that varies across the tree is phrased as the target and marked
"harmonize under OR24" with the variants and counts.

## Candidates

### G10.001 `asy_` file prefix marks async modules
- **Req**: A `src/` module whose public API is `async` carries the `asy_` prefix and a sync-only module does not; sensor drivers are named `asy_<chip>_driver.py`. Harmonize under OR24: 19 async modules carry the prefix, 8 async modules do not (`api_response`, `base_classes`, `captive_dns`, `config_manager`, `crc_checks`, `framing_codecs`, `print_log`, `system_service`), 2 sync modules correctly do not (`math_helpers`, `voc_algorithm`).
- **Class**: code-style
- **Sources**: none (code scan) · stated at SPECIFICATION.md A.2:96, C.1:1502, K.2:5759, L.1:6025 (driver form only)
- **Provenance**: C — driver form written (agent, spec consolidation `59f1614`); the general prefix rule is unwritten
- **Truth**: partly — driver form holds 6/6 and is enforced by `buildgen/driver_registry.py:85` (`f"asy_{driver}_driver"`); general form 19 of 27 async modules
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.1/C.2 (driver form only) → C.2 for the whole `src/` naming scheme

### G10.002 Module name and primary class name agree
- **Req**: A module's primary class name is derivable from its file name (`asy_<x>_service.py` → `<X>Service`, `asy_<x>_client.py` → `Asy<X>Client`-style, one rule). Harmonize under OR24: `asy_wifi_service`→`AsyConnTime`, `asy_notification_service`→`NotificationCoordinator`, `asy_uart_link_driver`→`UartLinkExerciser`, `asy_webserver_service`→`WebserverService`, `system_service`→`SystemService`, `asy_ntp_client`→`AsyNtpClient`, `asy_udp_socket`→`AsyUDPSocket`; the `Asy` class prefix is on 9 classes (`AsyConnTime`, 5 `AsyFram*`, `AsyNtpClient`, `AsyUDPSocket`) and absent from every other async class.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C — 28-module/63-class map at HEAD
- **Truth**: partly — 4 of 7 services/clients follow a guessable mapping, `AsyConnTime` follows none
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md C.2

### G10.003 One casing per chip name in class names
- **Req**: Every class named after a chip spells the chip exactly as its `_NAME` does, across `src/`, `digital_twin/` and test fakes. Harmonize under OR24: `asy_bmp3xx_driver.py` mixes `BMP3XX_I2C` with `BMP3xx_Reader`/`BMP3xx_DeviceSession` (`_NAME = "BMP3XX"`); the twin uses a third form (`Bmp3xxChip`, `Scd30Chip`, `Sgp40Chip`, `Isl29125Chip`) against `SCD30_*`/`SGP40_*`/`ISL29125_*` in `src/`.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1507-1510 cites both `BMP3xx_Reader` and `SCD30_I2C`
- **Provenance**: C — `grep '^class '` over `src/` and `digital_twin/`
- **Truth**: partly — 3 of 4 chip drivers consistent inside `src/`; 0 of 4 twin chip models match `src/`
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.2 → same, with the twin named

### G10.004 Compound class names: one scheme
- **Req**: Class names use one compounding scheme: `<CHIP>_<Role>` only where C.2 prescribes it (sensor + layer role), plain CapWords everywhere else, and acronyms cased one way. Harmonize under OR24: underscore compounds outside C.2's scope (`CRC_Base`, `CRC_Pass`, `Framing_Base`, `Framing_Pass`, `Framing_COBS`, `UART_Comm`, `FRAM_SPI`, `DFRobot_vocalgorithmParams`) sit beside CapWords (`LockableBuffer`, `PrintLogHistoryStore`, `UartLinkExerciser`); acronyms upper in `AsyUDPSocket`, `DNSServer`, `I2CDevice`, `VOCAlgorithm`, title-case in `AsyNtpClient`, `UartLinkExerciser`.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1507-1512; `pyproject.toml` ignores N801 globally (comment cites `VOC_Algorithm`, a name that no longer exists — the class is `VOCAlgorithm`)
- **Provenance**: C — 63 `src/` classes
- **Truth**: partly — N801 off, so nothing enforces either scheme; the N801 justification comment is drifted
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.2 + `pyproject.toml` comment → C.2

### G10.005 Driver file class order: consumer first
- **Req**: A sensor driver file declares its classes top-down: `<Chip>_Reader`, then `<Chip>_DeviceSession`, then `<Chip>_I2C`/`_SPI`, with module constants, `_VAL_*` schema tuples, tags and the namedtuple above them.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten (C.2's bullet list gives the reverse, bottom-up order, without claiming a file order)
- **Provenance**: C — 4 of 4 sensor drivers (`bmp3xx`, `isl29125`, `scd30`, `sgp40`)
- **Truth**: holds — 4/4
- **Fit**: extends OR24 (ordering) · pillar P4
- **Home**: none → SPECIFICATION.md C.2

### G10.006 DeviceSession boilerplate is one shared class
- **Req**: The device-session lock wrapper exists once (a shared `Lockable` subclass taking the bus device) rather than as a verbatim copy per driver.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1528-1536 ("pure boilerplate, identical in all three drivers ... Copy verbatim")
- **Provenance**: A — "Copy verbatim" from spec consolidation (`59f1614`, reworded `e5f31f2`)
- **Truth**: holds as written (4 byte-identical copies in `asy_bmp3xx/isl29125/scd30/sgp40_driver.py`); conflicts with Part G.1's "never reimplement"
- **Fit**: conflicts G10 C.2 text vs Part G.1 · refines OR24/OR46.b ("adopt the solutions at any place") · pillar P4
- **Home**: SPECIFICATION.md C.2 → Part G.2 primitive, C.2 cites it

### G10.007 `_NAME` constant and namedtuple type name
- **Req**: Every module that logs under a fixed name declares `_NAME = const("<NAME>")`, and a module with a measurement namedtuple names the type with the identical string, defined next to it.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1515-1524
- **Provenance**: A — spec consolidation `59f1614`
- **Truth**: holds — 14 modules declare `_NAME`; 7/7 namedtuples match (`BMP3XX`, `ISL29125`, `NOTIFY`, `NTP`, `SCD30`, `SGP40`, `WIFI`); identity is not machine-checked (`test_measurement_field_tuple_agreement.py` checks `_FIELDS` vs namedtuple fields only)
- **Fit**: restates C.2 · pillar P4
- **Home**: SPECIFICATION.md C.2 (unchanged)

### G10.008 Measurement field names
- **Req**: Measurement namedtuple fields are PascalCase abbreviations that become the REST keys unchanged, and the last field is always the `TS` timestamp.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1517-1518
- **Provenance**: A — spec consolidation `59f1614`
- **Truth**: holds — 7/7 end in `TS`; internal non-REST tuples (`GMTimeStruct`, `ListenResult`, `_ConstValue`) use snake_case fields, which is consistent with them not reaching the wire
- **Fit**: restates C.2 · pillar P4
- **Home**: SPECIFICATION.md C.2/C.6

### G10.009 Config key spelling: one scheme
- **Req**: Stored-config and REST setting keys are PascalCase, spell one concept one way (`Offset` or `Offs`, `Interv` or `Int`), and carry no per-driver prefix; the scheme is settled before the release freezes keys (OR52.a (2)). Harmonize under OR24: 47 keys; snake/upper legacy spellings `NTP_Host`, `NTP_Offset_S`, `NTP_Interv_H`; prefixed `SGPResetVOC` (legacy) and `ISLCalibrate` (new); `TempOffs` (SCD30) vs `TempOffset` (BMP3XX); `SeaLevelOffs` vs `PressOffset`; `MeasInt` vs `SampleInterv` vs `Interv`.
- **Class**: product
- **Sources**: none (code scan) · SPECIFICATION.md C.5.3:1799-1801 ("a field's wire name drops any redundant per-driver prefix (`"BackupPeriod"`, not `"SGPBackupPeriod"`)")
- **Provenance**: A — C.5.3 wire-name rule (agent, `e5f31f2` rewording); the legacy keys `TempOffs`, `MeasInt`, `NTP_*`, `SGPResetVOC`, `LedWifiOn` come from `modules/sensortask-*.py`, the rest are new
- **Truth**: drifted — C.5.3 says prefixes are dropped; `SGPResetVOC` (`asy_sgp40_driver.py:59`) and `ISLCalibrate` (`asy_isl29125_driver.py:141`) keep them; the BMP/SGP legacy prefixes (`BMPPressOffset`, `SGPBackupPeriod`) were dropped
- **Fit**: refines OR24.a (2), OR52.a (1)/(2) · pillar P4
- **Home**: SPECIFICATION.md C.5.3 → C.5.3 plus the config-key stability rule (OR52)

### G10.010 REST JSON key casing
- **Req**: The REST API uses one documented casing per key role: value/setting keys PascalCase, and the envelope and structural keys one other fixed style. Harmonize under OR24: envelope `res`/`code`/`descr`/`result` lowercase; `/status` sections `networking`/`system`/`sensors`/`notification`/`errcount` and entries `counter`/`history`/`num`/`type`/`error` lowercase, while the same data is `ErrCount`/`ErrNum`/`ErrType` internally (`asy_webserver_service.py:773-778`); `/system`'s `build` entry `firmwareVersion`/`websiteVersion`/`buildDate` camelCase; `/notification`'s `lightCmdLED` camelCase (legacy key) beside `PauseTime`/`SystemCmd`/`ResetErrors` PascalCase.
- **Class**: product
- **Sources**: none (code scan) · SPECIFICATION.md A.8:586-609 lists the keys without a casing rule
- **Provenance**: C — only `lightCmdLED` exists in legacy (`modules/sensortask-*.py`, 4 files); `errcount`, `SystemCmd`, `PauseTime`, `ResetErrors`, `build` are refactor-era
- **Truth**: partly — four casings on one API
- **Fit**: refines OR24.a (2) (change with every consumer: `js/`, mock, definitions, tests) · pillar P4
- **Home**: none → SPECIFICATION.md A.8

### G10.011 Website-facing tag and definitions keys camelCase
- **Req**: `@web`/`@web-group` tag attributes and definitions-JSON keys are camelCase and identical to each other (`submitGroup`, `onLabel`, `defaultValue`, `pollGroup`, `minLength`).
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md H.5, L.6.4
- **Provenance**: C — 87 `@web` tags, 14 attribute names; `html/definitions/wozi.json` keys
- **Truth**: holds — all tag attributes camelCase
- **Fit**: restates H.5 · pillar P8
- **Home**: SPECIFICATION.md H.5 (unchanged)

### G10.012 Schema tuple constant names
- **Req**: Every config field is one `_VAL_<ABBREV> = const(((...),))` tuple, and one field name maps to one constant name project-wide. Harmonize under OR24: 53 tuples; 2-3-letter abbreviations (`_VAL_SI`, `_VAL_POV`, `_VAL_TO`) beside spelled names (`_VAL_ON_H`, `_VAL_FLASH_BRI`, `_VAL_DEBUG_LEVEL`, `_VAL_AR_THRESH`, `_VAL_HOTSPOT_PW`); `_VAL_TO` is `TempOffset` in BMP3XX and `TempOffs` in SCD30.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1522
- **Provenance**: A — spec consolidation `59f1614`
- **Truth**: holds for the `_VAL_` prefix and `const()` wrapping (53/53, except the 4 concatenations in `asy_notification_service.py:96-101`, which `const()` cannot fold); abbreviation style varies
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.2

### G10.013 Config keys referenced through their schema tuple
- **Req**: Code refers to a config key through `name_cfg(_VAL_X)`, never by a repeated string literal. Harmonize under OR24: 47 `name_cfg()` sites; literal repeats in `asy_wifi_service.py:73,189,201` (`_RADIO_FIELDS`, `_push_callbacks["LedWifiOn"]`, the `PW`/`HotspotPW` mask) and `system_service.py:317,344,345` (`"DebugLevel"` ×3).
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — 47 vs 6 literal sites
- **Fit**: extends OR24; P8 (one source) · pillar P4
- **Home**: none → SPECIFICATION.md C.5

### G10.014 Module constants are private `const()`
- **Req**: Every module-level constant whose value `const()` can fold (int, str, bytes, bool, None, tuple of those) is `_`-prefixed and `const()`-wrapped; a public `const()` exists only for names other modules import. Harmonize under OR24: 430 of 454 module constants are `const()`, 424 private; eligible but plain: `asy_fram_driver.py:56-59` (four bytes commands, while `asy_dns_client.py:23-24` and `asy_fram_manager.py:36` wrap bytes), `asy_uart_link_driver.py:36` `_BANNER`, `asy_webserver_service.py:95` `_SYSTEM_CMDS`, `asy_dns_client.py:20` `_FALLBACK_DNS_SERVERS`; `asy_ntp_client.py:44` `_NTP_UDP_PORT` is plain on purpose for tests (OR36.a (1) removes that reason).
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2 (for `_NAME`/`_VAL_` only), F.5.6:3842-3846, F:3526-3529
- **Provenance**: F + C — MicroPython `py/parse.c:369-394` (`mp_parse_node_is_const`: small int, string/bytes, None/True/False, const object, empty tuple) and `:855-857` (a `_`-prefixed const is private, no global left)
- **Truth**: partly — 24 plain constants of which ~9 are eligible; F.5.6 says "This project's 21 `const()`-using files" — HEAD has 26 (drifted count)
- **Fit**: refines OR24; P6 (no global dict entry, inlined) · pillar P6
- **Home**: SPECIFICATION.md F.5.6 → C.2 (rule) with F.5.6 (fact)

### G10.015 Error numbers: literal or named, one way
- **Req**: `errno`/`wrnno` values are written one way project-wide — either literals checked against the C.7.1 catalog or named module constants — decided together with the OR28 global catalog. Harmonize under OR24: 192 literal call arguments; `asy_uart_comm.py:107-141` names all 30 (`_ERR_*`, `_WRN_*`, `_ERRNO_MIN/MAX`); `api_response.py:34` `_ERRNO_UNHANDLED_DISPATCH`; `asy_fram_manager.py:38` `_WRN_EPISODE_BASE` plus computed codes; `asy_bmp3xx_driver.py:49` `_ERR_CMD` is a datasheet register bit, not an errno, yet uses the errno prefix.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.7:1880-1890, C.7.1
- **Provenance**: C
- **Truth**: partly — two idioms, three constant prefixes (`_ERR_`, `_ERRNO_`, `_WRN_`) plus one name clash
- **Fit**: refines OR28/OR28.a (2)-(3) · pillar P7
- **Home**: SPECIFICATION.md C.7.1 → same (catalog), naming rule added

### G10.016 Every persisted log call names its number
- **Req**: Every `err_s()`/`wrn_s()` call passes `errno=`/`wrnno=` explicitly by keyword; the `_NO_ERR` default is never relied on.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.7:1880 (implied)
- **Provenance**: C — AST-balanced scan of 212 matches: 209 real calls, all with the keyword (3 matches are comments)
- **Truth**: holds — 209/209; not machine-checked (OR28.a (3)'s catalog test would enforce it)
- **Fit**: restates OR28.a (3) · pillar P7
- **Home**: none → the OR28 catalog test

### G10.017 Log call shape: message, then objects
- **Req**: A log call passes a fixed message string (capitalised, ending in `:` when an object follows) and the exception or values as separate arguments, never a pre-built string, so a suppressed debug level allocates nothing. Harmonize under OR24: 181 calls lead with a literal message and 102 pass `e` separately; `captive_dns.py:109,111,119,121` build f-strings per DNS request (evaluated even when `evt()` prints nothing); endings mix `:` (82), `!` (38), `.` (2).
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.4:1635-1637 (`err_s(_NAME, "...", e, errno=N)`)
- **Provenance**: C (shape) + A (C.4 text, spec consolidation)
- **Truth**: drifted — C.4 shows `_NAME` as first argument; 0 of 209 calls pass it (the name is the logger's own `self.name`, `print_log.py:114`)
- **Fit**: refines OR24; P6 (no allocation on a silent path) · pillar P6
- **Home**: SPECIFICATION.md C.4/C.7 → C.7

### G10.018 Console output only through print_log
- **Req**: `src/` never calls `print()` directly; every console line goes through a `PrintLog` level method so `DebugLevel` governs it.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten (Part G.2 "per-module logging" is the nearest)
- **Provenance**: C
- **Truth**: holds — 0 `print(` outside `print_log.py` (8 inside it); not enforced (`T20` is ignored globally in `pyproject.toml`)
- **Fit**: extends Part G.2 · pillar P4
- **Home**: none → Part G.2, with `T20` re-enabled for `src/` (per-file ignore for `print_log.py`)

### G10.019 Raised exception message style
- **Req**: Messages of exceptions raised by layer-2 code follow one style (case and form); f-strings are allowed there since it is a failure path. Harmonize under OR24: 24 capitalised vs 28 lower-case `raise X("...")` messages in `src/` (e.g. `asy_bmp3xx_driver.py:628` "Failed to find BMP3XX!" vs `:497` "failed to read ...").
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — no majority
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md C.3

### G10.020 Exception tuples in one order
- **Req**: A multi-type `except` lists its types in one canonical order (e.g. alphabetical), so equal handlers read equal. Harmonize under OR24: `(OSError, MemoryError)` 17 sites vs `(MemoryError, OSError, ...)` 3; `(ValueError, TypeError)` 3 vs `(TypeError, ValueError)` 2; `(OverflowError, OSError)` 6 vs `(IndexError, OverflowError, ValueError, OSError)`.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C — 40 distinct handler shapes in `src/`
- **Truth**: partly
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md D.10

### G10.021 Timer.init failures catch OSError and MemoryError
- **Req**: Every `Timer.init()` sits in a `try` whose handler catches `(OSError, MemoryError)`.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.9:2217-2220
- **Provenance**: A — spec consolidation `59f1614`
- **Truth**: holds — 13/13 timer `init()` sites (`bmp3xx:290`, `isl29125:849`, `ntp:281,332,343`, `scd30:214`, `sgp40:469`, `wifi:413,662`, `system_service:126,164,204,365`)
- **Fit**: restates C.9 · pillar P2
- **Home**: SPECIFICATION.md C.9

### G10.022 BaseException only for release-and-reraise
- **Req**: `except BaseException` appears only to release a held resource (lock, chip select) and re-raise unchanged.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: holds — 3/3 (`asy_fram_driver.py:134`, `asy_spi_driver.py:142,162`), all `release/deassert; raise`
- **Fit**: extends OR26 · pillar P2
- **Home**: none → SPECIFICATION.md D.2

### G10.023 TYPE_CHECKING import idiom
- **Req**: `TYPE_CHECKING` is imported as `try: from typing import TYPE_CHECKING except ImportError: TYPE_CHECKING = False`, and every typing-only import sits under `if TYPE_CHECKING:`.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.10:2315, D.6:2651-2653
- **Provenance**: A — spec consolidation `59f1614`
- **Truth**: holds — `src/` 22/22, `tests/` 69/69, `digital_twin/` 12/12, `tests_hardware/device_scripts/` 10/10; 0 unguarded `from typing` imports
- **Fit**: restates C.10 · pillar P4
- **Home**: SPECIFICATION.md C.10

### G10.024 No runtime typing shim
- **Req**: Narrowing a `struct.unpack()` result uses one project-wide idiom with no runtime cost; no module defines a runtime `cast()` shim. The written rule is made self-consistent.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.4.2:1676-1681, C.10:2318
- **Provenance**: A — C.4.2 ("the settled convention over a local `cast()` shim") from spec consolidation
- **Truth**: drifted — C.4.2 rejects a local shim and in the next sentence keeps `typing.cast()` for `SCD30_I2C._read_dev_register()`; `asy_scd30_driver.py:26-32` defines exactly such a runtime shim and calls it on every read (`:487`, `:621-623`); no other file has one
- **Fit**: refines OR24; P6 (a function call per reading) · pillar P4
- **Home**: SPECIFICATION.md C.4.2/C.10 → C.10

### G10.025 Annotation quoting
- **Req**: An annotation is quoted only when it names something imported or defined under `TYPE_CHECKING`; every other annotation is bare. Harmonize under OR24: of 451 quoted annotations in `src/`, 150 in 21 of 28 files name nothing `TYPE_CHECKING`-only (e.g. `"tuple[int, int, int]"`, `"str | None"`); the same starter signature is `"asyncio.Task[None]"` in `asy_isl29125/neopixel/notification/webserver` and bare in `asy_bmp3xx/ntp/scd30/sgp40/wifi/system_service`.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md D.6:2659-2662
- **Provenance**: O-confirmed — D.6 "(owner decision, 2026-09-24)", written by `58abf8f` ("Apply the owner's four consistency decisions")
- **Truth**: partly — D.6 itself says existing files "are not mass-edited"; OR24/OR24.a (1) authorise the consistency change
- **Fit**: conflicts D.6 "not mass-edited" vs OR24 "change, don't flag" — resolved by OR24.a (later, owner) · pillar P4
- **Home**: SPECIFICATION.md D.6 (reworded: rule stands, the not-mass-edited clause goes)

### G10.026 Union syntax and full annotation
- **Req**: Unions are PEP 604 `X | Y`; every parameter and return is annotated.
- **Class**: code-style
- **Sources**: none (code scan) · CLAUDE.md "Union type annotations"; SPECIFICATION.md C.10, D.6
- **Provenance**: O-confirmed — CLAUDE.md "confirmed directly" (the runtime test on the pinned interpreter); enforced by ruff `UP007`/`ANN` and mypy `--strict`
- **Truth**: holds — 0 `Union[`/`Optional[` in `src/`, `tests/`, `digital_twin/`
- **Fit**: restates CLAUDE.md · pillar P4
- **Home**: CLAUDE.md → SPECIFICATION.md C.10 (fact), CLAUDE.md keeps a pointer

### G10.027 Host code: one annotation-evaluation mode
- **Req**: Host-side CPython code (`buildgen/`, `scripts/`, `toolchain/`, `tests_scripts/`, `tests_hardware/`) uses `from __future__ import annotations` everywhere or nowhere. Harmonize under OR24: `buildgen/` 0/22, `scripts/` 4/5, `toolchain/` 2/2, `tests_scripts/` 9/62, `tests_hardware/` 40/99.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — 55 of 210 host files
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md L.5

### G10.028 Every file carries one header block
- **Req**: Every source file in every scope opens with exactly one header block (Python docstring or `#` block; JS `/** */` or `//` block) of at most three prose lines. Gaps: `tests/` 24 of 104 files have none (`microtest.py`, `test_api_response.py`, `test_asy_*` ×15, `test_base_classes.py`, `test_captive_dns.py`, `test_config_manager.py`, `test_crc_checks.py`, `test_math_helpers.py`, `test_print_log.py`, `test_system_service.py`, `test_voc_algorithm.py`); `tests_js/` 8 of 15 (`app/definitions/main/mock-server/nav/poll-manager/render/templates.test.js`); `eslint.config.js` places its block after the imports.
- **Class**: docs
- **Sources**: none (code scan) · CLAUDE.md working agreement "Every module gets exactly one header comment block"
- **Provenance**: O — CLAUDE.md: cap "tightened ... by the project owner on 2026-09-14 ... re-confirmed on 2026-09-18"
- **Truth**: partly — the cap holds (`tests_scripts/test_comment_block_cap.py` gates block length at zero over-cap) but presence is not checked; `src/` 28/28, `buildgen/` 22/22, `digital_twin/` 17/17, `tests_scripts/` 62/62, `tests_hardware/` 99/99 (manual included), `js/` 9/9
- **Fit**: restates CLAUDE.md, refines OR27.a · pillar P4
- **Home**: CLAUDE.md → same; presence added to `test_comment_block_cap.py`

### G10.029 Per-function explanation form, per scope
- **Req**: Each scope uses one form for per-function explanations: `src/` uses `#` comments (D.11), JS uses JSDoc blocks, and the host scopes choose one. Harmonize under OR24: function/class docstrings in `src/` 3 of 1,017 defs (class docstrings of the three wiring-default classes), `buildgen/` 21/179, `toolchain/` 47/72, `scripts/` 8/70, `tests_scripts/` 72/1,334, `tests_hardware/` 102/603; JS 39/39 top-level functions carry JSDoc.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md D.11:2698 ("never docstrings mixed into an individual function"), CLAUDE.md header rule ("module/function/class `"""..."""` docstrings")
- **Provenance**: A — D.11 from spec consolidation
- **Truth**: partly — `src/` holds with 3 exceptions; the host scopes mix both forms; CLAUDE.md's wording names function docstrings as allowed, D.11 forbids them in `src/`
- **Fit**: refines OR24, OR27.a · pillar P4
- **Home**: SPECIFICATION.md D.11 + CLAUDE.md → one rule in D.11, CLAUDE.md points to it

### G10.030 Third-party attribution header
- **Req**: A `src/` file derived from third-party code opens with `# SPDX-FileCopyrightText:`, `# SPDX-License-Identifier:`, then one line pointing to THIRD_PARTY_LICENSES.md, in that order, before the module docstring.
- **Class**: docs
- **Sources**: none (code scan) · THIRD_PARTY_LICENSES.md:25 ("SPDX headers in the files themselves")
- **Provenance**: C
- **Truth**: partly — 9/9 derived files carry both SPDX lines; 6 put the licence line second, 3 (`asy_bmp3xx`, `asy_fram_driver`, `asy_isl29125`) third after a continuation line
- **Fit**: extends LIC (OR52.a (7)) · pillar P4
- **Home**: THIRD_PARTY_LICENSES.md → same

### G10.031 Import grouping and no late imports in firmware
- **Req**: Imports are grouped stdlib → MicroPython platform (`machine`, `micropython`) → project, then the `TYPE_CHECKING` block; `src/` has no function-level imports.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten (enforced by ruff `I` and `PLC0415`, the latter exempted only for tests)
- **Provenance**: C
- **Truth**: holds — 0 function-level imports in `src/` (AST walk)
- **Fit**: restates OR46.a (imports level) · pillar P6
- **Home**: none → SPECIFICATION.md D.10 (one line; the tool enforces it)

### G10.032 Bus wrappers take the platform class name
- **Req**: A project wrapper around a `machine` peripheral keeps the peripheral's class name and imports the original under a private alias (`from machine import I2C as _I2C`).
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: holds — 3/3 (`asy_i2c_driver.py:11`, `asy_spi_driver.py:11`, `asy_uart_driver.py:12`)
- **Fit**: extends OR24 · pillar P4
- **Home**: none → SPECIFICATION.md C.3

### G10.033 Lock attribute naming
- **Req**: Lock attributes follow one naming rule (e.g. `<purpose>_lock`, private unless another object must take it). Harmonize under OR24: `asy_lock` (`Lockable`, 15 references), `async_lock` (`I2C`/`SPI` bus objects), public purpose names (`wifi_mode_lock`, `config_lock`, `value_lock`, `start_signal_lock`, `led_overl_lock`), private (`_op_lock`, `_connect_lock`, `_bus_lock`) and `_datalock` without separator.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — five styles
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md C.8

### G10.034 Locks acquired with `async with`
- **Req**: A lock is taken with `async with`; explicit `acquire()`/`try`/`finally: release()` only where `async with` cannot express it (a `wait_for` timeout on the acquire, or a hold spanning synchronous helpers), with that reason in a comment. Harmonize under OR24: 21 `async with` sites; explicit form in `asy_wifi_service.py` (8 sites, none needing it), `asy_fram_driver.py` (6, justified: session held across sync helpers, `wait_for` at `:408`), `asy_ntp_client.py:433`, `base_classes.py:36`.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten; `asy_wifi_service.py:236` comment "(and does not support "async with"!)" refers to `wlan.status("stations")`, not the lock
- **Provenance**: C + F — MicroPython `extmod/asyncio/lock.py:51-54` defines `__aenter__`/`__aexit__`
- **Truth**: partly
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md C.8

### G10.035 Shared mutable state through the Locked primitives
- **Req**: Coroutine-shared state uses `Lockable`/`LockedCounter`/`LockedFlag`/`LockedValue`; a bare `asyncio.Lock()` outside `base_classes.py` exists only where it guards an external resource, with the reason stated.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md G.2:4198-4200
- **Provenance**: A — Part G (agent)
- **Truth**: partly — 8 bare `asyncio.Lock()` outside `base_classes.py`: bus locks `asy_i2c_driver.py:32`, `asy_spi_driver.py:36` (external resource); `asy_fram_manager.py:68`, `asy_neopixel_driver.py:61-62`, `asy_udp_socket.py:59`, `asy_wifi_service.py:174`, `config_manager.py:223` unexplained against G.2
- **Fit**: restates Part G.2 · pillar P4
- **Home**: SPECIFICATION.md G.2

### G10.036 Nested device/bus lock idiom
- **Req**: A multi-transaction sequence takes the device-session lock as `async with self.i2c_<chip> as <chip>:` and then the bus lock as `async with <chip>.i2c_device as i2c:`, always in that order.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.8:2031-2039
- **Provenance**: A — `5b70e4b` wording, audit claim "audited across every driver"
- **Truth**: holds — attribute `i2c_<chip>` 4/4; local aliases vary (`bmp`, `isl`, `scd`, `sgp` vs `sgp40` in `asy_sgp40_driver.py:572-684`)
- **Fit**: restates C.8 · pillar P2
- **Home**: SPECIFICATION.md C.8

### G10.037 Task and timer starter shape
- **Req**: A task starter is a public method `start_asy_<what>()` returning `asyncio.get_event_loop().create_task(self.<coro>())`, and a timer starter is `start_timer()`/`start_<what>_timer()`; both are listed by `get_task_starters()`/`get_timer_starters()`. Harmonize under OR24: 18 of 22 task starters follow; `asy_webserver_service.py:741` `_start_serving` (private), `asy_wifi_service.py:656` `start_hotspot_timeout_watcher` (no `asy_`), `asy_uart_comm.py:1133` and `asy_uart_link_driver.py:139` return lambdas over `asyncio.create_task`; timer starters 7/7 follow.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.9:2205-2207 (the two getters), D.15 (starter category)
- **Provenance**: C
- **Truth**: partly
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.9 → C.9 with the naming

### G10.038 Task coroutine naming
- **Req**: The coroutine a task runs is named by one scheme (e.g. `_<what>_loop`) and has one visibility. Harmonize under OR24: `read_loop`, `monitor_loop` (public) vs `_listen_loop`, `_exercise_loop`, `_run`, `_base_trigger`, `_watch_hotspot_timeout` (private) vs `asy_ntp_time`, `ntp_time_hours_counter`, `time_counter`, `status_counter`, `wlan_connect`, `neopixel_signal`, `auto_led_override`, `scd_init_irq`.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.4.1 (`read_loop()` for sensors only)
- **Provenance**: C
- **Truth**: partly — only the sensor `read_loop()` is uniform
- **Fit**: refines OR24 · pillar P4
- **Home**: SPECIFICATION.md C.4.1 → C.9

### G10.039 Timer callbacks only set a flag
- **Req**: A `machine.Timer` callback does nothing but `.set()` an `asyncio.ThreadSafeFlag`; anything else (re-arming, reset, storage state) runs in a task woken by that flag.
- **Class**: product
- **Sources**: none (code scan) · SPECIFICATION.md C.9:2209-2211 ("never `time.sleep()` or business logic inside a callback"), F.1 (soft-callback drop)
- **Provenance**: A — spec consolidation `59f1614`
- **Truth**: partly — 10 of 13 callbacks only `.set()`; `system_service.py:126` (`action()`), `:167` (`_timer_sequencer()` re-arms the next one-shot from inside the callback), `:368` (`storage_pause(value=False)`, a ONE_SHOT whose drop under a full scheduler queue leaves storage paused with nothing to retry — defect candidate against C.9's "a dropped one-shot never fires again")
- **Fit**: restates C.9; refines OR47.a (3) (sequencer) · pillar P3
- **Home**: SPECIFICATION.md C.9

### G10.040 Allocation-free sleeps in recurring paths
- **Req**: A recurring path (read loops, command delays, animation, polling) sleeps with `asyncio.sleep_ms(<int>)` or an int number of seconds, never a float, because `asyncio.sleep(t)` computes `int(t * 1000)` and a float multiply allocates a heap float on rp2. Harmonize under OR24: 12 `sleep_ms` sites; float sleeps on recurring paths at `asy_scd30_driver.py:471,483,544` (every register read), `asy_sgp40_driver.py:574` (`round(delay_ms * 0.001, 3)`, every measurement), `asy_neopixel_driver.py:179,183` (animation step), `asy_i2c_driver.py:266,271`, `asy_wifi_service.py:235,345,531,533,621`, `asy_notification_service.py:374`.
- **Class**: product
- **Sources**: none (code scan) · SPECIFICATION.md D.4 ("avoid unnecessary allocations in anything called frequently")
- **Provenance**: F — MicroPython v1.29.0 `extmod/asyncio/core.py:62-63` (`return sleep_ms(int(t * 1000))`), `py/mpconfig.h:163` (`MICROPY_OBJ_REPR_A` default, not overridden in `ports/rp2`), `py/objfloat.c` `mp_obj_new_float()` (`m_new_obj`)
- **Truth**: partly — ~17 float-sleep sites vs 12 `sleep_ms`
- **Fit**: new (P6 gap: allocation on the steady-state path; ties to CLAUDE.md memory discipline) · pillar P6
- **Home**: none → SPECIFICATION.md D.4 / F.1

### G10.041 Unit suffixes in identifiers and TOML keys
- **Req**: A quantity's unit is one fixed suffix per unit across Python identifiers and TOML keys (`_ms`, one spelling for seconds, one for minutes, one for hours). Harmonize under OR24: milliseconds `_ms` uniform; seconds `_sec` (`trigger_sec`, `wifi_refresh_sec`), `_secs` (`trigger_secs`, `set_trigger_secs()`, `override_secs`) and `_s` (`retry_s`, `timeout_s`, `recv_fail_backoff_s`) — `trigger_sec` and `set_trigger_secs()` in one driver (`asy_bmp3xx_driver.py:130,216`); minutes `_min` vs `_minutes`; hours `_h` vs `_hours`. External-API mirrors (`timeout` in µs for `machine.I2C`, `frequency`, `baudrate`) stay as the platform spells them.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — three second spellings
- **Fit**: refines OR24 (TOML keys change with every consumer, OR24.a (2)) · pillar P4
- **Home**: none → SPECIFICATION.md C.2 and L.3

### G10.042 Constructor tail parameters
- **Req**: A module constructor ends with `max_module_error`, `name_ext`, `cfg_path`, the FRAM parameter, `history_length`, `debug` in that order (those it has), with defaults `5`, `""`, `""`, `None`, `10`, `None`; OR46.b's logging config object then replaces the last three everywhere at once.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.2:1537-1544; OR46.b (2)
- **Provenance**: A — spec consolidation; OR46.b is O
- **Truth**: partly — order holds in `BMP3xx_Reader`, `ISL29125_Reader`, `SensorReaderConfig`, services; deviations `SensorReader` (`name_ext`, `logger` after `debug`, `base_classes.py`), `SystemService` (`cfg_path` after `debug`, `system_service.py:64-72`), `SGP40_Reader` (`fram_storage` + `fram_ntp_callback` in the FRAM slot, `asy_sgp40_driver.py:140-141`); defaults hold 5/5, 19/19, 17/17
- **Fit**: restates C.2; refines OR46.b · pillar P4
- **Home**: SPECIFICATION.md C.2

### G10.043 Uniform module interface
- **Req**: Every task-owning module exposes `get_task_starters()`/`get_timer_starters()` (possibly empty), every logging module `get_error_counter()`, every top-level non-`SensorReader` module `get_error_sources()`/`get_loggers()`, so supervision and aggregation stay generic.
- **Class**: product
- **Sources**: none (code scan) · SPECIFICATION.md C.9, G.2 (fan-in), C.14.3
- **Provenance**: A — Part G/C (agent)
- **Truth**: holds — starters 12/12 task-owning modules, `get_error_counter` 15, fan-in 8 + base default
- **Fit**: restates C.9/G.2; P10 · pillar P10
- **Home**: SPECIFICATION.md C.9/C.14.3

### G10.044 `setup()` return contract
- **Req**: `async def setup()` has one return contract project-wide. Harmonize under OR24: 12 classes return `None`, 3 return `bool` (`asy_fram_manager.py`, `asy_uart_comm.py`, `asy_uart_link_driver.py`); `asy_bmp3xx_driver.py` and `asy_i2c_driver.py` add parameters.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md C.13 (gate, not return type)
- **Provenance**: C
- **Truth**: partly
- **Fit**: refines OR24, OR26.a (construction completeness), OR47.a (1) · pillar P2
- **Home**: SPECIFICATION.md C.13

### G10.045 Method ordering in a class
- **Req**: Dunders first, then private, then public; inside each group starters, getters, setters, others.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md D.15:2730-2737
- **Provenance**: A — `db9fb63` (2026-08-04, documenting a reorder done that session); OR24's "sorting" is the owner's ask behind it
- **Truth**: partly — dunder/private/public holds in 71 of 81 classes (deviating: `SCD30_Reader`, `SPIDevice`, `UART_Comm`, `UART`, `AsyConnTime`, `ConfigManager`, `Framing_Base`, `Framing_COBS`, `SystemService`, protocol `_ModuleLike`); role order deviates in ~22 groups (heuristic, e.g. `SystemService.feed_watchdog` before `start_asy_uptime_counter`; `setup` has no category in D.15)
- **Fit**: restates OR24 · pillar P4
- **Home**: SPECIFICATION.md D.15 (add `setup`/`reset` to a category)

### G10.046 Buffer-transfer method names
- **Req**: A buffer-taking transfer method is named one way: `read_into`/`write_into` for project APIs, or the platform's `readinto` where the method mirrors a `machine`/stream API, stated as a rule; synchronous variants carry `_sync`. Harmonize under OR24: `read_into`/`write_into` in `asy_fram_manager.py` (Part G.2's reference shape); `readinto` in `asy_i2c_driver.py:329`, `asy_spi_driver.py:75,205`, `asy_uart_driver.py:347`; `_sync` holds 5/5 (`write_sync`, `readinto_sync`, `write_readinto_sync`, `get_values_sync`, `set_values_sync`).
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md G.2:4209-4214
- **Provenance**: A — `6a2d43e` (UART promotion, 2026-09-11)
- **Truth**: partly — G.2 says "every transfer method comes in pairs" `read()`/`read_into(buf)`; `I2CDevice`/`SPIDevice` have `readinto` without a `read`
- **Fit**: refines OR24, Part G.2 · pillar P6
- **Home**: SPECIFICATION.md G.2

### G10.047 Per-field result words
- **Req**: Every per-field write result is one of `"Valid"`, `"Unchanged"`, `"Invalid"`, `"Failed"`, typed once as `WriteValidity`, and the JS mock uses the same four words.
- **Class**: product
- **Sources**: none (code scan) · SPECIFICATION.md C.5.2, A.8; OR42.c (2) relies on "Unchanged"
- **Provenance**: C
- **Truth**: holds — 52 literal uses in `src/` all among the four; `config_manager.py:212` `Literal[...]`; `js/mock-server.js` uses the same words
- **Fit**: restates C.5.2, OR42.a (2) · pillar P8
- **Home**: SPECIFICATION.md C.5.2

### G10.048 REST handler and dispatch naming
- **Req**: A route handler is `_<method>_<route>` on `WebserverService`, and every dispatch of a caller-supplied callback is a `_dispatch_<what>()` of Part G.2's guarded shape.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md G.2:4182-4185 (dispatch shape)
- **Provenance**: C
- **Truth**: holds — 11/11 handlers `_get_*`/`_put_*` (`asy_webserver_service.py:372-382`), 3/3 `_dispatch_*`
- **Fit**: restates G.2 · pillar P4
- **Home**: SPECIFICATION.md A.8

### G10.049 Mirrored JS constants cite their source
- **Req**: A JS constant mirroring a `src/` value is named like the Python constant without the leading underscore and carries a comment naming the live `src/` file and constant; the mirror changes in the same commit (Part G.2).
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md G.1:4172-4173, G.2 mirror entry
- **Provenance**: A — Part G
- **Truth**: drifted — `SYSTEM_CMDS`/`PAUSE_TIME_MAX` (`js/mock-server.js:16-17`) follow; `LIGHT_CMD_LED_*` (`:178-184`) cite `src/sensortask_wozi.py`'s `_notification_led_callback()`, a file that is now generated and never committed (5 references to it across `js/`, `html/`, `tests_js/`)
- **Fit**: restates G.2; P8 · pillar P8
- **Home**: SPECIFICATION.md G.2

### G10.050 JS naming and module style
- **Req**: JS files are kebab-case ES modules with named exports only; functions camelCase, module constants UPPER_SNAKE; tests are `<module>.test.js` using `describe`/`it`, helpers `_*.js`; no formatter, hand-kept style as in Python.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md H.2, H.8
- **Provenance**: C
- **Truth**: holds — 9/9 `js/` files, 0 default exports, 11/11 module constants UPPER_SNAKE; `it(` 202 vs `test(` 2 (`tests_js/live-backend.test.js:8,36`)
- **Fit**: restates H.8 · pillar P4
- **Home**: SPECIFICATION.md H.8

### G10.051 JSDoc on every JS function
- **Req**: Every top-level JS function carries a JSDoc block whose `@param`/`@returns` `tsc --checkJs --strict` verifies.
- **Class**: code-style
- **Sources**: none (code scan) · CLAUDE.md (JSDoc exemption from the cap), SPECIFICATION.md H.8
- **Provenance**: C
- **Truth**: holds — 39/39; `tsconfig.json:8,10` `checkJs`, `strict`
- **Fit**: restates H.8 · pillar P4
- **Home**: SPECIFICATION.md H.8

### G10.052 Controllers change state only through data attributes
- **Req**: Controllers (`render.js`, `nav.js`, `app.js`, `main.js`) express UI state only through the documented `data-*`/class contract, and every class a controller toggles is listed in that contract. Harmonize under OR24: `.hidden` is toggled or read by controllers at 7 sites (`app.js:37,57`, `main.js:29`, `render.js:303,304,369,373`) and `.nav-open` at 3 (`nav.js:25,29,45`); `templates.js:323-331` toggles it too, as cosmetic interactivity H.3 allows, neither listed in H.3's contract; `data-apply-status` follows the rule.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md H.3:4351-4358 ("Controllers only ever set the semantic `data-apply-status` value, never a color/class directly")
- **Provenance**: A — Part H (website redesign sessions)
- **Truth**: drifted — the contract text names `data-*` hooks and `.apply-button` only
- **Fit**: restates H.3 · pillar P4
- **Home**: SPECIFICATION.md H.3

### G10.053 CSS naming and tokens
- **Req**: CSS classes and HTML ids are kebab-case, component-prefixed (`errcount-row-name`), state is carried by `data-*` attributes, and every colour is a `--color-*` token defined once per theme; a class in markup has a rule or a documented hook role.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md H.3; `html/style.css:1-5`
- **Provenance**: C
- **Truth**: holds — 36 custom properties (32 `--color-*`), all 32 hex values inside tokens; 13 `[data-*]` state selectors vs 2 class states; `class="device-name"` (`html/index.html`) has no CSS rule
- **Fit**: restates H.3 · pillar P4
- **Home**: SPECIFICATION.md H.3

### G10.054 Shell script conventions
- **Req**: Every shell script starts `#!/usr/bin/env bash`, sets `set -euo pipefail` (a deviation states its reason on the same line), resolves the repo root with `cd "$(dirname "${BASH_SOURCE[0]}")/.."` when it touches repo paths, prints progress as `== ...`, errors as `error: ... >&2` and verdicts as `FAILED:`/`OK:`, and passes shellcheck.
- **Class**: tooling
- **Sources**: none (code scan) · unwritten (shellcheck in CLAUDE.md "Code quality tooling")
- **Provenance**: C
- **Truth**: holds — 14/14 shebang; 13/14 `-euo` + `_require_clean_hardware_run.sh:2` `-uo` with reason; root `cd` 12/14 (the other 2 touch no repo path); 33 `== ` lines; 10 `error:` lines all to stderr; 1 `shellcheck disable`
- **Fit**: extends OR15.a (2) (the common summary block builds on it) · pillar P4
- **Home**: none → SPECIFICATION.md L.5

### G10.055 Internal helper scripts start with `_`
- **Req**: A script that only other scripts call is `_`-prefixed; a user-facing entry point is not.
- **Class**: tooling
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: holds — 5 `_*` files in `scripts/` (`_digital_twin_ci_suite.py`, `_generate_sensortask_modules.py`, `_render_coverage.py`, `_require_clean_hardware_run.sh`, `_strip_type_checking.py`) called by `test.sh`/`build_firmware.py`/runner scripts
- **Fit**: extends OR15.a (README reference lists only entry points) · pillar P4
- **Home**: none → README.md reference section intro

### G10.056 Host CLI entry and error shape
- **Req**: A host CLI has `main() -> int` exited through one idiom, one domain error class raised for user errors, and `main()` turns that class into a one-line stderr message and a nonzero exit with no traceback. Harmonize under OR24: `sys.exit(main())` 4 files vs `raise SystemExit(main())` 3; `BuildError(Exception)` (`buildgen/errors.py:6`) vs `SetupError(RuntimeError)` (`toolchain/setup_toolchain.py:65`) vs `OverrideError(RuntimeError)` (`toolchain/micropython_overrides.py:15`); `scripts/build_firmware.py:67-90` re-raises as bare `RuntimeError`.
- **Class**: tooling
- **Sources**: none (code scan) · SPECIFICATION.md L.5; `buildgen/errors.py:1-3`
- **Provenance**: C (shape); OR23.a (3) is O
- **Truth**: partly
- **Fit**: refines OR23.a (3) · pillar P4
- **Home**: SPECIFICATION.md L.5

### G10.057 Test file per module
- **Req**: Each `src/` module has `tests/test_<module>.py`; each `buildgen/` module has `tests_scripts/test_buildgen_<module>.py` or a stated home; twin chip models are tested through `tests/test_digital_twin_<chip>.py`.
- **Class**: test-standard
- **Sources**: none (code scan) · SPECIFICATION.md K.6:5835 (drivers only)
- **Provenance**: C
- **Truth**: partly — `src/` 28/28; `buildgen/` 16/21 (`buildspec`, `codegen`, `errors`, `model` tested through other files; `pico_gpio` referenced by no test file by module name)
- **Fit**: refines OR25.a, OR46.a · pillar P5
- **Home**: SPECIFICATION.md K.6 → Part E

### G10.058 Test support files
- **Req**: Non-test files under `tests/` are `_`-prefixed helpers or fake platform modules named after the real module (`machine.py`, `network.py`, `neopixel.py`); scenario libraries are `_<tier>_scenarios.py` with a `register_for_device()` factory and one-call per-device wrappers.
- **Class**: test-standard
- **Sources**: none (code scan) · SPECIFICATION.md E.2.1
- **Provenance**: A — E.2.1 (2026-09-17 split)
- **Truth**: holds — 17/17 non-test files; 18/18 wrappers are `globals().update(register_for_device("<device>"))`
- **Fit**: restates E.2.1 · pillar P5
- **Home**: SPECIFICATION.md E.2.1

### G10.059 microtest file shape
- **Req**: A `tests/test_*.py` file holds only synchronous `def test_*` functions and ends with `if __name__ == "__main__": import microtest; microtest.run(globals())`.
- **Class**: test-standard
- **Sources**: none (code scan) · SPECIFICATION.md E.2
- **Provenance**: C
- **Truth**: holds — 3,198 sync `test_*`, 0 async; 85/87 end with the idiom, `test_digital_twin_poll_prewarm.py` and `test_strict_json.py` import `run` at the top
- **Fit**: restates E.2 · pillar P5
- **Home**: SPECIFICATION.md E.2

### G10.060 Shared test primitives live once
- **Req**: A test helper needed by several files (the coroutine driver, fast-sleep and fake-time doubles, bus/FRAM builders) lives once in a `tests/_*.py` module and is imported, the same "reuse before writing" rule Part G states for `src/`. Harmonize under OR24: `run(coro)` is defined in 50 test files (45 with the identical body `return asyncio.run(coro)`); `_FastAsyncSleep` 10 copies; `_FakeTime` 5; `make_i2c`, `make_fram_manager`, `make_port` 6 each; `_last_err` 3.
- **Class**: test-standard
- **Sources**: none (code scan) · SPECIFICATION.md G.1 (scope "`src/`, `js/`/`tests_js/`, and any future layer")
- **Provenance**: C
- **Truth**: partly — G.1 names `tests_js/` but not `tests/`; only `_uart_comm_harness.py` exports a shared `run`
- **Fit**: extends Part G to tests; OR39.a (3) (test-suite tidiness), OR24 · pillar P4
- **Home**: SPECIFICATION.md G.1 → G.1 scope + Part E

### G10.061 Test double naming
- **Req**: Test doubles follow one naming scheme (`_Fake<X>`, `_Recording<X>`, `_Raising<X>`, builders `make_<x>`), private to their file unless shared. Harmonize under OR24: `_Fake*` 25 vs `Fake*` 9; `make_*` vs `_make_*`.
- **Class**: test-standard
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly
- **Fit**: refines OR24 · pillar P4
- **Home**: none → Part E

### G10.062 Device set derived, never listed
- **Req**: Every device-parametrised test, script and page derives the device set from `devices/*.toml` at run time; a deliberately narrower set (the two hand-written definitions devices) is named as such and derived from its own criterion.
- **Class**: test-standard
- **Sources**: none (code scan) · OR43.a (3); `tests_scripts/_devices.py:1-3`
- **Provenance**: O — OR43.a "nothing may assume six devices" (owner, 2026-09-26)
- **Truth**: partly — `tests_scripts/_devices.py` derives `DEVICE_NAMES`; hard-coded `_DEVICES` 6-tuples in `tests/_sensortask_scenarios.py:62`, `_digital_twin_construction_scenarios.py:47`, `_webserver_concurrency_scenarios.py:61` plus 18 per-device wrapper files; `["wozi", "dev"]` literals in `tests_scripts/test_build_firmware.py:90,124,149,160`, `test_buildgen_definitions.py:65`, `test_buildgen_twin_wiring.py:48`, `test_digital_twin_boot_contiguity.py:42`, `test_device_tomls.py:21`, `js/app.js:16`, `digital_twin/machine.py:192`
- **Fit**: restates OR43.a (3) · pillar P10
- **Home**: Part E / L

### G10.063 pytest style
- **Req**: pytest suites use module-level `def test_*` functions with fixtures and `parametrize`; no `Test*` classes.
- **Class**: test-standard
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — 948 module-level tests vs 69 methods in 9 classes, all in `tests_scripts/test_micropython_overrides.py`
- **Fit**: refines OR24 · pillar P4
- **Home**: none → Part E

### G10.064 Hardware gate markers and their flags
- **Req**: Every opt-in hardware marker is registered in `tests_hardware/conftest.py` with its reason, is enabled by a flag named `--allow-<marker-in-kebab>`, and an unregistered marker fails a test. Harmonize under OR24: `flash_cycle`→`--allow-flash-cycle`, `neopixel_sweep`→`--allow-neopixel-sweep`, `scd30_extra_write`→`--allow-scd30-extra-write` follow; `persistence_write`→`--allow-persistence-writes` (plural), `multi_day_rollover`→`--allow-multi-day-rollover-wait`, `long_soak`→`--soak-tier`.
- **Class**: test-standard
- **Sources**: none (code scan) · CLAUDE.md wear rule; `tests_hardware/conftest.py:106-113`
- **Provenance**: O — CLAUDE.md wear rule "(project owner's explicit, standing direction, 2026-09-17)" for the gates; flag spelling C
- **Truth**: partly — registration holds (8 markers); unregistered markers are caught by `tests_scripts/test_persistence_write_marker_completeness.py:368` since `--strict-markers` is off; three flag spellings deviate
- **Fit**: refines OR37/OR42, OR15.a (1) (CLI reference) · pillar P9
- **Home**: `tests_hardware/README.md`

### G10.065 Hardware test file naming
- **Req**: Device scripts are `<subject>_<aspect>.py`, flash and bench tests `test_<topic>.py`, manual checks `manual_<topic>.py`.
- **Class**: test-standard
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: holds — 58 device scripts, 11 flash, 12 bench (one helper `bench/dns_probe.py`), 5 manual
- **Fit**: extends OR45.a (ladder naming) · pillar P5
- **Home**: none → `tests_hardware/README.md`

### G10.066 Device TOML layout
- **Req**: A device TOML has snake_case keys that mirror constructor parameters, sections in the fixed order `[device]`, `[device.wiring]`, `[bus.*]`, sensor `[[instance]]`s (each with an explicit `name_ext`), then singleton services (without `name_ext`), with the section banners; bus parameters keep the `machine` API's own names and units.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md L.3 (example)
- **Provenance**: C
- **Truth**: holds — 6/6 files in the same order with the same two banners (`dev.toml:148` adds a third for the UART pair, placed after the services); `name_ext` on 15/15 sensor instances plus the 2 multi-instance `uart_link`s, 0/18 singleton services; banner rule lengths differ per file (cosmetic)
- **Fit**: restates L.3 · pillar P10
- **Home**: SPECIFICATION.md L.3

### G10.067 Build-time limits for every TOML numeric
- **Req**: Every constructor parameter a device TOML can set to a number carries a `@limits` tag (or a `@requires` on its bus), so buildgen rejects an out-of-range value at build time.
- **Class**: tooling
- **Sources**: none (code scan) · SPECIFICATION.md L.6.4, L.6.6
- **Provenance**: C
- **Truth**: partly — `trigger_sec` carries `@limits` in `asy_bmp3xx_driver.py:120` and `asy_isl29125_driver.py:194` but not in `asy_scd30_driver.py` (`:123` `trigger_sec: int = 3`, `:143` `trigger_half_sec = 2 * int(trigger_sec)`), so a TOML `trigger_sec = 0` passes `buildgen/validate.py:386-388` (type check only) — defect candidate
- **Fit**: refines OR23, OR53 · pillar P10
- **Home**: SPECIFICATION.md L.6.4

### G10.068 One identifier per module across name spaces
- **Req**: A module's TOML driver key, its `_NAME`, its logger/errcount key and its config-store label derive from one source; no hand-kept table repeats them. Today `buildgen/definitions.py:110-118` hand-copies every `_NAME` into `_ERRCOUNT_NAME` (while `buildgen/driver_registry.py:108-113` reads `_NAME` from source), and the spellings differ by module (`notification`→`NOTIFY`, `dns`→`DNSSRV`, `bmp3xx`→`BMP3XX` labelled "BMP388 Config Store").
- **Class**: tooling
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly — one copy, not generated
- **Fit**: refines P8 (OR30/OR43.a one source), OR24 · pillar P8
- **Home**: none → SPECIFICATION.md L.4

### G10.069 Machine-read tags
- **Req**: `@web`, `@web-group`, `@wiring`, `@value-wiring`, `@limits`, `@requires` tags are one line per field at module level next to the schema they describe, and are the only source buildgen reads for them.
- **Class**: tooling
- **Sources**: none (code scan) · SPECIFICATION.md L.6.4; CLAUDE.md (tag lines exempt from the cap)
- **Provenance**: A — Part L (buildgen sessions)
- **Truth**: holds — 74 `@web`, 13 `@web-group`, 13 `@wiring`, 2 `@value-wiring`, 3 `@limits`, 3 `@requires`, all at column 0
- **Fit**: restates L.6.4; P8 · pillar P8
- **Home**: SPECIFICATION.md L.6.4

### G10.070 Generated module names and their documentation
- **Req**: Generated firmware modules are named `sensortask_<device>.py` and `<device>_boot.py`, and every doc names them that way; the legacy hyphenated `sensortask-<device>.py` appears only when the legacy tree is meant.
- **Class**: docs
- **Sources**: none (code scan) · `buildgen/generate.py:56-57`, `buildgen/codegen.py:697`
- **Provenance**: C
- **Truth**: drifted — SPECIFICATION.md A.2:123, B.11:947, C.1:1495, C.1:1503 and C.5.3:1805 describe the live architecture with `sensortask-*.py`/`sensortask-wozi.py` and legacy routes `/net/cmd`/`/led/cmd`
- **Fit**: restates OR13.a (factual drift, fixed in place) · pillar P8
- **Home**: SPECIFICATION.md C.1/C.5.3

### G10.071 Commit messages
- **Req**: A commit subject is one imperative, capitalised sentence (an optional `<scope>:` prefix in one agreed form) of at most 72 characters, and every agent commit carries the `Co-Authored-By` and `Claude-Session` trailers.
- **Class**: process
- **Sources**: none (code scan, `git log`) · unwritten in CLAUDE.md (trailers only in the harness reminder)
- **Provenance**: C — 1,652 commits: 1,561 Claude, 86 hundertvolt, 5 Nico
- **Truth**: partly — of 1,519 non-merge agent commits: 1,244 imperative capitalised, 211 `<scope>:` in mixed forms (`docs:`, `BACKLOG.md:`, file names, `test(buildgen):`), 674 subjects over 72 characters; trailers on 1,154 (Co-Authored-By) / 1,147 (Claude-Session)
- **Fit**: new (process, supports OR14.a history tracing) · pillar process
- **Home**: none → CLAUDE.md "Pull request workflow"

### G10.072 Suppression comments: codes always, one spelling
- **Req**: Every `# type: ignore` and `# noqa` names its codes; a multi-code list is written in one order and spacing (e.g. alphabetical, `", "`). Harmonize under OR24: 64 multi-code `type: ignore` sites in `tests/`/`digital_twin/` — `[assignment, method-assign]` 20, `[method-assign, assignment]` 15 + 2 unspaced, `[assignment,arg-type]` 11, `[assignment,misc]` 3 vs `[assignment, misc]` 4.
- **Class**: code-style
- **Sources**: none (code scan) · CLAUDE.md `method-assign` rule; ruff `PGH003`
- **Provenance**: C
- **Truth**: partly — 0 bare suppressions (codes always named, `PGH003` enforces); order and spacing vary
- **Fit**: refines OR24, OR50.a (suppression review) · pillar P4
- **Home**: none → SPECIFICATION.md D.6

### G10.073 No `assert` in firmware
- **Req**: `src/` narrows optional values with an explicit guard and sentinel return, never `assert`, keeping D.2's never-raise contract and needing no `S101` exemption.
- **Class**: code-style
- **Sources**: none (code scan) · SPECIFICATION.md D.2
- **Provenance**: C
- **Truth**: partly — one `assert` (`asy_webserver_service.py:653`) with a per-file `S101` ignore in `pyproject.toml`
- **Fit**: refines OR26, OR50.a (the per-file ignore goes with it) · pillar P2
- **Home**: SPECIFICATION.md D.2

### G10.074 Comment dividers
- **Req**: Section dividers in code are one form (`# ----` rule, optionally titled `# ---- X ----`).
- **Class**: code-style
- **Sources**: none (code scan) · CLAUDE.md comment-count rule (bare rule is punctuation, titled banner is prose)
- **Provenance**: C
- **Truth**: partly — 1,172 dash rules across scopes; 20 `# ====` rules in `tests/`
- **Fit**: refines OR24 · pillar P4
- **Home**: CLAUDE.md comment rule

### G10.075 CI job and action conventions
- **Req**: CI jobs are kebab-case ids, each with `timeout-minutes` and explicit `permissions`; every checkout sets `persist-credentials: false`; `actions/*` are tag-pinned and third-party actions SHA-pinned.
- **Class**: tooling
- **Sources**: none (code scan) · CLAUDE.md zizmor bullet; `.github/zizmor.yml`
- **Provenance**: C
- **Truth**: holds — 16/16 jobs with `timeout-minutes`, 15/15 checkouts, 2/2 third-party actions SHA-pinned (`codecov`, `dorny/paths-filter`)
- **Fit**: restates CLAUDE.md, OR30.a (timeouts are tunables) · pillar P4
- **Home**: CLAUDE.md / SPECIFICATION.md B.10

### G10.076 `asy`-marking of identifiers
- **Req**: Async-ness is marked one way in identifiers or not at all; if kept, the marker is `asy_` everywhere (modules, starters) and sync twins of async methods carry `_sync`. Harmonize under OR24: `asy_` in `start_asy_*`, `asy_lock`, `post_asy_fct`, `asy_ntp_callback`, `asy_ntp_time`, but not on `WebserverService`/`SystemService` coroutines nor on 8 async modules (G10.001); `async_lock` spells it out.
- **Class**: code-style
- **Sources**: none (code scan) · unwritten
- **Provenance**: C
- **Truth**: partly
- **Fit**: refines OR24 · pillar P4
- **Home**: none → SPECIFICATION.md C.2

